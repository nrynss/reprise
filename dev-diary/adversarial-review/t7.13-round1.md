# T7.13 round 1 review

Reviewed hash: `7bb91f47c972e92a5691f514980a96d6518b4c9d`
Worktree: `/home/nryn/work/reprise-wt/t7.13-rev-r1` (detached HEAD, confirmed with `git log -1`)
Base: `8c35dfc`
Diff scope: `git diff 8c35dfc..7bb91f47c972e92a5691f514980a96d6518b4c9d`

## What the commit does

The one shared batch client (`cmd/reprise/main.go:1260`, built once from
`loaded.TranscriptionModel`) now maps the dotted settings spelling
`universal-3.5-pro` to the dashed wire id `universal-3-5-pro` inside
`NewBatchClient`, through `normalizeBatchModel`. Both paid paths share
that client. The edit path passes `p.batch` into `transcript.Run`, and
the analysis path wraps the same client in `batchTranscriber`. Unknown
ids pass through unchanged and fail loudly at the provider with
`ErrBatchRequest`. The accepted set is pinned against the recorded
provider 400 text. No TOML file changes. No new settings key.

## Pins run in this worktree

All pins below ran in this worktree at the handed hash. The tree is
clean after the run (`git status --short` empty).

* New pins, green: `TestAcceptedModelsMatchRecordedShape`,
  `TestValidBatchModel`, `TestCreateSendsWireIDForSettingsSpelling`,
  `TestCreateSurfacesRecordedRejection` (`go test -race
  ./internal/assemblyai/`, pass).
* Neighbour packages, green: `go test -race ./internal/assemblyai/
  ./internal/settings/ ./internal/transcript/ ./internal/analysis/`,
  all ok.
* Mutation, load-bearing: replacing `model :=
  normalizeBatchModel(cfg.Model)` with `model := cfg.Model` (verbatim
  pass-through restored) fails
  `TestCreateSendsWireIDForSettingsSpelling` with `client model =
  "universal-3.5-pro", want the accepted wire id`. Reverted after the
  run. Pin and mutation both confirmed.
* Gate: `npm ci` in `web/`, then `./tools/check.sh` from the worktree
  root. First run hit a flaky `TestEraseRestartResumesAndFinishes` in
  `internal/privacy` (unrelated package, passes in isolation). Full
  rerun ends `OK all checks passed`, including the plan-references scan.
* Docs: `python3 dev-diary/tools/audit_docs.py` ends `OK planning tree
  is consistent`.
* Plan citations: `grep` over `internal/assemblyai/batch.go`,
  `internal/assemblyai/batch_test.go` and `internal/settings/settings.go`
  finds no task id, phase id, planning file, or section mark.

## Ten questions

1. Spend money without reserving budget first. No. The change maps a
   model string at client construction. Both paid paths keep their
   existing budget reservation (`transcript.Run` and `analysis.Run`
   take `Budgets`). No new paid call exists.
2. AssemblyAI session left open without `session.end` or a token cap.
   No. Only the batch REST client changes. Streaming sessions are untouched.
3. Private episode, stem, or transcript reaching anyone but its owner.
   No. No auth, visibility, or routing change.
4. Edit applied without a revertible record. No. No edit path changes.
5. Spoken callback or count from the prompt instead of a stored row.
   No. No prompt or keyterms change.
6. Code, comment, test, or migration citing a phase, task, or planning
   file. No. Grep over every touched source file is clean, and the
   gate plan-references scan passes.
7. Rebuilt Keel or Chaaya capability. No. The change is a one-string
   provider naming map plus tests, specific to this provider.
8. Review run on the handed hash in a worktree of its own. Yes.
   Detached worktree `t7.13-rev-r1` at `7bb91f4`, confirmed with
   `git log -1`. No code committed by this reviewer.
9. Paid job kind resuming after restart and spending twice. No. No job
   kind changes. Paid kinds stay non-idempotent.
10. Development check needing a person, a microphone, or a wall-clock
    threshold. No. New tests use `httptest` fakes with exact string
    assertions and no sleeps.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L | `dev-diary/PHASE-7-production-issues.md:349` at the reviewed hash | The commit carries a phase status revert (`in-progress:implement:t7.13-impl` back to `not-started`). Only the orchestrator writes status lines, and the path sits outside `owns`. Behaviour is unchanged, but the hunk must drop before landing. | `git diff 8c35dfc..7bb91f4 -- dev-diary/PHASE-7-production-issues.md` shows the one-line hunk | Re-applying the `not-started` line reintroduces the dirt |

Counts: C 0, H 0, M 0, L 1.

No OUT_OF_SCOPE defect found. The `internal/privacy` flake seen in one
gate run passes in isolation and on rerun, and that package sits
outside this task.

## Verdict: REMEDIATE

One L finding, the known status revert. Drop the phase-file hunk before
landing. Everything else approves: both batch paths carry the accepted
dashed id from the dotted setting, the accepted set is pinned against
the recorded 400 shape, the mutation proves the pin is load-bearing,
and the gate passes.
