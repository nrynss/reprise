# T5.4 review, round 1

Reviewed hash: `5aed2265468fb59cb885ad29fb366a23aee2e4e4` (two commits on claim base `7956a89`).
Worktree: `/home/nryn/work/reprise-wt/t5.4-rev-r1`, detached HEAD. `git log -1 --format=%H`
returns the handed hash. Diff reviewed: `git diff 7956a89..5aed226` (7 new files, all under
`internal/retention/`, matching the task `owns` line; no other paths touched).
No commit made. Working tree reviewed only through the handed commit.

## Verdict: REMEDIATE

Counts: C 0, H 0, M 1, L 0 in scope. OUT_OF_SCOPE: 1 (L, doc, orchestrator-owned) plus
2 declared wiring notes that are not defects.

## What was verified

* `go test -race -count=1 ./internal/retention/` passes in the review worktree (12 tests).
* `go vet ./internal/retention/` clean, `gofmt -l` empty.
* `git grep` with the exact `tools/check.sh` plan-reference patterns over
  `internal/retention/` returns nothing. No `panic`, `log.Fatal`, `**T`, or `interface{}`.
* Re-ran the key pins by reading and executing the suite: expired-guest full delete
  (rows, media rows and files, both covers, user and session rows, provider session ended,
  transcript soft-deleted) with fresh-guest counts before and after
  (`TestSweepDeletesExpiredKeepsFresh`); kept-episode survival with provider copies untouched
  (`TestKeepSurvivesGuestExpiry`); exact-cutoff survival (`TestSweepBoundaryKeepsGuestAtCutoff`);
  91-day sweep vs 89-day keep on the unset window (`TestDefaultWindowSweepsNinetyDayIdle`);
  owner skip (`TestSweepSkipsOwners`); restart resume (`TestSweepRestartResumesAndFinishes`);
  stuck-then-heal plus `ReSweep` (`TestSweepStuckFailsThenHeals`).
* Two reviewer probes were written as in-package tests, executed, then deleted. One passed
  (concurrent keep). One failed as expected and is the M pin below. Tree is clean after.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/retention/sweep.go:159-162` (`run`), `internal/retention/sweep.go:151-153` (comment) | A resumed or re-swept run reuses the failed run's cutoff instead of reading expiry fresh, so a guest who idles past the window while the sweep is down is missed by the retry. The `run` comment claims the union means such a guest "still expires". That holds only for fresh sweeps. It contradicts the task line that expiry reads last-seen time at sweep time rather than from a stamp. Workaround: run a fresh sweep. The error is toward keeping, never toward over-deleting. | `TestProbeResweepCatchesNewlyExpired` (reviewer probe, ran in-package against the handed hash, failed with `episodes holds 2 newly-expired rows, want none`): seed `guest-old` idle 91 days and `guest-mid` idle 89 days on a fixed clock with a 90-day window; fail the first sweep with a refused transcript delete; advance the clock 48 hours on a second service sharing the same stores; `ReSweep` the failed job after the outage clears; assert `guest-mid` owns zero episodes. Full source: seeds two guests via `seedGuest`, sets `fx.transcript.fail = true`, polls `fx.jobStore.Attempts` for `job.StatusError`, then opens `retention.New` with `Now` returning `testNow + 48h`, binds `fx.runner`, calls `ReSweep`, waits with `waitSweepDone`, and asserts `fx.count(t, "episodes", "guest-mid") == 0`. Against the handed hash `guest-mid` still owns 2 episodes because `plan` evaluates `expired` at the frozen `prior.Cutoff`. | Reintroduce by restoring the freeze in `run`: `cutoff := prior.Cutoff; if cutoff == 0 { cutoff = s.now().Add(-s.window).Unix() }`. With those two lines back, the pin fails again with the same count. |

## OUT_OF_SCOPE

| Severity | Where | What | Pin | Owning path |
|---|---|---|---|---|
| L | `dev-diary/project.md:159` | The open-decisions table still lists guest-data lifetime as undecided with `T5.4` as the needer, while the round handoff records the owner decision (ninety days) and the code ships `DefaultWindow`. The doc contradicts the landed decision. | Read `project.md` line 159 beside `t5.4-impl-handoff.md` ("Retention window: DECIDED") and `retention.go:35` (`DefaultWindow`). | `dev-diary/project.md`, orchestrator-owned. |
| Note, not a defect | `internal/retention/sweep.go` (`ReSweep`), `internal/retention/keep.go` (`Keep`) | No route exposes `ReSweep`, `Keep`, or the stuck-erasure retry control. The handoff declares this openly as wiring work. Same pattern as the T4.4 `ReErase` control. | Handoff "Contract changes for wiring". | Route table / wiring task. |

## The ten questions

1. Spend without reserving budget first? No. The sweep and the keep make zero paid calls.
   The only provider calls are session termination and transcript deletion, both free deletes.
   No budget, gate, or cost path is reachable from `internal/retention/`.
2. AssemblyAI session left open without `session.end` or a token cap? No. The sweep closes
   provider sessions through the inner erasure (`TerminateSession`, pinned `ended == true`
   for swept guests, untouched for fresh and kept ones). It opens no sessions and mints
   no tokens.
3. Private episode, stem, or transcript reaching anyone but its owner? No. This is the C
   class for this task and it was examined with teeth. Owners never enter the allowlist:
   `expired` filters `kind = 'guest'` (pinned by `TestSweepSkipsOwners`), prior snapshot
   guests derive from the same filter, and `set` replaces the whole set each run, so stale
   entries cannot widen it. Every destructive step is owner-scoped: `Erase` checks `Owns`
   at inventory, `ReErase` checks the recorded owner, `rowsTarget` deletes with
   `AND owner_id = ?`, `deleteStrays` touches only `media.owner = guest`, and
   `deleteGuestRows` skips a user that stopped being a guest. The one in-scope finding errs
   toward keeping, never toward over-deleting. Concurrent runs can flap the shared allowlist
   but every check above fails closed (spurious failure, no wrongful delete).
4. Edit applied without a revertible record? No. Deletion is the specified behavior (expiry
   deletes rather than hides). `Keep` is one transaction. No editorial edit exists here.
5. Spoken callback or count from the prompt instead of a stored row? No prompts, no counts,
   no greeting. Not applicable.
6. Code, comment, test, or migration citing a phase, task, or planning file? No. The
   `check.sh` pattern scan over `internal/retention/` is empty. The ninety-day comment states
   the value and its source (owner choice) without citing any planning file.
7. Rebuilt Keel or Chaaya work? No. The sweep drives `keel/erase` and `keel/job` and names
   only the product-specific targets, which is exactly the documented split. One sweep kind
   plus the single existing erasure kind are registered; no second eraser exists to steal a
   resume hook. No library gap surfaced, so no issue was owed.
8. Review run on the handed hash in a worktree of its own? Yes, stated at the top.
9. Paid job kind resuming after restart and spending twice? No. The sweep kind is idempotent
   because it performs only deletes, and every target replays safely (missing rows, blobs,
   and covers read as done; provider deletes repeat against soft-deleted copies). Nothing it
   resumes can spend.
10. Development check needing a person, a microphone, or a wall-clock threshold? No. Fixed
    clock, scripted provider doubles, real SQLite and media store. The 30 and 60 second
    bounds are liveness guards only.

## Scrutiny notes (asked, answered, no finding)

* Allowlist trust model: crafted or mistaken allowlists cannot reach a keeper because only
  the sweep writes the set and only from expired-guest ids. Reset-each-run verified at
  `sweep.go:167`.
* Keep transaction: `keptTables` matches every `owner_id` content table in
  `internal/store/migrations/0001_schema.sql` (episodes plus exactly the ten kept tables).
  Grouped blobs move; ungrouped strays correctly stay for stray deletion. Keeper existence
  is enforced (`TestKeepUnknown`). Ten rounds of barrier-started concurrent keeps to one
  keeper passed; `UNIQUE (owner_id, number)` makes any lost update fail closed rather than
  collide, and no concurrent callers exist (no routes yet).
* Cutoff edges: `<` favors keeping (exact-cutoff survives, pinned). Fresh sweeps read
  last-seen inside the job (pinned). The resume path is the M above.
* Seeded rows: no query predicates on `seeded`, so per-guest seed copies sweep by
  construction and shared-season rows (non-guest owner) never list (pinned via the owner
  test). No seeded-specific fixture exists, but there is no seeded-specific branch either.
* Resume convergence: snapshot publishes after every started and finished erasure and after
  every guest; `ErrComplete` reads as done in both retry paths; restart and outage pins pass.
* Live remeasure stays owed (doubles throughout), the same recorded deferral as T4.4.

## Verdict restated

REMEDIATE. One in-scope M (frozen cutoff on retry plus the over-claiming comment). No
residue claim is possible until a re-review confirms the pin passes on the remediated hash.
