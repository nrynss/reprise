# T6.1 round 2 review (re-review)

Reviewed hash: `b70a090ec26b4d8f2e26a2e2383f4ef4836e41f5`
Worktree: `/home/nryn/work/reprise-wt/t6.1-rev-r2` (detached HEAD, confirmed with `git log -1 --format=%H`). No commit was made from this worktree.
Base for remediation diff: `eda79cd5b67ccd7d509b87836547c59cb705b8c3` (round 1 hash). Remediation is three commits (`ead2715`, `b2868e7`, `b70a090`), no rebase. Remediation-only diff reviewed with `git diff eda79cd..b70a090`.
Verdict: **APPROVE**

Counts: H 0, M 0, L 0 in scope. Out of scope rows: 1 (H, non blocking, restated below).

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with H 1, M 0, L 2 in scope.

- H zero residue. The `docker rm -f` SIGKILL swap is gone. `deploy/run.sh:166-167` now runs `docker stop -t "${STOP_TIMEOUT}"` then `docker rm`, so SIGTERM reaches the process and the drain runs inside the daemon timeout. `grep -n 'rm -f' deploy/run.sh deploy/redeploy.sh` returns nothing. The stop doc at `deploy/run.sh:16-19` matches the new behavior. `STOP_TIMEOUT` stays 1830, above the 1800 session cap with the same 30 second margin. Claim: zero H residue.
- L zero residue (empty version gate). `deploy/redeploy.sh:111` now requires a nonempty reported version when an expected SHA is set. The pin matrix was re-run against the exact guarded condition: empty/pinned rejects, empty/empty passes (first boot preserved), both prefix directions pass, mismatch rejects. Claim: zero residue on this L.
- L zero residue (em dashes). `git grep -n` for the em dash over `deploy/`, `Dockerfile` and `cmd/reprise/` returns nothing. All three lines from round 1 are hyphens now. Claim: zero residue on this L.
- C zero. Round 1 had zero C findings and this diff introduces no demo break, recording loss, spend leak, or privacy exposure. Claim: zero C residue.
- M zero. Round 1 had zero M findings and nothing in the remediation diff creates a defect with a workaround. Claim: zero M residue.

## New findings

None. The remediation diff touches four files (`deploy/README.md`, `deploy/backup.sh`, `deploy/redeploy.sh`, `deploy/run.sh`) with nine insertions and seven deletions. Each hunk maps to a round 1 finding. No new logic, no new path, no behavior change beyond the three fixes.

Two candidate new defects were checked and cleared. The `docker stop` sits inside the existing `docker inspect` guard, so it never runs against a missing container. The nonempty guard preserves the unset SHA first boot pass and both true prefix directions, verified by the matrix above.

## What was checked

Remediation-only diff read in full. Round 1 pins re-run in this worktree: stop-then-rm shape confirmed, empty-version matrix confirmed, em dash grep returns nothing. Rollback path intact (`rollback()` restores the recorded image and exits 1). Stop timeout 1830 still clears the 1800 session cap. Doc updates match behavior. Round 1 working items re-confirmed: boot plan log, no secret values in `deploy/` (only `REPRISE_CONFIG` path flag), nil registry safe, health contract `ok <boot> version=<build>`, drain gate 503 shape, no plan or task cites in owns paths.

Gate: `go test -race ./cmd/reprise/` passes, `go vet` passes, `bash -n` passes on all three scripts, `docker build --build-arg VERSION=<handed>` succeeds on a live daemon. `./tools/check.sh` Go stages (`gofmt`, `go vet`, `staticcheck`, `go test -race`) all pass. The full `check.sh` exits 1 only at `svelte-kit sync` because `web/node_modules` is absent in this fresh worktree (`npm ci` not run here). The diff touches no `web/` file, so the web stages measure nothing this task owns.

## Out of scope (restated, non blocking)

| Severity | Where | What | Pin | Mutation | Owner |
|---|---|---|---|---|---|
| H | `.github/workflows/ci.yml`, `Install pinned ffmpeg and ffprobe` step | The pinned binary archive URL is dead upstream. CI fails at its media install step today. This file sits outside the task owns lines. It never blocks APPROVE. | `git diff eda79cd..b70a090 -- .github/workflows/ci.yml` is empty in this worktree, so the remediation left it untouched. The dead `mwader/static-ffmpeg` archive line is still present at line 26. | Repoint the install step at the dead archive URL. | T0.1, which owns `.github/workflows/ci.yml` and is done. |

## The ten questions

1. Does any path spend money without reserving budget first. No. The remediation adds no paid path. Stop, remove, version match, and doc lines move no spend.
2. Can any path leave a provider session open without `session.end` or a token cap. No. The fix shortens nothing. `stop -t 1830` bounds the drain inside the daemon timeout, and the session cap still bounds the wait.
3. Can a private episode, stem or transcript reach anyone but its owner. No. No media, auth, or serving path changed.
4. Does any edit apply without a revertible record. No. Nothing here edits episodes. Rollback still restores the recorded image and exits 1.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. No prompt or count code changed.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. A pattern scan over the touched paths returns zero matches.
7. Did this task rebuild something Keel or Chaaya provides. No. The remediation wires no library. It edits shell and docs only.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1 --format=%H` matches the handed hash. The worktree is detached. No commit was made and no other file was touched.
9. Can any paid job kind resume after a restart and spend twice. No. No job kind changed.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Re-run pins are file shape checks, grep probes, and a pure shell condition matrix. Tests use timeouts only as liveness bounds.

## Verdict

APPROVE. All three round 1 findings are fixed with passing pins, verified in this worktree against the handed hash. Zero residue is claimed explicitly: H zero, L zero times two, C zero, M zero. The out of scope CI row is confirmed still out of scope and untouched.
