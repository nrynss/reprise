# T3.4 remediation round 1

Remediation worktree `/home/nryn/work/reprise-wt/t3.4-rem-r1`, branch
`task/t3.4-rem-r1`, built on reviewed hash
`2a3cb576d0b5a78d1dfce384377273f7d975e571`. Two commits, one per
finding. No other finding was fixed and the verdict stands unchanged.

## Fixes

| Finding | Fix | Pin (committed) | Mutation |
|---|---|---|---|
| H: `ColdOpen` ignored decisions, so a reverted cold open still rendered | `store.go` reads the latest cold open proposal id, then its latest decision row. A `reverted` decision returns nil, so the episode starts at the top. Other decisions and no decision keep the span, and the intersect-kept-audio path is untouched | `TestRenderRevertedColdOpenRerenders` in `render_test.go`. It renders 40 words with a cold open, inserts `reverted` on the cold proposal, and re-runs. The second run returns a different hash with `reused=false`, `ColdOpen` reads nil, and the output runs seconds shorter | Deleting the decision lookup (restoring the proposals-only query) fails the pin. The second run reuses the first hash |
| L: `HashInputs` omitted the crossfade, the mix rate, and the encoder bitrates | `edit.go` gains `CrossfadeMs`, `MixRateHz`, `OpusBitrate`, and `AACBitrate` on `Input`, and all four join the hash preimage beside the targets and the gap. `run.go` fills them from the assembly constants. New `OpusBitrate` and `AACBitrate` constants replace the encode literals in `audio.go`. Cut order stays out of the hash | `TestHashInputsVariesWithAssemblyParams` in `edit_test.go`. It varies each new field alone, starting with crossfade 10 against 20, and requires a different hash each time | Removing the crossfade line from the preimage fails the pin |

## Gate

`go test -race -count=1 ./internal/render/` passes in the remediation
worktree. `go vet` and `gofmt -l` are clean. Pinned ffmpeg and ffprobe
report 9.0.1. `./tools/check.sh` passes after `npm ci` in the
worktree and ends with `OK all checks passed`.

Head after remediation: `4b9fc8cb723233ca4bb1434b883212c7fb2490a2`.
