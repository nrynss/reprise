# T3.1 remediation round 2

Rebased head `929750f` collided with landed T2.1: `internal/assemblyai/batch.go`
declared `Client` and `NewClient` in the same package as `internal/assemblyai/token.go`,
so `go vet` failed. Remediated in worktree `/home/nryn/work/reprise-wt/t3.1-rem-r1`
on branch `task/t3.1-rem-r1` with no rebase and no amend. New head `165c939`, one
commit on top of the rebased head. Tree is clean. The gate passes in this worktree,
including `go vet ./...` and the full `./tools/check.sh` (`OK all checks passed`).

| Finding | What changed | Pin now passing |
|---|---|---|
| Rebase collision on `Client`/`NewClient` | `internal/assemblyai/batch.go`: `Client` is now `BatchClient` and `NewClient` is now `NewBatchClient`, with every method receiver and constructor updated. References updated in `internal/assemblyai/batch_test.go`, `internal/transcript/job.go` (`Batch *assemblyai.BatchClient`), and `internal/transcript/run_test.go`. `internal/assemblyai/token.go` untouched. JSON shapes and behavior unchanged. | `go vet ./...` clean. `go test -race ./internal/assemblyai/ ./internal/transcript/` both pass. Round 1 pins still green: no `map[string]any` in `batch.go` production code, and `TestRunKeepsProviderCopyOnReceiptFailure` still asserts the error names `tx-9`. |

Nothing outside the task `owns` paths was touched.
