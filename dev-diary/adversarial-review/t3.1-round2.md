# T3.1 review round 2 (re-review)

Reviewed hash: `2b28fc300356746c56e359d23e338e30dc33b63e`, in worktree
`/home/nryn/work/reprise-wt/t3.1-rev-r2` (detached HEAD, `git log -1`
matches the handed hash). Diffs read: remediation alone
`3e6c08e..2b28fc3` (3 files, 19 insertions, 5 deletions) plus the round 1
context `63adb19..3e6c08e` as recorded in round 1. No commits made. Tree
is clean after the round.

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0. No new findings.

## Round 1 residue, severity by severity

| Round 1 finding | Severity | Residue |
|---|---|---|
| Receipt store error named no transcript id (`internal/transcript/job.go:129`) | M | Zero. Line 129 now returns `transcript: run %s: receipt store: %w` with `transcriptID`. `errors.Is` still reaches the store error (line 388 of `run_test.go` asserts it), the survive-for-retry behavior is unchanged, and the added assertion (`strings.Contains(err.Error(), "tx-9")`) pins the id. Re-ran the mutation here (dropped the `%s` verb and argument): `TestRunKeepsProviderCopyOnReceiptFailure` fails with `want it to name tx-9 for the explicit retry`. Restored via `git checkout`, tree clean, hash unchanged. |
| `Create` body built as `map[string]any` (`internal/assemblyai/batch.go:214`) | L | Zero. `Create` now sends the named `createBody` struct (`audio_url`, `speech_models`, `keyterms_prompt,omitempty`). `grep -rn 'map\[string\]any'` over both owned packages hits only test files (`internal/transcript/run_test.go` decodes fixture shapes, never production code). `TestCreateSendsModelAndKeyterms` passes unchanged, so the wire shape is preserved. The empty-keyterms path is identical by construction: the field stays nil unless `len > 0`, and `omitempty` drops len-0 slices exactly as the map version dropped the missing key. |

Explicit claim: M residue zero, L residue zero, and C/H zero in both the reviewed commit and the remediation diff. No `OUT_OF_SCOPE` defects found.

## New findings

None. The remediation diff touches only the two finding sites plus the one test assertion. No error-format regression (`%w` wrap kept on every path, `go vet` clean). No JSON shape drift (create test passes unchanged against the recorded shape). No broken callers (no caller matches on the receipt error text; `errors.Is` paths intact). No phase, task, or planning-file citations added.

## The ten questions

1. Spend without reserving first? No. Unchanged from round 1. `Run` reserves before `Upload`, settles after delete confirmation, releases via defer on every failure path including the fixed receipt path.
2. AssemblyAI session left open? Not applicable. Batch API, no voice session. `Wait` stays context-bounded.
3. Private material to anyone but its owner? No new path, and the round 1 caveat is now closed: the orphaned provider copy the M finding left unreachable is nameable by the id in the error, so the explicit retry can find and delete it.
4. Edit without a revertible record? No. `Replace` unchanged.
5. Callback or count from the prompt instead of a stored row? Not applicable. No model call.
6. Code, comment, test, or migration cites a phase, task, or planning file? No. The new identifiers (`createBody`, `AudioURL`, `SpeechModels`, `Keyterms`) and the new error verb carry no plan references.
7. Rebuilt Keel or Chaaya? No. Three-file diff uses only existing Keel cost, job, id, and sqlite surfaces.
8. Review on the handed hash in its own worktree? Yes, stated at the top. `go test -race` for both packages and both round 1 pins were run here. The M mutation was run here and reverted, leaving the tree clean at the handed hash.
9. Paid job resume spending twice? No automatic path. `Kind` unchanged (limit 2, non-idempotent, no resume).
10. Development check needs a person, a microphone, or a wall-clock threshold? No. Scripted `httptest` providers, generated tone, no new sleeps or thresholds.

## Verification summary

- `go test -race ./internal/assemblyai/ ./internal/transcript/`: both `ok`.
- `go vet` on both packages: clean.
- M pin (`TestRunKeepsProviderCopyOnReceiptFailure`, now asserting `tx-9`): PASS, and its mutation FAILs as shown above.
- L pin (grep for `map[string]any` in production code): no hits.

## Verdict

APPROVE. Zero findings in this round and zero residue against round 1, severity by severity.
