# T7.25 remediation round 2

Reviewed hash: `0249ab9931c47e9d54826f4a2b9869c6c842c9e1`
Worktree: `/Users/narayan/reprise-wt/t7.25-rem-r2` (branch `task/t7.25-rem-r2`)
New hash: `390d46e0c97c9b8cd5d4678e1527070db2493da2`
Parent of the new hash is the reviewed hash. It was not amended or rebased.

Round 2 found that the late-move proofs measure the destroyed return, not the path check. The path check and `onRecordPage` are gone. The destroyed return stays. The committed tree does not keep the mutation.

`npm ci` in this worktree `web/` added 236 packages. Preview serves `web/build`.

| Finding | What changed | Mutation | Restored runs |
|---|---|---|---|
| H. Both late-move proofs stay green after the path check is removed. They measure the destroyed return. | Removed the path check and `onRecordPage` from `goProcessing`. Kept the destroyed return. | Deleted only the destroyed return, rebuilt, and ran `npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts -g "finishes after the gallery"`. Both proofs reached `/processing`. Exit 1 with 2 failed. The served handoff assigned processing and did not read the path. | Restored the destroyed return and rebuilt. Run 1 passed 2. Run 2 passed 2. The full gallery spec then passed 10. `controller-complete.spec.ts` passed 2. A move that finishes on the record page still reaches processing. |
