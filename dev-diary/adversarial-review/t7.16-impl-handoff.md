# T7.16 implementer handoff

Commit `8026d0ec5a94b07dd4b2964134ce4f4df35df200` on branch `task/t7.16`
in worktree `t7.16-impl`. Base is main at `19305b6`. Gate passes in
the worktree. Two commits: `26dac47` (views read the live season,
fixtures behind `?fixture=1`) plus `8026d0e` (prerender opt-out for
the dynamic episode route).

## Out-of-owns commit for review to rule

`8026d0e` adds `web/src/routes/episode/[id]/+page.ts` with
`prerender = false`, outside handed owns. Without it the build
fails because live-first SSR emits no episode links for the crawler.
Sibling edit and share routes carry the identical line. Widen,
fold elsewhere, or drop knowingly.

## API gaps for follow-ups, not blockers

The wired detail exposes no cover bytes, duration, render address,
chapters, or transcript words. Cards show badge, title, state,
visibility. Episode view shows metadata, outcome, proposals, quoted
moments. Playback and following transcript stay fixture-only.
Thread quote links park on the covering proposal instead of seeking.

## Notes

`JobStream.attach()` needs an effect context an awaited fetch
leaves. Controllers pass an explicit cleanup runner now. Worth a
note to the library owners. Per-card detail reads fan out one fetch
per unfinished episode.
