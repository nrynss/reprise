TestFixtureSessions now skips loudly when the start record is absent instead of
failing. Each session subtest locates its clips first, so a wrong layout still
fails before the skip check runs. When clips.json is missing, the subtest calls
t.Skipf naming the full record path and the reason (no open task owes the
record). When the record is present, the subtest measures through
measureRealSession with the same 40 ms verdict as before. measureRealSession
and readClipStarts are untouched, so a missing record still errors loudly at
the unit level and TestRealSessionVerdictPath/no-starts still proves it.

Compared with the approved flip (d90e721), this adds one stat check plus a
comment update, both in internal/align/fixture_test.go. Nothing else changed.

Pins (all run in /home/nryn/work/reprise-wt/t2.9-skip):

* Absent record skips: `go test -run TestFixtureSessions -v ./internal/align/`
  exits 0, all three subtests SKIP with `has no .../clips.json: no open task
  owes this record` visible. Locator onsets match the round-1 table
  (3.8868/11.4111, 8.0167/21.0339, 4.3068/11.2708).
* Present-but-wrong layout fails: TestRealLayoutFailsLoud (record present,
  four breakages name their file) and TestDriverFailsLoudOnWrongLayout pass,
  proving layout errors still fail instead of skipping.
* Present-and-aligned measures: TestRealSessionVerdictPath/aligned passes
  with zero drift through the exact path the suite calls.
* Prior pins green with -race: synthetic layout (aligned zero drift,
  drifted exact 400-sample drift), boundary, OGG rejection (magic still
  ErrWAV, genuine OGG zero drift), locator (both placements within one
  sample), verdict-path (aligned, 1200-sample drift exact, no-starts names
  clips.json).
* Full ./tools/check.sh exits 0 (after npm ci in the worktree web dir).
