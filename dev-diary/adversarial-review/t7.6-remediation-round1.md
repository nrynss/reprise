# T7.6 remediation round 1

Reviewed hash: `39f90382fdcbd94f9a47928f5527fbd3bb512168`
Review file: `dev-diary/adversarial-review/t7.6-round1.md`
Worktree: `/home/nryn/work/reprise-wt/t7.6-rem-r1`, branch `task/t7.6-rem-r1`
New commits on top of the reviewed hash, no rebase, no amend.

## Scope notes

The M3 fix adds `internal/store/migrations/0002_stems_pair_unique.sql` and
updates one ledger count assertion in `internal/store/store_test.go`. The
finding cites the schema path, and a landed migration cannot change in
place, so a new migration plus the count update is the smallest correct
shape. No other file outside the handed owns changed.

The M1 and M2 pins live in the new `cmd/reprise/draft_schedule_test.go`.
That file holds only tests for the two findings. The M3 pin extends
`internal/episode/complete_test.go`.

## Findings

| Severity | Where | Fix | Pin result |
|---|---|---|---|
| M | `cmd/reprise/main.go`, `unfinishedEpisode` plus `ensureTranscript` and `ensureEditorial` | Starts now stamp the episode on the job record synchronously through `stampEpisode`, while the caller still holds the schedule lock. The cover matches described jobs only. A silent job covers no episode, because a start this scheduler made always carries its episode before any later schedule lists it. A stamp failure cancels the fresh job and reports the error, so no silent job keeps running. | `TestSilentTranscriptJobNeverBlocksAnotherEpisode` failed before with scheduled false and passes now. `TestSilentEditorialJobNeverBlocksAnotherEpisode` failed before the same way and passes now. `TestTranscriptJobStillCoversItsOwnEpisode` and `TestDescribedEditorialJobStillCoversItsEpisode` pass before and after, and prove a same episode job still covers. |
| M | `cmd/reprise/main.go`, stem completion handler | The editorial ensure error now maps `job.ErrLimit` to 429 with the retry code, the same answer the transcript path gives. Every other error still answers 500. | `TestFullEditorialQueueAnswersRetry` failed before with status 500 and the internal code. It passes now with 429 and the retry code. |
| M | `internal/episode/complete.go` plus `internal/store/migrations/0002_stems_pair_unique.sql` | `linkStems` no longer reads then inserts. Each role inserts with `ON CONFLICT(episode_id, role) DO NOTHING` under the new pair index, so concurrent completions converge on one pair with no orphan rows. Reads already keep the oldest row per role. The migration dedupes first, so it applies to databases that already hold orphans. | `TestCompleteStemsConcurrentKeepsOnePair` failed before on 3 of 5 runs with 3 to 5 rows. It passes now, 10 of 10 runs with exactly one pair. |

## Validation

- `go test -race -count=1 ./cmd/reprise/ ./internal/episode/ ./internal/store/` passes twice in a row.
- New cmd pins pass three repeats under `-race` with `-count=3`.
- The M3 pin passes ten repeats under `-race` with `-count=10`.
- No flake appeared in any run here. The handoff names a flaky privacy resume test, but no test carries that name, and the closest matches passed every run.
- Gate: `npm ci` in `web/`, then `./tools/check.sh` reports green. See the commit for the exact run.
