# T7.26 round 1 review

Reviewed hash: fee8b89cb2b8dee34babc802269bd7a8c5d111b1
Worktree: /Users/narayan/reprise-wt/t7.26-rev-r1 (detached HEAD)
Base for diff: 19630759e1a3518a6db3d7383665624c51b19291 (main has since moved to 9db88d9 via sibling T7.27 work, ignored per hand)

Confirm lines run in this worktree before review:

- `git rev-parse --show-toplevel` gave /Users/narayan/reprise-wt/t7.26-rev-r1.
- `git log -1 --format=%H` gave fee8b89cb2b8dee34babc802269bd7a8c5d111b1, equal to the handed hash.
- `git status --short` was empty.
- Diff scope was `git diff 19630759e1a3518a6db3d7383665624c51b19291..fee8b89cb2b8dee34babc802269bd7a8c5d111b1`, two files: cmd/reprise/main.go and cmd/reprise/pcm_stems_test.go. Nothing else reviewed. No commits made here.

## Ten questions

1. Does any path spend money without reserving budget first? No. The diff touches upload allowlist, WAV heading, stem completion, and edit inputs. No spend, budget, or provider call path changes.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. No AssemblyAI code touched.
3. Can a private episode, stem or transcript reach anyone but its owner? No. Stems persist with mediastore.Private. Reads stay owner scoped through linkedStemFiles and the confined media root. Full package suite passes, including existing privacy tests.
4. Does any edit apply without a revertible record? No. No edit path touched.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Duration comes from ffmpeg probes of headed bytes. Rate comes from the stored stem row.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep for T7.26, P7, adversarial-review, section marks, and plan filenames in cmd/reprise/main.go and cmd/reprise/pcm_stems_test.go is empty.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses keel mediastore, upload, sqlite, and ffmpeg seams. The WAV header builder is product specific (browser PCM plus stored stem rate) and belongs here.
8. Was this review run on the handed hash, in a worktree of its own? Yes, confirmed above.
9. Can any paid job kind resume after a restart and spend twice? No. No job kind touched. Completion still schedules exactly one transcript pass (pin asserts transcriptJobsFor equals 1).
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Pins synthesize sine PCM in memory and assert sample math plus probe durations with 0.001 tolerance on fixed inputs. No device or wall clock.

## What the commit does

Adds audio/pcm to the closed mediaContentTypes set. Adds raw s16le mono handling: isWAVDocument sniff, wavFromMonoPCM 44 byte header builder from the stored stem rate with empty, odd length, bad rate, and oversize refusals via errRawStemEmpty and errStemRateInvalid, and ensureWAVInPlace rewrite that leaves headed WAV untouched. Wires three converging paths: eager convertRawStems after CompleteStems link in stemsComplete.ServeHTTP, ensureWAVInPlace inside locateStems for render, and headedStemAudio plus probeHeadedAudio temp file in editInputs for crash convergence. Adds cmd/reprise/pcm_stems_test.go with four pins.

## Pins (commands plus outputs, all in this worktree)

- `go test -race -count=1 -run 'TestUploadOpenAcceptsPCMAndRefusesUnknown|TestWAVHeaderCarriesStoredRate|TestPCMStemsCompleteLinksToDraft|TestStoredPCMDurationReadsStoredRate' ./cmd/reprise/` gave ok in 1.959s, exit 0.
- `go test -race -count=1 ./cmd/reprise/` gave ok in 3.516s, exit 0.
- `go vet ./cmd/reprise/` gave no output, exit 0.
- `gofmt -l cmd/reprise/` gave no output.
- tools/check.sh was not run: it requires ffmpeg 9.0.1 and this box carries 9.0.2. Stated skew per hand. The package tests above exercise ffprobe through ffmpeg.Duration and pass on 9.0.2.

What each pin measures:

- TestUploadOpenAcceptsPCMAndRefusesUnknown posts a real chunked upload as audio/pcm through the real handler and reads the exact bytes back, then completes an audio/x-unknown take and requires 415 with unsupported_type. This proves the allowlist change and that refusal still lands at complete, not open.
- TestWAVHeaderCarriesStoredRate pins every header field (RIFF, WAVE, fmt, data, PCM 1, mono, rate, byteRate 2 times rate, blockAlign 2, 16 bit, data length) and the empty, odd, and zero rate refusals via sentinels.
- TestPCMStemsCompleteLinksToDraft persists both stems as raw PCM, posts the completion, requires the draft move with one scheduled pass, then reads the stored files (header present) and probes 1s duration at the stored rate.
- TestStoredPCMDurationReadsStoredRate stores raw takes at 16000 hertz, reads editInputs, requires duration equals samples over stored rate, requires headed bytes for the passes, and requires the in place convert plus probe agreement.

## Mutation (load bearing proof)

Edit: removed the single `\t"audio/pcm",` line from mediaContentTypes in cmd/reprise/main.go (one line deletion).

Result: `go test -race -count=1 -run 'TestUploadOpenAcceptsPCMAndRefusesUnknown' ./cmd/reprise/` failed as expected with `complete status = 415, want 201: {"error":{"code":"unsupported_type",...}}` at pcm_stems_test.go:196.

Restore: `git checkout -- cmd/reprise/main.go`, then `git status --short` empty and `git log -1 --format=%H` still fee8b89cb2b8dee34babc802269bd7a8c5d111b1. Re-ran the PCM header plus upload pins, ok.

## Findings

No in-scope findings. No out-of-scope findings.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| None | None | None | None | None |

Notes on things checked and judged not findings:

- New file cmd/reprise/pcm_stems_test.go sits beside owns entry cmd/reprise/main.go. Same package directory, required by the Done criteria pins, with T7.23 precedent approved. Judged not a defect, so no OUT_OF_SCOPE row per the hand rule.
- convertRawStems logs and continues on failure instead of failing the completion. The read paths (locateStems, editInputs) still head on the fly, so this converges rather than loses work. Not a defect.
- probeHeadedAudio error text says "probe user stem" though the helper is generic. Its only call site probes the user stem, so the text is accurate in context. Not a defect.

## Counts

- In scope: C 0, H 0, M 0, L 0.
- Out of scope: C 0, H 0, M 0, L 0.
- Total findings: 0.

## Residue

Round 1 has no prior rounds. Nothing to carry.

## Verdict

APPROVE with zero findings across all severities.
