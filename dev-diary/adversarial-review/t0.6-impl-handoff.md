# T0.6 implementer handoff

Commit `53656ca6697e9795db604cb6d669329f3e7e6c36` on branch
`task/t0.6`. Worktree `/home/nryn/work/reprise-wt/t0.6-impl` is clean.
Only `testdata/speech/` changed. No other file moved.

## What was pinned

The fixed `generate.sh` names all three pins inline.

- Base image by digest:
  `ubuntu:24.04@sha256:b3cc40b72b93588182b5410f723c7aaf142363311c2aa993d8a453ddcbb3ae15`.
- Synthesizer by package version:
  `espeak-ng=1.51+dfsg-12build1`, installed with that exact version.
- Resampler by digest:
  `mwader/static-ffmpeg:9.0.1@sha256:54e55b0cb8f672870fc38ceb2e6c411855cb3b39c505f5f3b2505ee01ed5f2b7`.
  This digest matches the digest the image build and the gate pin.

Synthesis writes wav files directly with `espeak-ng -v en-us -s 160 -w`.
Conversion runs in the media image with
`-ar 24000 -ac 1 -f s16le -acodec pcm_s16le`, which builds the 24 kHz
mono 16 bit PCM shape the probe streams. One run writes all four files.

## Byte comparison result

Regenerated PCM matches the committed streamed bytes exactly.

- `exchange-24k.pcm`: `cmp` identical against the committed copy.
  Hash `9241617ab0c52e5afd4a702516b437183d1bb6d03d3566e5cdfe1726a84f27f5`.
- `keyterm-24k.pcm`: `cmp` identical against the committed copy.
  Hash `3fd117e4d2968ff546668e119c320c4aa804acef2f9b24753ba1e6cd46fa6fc1`.

The PCM files are therefore unchanged in the commit. The wav files carry
valid headers now, with audio payloads identical to the committed ones.

- `exchange.wav`: header now declares RIFF size 157704 and data size
  157668 against real size 157712. Audio payload identical. Full file
  differs only in the patched header. New hash
  `fabab6e47603c5d6b5b239d057eb338a020035ddf353dd7ba9357cc5f724a5b9`.
- `keyterm.wav`: header now declares RIFF size 175338 and data size
  175302 against real size 175346. Audio payload identical. Full file
  differs only in the patched header. New hash
  `3e3644536eb47ac6c2f34db2e07fca482b0f5e408d996202b8ed4c0cc5bc9bcb`.

No record update is needed. Sizes and durations stand.

## Double run hashes

Two consecutive `generate.sh` runs produce identical bytes.

- Run 1 and run 2 share these hashes for all four files:
  `9241617ab0c52e5afd4a702516b437183d1bb6d03d3566e5cdfe1726a84f27f5`
  (exchange PCM),
  `fabab6e47603c5d6b5b239d057eb338a020035ddf353dd7ba9357cc5f724a5b9`
  (exchange wav),
  `3fd117e4d2968ff546668e119c320c4aa804acef2f9b24753ba1e6cd46fa6fc1`
  (keyterm PCM),
  `3e3644536eb47ac6c2f34db2e07fca482b0f5e408d996202b8ed4c0cc5bc9bcb`
  (keyterm wav).

## FFprobe evidence

Both wav files read with no warning and durations matching the record.

- `exchange.wav`: `pcm_s16le, 22050 Hz, 1 channel, Duration 00:00:03.58`.
  No `Packet corrupt` and no bitrate estimation warning.
- `keyterm.wav`: `pcm_s16le, 22050 Hz, 1 channel, Duration 00:00:03.98`.
  No `Packet corrupt` and no bitrate estimation warning.

## Gate result

- `bash -n` passes on the new script.
- `shellcheck` is absent on this machine. It was not installed, per
  instructions. The script was syntax checked with `bash -n` instead.
- `npm ci` in `web/`, then `./tools/check.sh`: exit 0, OK all checks
  passed. No consumer names hit appeared in this worktree. The known
  tracked mockup hit from other worktrees did not fire here.

## Sibling note

The T0.5 remediation asked whether this pattern covers its session wavs.
It does. The six session wavs carry the same placeholder sizes from the
same standard output cause, with intact audio, so `espeak-ng -w` fixes
them the same way. The sibling should apply this pinning pattern to its
own `recreate.sh`. Exact pin lines were shared over IRC during the run.
