# T3.5 round 2 review

Reviewed hash: `8998a3201944b1bb588420ea69d0423746afb2a3` (one commit on reviewed `1bdfbbc`, no rebase, no amend).
Worktree: `/home/nryn/work/reprise-wt/t3.5-rev-r2` (detached HEAD, confirmed `git log -1 --format=%H`).
Scope: `git diff 1bdfbbc..8998a32 --stat` shows `internal/analysis/chapters.go` plus `internal/analysis/chapters_test.go`. Both sit inside the task owns. Owns respected.
Validation: `go test -race -count=1 ./internal/analysis/` passes, `go vet` clean, `gofmt -l` clean, working tree clean after probes. No commit made. Working tree only reviewed.

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0. OUT_OF_SCOPE 0.

## Residue against round 1, severity by severity

Round 1 held one open finding (H, `splitLongest` emitting sub-10-second chapters) and zero findings at C, M, and L.

* H: zero residue. The single H is fixed. `middleBoundary` (`chapters.go:241`) now skips every word start that leaves either half under `MinChapterMs`, and the nearest to middle survivor wins. The guard keeps BOTH halves at or above the floor, not just the emitted one. When no start satisfies the floor, the split fails and `ensureCount` keeps fewer chapters, so short episode behavior stays intact. Re-ran the round 1 pin shape in this worktree (32 s single draft over `scheduleWords(40, 800)`): result is two chapters at 0 and 16000, each 16 s, with the `(continued)` mark. No chapter under 10 s. Green.
* C: zero residue. Round 1 had none, and this round finds none.
* M: zero residue. Round 1 had none, and this round finds none.
* L: zero residue. Round 1 had none, and this round finds none.

## New findings

None. The remediation diff touches only the guard, its comments, and two tests. Specific checks run:

* Exact floor boundary: the guard skips only halves strictly under the floor (`< MinChapterMs`), so an exactly 10 s half still splits. No off by one.
* Trailing stub drop: `dropIndex` is untouched, and `TestNormalizeDropsTrailingSliver` passes unchanged.
* Merge then split order and the `(continued)` marking: unchanged in `NormalizeChapters` and `ensureCount`. The 32 s probe result carries the continued mark on the second chapter.
* A 30 s single draft episode keeps two floor valid chapters instead of three short ones. Three is genuinely infeasible there: neither 15 s half can split into two 10 s halves, so keeping fewer is the specified outcome, not a defect.
* A 44.5 s single draft episode still reaches three floor valid chapters (`TestR2LongReachesThree` probe, green), so the guard does not over suppress splitting.
* Mutation is load bearing: deleting the two line guard in `middleBoundary` returns the code to the round 1 shape, and the new `TestNormalizeSplitFloorKeepsTenSeconds` (which demands two chapters) fails against three 8 s chapters. The pin measures `NormalizeChapters` output durations, not a log line.
* Scrutiny rulings (a) through (i) from round 1 stand unchanged. Nothing in this diff touches the scripted doubles, the adapter contract, `OffsetOf`, the replant warning, pricing, receipts, `Kind` flags, or style. No plan refs in tracked source (grep clean).

## The ten questions

1. Does any path spend money without reserving budget first? No. `Run` reserves before upload. Unchanged by this diff.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. Batch only, no session.
3. Can a private episode, stem or transcript reach anyone but its owner? No. All rows stay owner and episode scoped. No serving path here.
4. Does any edit apply without a revertible record? No. Replace semantics on explicit retry, disclosed in the handoff and the `StoreAnalysis` comment.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Mentions carry offsets and quotes from stored entities and phrases. The prompt only phrases chapters.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep over `internal/analysis/` is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses `keel/cost`, `keel/job`, `keel/id`, and the diary store.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Hash `8998a32` confirmed via `git log -1`, detached worktree, no commits, pins run inside it.
9. Can any paid job kind resume after a restart and spend twice? No. Kind is not idempotent with no resume path, pinned by `TestKindNeverResumes`. Unchanged.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Scripted doubles plus fixture bytes, millisecond poll interval. All pins here assert output durations against fixed inputs.

## Verdict

APPROVE. Zero findings across all severities, with explicit zero residue against the round 1 H. The split floor guard is correct on both halves, short episodes keep fewer chapters, and the full package suite passes with race.
