# T1.6a round 1 review

Hash reviewed: `99638ba356355643fc09a855d75ea49177288c31`.
Worktree: `/home/nryn/work/reprise-wt/t1.6a-rev-r1`, detached at the handed hash.
I confirmed the hash with `git log -1` before reviewing.
Base: `fd9f5319d973dcbe4d982f0ae70aebb44468e860` (current main).
Diff under review: `git diff fd9f5319d973dcbe4d982f0ae70aebb44468e860..99638ba356355643fc09a855d75ea49177288c31`.
Round 1 is the first round for T1.6a, so there are no prior rounds and no
residue to claim against. Baseline-clean statement: nothing to carry over.

## Scope

The diff touches exactly one file: `internal/settings/settings_test.go`.
I verified the name list with `git diff --name-only` on the range above.
That path equals the T1.6a `owns` line. No runtime file, TOML file, or
module file moved. Nothing else changed.

I read the owned test file whole (all 538 lines), plus AGENTS.md (binding),
the T1.6a block in `dev-diary/PHASE-1-foundations.md`, the implementer
handoff (`t1.6a-impl-handoff.md`), and both round 2 findings in
`t1.6-round2.md` that this task closes.

## What the commit does

`TestShippedFilesAgreeOnSecretNames` now opens both shipped files
(`config/reprise.local.toml` and `config/reprise.box.toml`). For each it
asserts the three secret blocks name `ASSEMBLYAI_API_KEY`,
`SESSION_SIGNING_KEY`, and a `file`-source credential with the shipped key
path; it cross-checks both env var names against `.env.example` in both
directions; it rewrites both temp paths the same way and loads each file,
revealing the credential. `TestBootLogShowsPlanWithoutValues` now splits
the plan into tab fields, finds the `secrets.gemini_credential` row, and
asserts source `file` with the key path and status `resolved`, keeping the
leak probe over every dummy value.

Documentation style holds. Grep over the test file for task ids, phase
ids, and planning file names returns empty. No secret values appear: the
only secret-looking strings are the `dummy-` test constants and env var
names used as failure-message pins.

## Done-when probes run independently at the reviewed hash

Scoped suite uncached first:
`go clean -testcache && go test -race -count=1 ./internal/settings/...`
passes (`ok ... 3.069s`).

### Probe 1: box-only rename fails the agreement test

I copied nothing; I edited the worktree's `config/reprise.box.toml`
in place, renaming `var = "SESSION_SIGNING_KEY"` to
`var = "SESSION_SIGNING_KEY_DRIFT"`, and ran the agreement test alone.
It failed with:

`settings_test.go:493: config/reprise.box.toml lacks "var = \"SESSION_SIGNING_KEY\""`

The failure names the file and the missing pin. I reverted the file with
the saved copy and confirmed `git status --porcelain` clean.

### Probe 2: re-pointed credential source fails the boot log test

I wrote a temporary probe test (untracked, removed after) that builds a
config with the credential on `source = "env_file"` with
`var = "GEMINI_CREDENTIAL"`, adds that var to the temp env file so the
helper exits 0, and then applies the reviewed test's own row assertions.
A first attempt without the env var exited 1 before reaching the pin, so
I re-planted faithfully: helper exit 0, output still holding the bare
string `file` (the old thin assertion would pass), and the new assertion
failed with:

`credential source = "env_file", want "file"`

The failure names the defect. I removed the probe file, reverted nothing
else, confirmed the tree clean, and confirmed green again with the
uncached race suite.

## Live probes (parent-authorized)

The AssemblyAI key is live in `/home/nryn/work/reprise/.env` (mode 600,
confirmed before use). I read it through the settings loader only and
never printed it. I listed env var names only, never values. Budget caps
hold: no network call was made, no spend occurred.

I built the settings test binary once with `go test -c` and ran it as
the plan helper against the real local config
(`REPRISE_CONFIG=config/reprise.local.toml`). Exit 0. Every row carries
4 tab fields. The credential row reads `secrets.gemini_credential`,
source `file`, the key path, status `resolved`. Both env rows read
`env_file` with their var names, status `resolved`. No secret value
appears in the output. I removed the helper binary after.

## Gate

I ran `npm ci` in `web/`, then `./tools/check.sh` in the review worktree.
Exit 0: `OK all checks passed`. Every Go stage, every web stage, the
svelte leakage scan, the consumer-names scan, and the plan-reference scan
pass.

Known-condition note: the consumer-names word from round 2
(`dev-diary/mockup/V1 Reprise-Interactive Prototype.html:11`) is present
in this worktree and tracked at base too (confirmed via
`git show fd9f531...`), but the gate exempts `dev-diary/` by path, so the
scan passes. Recorded here as OUT_OF_SCOPE per check.sh path exemption;
it is not a finding against this task.

## Ten review questions

1. Does any path spend money without reserving budget first. No. The
   commit changes only tests. Probes ran the loader in plan/load modes
   only, with no network calls.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap. No. No session code is touched.
3. Can a private episode or stem or transcript reach anyone but its
   owner. No. No tracked file holds a secret value. The live plan output
   names sources and paths only. Dummy values stay in temp files.
4. Does any edit apply without a revertible record. No. The change is a
   tracked commit on the task branch. Both plants were reverted; every
   probe file was removed; the tree is clean.
5. Does any spoken callback or count come from the prompt instead of a
   stored row. No. No spoken content is touched.
6. Does any code or comment or test or migration cite a phase, a task,
   or a planning file. No. Grep over the owned test file for task ids,
   phase ids, and planning file names returns empty, and the gate plan
   scan passes.
7. Did this task rebuild something Keel or Chaaya provides. No. Loading
   and the plan come from existing code; this commit adds only test pins.
8. Was this review run on the handed hash in a worktree of its own.
   Yes. `git log -1` showed `99638ba356355643fc09a855d75ea49177288c31`.
   The worktree is detached `t1.6a-rev-r1`. Every pin above ran there.
9. Can any paid job kind resume after a restart and spend twice. No. No
   job code is touched.
10. Does any development check need a person or a microphone or a
    wall-clock threshold. No. Tests use temp files, a helper process,
    exact string checks, and exit codes.

## Findings

In-scope severity counts: C 0, H 0, M 0, L 0. Zero in-scope findings, so
no finding rows follow. One out-of-scope observation follows. It does not
affect the verdict.

### Out of scope observation

Severity: L.
Where: `dev-diary/mockup/V1 Reprise-Interactive Prototype.html` line 11
at the reviewed hash. Owning path: `dev-diary/mockup/`, outside T1.6a
ownership.
What: The line holds the word `hackathon`. The gate exempts `dev-diary/`
by path, so the consumer-names scan passes here; at base the same line is
present.
Pin: `git grep -InE 'hackathon|lablab' -- dev-diary/mockup/` reports that
line in this worktree; `git show` of the base file reports the same line.
Mutation: add the word `hackathon` to line 11 of that mockup file and the
raw grep hits again; remove it and the grep goes quiet.

## Verdict

APPROVE. The strengthened pins close both round 2 findings: a box-only
rename fails the agreement test naming the file and pin, and a
re-pointed credential source fails the boot log test naming the source
defect. Scoped suite passes uncached with race detection, and the full
gate passes in the review worktree. I committed nothing. Plants reverted,
probes removed, tree clean at the end.
