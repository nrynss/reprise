# T7.34 review, round 3

- **Hash reviewed:** `ee572c9ee7dc3bc3d2647e23fb221e38337dff4a` (confirmed with `git log -1`)
- **Base:** `68039d0`. Round 1 reviewed `13248a4`. Round 2 reviewed `ef473d8`.
- **Worktree:** `/Users/narayan/reprise-wt/t7.34-rev-r3`, detached at the hash
- **New commit:** `git diff ef473d8..ee572c9` touches `web/src/lib/editor/draft.ts` only. It adds a
  `CutProposals` type of `Record<string, string | undefined>` and an `emptyCutProposals()` helper.
  All three creation sites now call the helper.
- **Task diff:** `git diff 68039d0..ee572c9`, 2 files, 67 insertions and 6 deletions. Both paths sit
  in the `owns` line.
- **Probes:** `/Users/narayan/reprise/dev-diary/probes/t7.34-rev-r3/residue.sh`, run as
  `bash residue.sh <worktree>`. It reruns every prior pin, applies each mutation, and reverts.

## Residue against round 1

Finding 1 (L), inherited keys read through the prototype.

- The round 1 pin `probes/t7.34-rev-r1/draft.prototype.test.ts` is byte identical to the committed
  `draft.prototype-keys.test.ts`. Copied in and run, it passes 5 of 5.
- The round 2 stored-keys probe passes 4 of 4.
- Mutation: `emptyCutProposals` returns `{} as CutProposals`, one line at `draft.ts:250`. That one
  line now covers all three creation sites. The pin fails 4 of 5, on `constructor`, `toString`,
  `__proto__` and `hasOwnProperty`. The stored-keys probe fails on `__proto__`.
- Reverted with `git checkout`. Both pass again.

**Residue against round 1: zero at C, zero at H, zero at M, zero at L.** The "keeps the same
behaviour" sentence in the `13248a4` message is now true of the cumulative change.

## Residue against round 2

- **Finding 1 (L), the record type claimed every id is backed.** The pin
  `probes/t7.34-rev-r2/draft.lookup-type.ts` makes `tsc --noEmit` exit 0 at `ee572c9`. Mutation:
  `type CutProposals = Record<string, string>` at `draft.ts:245`, one line. tsc exits 2 with TS2322
  on the probe. Reverted, tsc exits 0. Closed.
- **Finding 2 (L), a 33 word sentence in the `ef473d8` message.** The pin
  `probes/t7.34-rev-r2/sentence-length.py` still exits 1 on `ef473d8`, and exits 0 on `13248a4` and
  `ee572c9`. No role may amend a reviewed commit, so no code change can close it before landing. The
  orchestrator's plan to reword that message when it rewrites the commit at landing closes it. I
  agree with the plan. The orchestrator should run the same probe on every landed commit and see
  exit 0. It does not block this verdict.
- **Finding 3 (L), the test file sat outside `owns`.** The T7.34 `owns` line now names
  `web/src/lib/editor/draft.prototype-keys.test.ts`. The pin, `git diff --name-only 68039d0..ee572c9`
  filtered against the live `owns` line, prints nothing and exits 1. Mutation: drop the test path
  from the `owns` list. The pin prints the test path and exits 0. Closed.

**Residue against round 2: zero at C, zero at H, zero at M. At L, zero in the tree.** Finding 2
lives only in the `ef473d8` message and closes at landing, as above.

## Behaviour against the old Map

`ee572c9` changes types and moves `Object.create(null)` into one helper. The helper returns a fresh
null-prototype object on every call, exactly as each inline call did. It emits no new runtime logic.

| Site | Base `68039d0` | `ee572c9` | Same? |
|---|---|---|---|
| Field init, `draft.ts:265` | `new Map()` | `emptyCutProposals()` | Yes |
| Fixture reset, `:302` | `new Map()` | `emptyCutProposals()` | Yes |
| Live build, `:341` | `new Map()` | `emptyCutProposals()` | Yes |
| Write, `:350` | `.set(id, id)`, last wins | `[id] = id`, last wins | Yes, `__proto__` included |
| Swap, `:381` | assigns the new Map | assigns the new record | Yes |
| Read, `:519` | `.get(id) ?? ''` | `[id] ?? ''` | Yes, inherited names included |

Nothing under `web/` iterates, sizes, spreads or serialises `cutProposals`. Runtime behaviour matches
the Map.

## Types

- `CutProposals` is `Record<string, string | undefined>`. A read at `:519` types as
  `string | undefined`, as `Map.get` did, and the `?? ''` fallback is live to the compiler again.
- `Object.create(null)` returns `any`. The one `as CutProposals` cast sits inside the helper. An
  empty object satisfies the type, so the cast is sound. Every creation site now receives a typed
  value, not `any`.
- The write at `:350` assigns a `string` into `string | undefined`. That is fine.

The types are honest.

## Style

- The two new comments at `draft.ts:243` and `:247` run 11, 9 and 19 words per sentence. They have
  no semicolons and no em dashes. Each states its reason: the `undefined` matches a Map miss, and the
  null prototype keeps inherited keys out.
- The `ee572c9` message has two body sentences of 14 and 13 words. It has no semicolons, no em
  dashes, and no task id. The sentence-length probe exits 0 on it.
- No task id, phase id or planning file appears in code, test names or any of the three messages.

## Gate

`npm ci` in `web/` exited 0. Every step of `tools/check.sh` after its ffmpeg check ran verbatim,
from the worktree root, through `probes/t7.34-rev-r2/gate.sh`. I diffed that runner against
`tools/check.sh` and it matches step for step. It adds one extra `go test -race ./...`. The ffmpeg
9.0.2 against 9.0.1 mismatch is accepted by the owner, and that check was not run.

| Step | Exit |
|---|---|
| `gofmt -l cmd` | 0, empty output |
| `go list ./... \| grep -v web/` | 0 |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0, 22 packages ok |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0, 322 files, 0 errors, 0 warnings |
| `npm run lint` | 0 |
| `npm run test` | 0, 224 of 224 in 22 files |
| `npm run build` | 0 |
| `npm run test:e2e` | 0, 4 passed |
| svelte 4 leakage `git grep` | empty (raw exit 1, no match) |
| consumer names `git grep` | empty (raw exit 1, no match) |
| plan references `git grep` | empty (raw exit 1, no match) |
| `go test -race ./...` | 0 |

All three scans are empty. The done-when criterion holds. `git status --short` was clean after the
gate and after the residue probe.

## The ten questions

1. **Spend without reserving budget?** No. The change touches no paid path.
2. **AssemblyAI session left open?** No. No session code changed.
3. **Private episode, stem or transcript leak?** No. The change is a client lookup table.
4. **Edit applies without a revertible record?** No. A revert still writes its decision row before
   posting. The residue probes show every backed id posts its own proposal id, and every unbacked id
   posts nothing.
5. **Spoken callback or count from the prompt?** No. Untouched.
6. **Code, comment, test or migration cites the plan?** No. The plan references scan is empty, and no
   commit message names a task.
7. **Rebuilt something Keel or Chaaya provides?** No.
8. **Review run on the handed hash in its own worktree?** Yes. `ee572c9` in
   `/Users/narayan/reprise-wt/t7.34-rev-r3`. Probes ran there and were removed.
9. **Paid job kind resumes and spends twice?** No. Untouched.
10. **Development check needs a person, a microphone or a wall-clock threshold?** No. The committed
    test and the probes use stubbed fetch and the compiler.

## Findings

None.

## Counts

| Severity | Count |
|---|---|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |
| OUT_OF_SCOPE | 0 |

## Verdict

**APPROVE.** Zero residue against rounds 1 and 2 in the tree, at every severity. Runtime behaviour
matches the old Map, the types are honest, and the gate and all three scans are clean. Round 2
finding 2 closes when the orchestrator rewords the `ef473d8` message at landing. The orchestrator
should confirm that with `sentence-length.py` on the landed commits.
