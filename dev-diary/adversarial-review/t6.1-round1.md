# T6.1 round 1 review

Reviewed hash: `eda79cd5b67ccd7d509b87836547c59cb705b8c3`
Worktree: `/home/nryn/work/reprise-wt/t6.1-rev-r1` (detached HEAD, confirmed with `git log -1`).
Base for diff: `d975518`. No commit was made from this worktree.
Verdict: **REMEDIATE**

Counts: H 1, M 0, L 2 in scope. Out of scope rows: 1 (H, non blocking).

## What was checked

Diff covers `deploy/` (new `run.sh`, `redeploy.sh`, `backup.sh`, `README.md`),
`cmd/reprise/main.go` plus `main_test.go`, and `Dockerfile`. All sit inside
the declared owns lines.

`go test -race ./cmd/reprise/` passes. `go vet` on the touched packages
passes. `docker build` with a VERSION stamp succeeds on a live daemon.
The image runs `ffmpeg` and `ffprobe` 7.1 and runs as user `nonroot`.
The built binary was booted with a probe config. The log shows every source
as resolved with no value, `grep` finds no secret value in the log,
`/healthz` answers `ok <boot> version=dev`, and unknown paths serve the
shell with 200. A missing secret stops the process before it listens.
The full `./tools/check.sh` web stages were not rerun here. The diff
touches no `web/` file, and the passing `docker build` already compiled
the web stage.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `deploy/run.sh:165` (doc at line 18) | The script removes the running container with `docker rm -f`, which kills with SIGKILL. The SIGTERM drain in `main.go` never runs on this path. Every `redeploy.sh` run therefore cuts open sessions instead of letting them finish, and `--stop-timeout 1830` never takes effect. This contradicts the done criterion that a redeploy during a session lets it finish. | Probe with a SIGTERM trap writing to a bind mount. After `docker rm -f` the trap file is absent. After `docker stop` on the same image the trap file is present. The first command honors no stop timeout. The second one does. | Restore `docker rm -f "${NAME}"` in place of a stop then remove sequence. |
| L | `deploy/redeploy.sh:111` | The build match passes when the reported version is empty and an expected SHA is set. The reverse pattern becomes a bare star and matches anything. A versionless `ok ` responder would clear a pinned gate. | `HEALTH_VERSION="" EXPECTED_SHA="abc1234"` through the line 111 test prints `GATE_PASSES_WITH_EMPTY_VERSION`. A guard requiring a nonempty reported version flips it to reject. | Drop the nonempty guard and keep the two sided prefix match. |
| L | `deploy/README.md:1`, `deploy/backup.sh:3`, `deploy/redeploy.sh:3` | Three new tracked lines use em dashes. The documentation style forbids them in markdown and in comments. The base tree has zero em dashes in tracked source, so this commit introduces the only violations. | `git grep -n` for the em dash over `deploy/`, `Dockerfile` and `cmd/reprise/` returns exactly these three lines and nothing else. | Replace the hyphen in any of the three headers with an em dash. |

## Out of scope

| Severity | Where | What | Pin | Mutation | Owner |
|---|---|---|---|---|---|
| H | `.github/workflows/ci.yml`, `Install pinned ffmpeg and ffprobe` step | The pinned binary archive URL is dead upstream. CI fails at its media install step today, so no CI run can reach the gate. This file sits outside the task owns lines. It never blocks APPROVE. | `curl -fsSL https://github.com/mwader/static-ffmpeg/releases/download/v7.1/ffmpeg-linux-amd64` answers HTTP 404. The Dockerfile in this task already routes around the same outage by copying from the pinned image. | Repoint the install step at the dead archive URL. | T0.1, which owns `.github/workflows/ci.yml` and is done. |

## Scrutinized items from the handoff

Nil registry. `main` passes literal `nil` for the `sessions` interface.
`waitSessions` returns at once on a nil registry and `run` never
dereferences it elsewhere. `TestWaitSessionsReturnsAtOnceOnNil` pins this.
No nil dereference path exists. The lease manager carries `Active() int`,
so wiring `Leases()` later needs no interface change.

Health format. `/healthz` still serves GET with 200 and the `ok ` prefix,
and the shell fallback is untouched with passing tests. The `version=`
suffix extends the body without breaking prefix consumers. The redeploy
gate matches `ok *` and parses the suffix. The T0.1 contract still holds.

Limits. `--memory 1g --cpus 1.0` with `--memory-swap 1g` leaves headroom
on a box with about 2.9 GiB free and seven containers. The box file sets
render concurrency to one. No measurement proves the render fits, but no
evidence shows it does not. Overrides exist for both knobs. Observation
only, no finding.

Backup filename. The store opens an already open database and names no
file yet. The `reprise.db` default is provisional and documented, and
`backup.sh` fails loudly when the file is absent. `DB_FILE` overrides it.
The host path and the container path agree through the bind mount. No
silent mismatch exists.

Drain gate. Only `POST /api/sessions` gets the 503 with code
`server_draining` through the shared envelope. Health and all other
traffic pass through. That matches the spec, which closes only new
sessions. Tests pin the refusal, the envelope code, and the passthrough.

Redeploy shape except the H above. Container IP gating bypasses the edge
exactly as the box runbook demands. The expected build check and the
root 200 check both run before any prune. Rollback restores the recorded
image and exits 1. The version match hole is the L above.

Open items. Item 1 is the out of scope CI row above. Items 2 through 6
need the box, a workstation, DNS, or cron. None can run in this worktree.
Each one names its exact command and its expected output.

Secrets. `deploy/` and `Dockerfile` carry no secret value. The only
`--env` flag sets `REPRISE_CONFIG`, which is a path. The preflight
sources the env file in a subshell and reports missing names only.
`--stop-timeout 1830` stays above `session_max_seconds 1800` with a 30
second margin that matches the HTTP close grace.

## The ten questions

1. Does any path spend money without reserving budget first. No. This
   task adds no paid path. Boot, drain, scripts, and health move no spend.
2. Can any path leave a provider session open without `session.end` or a
   token cap. No. The drain bounds every wait by the session cap. The H
   above ends sessions too abruptly, never too late.
3. Can a private episode, stem or transcript reach anyone but its owner.
   No. No media, auth, or serving path changed.
4. Does any edit apply without a revertible record. No. Nothing here
   edits episodes. Container replacement is covered by image rollback.
5. Does any spoken callback or count come from the prompt instead of a
   stored row. No. No prompt or count code changed.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file. No. A pattern scan over the touched paths returns zero
   matches.
7. Did this task rebuild something Keel or Chaaya provides. No. It wires
   `keel/wire`, `keel/id`, and the lease manager through a one method
   interface, and it copies pinned media binaries instead of vendoring
   them.
8. Was this review run on the handed hash, in a worktree of its own. Yes.
   `git log -1` matches the handed hash. The worktree is detached. No
   commit was made and no other orchestrator file was touched.
9. Can any paid job kind resume after a restart and spend twice. No. No
   job kind changed.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold. No. Tests use timeouts only as liveness bounds.
    Probes used local binaries, files, and containers.

## Verdict

REMEDIATE. One H and two L findings stay open. The H breaks a done
criterion on the only redeploy path, so it cannot wait. The two L rows
are small and precise. Zero residue is not claimed.
