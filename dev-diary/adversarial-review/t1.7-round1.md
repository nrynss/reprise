# T1.7 round 1 review

Reviewed hash: `224e21a4a729d3c791dc0b81d96eb68069f85db4` (two commits on claim base `08e8420`).
Worktree: `/home/nryn/work/reprise-wt/t1.7-rev-r1` (detached HEAD, confirmed `git log -1 --format=%H` equals the handed hash).
Diff read: `git diff 08e8420..224e21a` (7 files, 686 insertions, 3 deletions).
This review ran on the handed hash only. Nothing was committed. The working tree was never reviewed.

Verdict: **APPROVE** with zero in-scope findings.

Counts: C 0, H 0, M 0, L 0. Out-of-scope rows: 2 (never blocking).

## What was verified by execution

* Full gate in this worktree after `npm ci` in `web/`: `./tools/check.sh` ends `OK all checks passed` (gofmt, vet, staticcheck, `go test -race` over every package, svelte-kit sync, svelte-check, eslint, vitest, build, playwright, svelte-4 leakage, consumer names, plan references).
* `go test -race` re-run independently for `internal/api`, `cmd/reprise`, `internal/broker`, `internal/limits`, `internal/host`, `internal/identity`: all pass.
* Pins re-run, not just read: owner-ceiling mismatch refuses wiring (`TestMountRefusesUnequalOwnerLimits`, both Sessions and Admin variants); gate plus middleware required (`TestMountNeedsGateAndIdentity`); every mounted route reaches its handler exactly once with the middleware mark (`TestMountServesHandlersThroughChain`, 7 route cases); SSE content type (`TestMountStreamsJobEvents`); stubs stay stubs without touching the chain (`TestMountKeepsStubsForUnmountedRoutes`); gate refusal reaches neither middleware nor handler (`TestMountGateRefusesBeforeMiddleware`); golden decode for the new table plus byte-identical stub refusal (`TestGoldenRouteTable`, `TestGoldenStubRefusal`, `types.test.ts` mirror and admin-pattern tests); cents to nanodollars conversion with negative and overflow refusals (`TestSpendCeilingConvertsCents`).

## Scrutiny answers (from the handoff brief)

(a) Middleware order. Task text governs and says gate outermost. The broker's own `Route` comment (`internal/broker/broker.go:289`) also says gate outermost. Only the T2.1 handoff snippet puts guest outside gate, and a handoff is advisory while the phase file task text is the spec. Gate-outside breaks nothing: Keel's per-client key derives from `RemoteAddr` (`gate/client.go`), never from guest identity, and the broker still reads the guest from context because the middleware runs before the handler in both orders. The refusal side is pinned both ways (pass mints exactly once, refusal mints zero), and the mutation that swaps the order fails `TestMountGateRefusesBeforeMiddleware`. Spec-literal correct, no defect.

(b) Owner-limit equality. Pinned, not assertion-free. `Mount` refuses `ErrMount` on any mismatch whenever Sessions or Admin is present, and both variants are tested. `main.go` feeds one `ceiling` value to both fields, so a future edit that splits the variable into divergent values fails loudly at boot instead of diverging silently. The skipped check when both handlers are nil is irrelevant because the ceilings mean nothing without either handler. No defect.

(c) `hostBuilder` in `main.go`. Acceptable boot glue, not misplaced code. The broker declares the `ConfigBuilder` interface and the host owns `Load`, exactly the consumer-declares-interface shape. `SessionConfig` has exactly three fields and the adapter forwards all three, so nothing is dropped. It lives in the composition root and duplicates no shape either side owns. No defect.

(d) Zero daily ceiling refuses boot. Fail-closed and spec-literal. `spendCeiling(0)` succeeds by itself, then `broker.New` rejects the non-positive owner limit, so boot stops loudly with a wrapped cause. No product text demands a zero-ceiling boot posture, and the runtime all-refuse path is the kill-switch flag, which needs no reboot. No defect.

(e) Drain registry still nil. Declared openly in the handoff. See OUT_OF_SCOPE row 1.

(f) Media types. Verified against Keel `v0.3.0` source: `mediaContentTypes` in `main.go` equals the library `defaultContentTypes` plus `audio/mp4`, and the library default indeed leaves `audio/mp4` out, so the handoff claim holds. Everything currently persisted is admitted: stems at `audio/ogg` (broker default recording type, reconcile tests), covers at `image/png`, WAV at `audio/wav`. A missing type fails loudly at persist (`ErrInvalidContentType`), so any future persisted type the list lacks will surface instead of hiding. No defect.

(g) Posture constants (lease quota 8, bursts 16/256, staging and db paths). Declared in the handoff, each changeable in `main.go` alone. The spec names no values, so there is nothing to violate, and wiring posture in `main` is what the task asked for. Observation only, no defect.

(h) `POST /api/sessions/{id}/end` stays a stub. Declared openly. See OUT_OF_SCOPE row 2.

(i) Hygiene. `check.sh` plan-references, svelte-4 leakage, and consumer-names checks pass. Exported identifiers carry doc comments starting with their names. One sentinel (`ErrMount`), wrapped with `%w`. No `**T`, no `interface{}`, `ctx` first, no non-nil value beside a non-nil error, no `log.Fatal` outside `func main`. TypeScript mirror adds only const table entries with a dedicated test.

Owns note (not a finding): the diff regenerates `web/src/lib/api/testdata/routes.json`, which sits under T1.3's owns line. Regenerating the golden the owned test writes is required by the task ("with the golden decode test covering the additions"), and `stub-refusal.json` is byte-identical as promised. No defect, recorded so landing needs no owns debate.

## Findings

No in-scope findings. The table is intentionally empty.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| none | none | none | none | none |

## Out-of-scope rows (never blocking)

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `cmd/reprise/main.go` wire path (drain registry argument) | The drain still passes nil for the session registry, so the T6.1 drain waits on nothing once real sessions mount. Declared openly in the handoff. Owner: T6.1 (boot, drain, image). | Existing `TestWaitSessionsReturnsAtOnceOnNil` measures the nil path returning at once. | Passing a live registry changes drain behavior, which is exactly the behavior change T6.1 owns. |
| M | `internal/api/routes.go` route table (`POST /api/sessions/{id}/end` stays on `handleStub`) | The end-of-session route still answers `not_implemented`, so session-end recording and reconciliation have no HTTP entry point yet. Declared openly. Likely owner: T2.3 (reconcile seam). | `TestMountKeepsStubsForUnmountedRoutes` pins the 501 on the end path today; the future handler task flips that case. | Implementing the handler without touching the table would leave the route a stub, which the same test would catch. |

## The ten questions

1. Does any path spend money without reserving budget first? No. The only paid path mounted is the broker, which reserves before minting (T2.1, unchanged). Upload, media, events, and admin spend nothing. The gate refuses before any of them run.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The token cap flows from `SessionMaxSeconds` through the broker as before. This task adds no session lifecycle.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The media route runs behind guest middleware plus `identitySvc.AuthorizeMedia`, the same authorizer T1.4 pinned, and private-by-default holds by construction.
4. Does any edit apply without a revertible record? No edit path is touched.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. `hostBuilder` forwards `host.Load` rows only, all three `SessionConfig` fields, with no invented content.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. `check.sh` plan-references passes, and a grep over the diff shows only runtime words (`settings.Load` plan value, "middleware around it").
7. Did this task rebuild something Keel or Chaaya provides? No. The spelled-out media list exists because the library exposes no append form (declared). The host adapter is seam glue between two Reprise-owned sides. Mounting is Reprise's own route table.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Detached worktree at `224e21a`, confirmed by `git log -1`, no commits, working tree never reviewed.
9. Can any paid job kind resume after a restart and spend twice? No job kinds are touched.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Every pin runs on fixed inputs or a cancelled context, and the full gate passed in this worktree.

## Verdict

APPROVE. Zero in-scope findings across all severities (C 0, H 0, M 0, L 0), with zero residue against every prior round: this is round 1 and there are no earlier rounds, no findings to carry, and the two out-of-scope rows above are explicitly non-blocking with their owners named.
