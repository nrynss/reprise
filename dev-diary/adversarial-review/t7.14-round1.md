# T7.14 round 1 review

Reviewed hash: `175db437d7ca3b0550c6ae483b8e50d1314d03e1`
Worktree: `/home/nryn/work/reprise-wt/t7.14-rev-r1` (detached HEAD, confirmed with
`git log -1`; tree clean; diff read as `git diff e3d6d0d..175db43`).
Base: main at `e3d6d0d`. Task spec: `dev-diary/PHASE-7-production-issues.md` T7.14
(live plan at `/home/nryn/work/reprise/`). Owns: `internal/privacy/`,
`internal/retention/`.

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 1. The single L is process residue the implementer
disclosed in its handoff and the orchestrator strips at landing under its L
exemption. No remediation round is needed. Nothing behavioral is outstanding.

## What the commit does

Both restart resume tests now model the crash faithfully. After reopening the
runner on the same job ledger, each test cancels the superseded attempt on the
old runner, waits for that first attempt to land through the existing poll
beat, and only then releases the provider double. The done helpers judge the
latest attempt only instead of any attempt. No production file changes. No
deadline changes. No quarantine.

## Evidence run in this worktree

* `go test -race -count=20 -run TestEraseRestartResumesAndFinishes
  ./internal/privacy/` passes (4.4s).
* `go test -race -count=20 -run TestSweepRestartResumesAndFinishes
  ./internal/retention/` passes (6.2s).
* Full gate passes after `npm ci` in `web/`: `./tools/check.sh` ends
  `OK all checks passed` (gofmt, vet, staticcheck, race suite, svelte-check,
  eslint, vitest, build, playwright, leakage, names, plan-reference scans).
* `python3 dev-diary/tools/audit_docs.py` ends `OK planning tree is consistent`.
* `gofmt -l` and `go vet` on both packages are clean.

## Pre-fix reproduction: not reproduced locally, weighed and accepted

The losing interleavings did not fail here. A pristine archive of the base at
`e3d6d0d` in `/tmp/opencode/t714base` passed `-race -count=20` on both tests,
plus `-count=40` (privacy) and `-count=30` (retention). This matches the task
statement. Both flakes pass in isolation and load decides the winner, and this
box does not reproduce CI load shape. I accept the fix without a local red run
because each losing interleaving is closed by construction.

* Retention: attempt 1 on the old runner could delete the episode row between
  attempt 2's presence check and its erase. Cancelling the old attempt and
  waiting for its terminal before releasing the provider double removes the
  second actor, so the interleaving cannot occur.
* Privacy: the done waiter accepted any done attempt, so it could observe the
  superseded attempt while the resumed attempt still worked. Judging the latest
  attempt only removes the early return.
* `old.Cancel` cannot touch the resumed attempt. Keel `v0.3.0`
  `Runner.Cancel` cancels only contexts the calling runner started and reports
  `ErrUnknownJob` otherwise, which the tests tolerate. Both provider doubles
  select on `ctx.Done` beside the block channel, so the cancelled attempt lands
  promptly instead of hanging the new terminal wait.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L | `dev-diary/PHASE-7-production-issues.md:373` at the reviewed hash | The commit carries a status line revert (`not-started`) in a planning file outside its `owns` paths. The orchestrator owns status lines. It must drop before landing and it changes no behavior. | `git diff e3d6d0d..175db43 -- dev-diary/PHASE-7-production-issues.md` shows the one-line unowned hunk | Re-add any status value on that line. The pin reappears because the hunk is the whole defect. |

Disposition of the L: the orchestrator strips the planning hunk at landing
under its L exemption (L only, no behavior change, named here, unarguable).
No remediator action exists that would not itself touch an orchestrator-owned
path.

## Checks against the task rules

* Test-only fix: verified file by file. The diff touches
  `internal/privacy/privacy_test.go`, `internal/privacy/resume_test.go`,
  `internal/retention/resume_test.go`, `internal/retention/retention_test.go`,
  plus the planning line above. No production code changes.
* No sleeps added: the new `waitFirstTerminal` helpers reuse the pre-existing
  20ms poll beat (`pastDeadline` in privacy, the same literal beat as the
  neighboring helper in retention). Deadlines are unchanged (15s privacy, 30s
  retention) and bound failure only.
* No threshold widened, no quarantine, no library defect claimed.
* Done criteria kept: each test still restarts mid-erase and mid-sweep and
  asserts it resumes and finishes exactly once (resumed erasure complete, one
  owner session ended, second owner untouched; idle guest rows gone, fresh
  guest intact).

## The ten questions

1. Does any path spend money without reserving budget first? No. The change
   touches unpaid erase and sweep test paths only.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. Both tests assert the provider sessions ended.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   Both tests assert the untouched owner or guest is unchanged.
4. Does any edit apply without a revertible record? No. No edit path changes.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. No callback or count changes.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The gate plan-reference scan passes and the changed
   lines state their own reasons.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses
   `keel/job` cancellation and recovery as designed.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Hash `175db43` confirmed with `git log -1` in detached worktree
   `t7.14-rev-r1`. Every pin ran there. No code committed.
9. Can any paid job kind resume after a restart and spend twice? No. These
   kinds are unpaid, and Keel recovery still marks the superseded attempt
   interrupted. The tests pin exactly this.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. Poll deadlines are pre-existing failure bounds. Passing
    depends on channels the tests own.
