# T7.19 round 1 review

Hash: `47c65764bbbd114ad34d55e9a319d5ad0dcfac68`
Worktree: `/home/nryn/work/reprise-wt/t7.19-rev-r1` (detached, confirmed via `git log -1`)
Base: `84d75c1`
Diff: `git diff 84d75c1..47c6576` touches `cmd/reprise/main.go` (+151/-28 logic) and `dev-diary/PHASE-7-production-issues.md` (status line only)

Verdict: REMEDIATE
Counts: C 0, H 2, M 0, L 2 (one L is OUT_OF_SCOPE and non-blocking)

## What was checked

Gate: `npm ci` in `web/`, then `./tools/check.sh` in this worktree. Result: OK all checks passed.
Docs: `python3 dev-diary/tools/audit_docs.py`. Result: OK planning tree is consistent.
Behavior probes: three throwaway Go tests written in this worktree, run, then deleted (tree clean after). All passed on the handed hash: per-route burst mechanics with honest 429 hints, mint isolation at inner-rule level, recovery to draft on garbage ids when linked, named missing ids when nothing links. A fourth throwaway proved nested Protect spends both buckets. Details under findings.

## Findings

### F1 (H, in-scope): take-path gate budgets are unpinned

Where: `cmd/reprise/main.go:451-471` (`takeRule`, `protectTake`) and `610-668` (outer plus seven inner rules)
What: the task Done clause requires a fixture take at real cadence to pass with zero 429s and a double-cadence burst to still trip, both pinned. No committed test references `takeRule`, `protectTake`, or any `take-*` rule name (grep over `*_test.go` returns zero lines), and no test calls `wireAPI` (only a comment at `cmd/reprise/main_test.go:344` mentions it). Sizing is argued only in prose. Any wrong number ships green.
Pin: throwaway `TestT719ThrowawayGateBursts` (deleted after the run) asserted the exact wired bursts through `protectTake` itself: 48 uploads pass and the 49th answers 429 with `Retry-After` and the `rate_limited` code, 8 stems pass and the 9th trips, 6 mints pass and the 7th trips, and a 60-request upload hammer leaves mint at 200. It passed on the handed hash. The committed suite asserts none of this, so the same gate run stays green if the numbers are wrong.
Mutation: change the `take-uploads` burst at `main.go:635` from 48 to 6. The full gate still passes because nothing committed exercises that rule.

### F2 (H, in-scope): retried-completion recovery is unpinned

Where: `cmd/reprise/main.go:1631-1639` (`resolveStems` call site) and `1727-1790` (`resolveStems`, `linkedStemBlobs`)
What: the production fix (a retried completion recovers to draft when a pair is linked, else answers `stems_not_found` naming `missing_media_ids`) has no committed handler-level test. The existing suite pins only the service layer (`TestCompleteStemsConcurrentKeepsOnePair`) and the full-queue 429 (`TestFullEditorialQueueAnswersRetry`). A regression that deletes the fallback ships green.
Pin: throwaway `TestT719ThrowawayRecoveryGarbageIDs` posted `garbage-user-orphan` and `garbage-host-orphan` for an episode with a linked pair and measured 200, state `draft`, and `"scheduled":true`. Throwaway `TestT719ThrowawayMissingIDsNamed` posted unknown ids with nothing linked and measured 404 containing `stems_not_found`, `missing_media_ids`, and both posted ids. Both passed on the handed hash and were deleted after. Nothing committed measures either path.
Mutation: short-circuit `resolveStems` to return the posted ids as missing without consulting `linkedStemBlobs` (two lines at `main.go:1741`). The full gate still passes.

### F3 (L, in-scope): `protectTake` comment overstates isolation

Where: `cmd/reprise/main.go:465-468`
What: the comment claims a refusal "spends nothing from any other route, so a hammered upload never blocks a session mint". Every take request also draws the shared outer budget: Mount wraps each pre-wrapped handler again (`internal/api/routes.go:168`), and the stems route is double-wrapped explicitly (`main.go:664`). A sustained hammer exhausts the shared outer bucket and crowds out mint on that client key. Isolation holds at moderate burst scale (verified: 60 upload hits leave mint at 200 on separate inner rules) because the outer burst (256) dwarfs the inner ones, so this is wording, not availability.
Pin: throwaway stacking probe (deleted after): inner burst 100 wrapped in outer burst 2 trips on the 3rd request, proving nested Protect spends both buckets. Code cites: `routes.go:168` plus `main.go:610-668`.
Mutation: lower the outer `PerClient` burst at `main.go:612` from 256 to 2. An honest mint plus one completion then trips, contradicting the comment.

### F4 (L, OUT_OF_SCOPE): phase status revert outside owns

Where: `dev-diary/PHASE-7-production-issues.md:494`
What: the commit flips T7.19 status from `in-progress:implement:t7.19-impl` to `not-started`. Task owns is `cmd/reprise/main.go` only. The handoff discloses this as the usual revert for review to flag. It drops before landing and never blocks APPROVE.
Pin: `git diff 84d75c1..47c6576 -- dev-diary/PHASE-7-production-issues.md`
Mutation: none (scaffolding, not behavior).

## Ten questions

1. Spend without reserving first: no. The diff adds rate-limit gates only. No paid call, no new job kind, no reservation path changed. Transcript and editorial scheduling still flow through the existing reserve-first seams.
2. AssemblyAI session left open: no. Mint, close, settle, and sweep paths are wrapped, not rewritten. Wrapping cannot skip `session.end` or the token cap.
3. Private episode, stem, or transcript reaching anyone but its owner: no. `resolveStems` checks both posted blobs against the caller owner, `linkedStemBlobs` filters by owner plus episode, the linked pair is revalidated against the owner, and the missing detail echoes only ids the caller posted.
4. Edit without a revertible record: no. Linking still goes through the guarded `CompleteStems` transition. Nothing applies an edit.
5. Spoken callback or count from the prompt: no prompts touched. Not applicable.
6. Code, comment, test, or migration citing a phase, task, or planning file: no. The `take-*` rule names are descriptive, not plan cites. The diff grep for task ids, phase ids, `§`, and round names is clean, and the gate plan-reference scan passes.
7. Rebuilt Keel or Chaaya work: no. Per-route shaping is product rate config over `keel/gate`, recorded under T2.1 in `libraries.md`. No library patched.
8. Review on the handed hash in its own worktree: yes. Detached worktree above, `git log -1` is `47c6576`, diff taken against base `84d75c1`, every pin run here. No code committed.
9. Paid job resuming after restart and spending twice: no. Paid-kind registration is untouched. Restart still marks paid kinds `interrupted`.
10. Development check needing a person, microphone, or wall-clock threshold: no new tests ship with this commit. The pre-existing `waitDescribed` poller is outside owns and untouched. The throwaway pins used exact counts, no thresholds.

## Notes for the remediator

The remediator owns F1 through F3. Likely shape: commit an honest-cadence gate test plus a burst test through the wired rules, commit handler-level recovery and named-missing tests, and reword the `protectTake` comment to state the actual guarantee (inner budgets isolate routes until the shared outer budget exhausts). F4 is the orchestrator's at landing.
Residual risk, not a finding: when posted ids are half new (one held, one stale) and an old pair is linked, the handler recovers to the old pair and schedules on old audio rather than naming the stale id. That matches the specified retry contract, so it is recorded here instead of filed.
Sizing sanity, reviewed by inspection plus production evidence: the old single shared burst was 16 across all routes and takes already passed everything except the draft move, so per-route inners of 6 to 48 with a 256 outer fit an honest take monotonically. The double-cadence calibration itself is exactly what F1 requires pinned.
No new settings key. TOML untouched. No contract change needed.
