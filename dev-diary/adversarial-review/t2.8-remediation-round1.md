# T2.8 remediation round 1

Reviewed hash: `151fa898ad3b308da3cc52e812dea408954288b4`.
Remediation head: `197699b2f4f55cc1307657ceef75512aa245ba81` on `task/t2.8-rem-r1`.
Worktree: `/home/nryn/work/reprise-wt/t2.8-rem-r1`. Built on the reviewed
commit, no rebase, no amend. One new commit. Fixes nothing nobody found.

| Finding | Fix | Pin | Prior pins |
|---|---|---|---|
| H-1 (`+page.svelte:17` to `26`, dead `record-end` lookup at mount) | Removed the mount time `getElementById('record-end')` lookup and its add and remove listener calls. The document click listener already used for `record-end-now` now also matches `closest('#record-end')` and calls `controller.endControl()`. The live rendered button is a native button, so delegation covers mouse click, Enter, and Space. No annotations added. Cap start, stop, wake, the warn region, and the harness clock are untouched. | New `web/src/routes/record/end-control.spec.ts`, two proofs: mock take then mouse click on the standing End session button reaches the Confirm end session state with the arm notice, and focus plus Enter reaches the same state. Both fail on the reviewed code (stashed fix, rebuilt, 2 failed) and pass with the fix (rebuilt, 2 passed). | Full `./tools/check.sh` passes in the worktree. Mock suite rebuilt with the fix: 9 passed, the 7 pre existing proofs (2 cap, 5 session) plus the 2 new pins. |

No verdict change. No other defect fixed. Verdict left for re-review.
