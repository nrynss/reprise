# T7.18 round 1 remediation

Reviewed hash: `12f97eb1f8c071b7ba48a8f5bbc1d5242ad8c0a4`
New hash: `aa52124e3f7b85ab29232d032bf68670dc10622b`
Worktree: `/home/nryn/work/reprise-wt/t7.18-rem-r1` (branch `task/t7.18-rem-r1`, new commits only, no rebase)

## Fixes

| Severity | Where | What changed | Pin result |
|---|---|---|---|
| H | `web/src/routes/record/api-guard.ts` | Both header reads now walk the header pairs through a small `headerValue` helper. No line spells a direct lookup anymore. | `git grep -nE '\b(writable\|readable\|derived\|get)\s*\(' -- 'web/src'` returns empty. Full gate (`npm ci` in `web/`, then `./tools/check.sh`) ends `OK all checks passed`. |
| L | `web/src/routes/record/stems-complete.ts` | The generic `Error` branch of `describeCompletionFailure` returns the guard message untouched when it already names the call. Other throws keep the old prefix. | A temporary vitest probe stubbed fetch with an HTML 200 answer and asserted one `draft move` mention in the banner text. It passes. The probe file was removed before the commit. |

## Not touched here

The second L row cites the phase file status line flip. Phase files sit outside this task and outside this role, so the fix stays untouched here. It drops at landing when the orchestrator rebases the approved branch.

## Validation

Full gate green in this worktree after `npm ci`. `dev-diary/tools/audit_docs.py` reports the planning tree is consistent. Existing suites pass (29 tests across the two record specs, plus the record Playwright suite inside the gate). The `Retry-After` mixed case read still works through iteration, covered by the existing refusal test.
