# T7.12 remediation round 1

Reviewed hash: `234d65cf950e67a75858bd2014778d960f188c29`.
Review file: `dev-diary/adversarial-review/t7.12-round1.md`.
Worktree: `/home/nryn/work/reprise-wt/t7.12-rem-r1`, branch `task/t7.12-rem-r1`.
New commits on top of the reviewed hash, no rebase, no amend.

## Findings

| Severity | Where | Fix | Pin result |
|---|---|---|---|
| M (F1) | `web/src/routes/record/cap.spec.ts` and `web/src/routes/record/mock-session.spec.ts` | Installed a route stub for `**/stems/complete` answering the draft answer in each spec that navigates through the automatic completion post. Two installs in `cap.spec.ts` (both cap paths) and one in `mock-session.spec.ts` (the pagehide path). Stub-only edits. No assertion changed. | Before: `cap.spec.ts` (2 cases) and `mock-session.spec.ts` leaving-page case timed out at `waitForURL(/processing/)` against a stub-less preview. After: `cap.spec.ts` 2 passed, leaving-page case passed, `stems-complete.spec.ts` plus `end-control.spec.ts` 5 passed in the same run. |
| H (F2) | `web/src/lib/voice/record-state.ts` with `web/src/routes/record/+page.svelte` | Exposed the controller completion outcome as `completionFailed` on `RecordSnapshot` (false in `emptySnapshot`, set in `emit` from the driver view). Added one `Draft move retry` section to the record template, visible only when `snap.phase === 'ending'` and `snap.completionFailed`, with button `#record-retry` named `Retry draft move` wired through document delegation to `activeController.retryCompletion()`. The controller reference lives in a rune `activeController`, set on mount and cleared on cleanup. Svelte runes only. | Before: failed stubbed automatic post wrote retry words to `role=status` but `getByRole('button', { name: /retry/i })` found no visible control (review probe). After: scratch pin (deleted after the run) failed the post, asserted status contains retry, asserted the retry button visible, clicked it, reached `/processing/` with the Processing heading. Passed. Mutation: removing the new section or its wiring returns the pin to no visible button. |

## Validation

- `controller-complete.spec.ts`: 2 passed.
- `cap.spec.ts`: 2 passed. `mock-session.spec.ts` leaving-page case: passed.
- `stems-complete.spec.ts` plus `end-control.spec.ts`: 5 passed.
- New controller retry pin (scratch spec, deleted after the run): 1 passed.
- Gate: `npm ci` in `web/`, then `./tools/check.sh` reports `OK all checks passed`.
- `dev-diary/tools/audit_docs.py` reports `OK planning tree is consistent`.
- No Svelte 4 idioms. The retry button carries an accessible name. Plan-reference scan clean.
