# T7.12 round 2 review

Reviewed hash: `ea929ca11a62bdaa95da92a4bf426281dd7d76b9`.
Worktree: `/home/nryn/work/reprise-wt/t7.12-rev-r2` (detached, confirmed by `git log -1`).
Remediation diff: `234d65cf950e67a75858bd2014778d960f188c29..ea929ca11a62bdaa95da92a4bf426281dd7d76b9` (4 files).
No commits were made from this worktree. The tree is clean after the run.

## Scope

The remediation adds stub-only completion installs to the two sibling specs
(F1) and one controller retry section to the record template with its snapshot
flag and delegation wiring (F2). The diff touches `record-state.ts`,
`+page.svelte`, `cap.spec.ts`, and `mock-session.spec.ts`. Nothing else changed.

## Verification run

* `npm ci` in `web/`, then `./tools/check.sh` from the worktree root.
  Result: `OK all checks passed`.
* `cap.spec.ts`: 2 passed. `mock-session.spec.ts` full file: 5 passed.
  `end-control.spec.ts` plus `stems-complete.spec.ts`: 5 passed.
  Combined named run: 12 passed.
* `controller-complete.spec.ts` (own pins): 2 passed.
* Throwaway retry pin (deleted after the run): failed the stubbed post,
  asserted `role=status` names a retry, asserted the retry button visible,
  clicked it, reached `/processing/` with the Processing heading. Passed.
* Mutation probe: gating the new retry section to false returned the pin to
  a button assertion failure. Restored after, tree confirmed clean, rebuilt.
* `dev-diary/tools/audit_docs.py`: `OK planning tree is consistent`.
* Runes scan of the diff: zero matches. Plan-reference scan: zero matches.
* The retry button carries `aria-label="Retry draft move"`.

## Residue, severity by severity

* F1 (M, sibling specs red): zero residue. Both spec files carry the
  `**/stems/complete` stub answering the draft answer. The edits add one
  helper plus one install line per navigating test. No assertion changed.
  All three formerly red proofs pass, plus the full sibling files.
* F2 (H, retry promised in words with no control): zero residue. The snapshot
  exposes `completionFailed` from the driver view. The template renders a
  `Draft move retry` section only in `ending` with that flag. Its button is
  named `Retry draft move` and delegates to `retryCompletion` on the mounted
  controller. The pin proves fail, button, click, processing. The mutation
  proves the pin is load bearing.
* C findings: none in round 1, none now. H findings: F2 closed above.
  M findings: F1 closed above. L findings: none in round 1, none now.

## The ten questions

1. No path spends without a budget reservation. The diff posts only the
   free completion route. It opens no paid call.
2. No path leaves a session open. Voice end, HTTP end, and guard close
   all still run before the completion post.
3. No private material reaches anyone new. The post carries the take's
   own episode and stem ids. Ownership checks are untouched.
4. No edit applies without a revertible record. Nothing here edits an
   episode. The change gates navigation and retry only.
5. No spoken callback or count comes from the prompt. No spoken content
   was added.
6. No code, comment, test, or migration cites the plan. The gate pattern
   over the diff matches nothing, and the gate step passes.
7. Nothing rebuilds Keel or Chaaya. The change reuses the standing driver
   plus the Chaaya uploader, guard, and audio helpers.
8. Yes. This review ran on the handed hash in its own detached worktree.
9. No paid job kind can resume and spend twice. No job kinds were touched.
10. No check needs a person, a microphone, or a wall-clock threshold.
    The proofs use the mock flag, generated input, and the harness clock.

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| None | None | None | No new defect found in this round | None | None |

## Dismissed observations (no findings)

* A bare run of the routes mock config with no spec names loads a vitest
  `.test.ts` file and errors. The file headers document named spec runs,
  the config predates this task, and the gate suite stays green. No finding.
* Concurrent `retryCompletion` calls still share one driver latch and one
  URL, as round 1 noted. No observable harm follows, so this stays dismissed.

## Verdict and counts

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0. Out-of-scope findings: 0. False positives: 0.
