# T7.11 implementer handoff

Commit `38d4696c49a8e5ce30822eeccbc5363a3141c820` on branch `task/t7.11`
in worktree `t7.11-impl`. Base is main at `e0551fa`. Gate passes in
the worktree. Only `web/src/routes/record/` changes.

## What it built

The record page posts stem completion with loud retry: caller plus
outcome messages plus retry driver in `stems-complete.ts`, page UI
with alert and retry, and proofs.

## Seam question for review

The production trigger is missing. The take end flow in
`web/src/lib/voice/record-state.ts` holds the only production
handles on the episode id and both upload receipt ids, and the page
cannot learn them without a seam there. The missing line is a
controller call to the post driver after both finishes and before
the processing navigation. Either widen owns to `record-state.ts`
plus the mock endpoint, or scope a follow-up task for that seam.
The done criteria (mock-driven draft move) cannot pass without one
of the two.

## Note

The route eslint parser predates type-only imports. Typed logic
lives in the plain TS module, page glue stays untyped.
