# T3.4 review round 2

Reviewed hash `4b9fc8cb723233ca4bb1434b883212c7fb2490a2` in worktree
`/home/nryn/work/reprise-wt/t3.4-rev-r2` (detached HEAD, confirmed with
`git log -1 --format=%H`). Base for the remediation diff is reviewed
hash `2a3cb576d0b5a78d1dfce384377273f7d975e571`. The diff touches
`internal/render/` only, which matches the declared `owns` line. Two
commits sit on the reviewed hash (`e9c62c0` cold-open decision,
`4b9fc8c` hash preimage), no rebase. No commit was made from this
review. One temporary mutation probe edited `store.go`, was run, and
was restored byte for byte (`git status --short` empty after).

Gate evidence from this worktree: `go test -race -count=1
./internal/render/` passes in full, `go vet` clean, `gofmt -l` clean,
pinned ffmpeg and ffprobe 9.0.1. `./tools/check.sh` was not rerun
here (it needs `npm ci` plus web checks; the diff touches no web
path, and the remediation file records `OK all checks passed` in its
own worktree).

## Residue against round 1

Round 1 returned REMEDIATE with C 0, H 1, M 0, L 1.

* H (`store.go`, cold open ignored decisions): zero residue. `ColdOpen`
  now selects the latest cold-open proposal id, then its latest
  decision row, and returns nil on `reverted`. The committed pin
  `TestRenderRevertedColdOpenRerenders` re-renders after a revert with
  a different hash, `reused=false`, nil cold open, and seconds shorter
  output. It passes here. The mutation probe (decision lookup deleted,
  proposals-only query restored) fails the pin, so the pin is
  load-bearing.
* L (`edit.go`, hash omitted assembly parameters): zero residue.
  `Input` gains `CrossfadeMs`, `MixRateHz`, `OpusBitrate`, and
  `AACBitrate`, all four join the preimage, and `run.go` fills them
  from the assembly constants while `audio.go` encodes through the new
  `OpusBitrate` and `AACBitrate` constants with identical values. The
  committed pin `TestHashInputsVariesWithAssemblyParams` varies each
  field alone and requires a different hash each time. It passes here,
  including the round-1 crossfade 10-vs-20 case.
* C: zero, as in round 1. No spend, privacy, or data-loss path exists
  in these paths.
* M: zero, as in round 1. No medium defect was open and none was
  found.

## Decision semantics (re-check)

Latest decision wins on the cold-open proposal: no decision keeps the
span (null scan), a non-reverted latest decision keeps it
(accept-after-revert renders the opening), and a `reverted` latest
decision returns nil (revert-after-accept starts at the top). The
`reverted` literal matches the editor contract (`draft.ts` posts
`decision: 'reverted'`), and no `DecisionReverted` constant exists in
`internal/editorial`, so the literal is the only spelling available.
Multiple cold-open proposals resolve to the latest row, the same rule
the pre-fix code used. The reverted run renders shorter with no gap
orphan (the gap emits only beside a placed opening), and the
intersect-kept-audio path is untouched.

## Hash preimage (re-check)

All four new fields are render-affecting in the retune sense: each
mirrors a constant the assembly reads (`JoinFades` and the graph fade
length for the crossfade, the mix `-ar` and gap source rate for the
mix rate, the two `-b:a` settings for the bitrates), and `describe`
fills every field from those constants, so a retune moves the preimage
together with the bytes. No dead field is hashed. No live field found
in the assembly is still missing: targets and gap were already hashed,
and the loudness targets remain package constants named in the
preimage as before. Cut-order exclusion is intact (the sort still
precedes the preimage, `TestHashInputsStableAcrossCutOrder` passes).
Intersect semantics are untouched.

## New findings

None. No new defect from either commit. Counts: C 0, H 0, M 0, L 0.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   package shells to ffmpeg and ffprobe and reads SQLite and
   mediastore. No paid call exists.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No AssemblyAI code lives in these paths.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Both outputs persist with `mediastore.Private`, owner set,
   grouped by episode. Tests assert private Ogg and MP4 blobs.
4. Does any edit apply without a revertible record? Not applicable.
   The package applies no user edits. Render rows are append-only and
   reruns reuse by hash.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? Not applicable. The only terminal output is the result
   JSON with hash, blob ids, and byte-measured loudness.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Grep over the remediation diff for task ids,
   phase ids, `PLAN`, `handoff`, and `§` returns nothing. Both commit
   messages are clean.
7. Did this task rebuild something Keel or Chaaya provides? No. Same
   position as round 1. The graph over `keel/ffmpeg` is product
   assembly, and the bitrate constants only name existing encode
   settings.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. HEAD equals the handed hash, the worktree is detached at it,
   the remediation diff was read against the reviewed hash, every pin
   ran inside this worktree, and no code was committed.
9. Can any paid job kind resume after a restart and spend twice? No
   paid job exists here. The kind is idempotent and resume-safe by
   content hash.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Every audio pin synthesizes its signal,
    and every measurement reads rendered bytes. The duration
    assertions use fixed margins on measured files.

## Verdict: APPROVE

Zero findings across all severities, with zero residue against round
1 (H fixed and pinned, L fixed and pinned, C and M zero throughout).
