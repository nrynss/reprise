# T7.5 round 2 review

Reviewed hash: `162ff9c8f520ff083077e7864862951cfa883567`
Worktree: `/home/nryn/work/reprise-wt/t7.5-rev-r2` (detached, confirmed with `git log -1`)
Base: `464f0c8`
Remediation diff reviewed: `git diff c5f8934ceaf9595cc402d8937959b1af44fec941..162ff9c8f520ff083077e7864862951cfa883567` (commit only, not the working tree)
Base diff: `git diff 464f0c8..162ff9c8f520ff083077e7864862951cfa883567`
Verdict: APPROVE
Counts: C 0, H 0, M 0, L 0

## Scope

Task T7.5 owns `dev-diary/probes/production.md` only. The base diff names only that path (343 added lines, zero deleted lines). The remediation diff touches two paths, but the net base diff is clean. See residue below.

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with C 0, H 0, M 0, L 2 (one OUT_OF_SCOPE, one in scope). Both are closed. Zero residue remains.

1. L, in scope, `dev-diary/probes/production.md:417-420` at `c5f8934` (SSE idle stream sub claim with no command plus output). Closed. The remediation appends 16 lines inside check 2 with the exact read only GET command (`curl -sS -o /dev/null --max-time 8 -H 'Accept: text/event-stream'` against `/api/jobs/does-not-exist/events`) plus its output block (`curl: (28) Operation timed out after 8001 milliseconds with 0 bytes received` and `http=200 bytes=0 time=8.001395s exit=28`). The round 1 pin (`sed -n '/## 2\. Decisions/,/## 3\. Private/p' dev-diary/probes/production.md | grep 'curl.*api/jobs'`) now exits zero in this worktree. The claim stands with evidence. No mint, no state change, no spend.
2. L, OUT_OF_SCOPE, `dev-diary/PHASE-7-production-issues.md:159` at `c5f8934` (commit wrote outside owns, flipping T7.5 status to `not-started`). Closed. The remediation reverts that one hunk to the base line `status:     in-progress:implement:t7.5-impl` as a new commit. `git diff 464f0c8..HEAD -- dev-diary/PHASE-7-production-issues.md` is empty in this worktree, and `git diff 464f0c8..HEAD --name-only` lists only `dev-diary/probes/production.md`. It never blocked APPROVE and now it is gone.

## Round 1 region byte identity and hash citation

The first 267 lines of the head file are byte identical to the full base file. `diff` of `git show 464f0c8:dev-diary/probes/production.md` against `head -267 dev-diary/probes/production.md` is empty. Both hash to `1523e9f72e91fe2c155d3ccbf32a1bae3acb94ed` via `git hash-object --stdin`. The base blob hashes to the same value.

On the cited hashes: the remediation record holds. Its `1523e9f...` matches both the base blob and the head prefix measured here. The round 1 citation `e0af56...` matches nothing measured here. The substance holds regardless. Round 1 is untouched apart from the check 2 append, and the diff on the probes file deletes zero lines.

## Spot checks and validation

I re-ran one read only GET from this worktree: `curl -sS https://reprise.nryn.dev/healthz` answers `ok 41b9e8a8796a09f06ee3170db29f1f03 version=464f0c895fe355c435bca21aded9fc57cc34202d`. The version matches. I minted nothing, flipped nothing, uploaded nothing, and spent nothing.

`python3 dev-diary/tools/audit_docs.py` gives `OK planning tree is consistent`.

A grep for key, token value, password, and private key patterns across the probes file returns nothing. Added lines carry no semicolons and no em dashes. The SSE output redacts nothing it needs to show, since a read only GET carries no secret.

## Findings

No new findings. No OUT_OF_SCOPE defect found.

## Ten questions

1. Does any path spend money without reserving budget first? No. No code changed.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. No code changed. The probe text still shows explicit `session.end` and `client_end`.
3. Can a private episode, stem or transcript reach anyone but its owner? No. No code changed.
4. Does any edit apply without a revertible record? No. No product edit is in this commit.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Check 7 still defers to the operator catalog.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Only the probes doc changed, and docs must name tasks for carries. No source file changed.
7. Did this task rebuild something Keel or Chaaya provides? No. No code changed.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` gives `162ff9c8f520ff083077e7864862951cfa883567` in detached worktree `t7.5-rev-r2`. I committed nothing and changed no tracked file.
9. Can any paid job kind resume after a restart and spend twice? No. No job code changed.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. All speech is generated. My spot check was one read only GET. The 8 second SSE timeout is a recorded measurement with its command plus output, not a gate threshold.

## Observations for the orchestrator (not findings)

The round 2 carry on stem completion scheduling still names its follow up task descriptively with no T number. Open that task before closing P7, then re-run check 2 live. This carries forward from round 1.
