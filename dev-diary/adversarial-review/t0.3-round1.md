# T0.3 round 1 review

Hash reviewed: `e782df796c98ad4517a09c3621a272eef8c47668`.
Worktree: `/home/nryn/work/reprise-wt/t0.3-rev-r1`, detached at the handed hash.
I confirmed the hash with `git log -1` before reviewing.
Base: `eb16b4ec7de3417096e58faaa7118679e11f6edb`.
Diff under review: `git diff eb16b4ec7de3417096e58faaa7118679e11f6edb..e782df796c98ad4517a09c3621a272eef8c47668`.
Round 1 is the first round for this task, so no prior rounds exist.
Baseline-clean statement: nothing carries over.

## Scope

The diff names four paths: `go.mod`, `go.sum`, `internal/gemini/doc.go`, `internal/gemini/live_test.go`.
I verified the name list with `git diff --name-only` on the range above.
That list matches the handoff claim.
The owned test plus the probe record plus the package clause cover the task.
`doc.go` holds only the package clause, the same necessity move the handoff names.
I read the owned test whole, all 366 lines.
I read `doc.go`, the handoff, the T0.3 block, the full binding file, and the probe record.
I read `internal/settings/settings.go` for the loader contract.
I judged the probe record at `/home/nryn/work/reprise/dev-diary/probes/gemini.md` against the done-when.
Main has moved since the probe base, so I judged this branch against its base only.

## What the commit does

The probe builds a Vertex client from settings plus the revealed key JSON.
It proves the file route with a text ping, then sends a long Opus stem inline.
It then judges two deliveries without any transcript and logs usage per call.
`go.mod` pins `google.golang.org/genai` at `v1.71.0` with `cloud.google.com/go/auth` at `v0.23.3`.
No `replace` directive appears.
`genai` enters only through `internal/gemini`, so the frozen stack holds.
The commit message uses active voice in short sentences with no banned tokens.

## Done-when check

The record holds the credential route, the size result, token counts, usage fields, and the listening answer.
The ping answer reads PONG with finish STOP.
The audio call accepts a 3450676 byte Opus stem with no bucket step.
The listening call picks the second take for its tone.
Response shape notes name candidates, parts, finish reason, logprobs, model version, response id, usage, and feedback.
Usage notes name prompt, candidates, total, cached, thoughts, plus modality details.
Spend totals three calls with about 30289 tokens and no storage use.

### Quote check one

The record states the ping answer verbatim as PONG.
The test logs it with `ping text` plus the model version.
The record usage row reads 8 prompt tokens, 2 candidate tokens, 29 total, 0 cached, 19 thoughts.
That row matches the `logUsage` shape for prompt, candidates, total, cached, and thoughts.
It also matches the modality detail shape for TEXT rows.

### Quote check two

The record states the listening answer verbatim across two sentences.
It opens with the second take taking the opener slot.
It grounds the choice in the laugh plus the conversational tone at the end.
The test logs the full choice text plus the model version.
The record usage row reads 40 text plus 225 audio prompt tokens, 47 candidate tokens, 962 total, 0 cached, 650 thoughts.
That row matches the same `logUsage` shape with TEXT and AUDIO details.

## Gate and scoped probes

I ran `npm ci` in `web`, then `./tools/check.sh` in the review worktree.
The gate exits 0 with `OK all checks passed`.
`go vet -tags live ./internal/gemini/...` exits 0.
Plain `go vet` exits 0.
Plain `go test ./internal/gemini/` passes with no test files.
I did not run the live test.
It spends money and needs the key, and the record already carries its output.
`gofmt` reports no owned file.
Grep over owned tracked files finds no task ids, no phase ids, no planning names, and no secret values.
The real key file has mode 600, checked with `stat` alone.
No key bytes entered any output.
No `LICENSE` file rides in the commit.

### Mutation one, missing file refusal

I ran a scratch program outside tracked files with a temp config.
It points the credential at a missing file and calls the loader.
The load fails and the error names the key, the source, and the path.
The exact shape starts with the secret prefix and ends with the file path.
The reviewed test asserts the same three names, so the pin holds.

### Mutation two, plan names the key path

The same scratch run loads a config with a present key file.
The plan names `secrets.gemini_credential` with source `file` plus the key path.
It marks the row resolved.
The reviewed client logs that plan string, so the T1.6a pin standard holds.

### Loose file refusal

A second scratch run sets a temp key at mode 0640.
Reveal refuses it and names the key, the path, and the mode.
The reviewed test asserts the key, the source, and the file name.
The live error adds the mode, so the pin holds with margin.

## Ten review questions

1. Does any path spend money without reserving budget first. No production path spends. The three Vertex calls are the task-authorized probe behind the live tag. They never run in CI. The record counts every call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. This task touches no session code.
3. Can a private episode, stem, or transcript reach anyone but its owner. No. No tracked file holds a secret value. Logs carry lengths and names only.
4. Does any edit apply without a revertible record. No. The change is one commit on the task branch. All scratch plants lived outside tracked files. The tree is clean.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. This task speaks no callback and stores no count.
6. Does any code, comment, test, or migration cite a phase, a task, or a planning file. No. Grep over owned tracked files returns empty. The gate plan scan passes.
7. Did this task rebuild something Keel or Chaaya provides. No. Settings, plan, and secret sources come from Keel. Token refresh and audio encoding ride the planned client.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` showed the handed hash. The worktree is detached. Every pin ran there.
9. Can any paid job kind resume after a restart and spend twice. No. This task adds no job code.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Audio comes from local synthesis. Checks use exact names, modes, and word counts.

## Findings

In-scope severity counts: C 0, H 0, M 0, L 2.
Zero out-of-scope observations.

### Finding 1

Severity: L.
Where: `internal/gemini/live_test.go` lines 7 through 9 at the reviewed hash.
What: The file header sentence about the probe runs 33 words, over the 30 word limit.
Pin: A word count over the exact sentence returns 33 against the limit of 30.
Mutation: Rejoin the split fix into the single long sentence and the count returns 33 again.

### Finding 2

Severity: L.
Where: `dev-diary/probes/gemini.md` lines 29 through 32 in the main checkout copy.
What: The credential paragraph sentence about the key JSON runs 32 words, over the 30 word limit.
Pin: A word count over the exact sentence returns 32 against the limit of 30.
Mutation: Rejoin the split fix into the single long sentence and the count returns 32 again.

## Verdict

REMEDIATE. Two in-scope style findings remain. The probe, the record, the dependency pin, the gate, and both spend-free mutations all pass. I committed nothing. Plants lived in scratch copies only. The tree is clean at the end.
