# T7.30 remediation round 1

Reviewed hash: `3f37fec4428f6aa38d81e5a4c1465179b0a17dfd`
New head: `9dcf009f6dcabc5367178816b4f751b8bbfc4443` on branch `task/t7.30-rem-r1`, one commit on the reviewed hash, no rebase, no amend. Worktree `/home/nryn/work/reprise-wt/t7.30-rem-r1` is clean after the commit. No lambo tools exist in this environment, so nothing was recorded outside git. No other repository was touched.

## Fix rows

| Finding | Fix | Pin | Verification | Gate exits |
|---|---|---|---|---|
| M, round 1: live episodes render no publish or erase controls (`+page.svelte:118`) | The Release section now renders for live and fixture rows. Only the fixture link line and the export button stay behind `{#if !snap.live}`. Fixture DOM is unchanged: same buttons in the same order with the same text. Live rows get Publish, Revoke, and the two step erase, wired to the live controller branches that call the real routes. | Round 1 python probe (first `{#if !snap.live}` block to its matching `{/if}`, control lines are `publishState`, `controller?.erase`, `<h2>Release</h2>`, pass needs one outside). Fixed tree: block is lines 121-123, all four hits outside, exit 0. Guard restored: block is lines 118-135, zero hits outside, exit 1. New Playwright walk `a live episode walks its release controls against the routes` in the owned threads spec: stubs season plus detail, publish POST plus DELETE, and episode DELETE, then walks publish, revoke, and the two step erase on `/episode/e9`. | Probe run on the fixed tree (exit 0) and on the stashed unpatched tree (exit 1). Playwright walk passes on the fixed build (11 of 11 in the threads config) and fails on a rebuilt unpatched build, because no Publish button renders for the live row. | See gate record below. |
| H, from T7.31 round 1: erasing one seeded copy deletes the shared catalog audio (`erase.go`, `sweep.go`) | `inventory` reads the seeded flag first and returns an empty ref for a seeded copy, so its erasure carries rows plus cover only and never names a blob, session, or transcript. `eraseEpisode` branches the same way: a seeded copy goes through a new `dropSeeded` row delete scoped to its guest, which cascades to the copy render and link rows and keeps the shared blobs. Recorded episodes flow exactly as before in both paths. The seeded copy design is untouched. Only owned paths changed. | `TestEraseKeepsSharedCatalogAudio` (`internal/privacy/seeded_erase_test.go`): two seeded copies share one catalog render blob through real media rows, erase one copy through the real erasure to job done, then assert the copy rows are gone while the catalog blob row, blob file, catalog rows, and the other copy render plus file survive. `TestSweepKeepsSharedCatalogAudio` (`internal/retention/seeded_sweep_test.go`): an expired guest owns one seeded copy plus one recorded episode, sweep to done, then assert the guest is fully gone, the recorded blobs and files are gone, and the catalog blob row, blob file, episode, and link survive. | Both tests pass on the fixed tree, including `-count=2` with race. With the two source fixes stashed, both fail on the unpatched code with `media holds 0 catalog blob rows`, which is the defect signature. Restored after. | See gate record below. |

## Gate record (all run in the remediation worktree)

`./tools/check.sh` stops at the standing workstation exception (`ffmpeg is 9.0.2, not the pinned 9.0.1`). Every later step ran by hand from the worktree root.

| Step | Exit |
|---|---|
| `gofmt -l cmd internal` | 0, no output |
| `go vet ./cmd/... ./internal/...` | 0 |
| `go test -race ./cmd/... ./internal/...` (full) | 0 |
| `npm ci` in `web/` | 0 |
| `npx svelte-kit sync`, `npm run check` | 0, 0 errors, 0 warnings |
| `npm run lint` | 0 |
| `npm run test -- --run` | 0, 23 files, 241 passed |
| `npm run build` | 0 |
| Playwright threads config (default Chromium browser) | 0, 11 passed, including the new live walk |
| Playwright share config | 0, 3 passed |
| Playwright episode edit config | 0, 5 passed |
| svelte 4 leakage scan over touched web files | empty |
| plan reference scan over all touched files | empty |
| WebKit | cannot launch on this host, CI must cover it |

staticcheck is not installed here, so that step was not re-run. Nothing else was fixed. No new findings.
