# T4.3 round 1 review

Reviewed hash: e0417e6f420147faf2db1f9535c4b09c9cd7c1e3.
Worktree: /home/nryn/work/reprise-wt/t4.3-rev-r1 (detached HEAD, confirmed
with `git log -1 --format=%H`). No commits made. Scratch probe edits (h1
swap for the shell-spec mutation, since reverted) were fully reverted.
`git status --short` is clean. `npm ci` ran inside the worktree's `web/`
only. The main checkout was never touched.

Base for scope: claim commit fb96913. The fb96913..e0417e6 range also
carries landed T4.1 and T4.4 work, so scope was judged on the declared
owns only: `web/src/routes/+page.svelte`,
`web/src/routes/episode/[id]/+page.svelte`, `web/src/routes/threads/`.

## What was re-run (all in the review worktree, at the handed hash)

* `npx vitest run src/routes/threads/threads.test.ts`: 17 passed.
  Covers season order newest-first, every thread quote sitting in its
  transcript, every quote offset opening the turn that carries it,
  word-edge landing, high-water never backwards, and WAV/SSE shapes.
* `npm run build` then
  `npx playwright test -c src/routes/threads/threads.playwright.config.ts`:
  6 passed. Thread click lands `/episode/ep-2?t=20` with readout
  `0:20 of 1:10`, playback starts at or past 19.5 after one gesture.
  Progressbar reads at or past 50, survives reload non-decreasing.
  Newest-first order, word-click seek to `0:14`, release-states-only
  behavior, and both gates on all three screens all pass.
* `npx svelte-check --no-tsconfig`: 0 errors, 0 warnings.
* `npx eslint` over the three owned paths: clean.
* `npx playwright test tests/shell.spec.ts` (default config, fresh
  build): 1 failed, quoted below. Outside owns.
* `grep` over owned routes for `.get(`, `export let`, `$:`,
  `svelte/store`, sentiment/gauge/chart/score shapes, and plan refs
  (`T4.3`, `product.md`, `project.md`, `§`): no hits, except the prose
  disclaimer "No scores, no gauges" on the threads screen, which states
  the constraint rather than violating it.

## Headline ruling: the shell.spec failure is OUT_OF_SCOPE, option (a)

The full gate fails on `web/tests/shell.spec.ts`, which sits outside the
declared owns (T0.1's shell test). The implementer correctly left it
untouched. The gallery heading is the task working as specified: the T4.3
spec builds the gallery at the root route with episodes newest first, and
the mockup gallery view heads the season, not the app name. App branding
is intact elsewhere (`web/src/lib/shell.ts` still exports
`appName = 'Reprise'`, and document titles still read `Gallery · Reprise`).
So this is a stale one-line assertion in another task's file, not a
design defect in the gallery. It never blocks APPROVE. Landing still
needs a green gate, so whoever owns `web/tests/shell.spec.ts` updates the
expected root heading (or retargets the test) before or beside landing.
Recorded as OUT_OF_SCOPE M below, with pin and mutation both executed
here in both directions.

## Findings

In-scope findings: none.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| (none) | | | | |

OUT_OF_SCOPE rows (do not block APPROVE):

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `web/tests/shell.spec.ts:5` (owning path outside T4.3 owns) | Stale assertion expects the root h1 to read `Reprise`; the gallery now heads the season view with `The season so far`. | `npx playwright test tests/shell.spec.ts` at the handed hash fails: `locator resolved to <h1>The season so far</h1>, unexpected value "The season so far"`, `expected 'Reprise'`. Re-ran here after a fresh build. | Scratch edit of `web/src/routes/+page.svelte` h1 to `Reprise`, rebuilt, shell spec passed (`1 passed`). Reverted, rebuilt, shell spec failed again. Load-bearing both ways. Tree clean after. |

## Other scrutiny, with evidence

* Same JobStream, read-only verified. `web/src/lib/voice/processing-state.ts:8`
  and `web/src/routes/threads/threads.ts:7` both import `JobStream` from
  `@nrynss/chaaya/job`. The gallery follower constructs it with
  `url` plus `fetchState` and `attach()`, the same mechanism T2.4's
  processing screen reads. The T2.4 file was read, never edited.
* High-water mark. Seeded from browser storage on follow, rendered
  instead of the live value, written back monotonically. Vitest
  `never moves backwards across writes and restarts` passes, and the
  reload proof passes. Replayed streams cannot drag a card backwards.
* Quote to offset to seek. Every quote-in-transcript and every
  offset-opens-its-turn assertion passes in vitest. The deep link parks
  the playhead before any gesture and invites the press; playback only
  starts from a click handler. No autoplay race.
* Release controls render states only. `publishState` flips a local flag
  with a notice. `erase` attempts the endpoint and degrades to the
  refusal notice when the fixture has no backend (proof asserts the
  exact notice after the fixture 404). `exportNotes` sets a notice.
  Actions stay with T4.4 and T5.1. Correct.
* No sentiment gauges, charts, or scores. Grep clean apart from the
  constraint disclaimer. Thread cards show quotes and links only.
* Fixture sharing via the threads directory. Both pages import from
  `./threads/threads` inside the task's own owns. No cross-task
  coupling, no ownership breach. Acceptable with the handoff's cleanup
  note. Not a layering defect, no finding filed.
* Stale preview server anecdote. No committed workaround found: no
  port-killing, no retry hacks. Both controllers clean up in
  `destroy()` (interval cleared, streams closed, fetch restored,
  player paused). The route config uses `reuseExistingServer: false`.
  Process anecdote only.
* Runes only, keyboard, names. `$state` and `$effect` throughout, no
  `export let`, no `$:`, no store import. Every interactive element the
  proofs touch resolves by role with an accessible name, and the gate
  proof asserts both gates on all three screens.

## The ten questions

1. Does any path spend money without reserving budget first? No. Owned
   paths hold fixture screens only. No paid call exists in them.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. No session code in the owned paths.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Fixture data only, no backend, no serving path in owns.
4. Does any edit apply without a revertible record? No. No edit applies;
   publish and erase are local fixture states with notices.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No spoken callback exists here. Count strings are
   disclosed scripted-fixture display text, and the quote backing each
   thread item is pinned to sit in its transcript.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Grep over owned routes is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. Job
   progress rides Chaaya `JobStream`, playback rides `AudioPlayer`,
   word sync uses `activeWordAt` from the transcript module, gates come
   from the testing module. The WAV wrapper and tone generator are
   fixture harness, matching the T2.4 precedent.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Hash confirmed, detached worktree, no commits, probes reverted,
   tree clean.
9. Can any paid job kind resume after a restart and spend twice? No. No
   paid job in the owned paths. The mock render job spends nothing.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Vitest and the six route proofs run on
    the generated tone with scripted streams. Poll timeouts are test
    bounds, never assertions about audio timing.

## Verdict

APPROVE. Counts: C 0, H 0, M 0, L 0 in scope, plus one OUT_OF_SCOPE M
(the stale shell heading assertion, owned by `web/tests/shell.spec.ts`).
No residue questions apply; this is round 1. Landing unblocks when the
shell spec owner updates the one-line root heading expectation.
