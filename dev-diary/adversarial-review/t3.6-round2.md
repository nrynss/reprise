# T3.6 round 2 review (re-review)

Reviewed hash: `efe1d2c3c3dd1791420fb903d9a2f84a253eab68`
Worktree: `/home/nryn/work/reprise-wt/t3.6-rev-r2` (detached HEAD, confirmed `git log -1 --format=%H` equals the handed hash)
Base for this round: `69d268d7fd93cd41ec3340e9bd627b4caa4557de` (`git diff 69d268d..efe1d2c` touches `internal/cover/cover_test.go` only, 40 added lines, inside the declared `owns`)
Verdict: APPROVE
Counts: C 0, H 0, M 0, L 0

The reviewer never committed, never edited tracked code, and ran every pin below in its own worktree. `git status --short` is empty after all checks. `go test -race -count=1 ./internal/cover/...` passes, and `-count=2` passes. `go vet ./internal/cover/...` is clean. `gofmt -l internal/cover/` reports nothing. `git grep` for plan ids, phase ids, planning filenames, and section marks over `internal/cover/` returns nothing.

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with a single L finding (`internal/cover/cover.go:232-267`, rerun-overwrite unpinned) and stated everything else green with load-bearing pins.

- C residue: zero. Round 1 had zero C findings, and this diff adds a test only. No spend, privacy, or loss path changed.
- H residue: zero. Round 1 had zero H findings. The added test touches no paid seam, kind flags, or fallback path.
- M residue: zero. Round 1 had zero M findings. No workaround-grade defect was open and none is introduced.
- L residue: zero. The one round 1 L is fixed by the committed `TestRerunReplacesCoverRow` (`internal/cover/cover_test.go`). The pin was re-run in this worktree and passes. The mutation was re-proven here: removing the `DELETE FROM covers WHERE episode_id = ?` block from `storeImage` makes the new test fail with `UNIQUE constraint failed: covers.episode_id` on the second `Run`, and restoring the line returns the suite to green. The pin tests behavior (replace semantics: same file, same bytes, one row, fallback mark clear) rather than implementation (it would pass under any replace strategy, such as an upsert, so it does not lock the DELETE statement itself).

## New findings

None. The remediation diff is one test. It was hunted for new defects:

- Fixture coupling: none. The test builds its own diary file and its own covers dir from `t.TempDir()`, seeds one episode with the same helpers as its siblings, and shares no state.
- Parallel safety: none. `t.Parallel()` matches every sibling, and all state (DB file, cover dir, scripted model, fake budget, receipt) is per-test.
- Timing: none. No clock, sleep, or wall-clock threshold. The two `Run` calls are sequential in one test.
- Interference with sibling pins: none. The full package passes under `-race -count=2`, including the budget, fallback, kind, and fixture tests round 1 cited.
- Budget accounting on the double run: the test asserts `model.Calls == 2` (one paid call per invocation, no hidden retry) and the row-count/file/sha assertions pin the replace contract. It does not assert settle counts for the two runs, which is acceptable because per-run settle behavior is already pinned by `TestRunStoresModelCover` and the kind forbids resume rather than rerun.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| (none) | | | | |

## The 10 questions

1. Does any path spend money without reserving budget first? No. The diff adds no production path. Round 1's reserve-before-call evidence stands untouched.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This task touches no AssemblyAI surface.
3. Can a private episode, stem or transcript reach anyone but its owner? No new exposure. The diff is a test. Production bytes remain `0600` with `owner_id` rows.
4. Does any edit apply without a revertible record? No. The rerun overwrite the new test pins is the disclosed episode-named regeneration, with no editorial state touched.
5. Does any spoken callback or count come from the prompt instead of a stored row? Not applicable. The brief still reads stored `title` and `show_notes` proposal rows.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No, verified by `git grep` over `internal/cover/`. The new test comment states its reason directly.
7. Did this task rebuild something Keel or Chaaya provides? No. No production code changed.
8. Was this review run on the handed hash, in a worktree of its own? Yes, stated and confirmed above. The mutation probe was applied and fully reverted in this worktree with no commit, and `git status --short` is empty.
9. Can any paid job kind resume after a restart and spend twice? No. Kind flags are untouched, and the new test confirms a deliberate rerun makes exactly one model call per invocation with no resume path.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The new test runs offline through the scripted double with exact assertions.

## Verdict

APPROVE. Zero findings across all severities. Zero residue against round 1: the single L is fixed with a passing load-bearing pin whose mutation was re-proven in this worktree, and C/H/M residue is zero.
