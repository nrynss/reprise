# T4.4 round 2 review (re-review)

Reviewed hash: 8d175584de1193b9811bb343f3325050e668f1de.
Worktree: /home/nryn/work/reprise-wt/t4.4-rev-r2 (detached HEAD, confirmed
with `git log -1 --format=%H`). No commits made. Temporary probe edits
(schema cascade drop, publish-order swap, unpublish latest-only restore)
were all reverted. `git status --short` is clean.

Base for remediation alone: c504d03f47db531a8fe438e1be6c83824ac4822d.
`git diff c504d03..8d17558 --stat` shows only `internal/privacy/`:
`publish.go`, `publish_test.go`, `privacy_test.go`. Inside owns. Three
commits, no rebase, no amend of the reviewed commit.

## What was re-run

* `go test -race -count=1 ./internal/privacy/`: pass.
* `gofmt -l internal/privacy/`: clean.
* `npm ci` in `web/`, then full `./tools/check.sh`: every step passes
  (go tests, svelte-kit sync, svelte-check, eslint, vitest, build,
  playwright) except one FAIL, `-- svelte 4 leakage`, on
  `web/src/routes/share/[token]/+page.svelte:20`. See Finding 1.
* `TestUnpublishRevokesEveryRender`: pass (round 1 C pin shape).
* `TestPublishKeepsEpisodePrivateWhenBlobMissing`: pass (round 1 H pin).
* Round 1 L mutation re-executed here: dropped `ON DELETE CASCADE`
  from `mentions.episode_id` in a scratch copy of the migration. The
  suite failed with `erase: stuck targets: rows` (FK constraint), then
  the file was restored and the suite passed again.
* Round 1 C mutation re-executed here: restored the latest-row-only
  flip in `Unpublish`. The new test failed with `signed-out first opus
  status 200, want 404`. Restored.
* Round 1 H mutation re-executed here: restored the episode UPDATE
  before `setVisibility` in `Publish`. The new test failed with
  `episode visibility "public", want private`. Restored.
* Plan-reference pattern set over the owned paths: clean.

## Residue vs round 1, severity by severity

* C: zero. The superseded-blob leak is fixed and the pin is
  load-bearing (mutation re-run fails 200, fix passes 404 on both
  opus blobs with private visibility).
* H: zero. The publish-before-flip ordering is fixed and the pin is
  load-bearing (mutation re-run reads public, fix reads private).
* M: zero. Round 1 filed none in scope.
* L: round 1's seeded-tables gap is fixed with zero residue (all ten
  content tables seeded, cascade-drop mutation fails loudly instead
  of passing silently). One NEW L below, from the gate, not from
  round 1.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L | `web/src/routes/share/[token]/+page.svelte:20` (`page.url.searchParams.get('fixture')`) | The enforced `check.sh` svelte-4-leakage scan fails on this line, so the gate is red at the handed hash. The pattern `\bget\s*\(` matches `URLSearchParams.get`, which is standard Web API and not a Svelte 4 store read. Semantically a scan false positive, but the scan is enforced and this task introduced the line (the file is new in this task, byte identical since the reviewed commit). T2.4 rewrote identical `.get(` hits instead of blessing them. Behavior is intact: svelte-check, eslint, vitest, build, and Playwright all pass. | Run `./tools/check.sh` after `npm ci`. It ends `FAIL [svelte 4 leakage]` naming this line. | Rewrite the read without `.get(` (for example through `Object.fromEntries` of the params) and the scan passes with identical page behavior. |
| OUT_OF_SCOPE | `tools/check.sh:94-99` (leakage pattern) | The scan pattern also matches any future `Map.get`, `URL.get`, or param `.get(` call, so the next task to write one trips the same red gate. Narrowing the pattern to the store import belongs to the gate owner, not to this task. Non-blocking. | N/A (gate ownership, not a regression). | N/A. |

Round 1 OUT_OF_SCOPE rows (missing `ReErase` route, owed workstation
remeasure) stand untouched and still do not block.

## New-defect hunt over the three commits

* Unpublish sweeps opus and export blobs of every render row. The
  export copy was never public, so its flip is a harmless no-op. The
  cover is a file gated on `LookupShare`, and the episode row goes
  private first, so the cover dies with the token. No new leak path.
* Publish flips the blob before the episode row. A crash between the
  two leaves a public blob behind a private episode, which no share
  lookup can reach, and a retry heals it. Fail-closed, same class the
  round 1 fix accepted.
* The sweep collects blob ids before flipping, so no read-write
  deadlock on one connection. Missing blob rows stay tolerated, which
  the comment states. Concurrent double publish leaves last-token-wins
  with the blob public either way. Nothing found.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   touched code performs deletes and visibility flips only.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Untouched by the remediation, still closed
   through `sessionTarget`.
3. Can a private episode, stem or transcript reach anyone but its
   owner? No, with teeth: the committed every-render test publishes
   twice, unpublishes, then fetches both opus blobs signed out and
   reads 404 with private visibility (re-ran green). The mutation
   re-run reads 200, which proves the test measures the sweep. Stems,
   the export copy, and revoked-token 404 identity are unchanged from
   round 1.
4. Does any edit apply without a revertible record? No. Publish and
   unpublish remain reversible flips.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. Untouched share metadata reads stored rows only.
6. Does any code, comment, test or migration cite a phase, a task, or
   a planning file? No. Pattern set over owned paths is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. The
   remediation adds no new surface, only ordering and sweep scope.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Hash confirmed, detached worktree, no commits, probes
   reverted, tree clean.
9. Can any paid job kind resume after a restart and spend twice? No.
   No paid call touched; erasure targets unchanged.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Full gate ran here except the scan FAIL
    above; no live device or person involved.

## Verdict

REMEDIATE. Counts: C 0, H 0, M 0, L 1, plus one OUT_OF_SCOPE note for
the gate owner that does not block. Round 1 residue is zero at every
severity. The single L is the red leakage scan on a line this task
introduced: behavior intact, gate red by definition. The fix is a
mechanical rewrite of one read, and it may qualify for the
orchestrator L exemption if all four conditions hold.
