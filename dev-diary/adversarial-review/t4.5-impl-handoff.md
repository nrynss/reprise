# T4.5 implementer handoff

Updated the one root-h1 expectation in `web/tests/shell.spec.ts` from `Reprise` to `The season so far`, the gallery heading T4.3 renders at the root route.

If the gallery heading ever changes, this spec must move with it.

Gate note: the shell spec fails in this worktree because the base predates the gallery route, so `/` still renders `<h1>Reprise</h1>`. It passes once this lands beside the gallery route. Scope was not expanded to fix that here.
