# T7.29 review, round 1

- **Hash reviewed:** `9892c64652e75b629366afac456726dd050129d9` (`git log -1` confirmed it)
- **Worktree:** `/Users/narayan/reprise-wt/t7.29-rev-r1`, detached, fresh
- **Base:** `78f8ab3`. Diff read with `git diff 78f8ab3..9892c64`.
- **Probes:** `dev-diary/probes/t7.29-rev-r1/finish_probe_test.go` (copy into `cmd/reprise/`, then
  run `go test -race -run TestProbe ./cmd/reprise/`) and `dev-diary/probes/t7.29-rev-r1/wire_mutation.sh`
  (run from the worktree root).

**Verdict: REMEDIATE**

| Severity | In scope | Out of scope |
|---|---|---|
| C | 0 | 0 |
| H | 1 | 0 |
| M | 2 | 1 |
| L | 1 | 2 |

## Gate

The base issues were handled as briefed. I created an untracked `dev-diary/probes/go.mod`
(`module probes`, `go 1.27.1`) before the gate and deleted it afterwards. `check.sh` stops at the
accepted ffmpeg 9.0.2 tool check, so I ran every later step verbatim from the worktree root in bash.

| Step | Exit |
|---|---|
| `cd web && npm ci` | 0 |
| `gofmt -l cmd` | 0, no output |
| `go list ./...` (web filtered) | 0, 22 packages |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| svelte 4 leakage `git grep` from the root | 1, no match (not even the known `draft.ts` hit) |
| consumer names `git grep` from the root | 1, no match |
| plan references `git grep` from the root | 1, no match |
| `go test -race ./...` | 0 |
| rebuild, then `playwright test -c src/routes/gallery.playwright.config.ts` | 0, 8 passed |
| rebuild, then `playwright test -c src/routes/threads/threads.playwright.config.ts` | 0, 10 passed |
| round 4 probe `gallery-rendering-card.probe.test.ts` from `web/src/routes/threads/` | 0, passes |

`TestMarkDoneReachesReadyOffline` and `TestDuplicateKindRefusesBoot` both pass at the hash.

## Done when, with mutations

- **Mark done to ready, no second chain.** In `advance`, changing `if err != nil || pass.Found {` to
  `if err != nil {` makes `TestMarkDoneReachesReadyOffline` fail (the episode never leaves
  `analysing`). The test is load-bearing for the idempotent start.
- **Duplicate kind refuses the boot.** Changing `taken {` to `taken && false {` in `mergeKinds` makes
  `TestDuplicateKindRefusesBoot` fail at `finish_test.go:686`. Load-bearing.
- **`main.go` calls every hook.** Not pinned. See finding 3. Dropping the mount call or the feature
  kinds from `wireAPI` leaves the whole suite green.
- **Racing completions.** `TestProbeRacingRenderCompletionsStartOneChain` runs two `settleRender`
  calls on one finished render at once, under `-race`, three times. One analysis, one cover and one
  marking pass run, and the batch client sees one create. `markRendered` holds the schedule lock
  across the state check and the move, so this holds.

## The ten questions

1. **Spend without reservation?** No. Analysis, cover and marking reserve through their owner budget
   before they call out, as before. `analysis.Run` now refuses an empty render id before the locator
   or any paid call. `plainArt` books nothing and calls nothing paid. The fallback cover runs
   `cover.Run` with a model that always refuses, so no request leaves the box.
2. **AssemblyAI session left open?** No. This task opens no voice session.
3. **Private data to a non-owner?** No. Each new outcome reads `s.Get(owner, episode)` first, so a
   foreign episode answers `ErrNotFound`. `TestEpisodeDetailCarriesRenderPassOutcomes` shows a
   stranger gets 404 with no job id or error text. `RenderedWords` checks the owner before it reads.
4. **Edit without a revertible record?** No. The chain applies no edit.
5. **Callback or count from the prompt?** No.
6. **Plan citations in code?** No. The plan reference scan is empty, and I read every new comment
   and the migration.
7. **Rebuilt Keel or Chaaya?** No. Kinds, resume, attempts and limits come from `keel/job`.
8. **Handed hash, own worktree?** Yes. The main checkout was only read, apart from this file and the
   probe folder.
9. **Paid kind resumes and spends twice?** No. Analysis, cover, marking, editorial and transcript
   stay non-idempotent with no resume, so Keel marks them `interrupted`. `advance` treats an
   interrupted pass on the newest render as found and terminal, so it never restarts one.
   `ensureEditorial` treats an interrupted editorial job as found, so `recoverDraftPipeline` never
   restarts it either. `TestRestartInterruptsPaidPassesAndShips` and
   `TestRestartKeepsDraftAfterLostEditorial` pin both. Only the render resumes, and it calls nothing
   paid. The passes after a resumed render still check for a pass on that render before they start.
10. **Check needs a person, microphone or wall-clock threshold?** No. The waits poll stored state.
    Their two-minute deadline only stops a hung chain.

## Judgements the brief asked for

- **Interrupted editorial keeps the draft.** Accepted. `product.md` and invariant 14 say an episode
  always ships with a plain title when the editorial model fails. A plain editorial error already
  left the draft in place at base. Failing the draft only on an interrupt made the two cases
  disagree. It also pushed the owner to Retry, which reruns a paid pass. The new path spends nothing
  more.
- **Rendered words tied to their render.** The namespaced migration applies cleanly on top of the
  existing schema, and a second run changes nothing. Existing words with no link row stop being sent.
  That is correct, because no live episode has reached `ready`. But the table's owner column breaks
  retention (finding 2).
- **Wiring split.** The hooks, `kindWiring`, `routeWiring` and the boot refusal are sound. A later
  task can wire itself from its own file. Privacy still needs `internal/api/routes.go` to drop its
  stubs, and the plan already gives T7.30 that path. Privacy can build its own provider clients from
  `Settings`, which carries the resolved key. A feature that shares one service between its kinds
  hook and its mount hook has to keep it at file level. That is workable, and not a defect.
- **Deploy note.** Starting a render at boot for a stuck `rendering` episode respects invariant 13.
  The render is free, and the paid passes after it never ran, so nothing reruns. Each pass reserves
  first. The spend is what the owner's mark done already asked for. The note is wrong on one count,
  though. Only one stuck episode gets a render per boot, and the rest stay stuck (finding 1).

## Findings

### 1. A boot render refused at the render limit leaves its episode rendering forever

| Column | Value |
|---|---|
| Severity | H |
| Where | `cmd/reprise/finish.go:536` (`recoverRender`, the `!last.Found` branch), with `cmd/reprise/finish.go:484` (`advanceAll` lists `analysing` only) |
| What | The render kind has `Limit: 1`. At boot, `recoverRender` starts a render for the first stuck episode. For every further episode with no render job, `StartEpisodeKind` returns `job.ErrLimit`, which is only logged. Nothing retries. `runAdvanceLoop` walks `analysing` episodes, never `rendering` ones. So those episodes stay `rendering` with no job behind them, and their cards read "rendering" forever. The handoff's deploy note says every stuck episode on the box will render, but only one will. A render that Keel resumed at boot takes the slot too, and then every unstarted one stays stuck. |
| Pin | `TestProbeRecoverRendersEveryStuckEpisode` in `dev-diary/probes/t7.29-rev-r1/finish_probe_test.go`. It plants two `rendering` episodes with no render job, boots, and waits for the first to reach `ready`. Then it ticks `advanceAll`. It fails with "second stuck episode still rendering with 0 render jobs". |
| Mutation | Once the fix is in (for example, `advanceAll` also calls `recoverRender` for each `rendering` episode), delete that call and the probe fails again. |

### 2. The render link row keeps the guest as owner, so the guest sweep cannot delete a guest after Keep

| Column | Value |
|---|---|
| Severity | M |
| Where | `internal/analysis/migrations/0001_rendered_source.sql:9` |
| What | `rendered_sources.owner_id` references `users (id)` with no cascade. `retention.Keep` moves a guest episode to the owner and promises that no row still names the guest. It repoints its fixed table list, which does not know this new table. After Keep, the link row still names the guest. The sweep's final `DELETE FROM users` for that guest then fails the foreign key. The column adds nothing, because the episode already names its owner and `RenderedSource` reads by episode alone. |
| Pin | `TestProbeKeepLeavesRenderLinkOnTheGuest`. It stores words through `analysis.ReplaceWords`, keeps the episode, and then runs the sweep's user delete. It fails with `FOREIGN KEY constraint failed (787)`, and the link owner reads the guest. |
| Mutation | After the fix (drop the owner column from the table and from `ReplaceWords`, or cascade it), put back `owner_id TEXT NOT NULL REFERENCES users (id),` and the probe fails again. |

### 3. Nothing pins that the boot calls the hooks, or the rest of the wired path to ready

| Column | Value |
|---|---|
| Severity | M |
| Where | `cmd/reprise/main.go:715` (`mountFeatures`), `:652` (`extra` into `openJobs`), `:560` (`analysis.Migrate`), `:729` (`runAdvanceLoop`). `cmd/reprise/finish_test.go:717` (`TestFeatureHooksAreWired`). |
| What | "Done when" asks for proof that `main.go` calls every hook. The handoff cites `TestFeatureHooksAreWired`, but that test only checks the `features()` list and calls the helpers itself. `wireAPI` has no test. `TestMarkDoneReachesReadyOffline` boots `startJobs` over a fixture that runs its own migrations, not the binary's boot. So four edits to the boot leave the suite green. The first skips the mount hooks. The second drops the feature kinds, which also turns off the duplicate-kind refusal. The third skips the analysis migration, and then every analysis would fail at `ReplaceWords` after its paid calls. The fourth stops the advance loop that starts passes a full kind refused. |
| Pin | `dev-diary/probes/t7.29-rev-r1/wire_mutation.sh`, run from the worktree root. It applies each boot mutation, runs `go test ./cmd/reprise/`, and restores `main.go`. It exits 1 at the hash, and prints `SURVIVED` for all four. |
| Mutation | The four edits in the script, each one line. For example, replace `mountFeatures(ctx, feats, routeWiring{` with a no-op func literal. Once `wireAPI`, or a boot seam it shares with the tests, carries these calls under a test, each mutation must fail it. |

### 4. The settled map never drains for an episode that shipped

| Column | Value |
|---|---|
| Severity | L |
| Where | `cmd/reprise/finish.go:283`, with the early return at `:290` |
| What | `advance` records each settling pass in `j.settled` before it checks the state. The entry is removed only on a later `advance` that reads the pass record as terminal. Once the episode is `ready`, `advance` returns at the state check first. So the last pass entries (marking and cover) stay for the life of the process, which is two entries per shipped episode. |
| Pin | `TestProbeSettledMapDrains`. It ships one episode, ticks `advanceAll`, and reads `len(j.settled)`. It fails with "settled holds 2 entries after the episode shipped". |
| Mutation | After the fix (for example, drop this episode's keys when `advance` meets a state other than `analysing`, or after `MarkReady`), delete that line and the probe fails again. |

## OUT_OF_SCOPE

| Severity | Where | What | Pin | Owner path |
|---|---|---|---|---|
| M | `internal/retention/keep.go:23` | `keptTables` does not list `covers` or `resolutions`. Both have owner columns that reference `users`. After Keep, the cover row still names the guest, and the sweep's guest delete fails the foreign key. | `TestProbeKeepLeavesCoverOnTheGuest` fails with `FOREIGN KEY constraint failed (787)`. | `internal/retention/` (T7.30 owns it) |
| L | `internal/render/run.go:53` | `KindOf` marks the render resumable but sets no `MaxAttempts`, so Keel never resumes a render. Each stage report also drops the descriptor that `Resolver.Resume` reads, and a reused render appends no row. The binary works around all three (contract changes 1 and 2). | `TestProbeRenderKindResumes` fails with "MaxAttempts = 0". | `internal/render/` |
| L | `web/src/routes/record/mock.playwright.config.ts:7` | `testDir: '.'` with no `testMatch` loads the vitest files beside it, and the run exits 1 (contract change 8). | `npx playwright test -c src/routes/record/mock.playwright.config.ts` exits 1 at `stems-complete.test.ts:35`. | `web/src/routes/record/` (T7.33 is closest) |

The other contract changes are not defects. Change 3 (`cover.StoreFallback`) is a convenience.
Change 4 is already fixed on `main`. Change 5 is correct behaviour, so the stale round 2 probe needs
its link row, not a code fix. The plan already scopes changes 6 and 7 into T7.30 and T7.31.

## Residue

Round 1 has no prior round.
