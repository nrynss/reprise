# T7.11 round 1 review

Reviewed hash: `38d4696c49a8e5ce30822eeccbc5363a3141c820`
Worktree: `/home/nryn/work/reprise-wt/t7.11-rev-r1` (detached, confirmed with `git log -1`)
Base: `e0551fa`
Diff scope: `web/src/routes/record/+page.svelte`, `web/src/routes/record/stems-complete.ts` (new), `web/src/routes/record/stems-complete.test.ts` (new), `web/src/routes/record/stems-complete.spec.ts` (new). All four sit inside the task `owns` line. Nothing else changes.

## Pins run

All pins ran in this worktree after `npm ci` in `web/`:

* `./tools/check.sh` from the worktree root passes. Output ends with `OK all checks passed`. This covers Go tests with race, svelte-check, eslint, vitest, the production build, the default Playwright suite, Svelte 4 leakage, consumer names, and plan references.
* `npx vitest run src/routes/record/stems-complete.test.ts`: 18 passed.
* `npx playwright test -c src/routes/record/mock.playwright.config.ts stems-complete.spec.ts`: 3 passed. Cases cover the first completion moving to draft with one job, a repeat reporting the standing outcome, and a failure refusing loudly with a working retry.
* `python3 dev-diary/tools/audit_docs.py`: `OK planning tree is consistent`.

## Load-bearing check

One mutation proves the completion trigger pin measures the code under review. Renaming the `__stems` window handle in `stems-complete.ts` to `__stems_disabled`, rebuilding, and rerunning the mock spec fails the draft move case because the spec has no handle to drive. Restoring the file and rebuilding returns the suite to green. `git status --short` shows a clean tree and `git diff --stat` shows nothing against the handed hash.

Static grep proves the seam claim below. `driver.run(` appears exactly once in non-test source, inside `exposeStemsMock` in `stems-complete.ts:178`. `+page.svelte` calls only `completionDriver.retry()`. `record-state.ts` holds zero references to the completion call. The only production of `run` arguments is the mock handle.

## Seam ruling

The production trigger is missing. Nothing in a real take calls the completion driver. The controller in `web/src/lib/voice/record-state.ts` finishes both uploads in `endTake` and navigates to the processing screen without posting stem completion. The done criteria do not pass without that call, because the caller stays reachable only through the mock handle behind `mock=1`.

This review scopes a follow-up instead of widening. The trigger must live in `web/src/lib/voice/record-state.ts`, which sits outside this task `owns` and inside done work from the earlier uploader task. The follow-up owns `web/src/lib/voice/record-state.ts` plus the mock harness surface it needs. It wires the controller to post the stored pair after both uploads finish and before the processing navigation, reuses the driver and its loud retry, and extends the mock proof so the automatic path fires without a hand driven handle. No reviewer here demands edits outside `owns`. The in-scope verdict below stands on its own.

## Ten questions

1. Does any path spend money without reserving budget first? No. The new code posts one link call to an existing route and uploads nothing. No paid path opens.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The diff never touches the socket or the session.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The post goes to the episode route under the existing session. The spec stub never serves bytes.
4. Does any edit apply without a revertible record? No. Nothing here edits content. Retry reposts a link and never uploads twice.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Every shown job id and state arrives in the server answer. The copy only phrases it.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep over all four diff files finds no task ids, no phase ids, no planning file names, no section marks. `check.sh` agrees.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses the Chaaya API client and the local socket rate constant. The uploader stays as wired.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` shows the handed hash on a detached worktree. No code was committed.
9. Can any paid job kind resume after a restart and spend twice? No. Retry reposts the completion link only. Double scheduling stays guarded server side.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Unit cases stub fetch with fixed answers. The Playwright run uses the mock flag with generated input and the hand moved harness clock. Visibility waits are framework mechanics, not proof thresholds.

## Findings

No in-scope findings. Counts in scope: C 0, H 0, M 0, L 0.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| OUT_OF_SCOPE (C) | `web/src/lib/voice/record-state.ts` `endTake`, owned outside `web/src/routes/record/` | No production caller posts stem completion. A real take finishes both uploads and navigates away without calling the new driver, so episodes stall before the draft move outside mock and curl flows. | Grep shows `driver.run(` only inside `exposeStemsMock` (`stems-complete.ts:178`), `+page.svelte` only calls `completionDriver.retry()`, and `record-state.ts` holds zero completion references. The mock spec passes only because it hand drives `__stems.complete()`. | Rename the `__stems` handle, rebuild, rerun the mock spec: the draft move case fails. This proves the mock handle is the sole trigger. Scope the follow-up named in the seam ruling. It never blocks this APPROVE. |

## Verdict

APPROVE. Zero in-scope findings across all severities. The driver, the page UI with alert plus retry, and both proof layers are correct and pinned inside `owns`. One OUT_OF_SCOPE C finding scopes the production trigger follow-up on `web/src/lib/voice/record-state.ts`.
