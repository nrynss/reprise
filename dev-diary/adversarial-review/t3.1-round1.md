# T3.1 review round 1

Reviewed hash: `3e6c08ea5e8ffe26fd8690cf00971248d7a98cab`, in worktree
`/home/nryn/work/reprise-wt/t3.1-rev-r1` (detached HEAD, `git log -1`
matches the handed hash). Diff read: `63adb19..3e6c08e`. No commits made.
Tree is clean after the round.

Verdict: REMEDIATE.

Counts: C 0, H 0, M 1, L 1.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/transcript/job.go:129` | A receipt store failure returns `receipt store: %w` with no transcript id and an empty `Result`. The provider copy survives by design, but nothing names it, so the explicit retry the handoff promises cannot find and delete it. The copy orphans on the provider with no owner. | Throwaway `TestPinReceiptFailureKeepsID` (scripted provider, `SaveRaw` returns `disk full`): asserts the run error names `tx-9`. Ran in this worktree, failed as `run error = transcript: run: receipt store: disk full, want it to name tx-9`. File removed after the run. The committed `TestRunKeepsProviderCopyOnReceiptFailure` pins `deletes == 0` but never checks the id survives. | Format the error with the id (`transcript: run %s: receipt store: %w`, `transcriptID`, `err`), then delete the `%s` verb and the argument. The pin fails again. |
| L | `internal/assemblyai/batch.go:214` | `Create` builds the request body as `map[string]any`. The enforced Go style says wire shapes are named structs, never `map[string]any`. | `grep -rn 'map\[string\]any' internal/assemblyai/batch.go` hits line 214 (production code; the remaining hits are test doubles). | Replace the named struct with the map literal. The grep hits again. |

## The ten questions

1. Spend without reserving first? No. `Run` reserves the estimate before `Upload`, and `TestRunRefusesWithoutBudget` pins zero provider calls and zero stored words on refusal. Settle follows the delete confirmation. Every failure path releases via defer.
2. AssemblyAI session left open? Not applicable. This is the batch API, not a voice session. `Wait` is bounded by the context, and `TestWaitHonoursCancel` pins cancellation.
3. Private material to anyone but its owner? No new path. The key stays server side, the receipt goes to the wiring-provided sink, and the provider copy is deleted and confirmed. Caveat: the M finding above leaves one orphaned provider copy unreachable, which extends provider retention of a user stem transcript.
4. Edit without a revertible record? No. `Replace` is an atomic delete plus insert of a machine-built timeline, rerun-safe per `TestReplaceRerunWritesNoDuplicates`. User-facing revert belongs to the proposal tasks.
5. Callback or count from the prompt instead of a stored row? Not applicable. No model call here. Keyterms come from `mentions` rows, recent first, pinned.
6. Code, comment, test, or migration cites a phase, task, or planning file? No. Grep over the new files finds only `rounded` and `surrounding`. The commit message is clean.
7. Rebuilt Keel or Chaaya? No. Cost, job kind, ids, and the diary store come from Keel. The provider adapter and the edit model are Reprise-owned per `libraries.md`.
8. Review on the handed hash in its own worktree? Yes, stated at the top. Every pin below was run here.
9. Paid job resume spending twice? No automatic path. `TestKindNeverResumes` pins limit 2, non-idempotent, no resume, one attempt. A manual operator retry after a delete failure pays again, which the handoff declares explicit and the error surfaces with words already stored.
10. Development check needs a person, a microphone, or a wall-clock threshold? No. Providers are scripted `httptest` servers, the 1 ms poll interval sets speed rather than asserting a threshold, and the clip is generated tone. No `time.Sleep` in the new code. The 50 ms assertion compares fixed scripted input with exact arithmetic.

## Note on the 50 ms bar (not a finding)

`TestWordsLandWithin50ms` is load-bearing for merge defects but tautological
for provider accuracy by construction. A +100 ms shift mutation failed it and
both timeline tests, and the tree was restored clean. But the scripted provider
returns exact times that the test compares against themselves, so the test
cannot measure real transcription timing. The probe record shows live start
error up to about 518 ms. The handoff already schedules a live remeasure once
fixture sessions land, and this commit cannot perform it without fixtures. No
fix is available in this task's `owns` paths, so this stays a recorded limit
rather than a finding.

## Verdict

REMEDIATE. Two findings, one M and one L, each with a pin and a mutation.
No out-of-scope defects found.
