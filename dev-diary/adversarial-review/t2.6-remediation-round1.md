# T2.6 remediation round 1

Reviewed hash: `e3f6c3fc2bba687faa6c4f2c31e0985b7e1c6df4`
Remediated head: `6c299d2393c47f0a756c811fda98b8dd8a9f5998`
Worktree: `/home/nryn/work/reprise-wt/t2.6-rem-r1` (branch `task/t2.6-rem-r1`, built on the reviewed commit, no rebase, no amend)

## Fix

| Severity | Where | What changed | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/broker/sweep.go` (`alert`, `sweepOne`, `reviewGone`) plus `internal/broker/sweep_test.go` (`TestSweepAlertFailureKeepsOutcome`) | `alert()` no longer writes the sweep row on delivery failure. It returns the error and the caller appends the failure note to the detail before the single outcome write lands, so the real status and seconds win the first write at both call sites. | `TestSweepAlertFailureKeepsOutcome` (failing alerter plus open 3600 second fixture): passes on the fix with row `created`/`3600` and detail noting `alert failed: delivery down`. Stashed the `sweep.go` fix and reran: the test fails on the old code with row `""`/`0`, so the pin is load-bearing. | Restore the `markSwept` call inside `alert()` on delivery failure ahead of the outcome write. The test fails again with `""`/`0`. |

## Untouched

Settle then delete order, shortfall amounts, idempotent delete, and the reconcile seams are unchanged. Both out of scope rows (landed `token.go` comment, provider adapter shape) are left alone. The live test is not run and not touched.

## Gate

`npm ci` in the worktree `web/`, then `./tools/check.sh`: OK, all checks pass, including `go test -race` across every package.
