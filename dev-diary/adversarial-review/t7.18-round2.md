# T7.18 round 2 review

Reviewed hash: `aa52124e3f7b85ab29232d032bf68670dc10622b`
Worktree: `/home/nryn/work/reprise-wt/t7.18-rev-r2` (detached HEAD, confirmed with `git log -1`)
Base: `fa3d6b5`
Remediation diff reviewed: `git diff 12f97eb1f8c071b7ba48a8f5bbc1d5242ad8c0a4..aa52124e3f7b85ab29232d032bf68670dc10622b`
Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. OUT_OF_SCOPE rows: 0 new (round 1 rows stand as follow up material, none blocking).

## What the remediation changed

Two files, 16 insertions, 2 deletions. `api-guard.ts` gains a `headerValue` helper that walks header pairs instead of calling the direct lookup, clearing the Svelte 4 leakage scan. `stems-complete.ts` gains a branch in `describeCompletionFailure` that returns the guard message untouched when it already names the call, removing the doubled naming. No other path changed.

## Residue against round 1, severity by severity

Round 1 returned C 0, H 1, M 0, L 2, plus 2 OUT_OF_SCOPE rows.

- H (`api-guard.ts` gate failure on the forbidden spelling): zero residue. `git grep -nE '\b(writable|readable|derived|get)\s*\(' -- 'web/src'` returns empty. Full gate after `npm ci` ends `OK all checks passed` with exit 0.
- L (banner names the call twice): zero residue. A vitest probe stubbed fetch with an HTML 200 answer and counted `draft move` mentions in `describeCompletionFailure` output. Count is exactly 1. A second probe case confirmed mixed case `Retry-After` still reads 7 seconds through the new iteration helper. Probe files were removed after the run. `git status --short` is clean.
- L (phase file status line flip): zero residue on remediator owned ground. `git diff 12f97eb..aa52124 -- dev-diary/` is empty, so the remediator touched no planning file. The flip hunk from the implement commit still rides the branch and must fall out at landing, exactly as round 1 prescribed. Only the orchestrator edits phase files, and only in the main checkout, so no remediation round can clear it. Flagged here so the landing step does not miss it.
- OUT_OF_SCOPE H and M (unchecked voice layer client, take clock ticking after failure): unchanged and still outside `web/src/routes/record/`. They never blocked approval. They still want a follow up task owning `web/src/lib/voice/`.

## New findings

None. No table follows because there is nothing to pin.

## Ten questions

1. Does any path spend money without reserving budget first. No. The diff touches only the draft move guard and its banner. It opens no paid call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. Session lifecycle code is untouched.
3. Can a private episode, stem or transcript reach anyone but its owner. No. No read path changes.
4. Does any edit apply without a revertible record. No. No episode mutation changes.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. Only error wording changed.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. The plan reference scan over the touched files returns empty, and the gate plan references check passes.
7. Did this task rebuild something Keel or Chaaya provides. No. The helper walks standard `Headers` pairs. Error shapes still reuse Chaaya `ApiError` and `parseErrorEnvelope`.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` gives `aa52124e3f7b85ab29232d032bf68670dc10622b` in `t7.18-rev-r2`. Every pin ran there. No code was committed.
9. Can any paid job kind resume after a restart and spend twice. No. No backend or job registration changes.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Probes use stub fetch with fixed answers. No duration threshold appears.

## Gate and docs

Full gate (`npm ci` in `web/`, then `./tools/check.sh` from the worktree root) ends `OK all checks passed`, exit 0. That covers gofmt, vet, staticcheck, `go test -race`, svelte-check, eslint, vitest, build, Playwright, the Svelte 4 leakage scan, consumer names, and plan references. `dev-diary/tools/audit_docs.py` reports the planning tree is consistent.

One observation for the orchestrator, not a finding. The first gate run in this worktree failed in `internal/retention` (`TestSweepRestartResumesAndFinishes`, race detector) while the second run passed fully green. That package sits outside this task's owns line, the failure did not reproduce, and the recorded gate result above is green. Mentioned here so a flaky retention test does not surprise a later landing.
