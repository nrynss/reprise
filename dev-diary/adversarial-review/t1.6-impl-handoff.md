# T1.6 implementer handoff

Commit `5056e610cfd80c4b0364dc1dbfcb7d7efed78243` on branch `task/t1.6` in `/home/nryn/work/reprise-wt/t1.6-impl`. Only owned paths changed: `internal/settings/`, `config/reprise.local.toml`, `config/reprise.box.toml`, `.env.example`. The tree is clean. No `go.mod` or `go.sum` change was needed because Keel `v0.3.0` already carries the `file` source.

## What was built

The credential for the editorial model now reads a service account key file. Local boots use `/home/nryn/.config/reprise/gemini-sa.json` outside the repository. Box boots use `/etc/reprise/gemini-sa.json`. Both TOML blocks use `source = "file"` with that path. The account name lives in the TOML comments and in the `GeminiCredential` doc comment. No code sends the name anywhere. `vertex_project` is `nryn-personal` and `vertex_location` is `europe-west1` in both TOML files, as plain inline strings. `.env.example` now lists only `ASSEMBLYAI_API_KEY` and `SESSION_SIGNING_KEY`.

Tests use temp key and env files with dummy JSON contents. New coverage pins the pass path, the missing file refusal, the group-readable file refusal, the credential plan line naming `file` with its path, the whole boot log carrying no fragment of the key JSON, and both tracked TOML files agreeing with the code. The retained Keel refusal tests still pass unchanged in shape.

## What surprised me

No Keel file-source behavior differs from the plan. The `file` source refuses a missing path and a group-readable file through the same error path as the existing loose secrets probe, so both refusals map to `ErrSecret` with the key, the source and the path named. The plan printer names the credential line as `secrets.gemini_credential` with source `file`, locator `path=<key path>` and status `resolved`. One detail: Keel reads the whole file as the value and trims one trailing newline, so the key file must hold exactly the JSON document with no wrapper.

## Region default choice

No planning file decides the region. `PHASE-0-ground.md` says the location comes from settings but never names a value. The contract fallback applies, so both TOML files use `europe-west1` as the documented default.

## Exact commands for the reviewer

Run these in the review worktree at the handed hash. The implementer did not run the toolchain or the gate because a sibling agent owns them in parallel.

1. `gofmt -l internal/settings/` must print nothing.
2. `git diff 4632df2..HEAD --stat` must list only `.env.example`, `config/reprise.local.toml`, `config/reprise.box.toml`, `internal/settings/settings.go` and `internal/settings/settings_test.go`.
3. `go test -race ./internal/settings/` must pass.
4. `./tools/check.sh` must exit 0 after `npm ci` in `web/`.
5. Independent pins: boot with a readable temp key file and confirm the plan line names `file` with its path. Delete the key file and confirm startup stops and names `gemini_credential`, `file` and the path. Set the key file to `0640` and confirm startup stops and names all three. Probe the whole boot log for every fragment of the key contents and find none.
