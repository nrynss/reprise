# T7.25 round 1 review

Reviewed hash: `2f42eed395f6915a2213ccf73f6be664c840ee64`
Worktree: `/Users/narayan/reprise-wt/t7.25-rev-r1` (detached HEAD).
`git log -1` matches that hash. `git status --short` was empty before the pins and empty again after the mutation was restored.
Diff reviewed: `git diff 742c0363db343743fcd4143e0b4aa65cfd1e7293..2f42eed395f6915a2213ccf73f6be664c840ee64` and nothing else.
No code commits. The tree is still that hash.

## The ten questions

1. Does any path spend money without reserving budget first? No. The diff adds no paid call. It only skips a browser navigation after the record page is gone.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The draft move starts only after `voice.end` and the HTTP close have finished. `destroy` on an ending take does not open another session. A leave during the live phase still calls `voice.end`. The token duration cap is unchanged.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The commit changes no media URL, cookie, or owner check.
4. Does any edit apply without a revertible record? No. Nothing applies an edit. A late success only skips `location.assign`.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. New comments describe the destroyed flag and the gallery proofs. They do not cite a task id, a phase id, or a planning file.
7. Did this task rebuild something Keel or Chaaya provides? No. The guard is a product navigation check on the existing controller. `destroy` still uses Chaaya `SessionGuard`.
8. Was this review run on the handed hash, in a worktree of its own? Yes. This detached worktree is the only tree the review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind. Skipping the assign does not post the move again.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The gallery proofs and the neighbouring specs use the mock take. None waits on a person, a microphone, or a clock threshold.

## What the commit does

`destroy` sets `destroyed` before it removes listeners (`web/src/lib/voice/record-state.ts:274`). `goProcessing` returns when that flag is set (line 517). `endTake` and `retryCompletion` both call `goProcessing`. The in-flight draft move is not cancelled. A success while the controller is still mounted still assigns `/processing`.

The harness gains `awaitRetry`. The retry proof waits on the post already in flight. The helper does not start a second post. `harness` still returns null unless the mock take is up.

`web/src/routes/record/gallery-link.spec.ts` is the authorized widening. No other path changed. The new helper holds `**/stems/complete`, then fulfills HTTP 200 with one draft body. Aborting the held call would hide the late assign. One proof ends the take, opens the gallery, then fulfills. A second proof does the same after a refused move and a retry.

## Pins

`npm ci` in this worktree's `web/` added 236 packages. Exit 0. `node_modules` was missing here.
`npm run build` in `web/` exited 0. Preview serves that build.
`npx svelte-kit sync` then `npm run check` from `web/` reported 0 errors and 0 warnings.
`npx eslint src/lib/voice/record-state.ts src/routes/record/gallery-link.spec.ts` from `web/` exited 0.

`npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts end-control.spec.ts cap.spec.ts stems-complete.spec.ts mock-session.spec.ts` from `web/` passed 23 tests in 11.6s.
That run includes the ten gallery proofs and the thirteen neighbouring record proofs.
The two new proofs stay on `/` after the held move fulfills.
The eight older gallery proofs still reach the season heading.
The preview log shows `404` for `GET /api/episodes`. The season heading still renders. That 404 is the preview server, not this commit.

`npx playwright test -c src/lib/voice/mock.playwright.config.ts controller-complete.spec.ts session-guard.spec.ts --workers=1` from `web/` passed 5 tests in 1.5s.
Ending a mock take still reaches processing.
A refused move's retry still reaches processing.
The cap proofs and the challenged-session retry still reach processing.
A `finishTake` after `pagehide` still reaches processing, because `pagehide` does not set `destroyed`.
The guard does not skip every assign.

`ffmpeg` on this machine is 9.0.2. The pin is 9.0.1. `./tools/check.sh` was not run past that pin. This commit does not change the pin.

## Mutation

The guard at line 517 was deleted, so `window.location.assign` runs after the page has left. The web build was refreshed, because preview serves `build` and not the source. Both new proofs were run again. Both failed.

The end proof navigated to `/processing?episode=mock-episode&uploads=done&userBytes=98304&hostBytes=192000&mock=1`. The season heading was gone.
The retry proof settled on pathname `/processing`.
Neither proof stayed green. The pin is load-bearing.

`git checkout` restored `web/src/lib/voice/record-state.ts`. `git status --short` was empty. `git grep` shows the guard at line 517 again.

## Findings

None.

## Counts

C 0, H 0, M 0, L 0. In-scope findings: zero. Out of scope findings: zero.

## Verdict

APPROVE
