# T7.30 round 1 review

Reviewed hash: `3f37fec4428f6aa38d81e5a4c1465179b0a17dfd`
Worktree: `/home/nryn/work/reprise-wt/t7.30-rev-r1` (detached HEAD, `git log -1` confirms the handed hash, tree clean)
Base: `de367d2`. Diff read in full: 8 files, all inside `owns` except none outside it.
Task text: `dev-diary/PHASE-7-production-issues.md` T7.30 block. Handoff: `dev-diary/adversarial-review/t7.30-impl-handoff.md`.
Lambo: no lambo tools exist in this environment, so nothing was recalled or recorded outside git.

## Gate record (all run in the review worktree)

`./tools/check.sh` stops at the standing workstation exception (`ffmpeg is 9.0.2, not the pinned 9.0.1`), same as the handoff records. Every later step ran by hand from the worktree root:

| Step | Exit |
|---|---|
| `gofmt -l cmd internal` | 0, no output |
| `go vet ./cmd/... ./internal/...` | 0 |
| `go test -race ./cmd/... ./internal/...` (full) | 0 |
| `npm ci` in `web/` | 0 (must run from `web/`, not the root) |
| `npm run check` | 0, 0 errors, 0 warnings |
| `npm run lint` | 0 |
| `npm run build` | 0 |
| `npm run test -- --run src/routes/threads/threads.test.ts` | 0, 32 passed |
| Playwright share config (Chromium) | 0, 3 passed |
| Playwright threads config (Chromium) | 0, 10 passed |
| Playwright episode edit config (Chromium) | 0, 5 passed |
| svelte 4 leakage / consumer names / plan reference scans | empty (grep clean) |
| WebKit | cannot launch on this host, CI must cover it |

staticcheck is not installed here, so that handoff row was not re-run. `go vet` plus the full race suite passed.

## Pins re-run independently (mutations in a scratch copy under `/tmp/opencode`, worktree untouched)

- `TestKeepMovesCoverAndResolutionRows`: passes. With the two move calls deleted it fails (`cover owner guest-gone, want owner-keeper`). Load-bearing.
- `TestMountLeavesPrivacyPatternsForTheFeatureHook`: passes. With the `privacyPatterns` skip removed it fails (`POST /api/episodes/e01/publish: status 501, want 404`). Load-bearing. The share round trip test mounts core routes before the hook, so a regressed stub would panic instead of hiding.
- Threads controller live publish / refused publish cases: pass. With the live branch forced to the fixture path, 2 fail. Load-bearing.
- `TestPrivacyKindsRegisterEraseAndSweep`, `TestPrivacyShareOpensAndRevokes`, `TestPrivacyEraseRemovesEveryCopy`, `TestPrivacySweepExpiresTheIdleGuest`: all pass unmutated. The erase test asserts no episode row, no media row, no media file, no cover file, and deleted provider session plus transcript. The sweep test asserts no episode, no user, 404 share, and deleted provider copies.
- `rendered_sources` carries no owner column (`internal/analysis/migrations/0001_rendered_source.sql`), so the Keep claim that it needs no move is confirmed against the schema, not the comment.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `web/src/routes/episode/[id]/+page.svelte:118` at reviewed hash | A live episode exposes no publish or erase controls. The whole Release section (lines 119-136, the only `Publish…`, `Revoke link`, and `Erase this episode` buttons in the file) sits inside `{#if !snap.live}`. Live episodes set `live: true` (`web/src/routes/threads/threads.ts:1742`), so they never render it. The task bullet "The episode page publish and erase controls call those routes and show their real outcome" holds for fixture episodes only. The routes themselves work, so a cookie holder can publish, revoke, and erase with `curl`, which is the workaround that keeps this an M. The only Playwright coverage of the Release section is fixture-scoped (`threads.spec.ts:252`, `/episode/ep-4?fixture=1`). No spec walks a live episode's controls, because there are none to walk. | Probe (runs from the worktree root, exit 1 on the reviewed hash): `python3 -c` parses `web/src/routes/episode/[id]/+page.svelte`, collects the lines containing `publishState`, `controller?.erase`, or `<h2>Release</h2>`, and asserts at least one sits outside the `{#if !snap.live}` (line 118) to `{/if}` (line 137) block. Result on this hash: all four hits (lines 120, 123, 125, 129) are inside the block, zero outside, so the probe fails, measuring that no publish or erase control can render when `snap.live` is true. | Reintroducing edit: restore (or keep) the `{#if !snap.live}` guard around the Release section after the fix moves those controls out of it. With the guard back, the pin fails again, which proves the pin is load-bearing on exactly this defect. |

Note on placement: the guard lives in `web/src/routes/episode/[id]/+page.svelte`, which is outside this task's `owns` line. It is recorded here as an in-scope finding anyway, because the defect is this task's own claimed outcome (the dispatch directs exactly this), and the owned half of that outcome (`web/src/routes/threads/threads.ts` live `publish()`/`erase()` branches) is already correct and pinned. The fix belongs on the unowned page.

OUT_OF_SCOPE defects: none found. No real defect outside `owns` was observed.

## The ten review questions

1. Does any path spend money without reserving budget first? No. Publish, revoke, and erase are DB plus deletion calls. The erasure targets (`internal/privacy/erase.go:287-422`) are row, blob, cover file, provider session end, and provider transcript delete. None opens a metered session or a generation call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. Erasure ends the provider session (`sessionTarget.Delete`), and no new code opens one.
3. Can a private episode, stem or transcript reach anyone but its owner? No. Share metadata and cover resolve by token plus `visibility = public` (`internal/privacy/share.go:47-61`), and unknown, revoked, unpublished, and renderless tokens share one 404. The `Share` payload names only id, title, number, render blob, and cover path. No stem, transcript, thread, or other episode is reachable from a token. Stranger 404 on owner routes is pinned.
4. Does any edit apply without a revertible record? Not applicable. Publish and revoke are visibility flags, and erase is an explicit owner deletion through a job, not an editorial edit.
5. Does any spoken callback or count come from the prompt instead of a stored row? Not applicable. Nothing here prompts a model.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep over every touched source, test, and migration file is empty, and the commit message is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. It wires `keel/erase`, `keel/job`, `keel/mediastore`, and `keel/sqlite`, and the handoff records no library workaround.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` shows `3f37fec4428f6aa38d81e5a4c1465179b0a17dfd` in `/home/nryn/work/reprise-wt/t7.30-rev-r1` (detached), the tree is clean, and every pin ran there. Nothing was committed.
9. Can any paid job kind resume after a restart and spend twice? No. The two registered kinds perform expiry and deletion only. The sweep kind is `Idempotent: true` with a 5-attempt cap (`internal/retention/retention.go:170`), and resume repeats deletes, never a paid call.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The only `time.Sleep` in the new tests is a bounded 20 ms poll inside a job-wait helper with a loud deadline (`cmd/reprise/privacy_test.go:330-348`). It fails on timeout rather than asserting timing.

## Counts

C: 0. H: 0. M: 1. L: 0. OUT_OF_SCOPE: 0.

REMEDIATE
