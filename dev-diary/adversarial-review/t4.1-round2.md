# T4.1 review round 2: APPROVE

Reviewed hash: `e4eb7fae38344f2ad03618b654a551087202561c` (four commits on
reviewed `b81c13f`, no rebase: `b27c04d`, `5a3151d`, `90c3629`, `e4eb7fa`).
Confirmed with `git log -1 --format=%H` in detached worktree
`/home/nryn/work/reprise-wt/t4.1-rev-r2`. Diff read as
`git diff b81c13f..e4eb7fa` (only `internal/memory/commitments.go`,
`internal/memory/resolutions.go`, `internal/memory/memory_test.go`).
No commit made here. Throwaway probe files used for extra checks were
deleted afterwards. `git status --short` is empty.

Verification: `go test -race -count=1 ./internal/memory/...` passes.
`go vet` and `gofmt` are clean. The full `./tools/check.sh` passes with
`OK all checks passed` after `npm ci` in the worktree's `web/`
(the first run failed only on missing `web/node_modules`, which is
environmental and unrelated to this Go-only diff).

Verdict: APPROVE. Counts: C 0, H 0, M 0, L 0. The round 1 OUT_OF_SCOPE M
(analysis rerun orphan) remains out of scope and untouched, and it does
not block.

## Residue against round 1, severity by severity

- H, zero residue (2 of 2). H1 time-travel close: both entry points now
  compare episode numbers and fail with `ErrInvalid`, and the model path
  checks before reserving budget. H2 evidence deletion: a re-close
  deletes only the resolution link, and every evidence row survives.
- L, zero residue (2 of 2). L1 trailing garbage: both parsers take a
  second decode and fail with `ErrModel` unless it meets `io.EOF`.
  L2 unknown and foreign ids: `StoreResolution` looks the commitment up
  scoped to the caller and fails with `ErrNotFound` before any write.
- C, zero. No C finding stood in round 1, and none appears here.
- M, zero. No M finding stood in round 1, and none appears here.

## Round 1 pins, re-run green

All four pins now live as committed tests, and each passes under
`-race`: `TestStoreResolutionRejectsBackdatedClose` and
`TestStoreResolutionRejectsSameEpisodeClose` fail backdated and same
episode closes with `ErrInvalid` and no rows.
`TestResolveEpisodeRejectsBackdatedEpisode` fails the model path with
`ErrInvalid`, zero provider calls, and zero reservations.
`TestStoreResolutionRecloseKeepsHistory` closes in episode 2 and again
in episode 3, then checks both doing rows survive, one link remains,
and the gallery reads the latest evidence.
`TestParseRejectsTrailingGarbage` fails trailing data on both parsers
and still accepts clean bodies. `TestStoreResolutionUnknownCommitment`
and `TestStoreResolutionForeignCommitment` fail unknown and foreign ids
with `ErrNotFound` and no rows.

## Extra probes this round (run, then deleted)

- Pretty-printed JSON still parses. Bodies ending in newline or blank
  lines pass, while a second JSON value fails with `ErrModel`.
- A third close keeps the shape. Three closes leave three doing rows,
  one link, and the gallery reading the latest evidence from episode 4.
- A same-episode rejudge keeps the shape. Two closes on the later
  episode leave two doing rows and one link, so each close adds exactly
  one row and never stacks outcomes.
- The model path guards the owner too. Resolving another owner's
  commitment through `ResolveEpisode` fails with `ErrNotFound`, with
  zero provider calls and zero reservations.

## New findings

None.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   new episode-order checks in `ResolveEpisode`
   (`commitments.go:587`) run before `Reserve` (`commitments.go:607`),
   and the backdated pin records zero calls and zero reservations.
   Every exit path still settles or releases.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No such path exists in this package. Nothing here
   speaks HTTP.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Commitment reads stay owner scoped on both write paths, and the
   foreign probes return `ErrNotFound` with an empty resolved list.
   Episode ids arrive from the caller, as before, and this diff adds no
   new cross-owner read.
4. Does any edit apply without a revertible record? No. A re-close now
   replaces only the link, and the history probe shows both evidence
   rows surviving with the gallery on the latest.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. Word verification still gates marking, and evidence
   verification still gates closing. The stricter parsing only narrows
   what reaches those gates.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Grep over `internal/memory/` for task ids, phase
   ids, `product.md`, `project.md`, and `handoff` returns nothing, and
   the four commit messages carry plain descriptions. The full gate's
   plan-reference scan passes.
7. Did this task rebuild something Keel or Chaaya provides? No. The
   diff uses `keel/cost`, `keel/id`, and `keel/sqlite` through the same
   seams, with no vendored copies.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes, stated above. No code was committed here.
9. Can any paid job kind resume after a restart and spend twice? No job
   kind is defined here, so invariant 13 still has nothing to bite on.
   An invalid request now fails before any reservation, which narrows
   the retry window rather than widening it.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The suite binds a scripted `Model` and a
    scripted `Budget`, and everything runs offline under `go test
    -race`.
