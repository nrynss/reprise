# T3.5 round 1 review

Reviewed hash: `1bdfbbcd7d531b251a62aacfce8c75ee076b6bac` (two commits on claim base `e8cb52a`).
Worktree: `/home/nryn/work/reprise-wt/t3.5-rev-r1` (detached HEAD, confirmed `git log -1 --format=%H`).
Scope: `git diff e8cb52a..1bdfbbc --name-only` shows 12 files, all under `internal/analysis/`. Owns respected.
Validation: `go vet` clean, `gofmt -l` clean, `go test -race -count=1 ./internal/analysis/` passes in this worktree.
No commit made. Working tree only reviewed.

Verdict: REMEDIATE.

Counts: C 0, H 1, M 0, L 0. OUT_OF_SCOPE 0.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `internal/analysis/chapters.go:209` (`splitLongest`, via `ensureCount` at line 186) | Splitting the longest span at its middle word can produce chapters under 10 s, breaking the rule that every chapter lasts at least 10 s. A 32 s episode with one draft yields chapters of 8 s. No check keeps both halves above `MinChapterMs`. | `NormalizeChapters([]ChapterDraft{{Title:"Whole", StartMs:0}}, scheduleWords(40, 800), 32000)` returns a first chapter of 8000 ms. Fails on the reviewed hash (probe run in this worktree, then removed). The existing `TestNormalizeSplitsToThree` only covers a 44.5 s input where middle splits stay long, so it misses this. | Keep the pure middle split in `splitLongest` with no both-halves guard against `MinChapterMs` (the current code). A fix that adds the guard is reverted to this defect by deleting that guard. |

## Scrutiny rulings

(a) Scripted doubles. The batch client lacks entity, phrase, summary, and chapter shapes, so the pass proves behavior through scripted doubles with one compat test pinning only the upload half. The implementer stayed inside `owns` and raised the adapter extension as a contract change, which is the correct move. The in-scope shaping invariant (every start equals zero or a stored word start) is pinned and proven. The audio-landing half of the done criterion is a recorded live remeasure owed with the adapter extension, not a blocking finding. Same deferral pattern T3.1 landed with.

(b) Merge-then-split order is specified behavior, not padding. Dropping a trailing stub is forced by the 10 s rule, and reaching three chapters on an episode that allows them is forced by the count rule. The `(continued)` suffix marks splits honestly. Short episodes keep fewer chapters, pinned by test. The only defect is the H above: splits do not enforce the 10 s floor on both halves.

(c) `OffsetOf` edges are sound. Gaps take the next word and past-end takes the last word, so drift never loses a real entity. Both cases are pinned. Verbatim provider types as mention kinds match the handoff contract, and no T4 file specifies normalisation yet, so there is no mismatch to fix here.

(d) Rerun replant warning is stated on `StoreAnalysis` itself (`store.go:157`), exactly where the retry caller reads. Not a trap.

(e) `GatewayPrice` books usage at `cfg.Rates`, which the wiring fills from the catalog beside the model id. No price lives in code. `ChapterMaxTokens` is a code const with a measured justification, and the T3.5 spec places no cap-source rule. `GatewayModel` comes from settings. Fine.

(f) `Estimate` prices batch audio plus the chapter token cap, pinned by `TestEstimatePricesBatchAndGateway`, including the one-hour batch rate and the dearer-side cap.

(g) Receipt sink names `transcript` and `chapters` are pinned in the run test, with both receipts kept. The seam points at the episode media area per the wiring notes.

(h) `Kind` limit 2, non-idempotent, no resume path, one attempt, pinned by `TestKindNeverResumes`. `Run` makes no second attempt on any failure.

(i) No plan refs in tracked source (grep over `internal/analysis/` matches only the word "rounded"). Go style holds: `ctx` first, `%w` wrapping, one sentinel per condition, doc comments on exported identifiers, no `panic`, `log.Fatal`, `**T`, or `interface{}`. `deleted()` matches the `assemblyai.DeletedText` marker convention T3.1 landed, not provider error text. No HTTP code here, so the single-adapter rule holds.

## The ten questions

1. Does any path spend money without reserving budget first? No. `Run` reserves before upload. Refusal reaches no endpoint, pinned.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. Batch only, no session.
3. Can a private episode, stem or transcript reach anyone but its owner? No. All rows are owner and episode scoped. No serving path here.
4. Does any edit apply without a revertible record? No. This pass stores analysis rows with replace semantics on explicit retry, disclosed in the handoff and the `StoreAnalysis` comment.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Mentions carry offsets and quotes from stored entities and phrases. The prompt only phrases chapters.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses `keel/cost`, `keel/job`, `keel/id`, and the diary store.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Hash confirmed, detached worktree, no commits.
9. Can any paid job kind resume after a restart and spend twice? No. Kind is not idempotent with no resume path, pinned. Failures keep the provider copy for explicit retry and free the reservation.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Scripted doubles plus fixture bytes, millisecond poll interval.

## Verdict

REMEDIATE. One H finding: chapter splitting can emit sub-10-second chapters. Fix `splitLongest` or `ensureCount` so every emitted chapter lasts at least `MinChapterMs` whenever the episode allows three, and add a pin near 30 to 40 s inputs. Zero residue against prior rounds is not yet claimable: this is round 1 with one open finding.
