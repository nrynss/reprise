# T7.31 round 1 review

Reviewed hash: 713ef9e42593fbd009d97e3991dac9d815a2069d.
Worktree: /home/nryn/work/reprise-wt/t7.31-rev-r1 (detached HEAD, confirmed with `git log -1`).
Base: de367d2. Diff read in full with `git diff de367d2..713ef9e42593fbd009d97e3991dac9d815a2069d`.
Task owns: cmd/reprise/seed.go, internal/seed/, web/src/routes/welcome/.
No lambo tools exist in this environment, so nothing was recorded outside git.
Reviewer never implemented this task, wrote no code, and commits nothing.

## Scope check

All eleven changed paths sit inside `owns`. `git status --short` is clean
apart from the review work itself. The probe file used below was deleted
after its run.

## Gate

`./tools/check.sh` stops at its tool step on this host: ffmpeg is 9.0.2
against the pinned 9.0.1. Every later step ran by hand from the worktree
root, Chromium-scoped for Playwright (WebKit cannot launch on this host,
missing system libraries).

| Step | Exit |
|---|---|
| gofmt (`cmd`, `internal`) | 0 |
| go vet (changed packages) | 0 |
| staticcheck (changed packages) | 0 |
| go test -race (all Go packages) | 0 |
| svelte-check | 0 errors, 0 warnings |
| eslint | 0 |
| vitest | 0 (23 files, 245 tests) |
| web build | 0 |
| Playwright welcome config, Chromium | 0 (4 passed) |
| svelte 4 leakage scan over owned paths | clean |
| plan reference scan over owned paths | clean |

## Pins re-run with mutations

Each pin below was re-run independently, then a one or two line mutation
was applied, the pin was re-run to confirm it fails, and the mutation was
reverted with `git checkout`. The tree is clean after every revert.

- `TestCatalogRenderLinksWords` passes. Mutation (drop the catalog
  `rendered_sources` insert in `internal/seed/catalog.go`) fails with
  "catalog link names a render the detail never plays". Load-bearing.
- `TestCopyRenderKeepsLink` passes. Mutation (drop the copy
  `rendered_sources` insert in `internal/seed/copy.go`) fails with
  "copy link names a render the detail never plays". Load-bearing.
- `TestSeedBootRefusesBadCatalog` passes. Mutation (return a keyless
  error from `mountSeed` in `cmd/reprise/seed.go`) fails with "want the
  file key". Load-bearing.
- `TestOwnerGetsNoCopy` passes. Mutation (disable the
  `KindOwner`/`KindSeed` guard in `internal/seed/copy.go`) fails with
  "copy for owner = 2, want 0". Load-bearing.
- `TestSeedWelcomeCopiesOneEpisodeOnce`, `TestSeedWelcomeEmptyCatalog`,
  `TestDropRemovesCopyRender`, `TestWelcomeReadsCopiesFirst`,
  `TestSeededLinksDoNotLeakAcrossOwners`, `TestCopyRunsOnce`,
  `TestRowsOnlyFileStoresNoRender` all pass unmutated.

## Watch item judgements

1. Erasure and sweep branching on the seeded flag. Confirmed as a real
   defect, recorded as OUT_OF_SCOPE-1 below. The seeded copy itself is
   complete: catalog and copy rows each carry their own render row and
   link row, rows-only files carry neither, drops cascade the copy
   render and link and keep the catalog audio. All pinned in scope.
2. Repeat import on the welcome read, not the episode list. The shipped
   season readme promises import on boot and on every season list
   without a receipt. The boot import landed here. The repeat import
   runs on the welcome read. The task bullet says "on the first season
   read", and the welcome read is the first season read a new guest
   makes. The done condition is fully met: one fixture episode appears
   once in a new guest gallery with the seeded flag, its mention feeds
   the host opening, and the empty catalog renders only the record
   button. The episode-list trigger lives in `internal/api/`, outside
   this task's `owns`, so wiring it here would break the ownership
   rule. No owned behavior breaks. No finding.
3. "A live render plays to its end. Generated tone keeps the thirty
   second cap." Verified. `loadSource` in `web/src/routes/welcome/welcome.ts`
   marks a live render address uncapped and generated tone capped. The
   ticker clamps only capped sources at `TEASER_SECONDS` and ends only
   capped sources there. An uncapped source ends when the player stops.
   The live controller unit test pins `capped: false` with the render
   address, and the Chromium teaser spec plays the capped tone after
   the first gesture. No finding.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| OUT_OF_SCOPE, H | `internal/privacy/erase.go:79-93`, `internal/privacy/erase.go:152-156`, `internal/retention/sweep.go:277-282`, `internal/retention/sweep.go:400` at reviewed hash | Erasing one seeded copy deletes the shared catalog audio. The copy render keeps the catalog blob ids by design (`internal/seed/copy.go` render copy), and the erasure inventory collects every render blob behind the erased episode and deletes each one. The retention sweep erases every guest episode through that same erasure with no seeded branch (`grep seeded` over non-test retention and privacy code is empty). After one copy erase the catalog blob row is gone, so the catalog and every remaining copy lose their audio. Owning paths: `internal/privacy/`, `internal/retention/` (T7.30), never blocks this task. | Throwaway probe `TestOOScopeSeedEraseDeletesSharedCatalogAudio` (written, run, deleted, never committed): sync a one episode catalog with audio, copy to a guest, erase the copy through the real privacy erasure to job done, then count the catalog blob in `media`. Fails on the reviewed hash with "catalog blob rows = 0, want the shared audio to survive one copy erase". | Restrict the render blob query in `inventory` to match nothing (`AND 1 = 0` on `internal/privacy/erase.go:80`): the probe passes. This proves the pin measures that collection path. Reverted. |

Note: the retention keep path still misses link-adjacent rows
(`internal/retention/keep.go:23-26` names no `rendered_sources`,
`covers`, or `resolutions`). That gap is already stated work inside
T7.30's own task text, which owns those paths, so it is tracked there
and needs no second finding here. The new link rows need no owner
update themselves (they carry no owner column), so this task adds no
new keep obligation.

## Ten review questions

1. Does any path spend money without reserving budget first? No. The
   hook registers zero job kinds, and sync, copy, welcome, and drop are
   local database and media work with no provider call.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Nothing here touches AssemblyAI.
3. Can a private episode, stem or transcript reach anyone but its
   owner? No. The welcome read serves only the catalog (shared demo
   material) or the caller own copies, scoped by the session user id.
   Catalog blobs are public by design.
4. Does any edit apply without a revertible record? No. Copies are new
   rows. Drop removes only seeded copies and refuses recorded episodes
   with `ErrNotSeeded`, keeping the erasure path.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. Teaser lines come from the mentions table, the
   season notice counts stored rows, and the greeting pin reads the
   copied mention.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Both scans over the owned paths are clean.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses
   Keel id, sqlite, mediastore, and job types, and the Chaaya player.
   The render link table migration is reused through the analysis
   package own migrate function, not copied.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. `git log -1` reads the handed hash in the detached review
   worktree, and every pin ran there.
9. Can any paid job kind resume after a restart and spend twice? No.
   No kind is registered here.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Browser proofs play generated tone, and
    the position poll asserts state, never timing.

## Residue

Zero in-scope findings across C, H, M, and L. The single OUT_OF_SCOPE
finding belongs to T7.30 paths and never blocks APPROVE. No prior
rounds exist, so there is no cross-round residue: zero C residue, zero
H residue, zero M residue, zero L residue.

## Verdict

APPROVE
