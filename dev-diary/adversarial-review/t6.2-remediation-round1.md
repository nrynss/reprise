# T6.2 remediation round 1

Reviewed commit: `dffb2aabb94735e56a41534cf2c8c7480ab79cd9`
Worktree: `/home/nryn/work/reprise-wt/t6.2-rem-r1`, branch `task/t6.2-rem-r1`, built on the reviewed commit with no rebase or amend. No verdict is changed here. The three out of scope status hunks are untouched, as instructed, since only the orchestrator writes phase files.

| Severity | Where | Fix | Pin result |
|---|---|---|---|
| L (F4) | `dev-diary/submission/descriptions.md:19` | `provider confirmed closes` now reads `provider confirmed close`, so the draft no longer claims provider confirmation for every session. Only rounds 1 and 2 carry Sessions API reads. | `grep -n 'client_end' dev-diary/probes/production.md` still hits only lines 230, 238, 546, 553, with no Sessions API read between lines 614 and 842 (round 3). The singular no longer overclaims that third session. |
| L (F5) | `dev-diary/submission/slide-deck.md:21` | ``Provider records confirm `client_end` closes`` now reads ``Provider records confirm `client_end` close``, the same one word singular fix as F4. | Same pin as F4: two provider `client_end` records exist and the third session has none. The singular covers the two records without claiming a third. |

Validation: `python3 dev-diary/tools/audit_docs.py` reports the planning tree is consistent. Changed sentences hold no semicolons or em dashes and each runs under 30 words. Nothing else was changed.
