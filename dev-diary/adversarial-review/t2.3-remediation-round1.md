# T2.3 remediation round 1

Reviewed hash `11a51eca269db761555f7be21e824c0b24cb1e56`, remediated in
worktree `/home/nryn/work/reprise-wt/t2.3-rem-r1` on branch
`task/t2.3-rem-r1`. New head `af521f3279856ef6a1dc906f403ddf6633f10c22`.
Two commits, one per finding, on top of the reviewed commit with no rebase
or amend. Tree is clean. The gate passes in this worktree after `npm ci`,
including `go test -race` and the full web checks
(`OK all checks passed`).

| Finding | What changed | Pin now passing |
|---|---|---|
| H, expired lease parks the session in review forever | `internal/broker/reconcile.go`: after this run settles the hold under its own claim, a close that fails on an expired lease completes the tail (`completeExpiredTail`: write duration, persist artifacts, mark settled) instead of reviewing. The lease row stays expired and unreconciled, because expiry is terminal. Resume on an expired lease still reviews, because it cannot prove the settle ran and a second settle would book spend twice. Committed as `d0ca827`. | `TestReconcileExpiredLeaseCompletesTail` in `internal/broker/reconcile_test.go`: real cost store, real lease manager with a 150 ms cap and an injected clock moved one second past the cap, stub reader serving `prov-short` with 10 seconds through the real parser, token cap 1800. The run does not need review, the row reads 10 seconds, the recording persists with the artifact bytes, spend moves exactly once, and a retry returns the identical result with no second settle. Against the reviewed code the first assertion fails with `NeedsReview: true`. No sleep appears in the test. The clock moves through the lease manager clock seam. |
| L, over cap alert refires per retry | `internal/broker/reconcile.go`: the artifact tail alerts once per session on a durable `over_cap_alerted` claim flag. Delivery failure leaves the flag unset, so a retry alerts again, and a mark failure returns an error. Fresh tables carry the column from the schema. Older tables gain it through `ensureAlertColumn`, which checks `PRAGMA table_info` and alters only when missing. Committed as `af521f3`. | `TestReconcileOverCapAlertsOnce` in `internal/broker/reconcile_test.go`: reconcile the over cap fixture twice and the alerter holds exactly one alert for the session, with spend settled once. Against the reviewed code the alerter holds two identical alerts. `TestReconcileAlertColumnMigrates` drops the column, reopens the reconciler, and proves the column returns. |

Nothing outside the task `owns` paths was touched. The verdict is unchanged.
No other finding was fixed, and no behavior beyond the two findings moved.
