# T7.3 round 1 review

Reviewed hash: `a4de72f0cc52d7c69b354dab1cbedd3fecc3dc06`
Worktree: `/home/nryn/work/reprise-wt/t7.3-rev-r1` (detached HEAD, confirmed with `git log -1`)
Base: `42ea9cf`. Diff scope: `cmd/reprise/main.go`, `cmd/reprise/main_test.go`.
Gate: `npm ci` in `web/`, then `./tools/check.sh` prints `OK all checks passed`.
Docs audit: `dev-diary/tools/audit_docs.py` prints `OK planning tree is consistent`.
Tree left clean. No code committed.

## What the commit does

`withUploadOwner` runs inside the guest middleware on the upload subtree.
It replaces the posted open owner with the resolved session user id, so
the stored blob carries the id the media authorizer compares. Chunk,
state, and complete calls pass through untouched. `withMediaRefusalHeader`
stamps a bare media 404 with `Cache-Control: private, no-store` and leaves
every other response alone. `TestUploadOpenResolvesSessionOwnerAndRefusalCarriesHeader`
pins the full path. It opens with literal `guest`, completes one chunk,
reads exact bytes back as owner, and reads 404 with the header as nobody
on the stored blob and on an unknown id.

## Production ordering check

`internal/api/routes.go:168` builds each route as gate, then guest
middleware, then the handed handler. The handed upload and media handlers
are the wrapped ones, so production order matches the order the test
builds. The context lookup inside `withUploadOwner` therefore sees the
same session the test exercises.

## Library behavior check

Keel `v0.3.0` mediastore answers refusals and unknown ids with a bare
`http.NotFound` and no header. It sets `private, no-store` on successful
private reads and `public, max-age=31536000, immutable` on public reads.
The wrapper only stamps a 404 with an empty header, so successful reads
keep the headers the library sets. The handoff names the Keel issue as
`https://github.com/nrynss/keel/issues/1` and keeps the wrapper until the
library ships the header. No library code was patched or vendored.

## Pin runs

The pin passes in this worktree. Both halves proved load-bearing with
transient mutations, each reverted right after. Replacing the owner
rewrite with a pass-through fails the test at the owner check with
`open owner = "guest"`. Weakening the stamped value to `private` fails
the test at the anon check with the wrong header. The tree matches the
handed hash after both reverts.

## Ten questions

1. Does any path spend money without reserving budget first. No. The
   commit touches upload open and media reads only.
2. Can any path leave a session open without `session.end` or a token
   cap. No. No session code changed.
3. Can a private episode, stem or transcript reach anyone but its owner.
   No. The stored blob now carries the session id the authorizer
   compares. Anon reads answer 404 with no-store on the blob and on
   unknown ids alike. The refusal reveals nothing about existence.
4. Does any edit apply without a revertible record. No. No episode edit
   path changed.
5. Does any spoken callback or count come from the prompt instead of a
   stored row. No. No prompt or count changed.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file. No. Grep over `cmd/reprise` finds no plan references,
   and the gate plan-reference check passes.
7. Did this task rebuild something Keel or Chaaya provides. No. Both
   wrappers sit at the server boundary. The library stays untouched.
8. Was this review run on the handed hash, in a worktree of its own.
   Yes. Detached worktree above, hash confirmed, tree clean, no commits.
9. Can any paid job kind resume after a restart and spend twice. No. No
   job code changed.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold. No. The pin uses synthetic bytes with exact
    assertions and no sleeps.

## Findings

No findings. Counts: C 0, H 0, M 0, L 0.

The done clauses hold. The reload path completes over the persisted bytes
against a server authorizer. Owner reads return the exact bytes. Anon
reads answer 404 with the header on the blob and on an unknown id.
Chunk and complete pass through the wrapper unmodified. No frontend
change was needed and none appears.

## Verdict

APPROVE with zero findings.
