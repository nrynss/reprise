# T7.32 round 1 review

Reviewed hash `3b136377ab403fb7c19b61cfce6b2b63588ea3ef` in worktree
`/home/nryn/work/reprise-wt/t7.32-rev-r1` (detached HEAD, confirmed with
`git log -1`). Base `1729b99`. Diff read in full. Task block and the
whole implementer handoff read, including the gate table. No lambo
tools exist in this environment, so no recall or derive ran. Nothing
was committed. The worktree is clean at close.

## Done condition, verified independently

A fixture `ready` episode exports one zip with audio, video, captions,
cover, and chapter description. `TestExportServesBundleToOwner` passes
in this worktree. A temporary probe built a bundle from generated AAC,
a generated square cover, and synthetic words, then wrote the entries
to `/tmp/opencode/t732-rev/` for direct measurement. The probe file
was deleted after the run.

Independent `ffprobe` results on the probe output:

- Audio is AAC, 44100 Hz, mono. It matches the render bytes.
- Video is H264 at 1920 by 1080 with an AAC audio stream.
- The SRT carries timed cues. The WebVTT opens with its header.
- The cover decodes as a 512 by 512 square PNG.
- The description opens with the show notes and lists chapter lines
  for each stored chapter.
- The zip holds six files in `BundleOrder`: audio, video, SRT, VTT,
  cover, description.

Refusals, all re-run here:

- A stranger with another owner cookie reads 404 with `not_found`.
- A signed-out request with no cookie reads the same 404.
- An unknown id reads 404 for the owner too.
- A cross-owner read of a seeded catalog copy reads 404.
- A non-ready episode reads 409 with `export_not_ready`.
- A POST to the route reads 405.

Every export query scopes the owner beside the episode. The episode,
render, word, analysis, and cover reads each name `owner_id` with the
episode id. Seeded copies share blob bytes but read only their own
rows, and `TestExportNamesOnlyRequestingOwner` pins that split.

The episode page shows the export control only on live `ready`
episodes. Fixture mode keeps the standing `Export for YouTube` path.
The controller downloads the bundle on 200 and names each refusal on
its own line. All five vitest cases pass here.

## Gate record, run in this worktree

`./tools/check.sh` stops at the standing exception: ffmpeg and ffprobe
are 9.0.2 against the pinned 9.0.1. Every later step ran by hand in
gate order.

- gofmt: PASS, clean on `cmd` and `internal/export`.
- go vet: PASS.
- staticcheck: PASS, exit 0 on the touched packages.
- go test -race `./...`: PASS, full tree.
- svelte-kit sync: PASS. svelte-check: PASS with zero errors.
- eslint on the three touched web files: PASS.
- vitest: PASS, 24 files and 255 tests.
- build: PASS.
- Playwright main config, chromium project: PASS, 2 passed.
- Per-page configs as the gate runs them: PASS. Voice mock 5,
  record mock 23, edit 5, gallery 4, share 3, threads 11, welcome 4.
  WebKit cannot launch on this host, which is the standing exception.
- Svelte 4 leakage scan: PASS, no matches. Consumer names: PASS, no
  matches. Plan references: PASS, no matches.

## Pin mutations, each reverted

- M0 dropped the `owner_id` predicate from the episode row query. The
  stranger and seeded tests still passed, because the `Owns` gate and
  per-copy episode ids already isolate. This shows defense in depth,
  not a gap. No finding follows.
- M1 bypassed the `Owns` gate. `TestExportRefusesStranger` failed.
  The stranger pin is load-bearing.
- M2 answered `ErrNotReady` with 404. `TestExportRefusesUnfinished`
  failed. The 409 pin is load-bearing.
- M3 dropped the description from `BundleOrder`. The bundle unit test
  and the owner round trip both failed. The order pin is load-bearing.
- M4 rewrote the 404 refusal line. One vitest case failed. The screen
  pin is load-bearing.

The tree holds no mutation residue. `git status --short` is empty.

## Ten review questions

1. No path spends money without reserving budget first. The new code
   names no price, no reservation, and no settle. The kind carries a
   limit only.
2. No path leaves an AssemblyAI session open. The new code never names
   AssemblyAI. It probes local files with ffprobe only.
3. No private episode, stem, or transcript reaches anyone but its
   owner. Stranger, signed-out, unknown, and cross-owner reads all
   answer 404. The download carries `no-store`.
4. No edit applies without a revertible record. The route is a
   read-only download. The new code runs no write query.
5. No spoken callback or count comes from the prompt. The description
   renders stored notes with stored chapter lines. No model runs.
6. No code, comment, test, or migration cites a phase, a task, or a
   planning file. The scan finds nothing.
7. This task rebuilt nothing Keel or Chaaya provides. It uses the
   media duration helper, the job kind, the sqlite handle, and the
   shared error envelope. Captions and video build on the owned
   package helpers from earlier tasks.
8. Yes. This review ran on the handed hash in a worktree of its own.
   The hash check, the diff, every test, and every probe ran there.
9. No paid job kind can resume after a restart and spend twice. The
   export kind runs inline in the request and declares no resume work.
   Nothing outlives its request.
10. No development check needs a person, a microphone, or a wall-clock
    threshold. Audio is a generated sine, the cover is generated, and
    words are synthetic. Browser proofs run against mocks.

## Findings

No findings. No C, no H, no M, no L. No `OUT_OF_SCOPE` defect. The
two colocated test files sit beside owned paths and state their own
reasons, so they are coverage, not drift.

Zero residue, severity by severity: no C residue, no H residue, no M
residue, no L residue. Every prior round concern is absent because
this is round 1 and the pins above hold without qualification.

## Verdict

APPROVE
