# T3.2 remediation round 2

Reviewed hash: `abfeb08dd4c21ddbef4d6fd40a71a04572c1855a`.
New head: `917d5d078889603c5a4d44355aecc407d292c476` on branch `task/t3.2-rem-r1`,
worktree `/home/nryn/work/reprise-wt/t3.2-rem-r1`.
Scope: `internal/editorial/run.go`, `internal/editorial/run_test.go` only.
The model-failure fallback store path is untouched and still releases.

| Finding | Fix | Pin |
|---|---|---|
| N1 (M, `run.go:162-164`): decode-failure fallback store path returned with `settled=false`, so the deferred `Release` freed a spent reservation | Same 4-line `Settle` pattern as the two round-1 paths: settle the estimate, join a failed settle into the store error, mark settled, then return the store error | New `TestRunSettlesSpentCallOnFallbackStoreFailure` (drop `proposals`, answer `"not json"`, one billed call): passes with `reserved=1 settled=1 released=0`. Mutation check (settle removed) fails with `settled=0 released=1`, so the pin is load bearing |

Round-1 fixes intact: `TestRunSettlesSpentCallOnStoreFailure`,
`TestRunKeepsProviderCopyOnReceiptFailure`, `TestRunStoresDraft` (settle pair),
and `TestRunFallsBackOnUnusableAnswer` all pass. Gate `./tools/check.sh` passes,
including `go test -race`.
