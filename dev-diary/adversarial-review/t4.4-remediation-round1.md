# T4.4 remediation round 1

Reviewed hash: c504d03f47db531a8fe438e1be6c83824ac4822d.
Worktree: /home/nryn/work/reprise-wt/t4.4-rem-r1, branch task/t4.4-rem-r1,
built on the reviewed commit with no rebase and no amend. Tree clean.
Three commits, one per finding, all inside owns (`internal/privacy/`):

* 16a9bb8c1f5d7932e6a4e1df0fa92c7ad1d76a96, revoke every render blob on unpublish.
* b02fbc0c45d18c409b7437c220f8843f87717780, flip render blob before marking episode public.
* 8d17558de1193b9811bb343f3325050e668f1de, seed every content table in erase fixture.

New head: 8d175584de1193b9811bb343f3325050e668f1de.
The two OUT_OF_SCOPE rows from round 1 stand untouched.

| Severity | Finding | Fix | Pin re-run | Mutation |
|---|---|---|---|---|
| C | Unpublish flipped only the latest render row blobs to private, so a superseded opus blob served 200 signed out after unpublish. | `Unpublish` (publish.go:138) now sweeps `opus_media_id` and `aac_media_id` of every render row for the episode to private. Missing blob rows stay tolerated, because the rotated token already killed the link. The comment now describes the sweep it performs. Publish still marks only the current row opus public, so the pin below measures the unpublish sweep alone. | New `TestUnpublishRevokesEveryRender` (publish_test.go:134) follows the review pin shape: publish, second render row, publish, unpublish, then both opus blobs fetch 404 signed out with private visibility. Passes. | Restore the latest-row-only flip and the new test fails with 200 on the first opus blob. |
| H | Publish marked the episode public before the blob flip, so `ErrMediaMissing` left a dead public episode. | `Publish` (publish.go:105) now flips the render blob public before the episode row update. A failed flip returns before any episode write, so the episode stays private with its old token. The comment states the ordering and its reason. | New `TestPublishKeepsEpisodePrivateWhenBlobMissing` (publish_test.go:175) follows the review pin shape: delete the opus blob row, publish, observe `ErrMediaMissing`, then read episode visibility as private. Passes. | Restore the episode UPDATE before `setVisibility` and the new test reads public. |
| L | The erase fixture seeded 5 of 10 content tables, so the zero-rows assertion never measured turns, proposals, decisions, mentions, or callbacks. | `seedOwner` (privacy_test.go:242) now seeds one row per content table, with decisions pointing at the seeded proposal and callbacks pointing at the seeded mention. The fixture comment lists the full contents. | Full package suite passes with the new seeds, so the zero-rows assertion now measures all ten tables. A scratch probe (since removed) rebuilt mentions without its episode cascade and watched the erasure go stuck on the rows target, which proves the seeded mention row measures the cascade. | Drop `ON DELETE CASCADE` from `mentions.episode_id` and the erasure fails instead of passing silently. |

## Gate

* `go test -race -count=1 ./internal/privacy/`: pass. `gofmt`: clean.
* `npm ci` in `web/`, then `./tools/check.sh`: every check passes except
  `-- svelte 4 leakage`, which flags
  `web/src/routes/share/[token]/+page.svelte:20`
  (`page.url.searchParams.get('fixture')`). That line is a false
  positive in the script pattern (`get\(` matches `URLSearchParams.get`),
  the file imports `page` from `$app/state`, and the line is byte
  identical at the reviewed hash. It is pre-existing, outside every
  round 1 finding, and untouched here. Fixing the pattern belongs to
  whoever owns `tools/check.sh`.
