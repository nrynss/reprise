# T7.15 round 2 review

Reviewed hash: `3c18617da22033fac66367ee6b31d5561d1b73e4`
Worktree: `/home/nryn/work/reprise-wt/t7.15-rev-r2` (detached HEAD, confirmed `git log -1`, tree clean)
Base: `afd1b4d`
Remediation diff: `aa0f71a73cc339c5ae26e19394ad95cdc1a21387..3c18617da22033fac66367ee6b31d5561d1b73e4`, one commit on top, no rebase, no amend of the reviewed commit.

## Scope checks

- The remediation diff touches `dev-diary/probes/production.md` (Round 5 section 4 only, +20 minus 1 lines) plus adds `dev-diary/adversarial-review/t7.15-remediation-round1.md` (the round 1 record itself).
- Rounds 1 through 4 byte-untouched: the remediation diff hunks sit entirely under Round 5 section 4. Nothing else in the probe record moved.
- Section 4 now carries the fresh-mint command plus output block: a workstation `POST https://reprise.nryn.dev/api/sessions` mint with `HTTP 201` output, token redacted, plus the explicit statement that the socket never opened so no provider session exists and no connected second bills.
- No secrets: `sk-`, key, and bearer patterns absent from the added text. Token redacted, no cookie jars or mint bodies tracked.
- Doc style: no semicolons, no em dashes, no sentence over 30 words outside code blocks in the added text. Active voice.
- `audit_docs.py` re-run in this worktree: `OK planning tree is consistent`, exit 0.
- Process note (not a finding): the remediation commit carries `t7.15-remediation-round1.md`, which is outside the task `owns` line (`dev-diary/probes/production.md`). Round files normally land via the orchestrator and never ride a task commit. Content is correct and wanted on main either way, so the orchestrator resolves it at landing by dropping the file from the branch or accepting it. No defect in the product record follows from it.

## Ten questions

1. Spend without reserving budget first: No. Docs only, no code path. The new mint block is a `POST /api/sessions` only with no socket open, so it bills nothing.
2. AssemblyAI session left open: No. Round 5 record still shows explicit `session.end` with `client_end` on the Sessions API.
3. Private episode, stem, or transcript reaching anyone but its owner: No. No access pattern changed.
4. Edit applied without a revertible record: No edits apply here.
5. Spoken callback or count from the prompt instead of a stored row: No. No proposal text changed.
6. Code, comment, test, or migration citing a phase, task, or planning file: No. Added text carries no `T7.`/`P7`/`handoff` patterns.
7. Rebuilt something Keel or Chaaya provides: No.
8. Review run on the handed hash in a worktree of its own: Yes, shown above.
9. Paid job kind resuming after restart and spending twice: No. No code touched.
10. Development check needing a person, microphone, or wall-clock threshold: No. No new check added. The mint output is a recorded observation, not a gated threshold.

## Residue against round 1, severity by severity

- C: round 1 had 0. Still 0.
- H: round 1 had 0. Still 0.
- M: round 1 had 0. Still 0.
- L: round 1 had 1 (section 4 fresh-mint claim asserted, not measured). Fixed. Re-ran the round 1 pin in this worktree: split at `## 4. Provider cross-check and settle`, count `POST https://reprise.nryn.dev/api/sessions` blocks before `## Verdict`. Count is 1. Pass. Mutation check: deleting the added command plus output block returns the count to 0, so the pin is load-bearing. Zero L residue.

## Findings

No findings. No out-of-scope findings.

Counts: C 0, H 0, M 0, L 0.

## Verdict

APPROVE. Zero residue against round 1 across all severities.
