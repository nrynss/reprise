# T2.1 round 1 review

Reviewed hash: `e0a46229f219e03006d07a6d4b6b988a602e153b`
Worktree: `/home/nryn/work/reprise-wt/t2.1-rev-r1` (detached HEAD, confirmed by `git log -1`)
Base for diff: `8ebcb1d`
Diff scope: `internal/assemblyai/token.go`, `internal/broker/broker.go`, `internal/broker/broker_test.go`, `internal/broker/diary.go`. All four sit inside the declared owns. The tree was clean before and after every check.

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. No findings. No false positives. No OUT_OF_SCOPE defects.

## What the review ran

`go test -race ./internal/broker/ ./internal/assemblyai/` passes. The broker package passes with race detection. The assemblyai package holds no non-live test files, so it reports no test files. Its only other file is a pre-existing live probe behind the live build tag, outside this diff, so CI never runs it.

`go vet` on both packages passes. The full `./tools/check.sh` passes after `npm ci`, ending in `OK all checks passed`. That run covers gofmt, vet, staticcheck, the whole Go suite with race detection, the web build, e2e, and the plan reference check. This task touches no web paths, and the web steps pass unchanged.

## Findings

None. Each required behavior below names the test that pins it, so any regression fails loudly.

Pass path (`TestPassMintsOneOfEverything`): one POST mints exactly one token through the real client against a fake server, holds exactly one estimate on the ceilings, holds exactly one lease, and writes one episode plus one session row. The token call carries `expires_in_seconds=60` and `max_session_duration_seconds=1800`. The 201 body echoes the expiry and the cap, and the key appears nowhere in it.

Refusal paths, each stopping at the first failed check with nothing held: gate (`TestGateRefusesBeforeAnythingMints`, 429 `rate_limited`), kill switch (`TestKillSwitchRefusesAtRequestTime`, 503 `sessions_paused`), guest quota (`TestGuestQuotaRefuses`, 403 `guest_quota_reached`), owner ceiling (`TestOwnerCeilingRefuses`, 503 `budget_exhausted`), global ceiling (`TestGlobalCeilingRefuses`, same code), lease slots (`TestLeaseSlotsRefuse`, 429 `too_many_sessions`), token failure (`TestTokenFailureReleasesEverything`, 502 `session_provider_unavailable`), config failure (`TestConfigFailureReleasesEverything`, 500 `internal_error`), store failure (`TestStoreFailureReleasesEverything`, same code), anonymous (`TestAnonymousRequestRefuses`, 401 `session_required`). Every refusal answers through `wire.WriteError` with a stable code. Every post-reservation refusal asserts zero held reservations and zero open leases. The lease-slot refusal asserts the second request adds no hold, which pins the release on that path.

Provider error secrecy (`TestProviderErrorBodyCarriesNoKey`): a 503 from the provider becomes a 502 without the key in the body.

## Judgements the handoff asked for

ConfigBuilder stub versus landed host shape. The broker declares `SessionConfig` and `ConfigBuilder` instead of importing the landed host package. This is a valid seam, not a defect. The broker must not require the host task, and consumers declare interfaces. The host `Config` carries identical JSON names, so the wiring adapter is a field copy. No finding.

Missing lease to session link column. The broker leaves `provider_session_id` empty at mint and exposes `Leases`, `Budgets`, and `Diary` for the reconciler. The handoff discloses the gap and assigns any migration to the store owner. Nothing in this commit mislinks rows, so there is no defect to record here.

Cancelled context during cleanup. Cleanup runs lease close and reservation release on the request context. A client disconnect in that window could fail both calls. Both holds self heal. Reservations expire after the configured TTL, and leases expire at the cap through the sweep. The impact is a transient ceiling reduction, never spend and never permanent loss. This stays an observation, not a finding.

## The ten questions

1. Does any path spend money without reserving budget first? No. The token call runs only after `reserve` succeeds. The pass test pins exactly one hold for one session.
2. Can any path leave a session open without `session.end` or a token cap? No. Every mint carries `max_session_duration_seconds` set to the cap. The fake server test pins the parameter. No socket code lives in this task.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The 201 body carries ids, token, and config only. It sets `Cache-Control: no-store`. No media serving lives here.
4. Does any edit apply without a revertible record? No edits exist here. The broker only inserts rows.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Config arrives through the builder seam. The broker invents no names and no counts.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The gate plan reference check passes. An early grep hit on the word round inside round trip was prose, not a review round citation.
7. Did this task rebuild something Keel or Chaaya provides? No. Gate, ceilings, flag, lease, and wire all come from Keel. The `passMeter` fills the lease manager meter seam with bookkeeping because the durable hold already covers the estimate. Keel ships no bookkeeping meter, so nothing was duplicated.
8. Was this review run on the handed hash, in a worktree of its own? Yes. The worktree sits detached at the handed hash. The tree held zero dirty files throughout.
9. Can any paid job kind resume after a restart and spend twice? No job kind lives here. The broker holds no idempotent kind. A retry after refusal mints a fresh token while the old one stays unused and unbilled.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. All checks run against stubs, the local database, and a fake HTTP server. The one `time.Sleep` in the tree sits in a pre-existing live probe outside this diff.

## Verdict

APPROVE with zero findings across all severities. This is round one, so there are no prior rounds and no residue to carry. C 0, H 0, M 0, L 0.
