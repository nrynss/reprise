# T7.29 review, round 5

- **Reviewed hash:** `1e3c691dafa4b6ba2442b4b6ccf9a93c58a852b8`
- **Worktree:** `/Users/narayan/reprise-wt/t7.29-rev-r5` (detached HEAD)
- **Verdict:** **APPROVE**
- **Findings:** C 0, H 0, M 0, L 0

I verified `git log -1` at the handed hash in the detached review worktree. I reviewed
the remediation diff against `680102b53dfd02f344e37dc66fb5b6ffcc22cd42`, all four
earlier rounds, their remediation records, and the live task specification.

## Review

The changed branch is narrow and correct. `startWaitingRender` still leaves a capacity
refusal in `rendering`, preserving its oldest-first queue. Any other failure from the
durable start-and-stamp operation now transitions the same still-rendering episode to
`failed` under the scheduler lock. The owner can therefore use the established retry
route. A concurrent start cannot overwrite the result because that route and the
waiting scheduler share `schedMu`, and the guarded transition cannot regress an episode
which a racing render has already moved on.

`TestRetryFailedRenderStartReturnsToFailed` uses the real runner and fixture database.
It forces an initial 503, retries through the owner route, then makes only the queued
render's linkage write fail. It observes the durable episode state as `failed`.
The remediation record's mutation, removing the new transition, changes that observation
back to `rendering`, so the pin is load-bearing.

## Verification

| Check | Result |
|---|---|
| Focused round-4 pin plus waiting, busy-queue, double-start, and settled-outcome regressions under `-race` | pass |
| `go test -race ./internal/episode ./internal/api` | pass |
| All application `cmd/...` and `internal/...` packages: vet, staticcheck, and race tests | pass |
| `web`: `npm ci`, Svelte sync, check, lint, Vitest, build | pass |
| Browser suite | pass on rerun, 4 passed |
| Gallery browser suite | pass, 8 passed |
| Threads browser suite | pass, 10 passed |
| Formatting, Svelte 4 leakage, consumer-name, and planning-reference scans | clean |
| `./tools/check.sh` | stops only at the established local ffmpeg 9.0.2 versus pinned 9.0.1 mismatch |

The shared post-ffmpeg gate helper cannot enumerate `./...` while preserved probes have
different package names. I ran the equivalent checks over every application package,
excluding the deliberately standalone probe directories. The review worktree is clean.

## The ten questions

1. **Spend without reservation?** No. The new branch starts only a render. Paid analysis,
   cover, and memory work remain behind their existing reservations.
2. **AssemblyAI session left open?** No. This change opens no live session.
3. **Private episode, stem, or transcript to a non-owner?** No. Retry remains owner-scoped,
   and this scheduler-only change exposes no media or detail route.
4. **Edit without a revertible record?** No. It changes no editorial decision.
5. **Callback or count from the prompt?** No.
6. **Plan citation in code, comments, tests, or migrations?** No. The source scan is clean.
7. **Rebuilt Keel or Chaaya capability?** No. It uses Keel's durable job runner and limits.
8. **Handed hash in a separate worktree?** Yes. The hash above was reviewed in the detached
   worktree above.
9. **Paid kind resumes and spends twice after restart?** No. Only the unpaid render is
   requeued. Existing paid kinds remain non-idempotent and interrupt after restart.
10. **Development check needs a person, microphone, or wall-clock threshold?** No. The pin
    uses fixture state. The browser tests use generated browser inputs.

## Residue

| Earlier round | Residue |
|---|---|
| 1 | C 0, H 0, M 0, L 0. Render recovery, episode-scoped links, boot wiring, and settled outcomes remain covered and pass. |
| 2 | C 0, H 0, M 0, L 0. A full render slot returns one honest queued response, starts no duplicate render, and later ships. |
| 3 | C 0, H 0, M 0, L 0. Editor and gallery distinguish queued and refused outcomes. A failed initial start has a meaningful owner retry. |
| 4 | C 0, H 0, M 0, L 0. A non-capacity failure while starting that retry returns the durable state to `failed`, while capacity remains queued. |

No new finding and no out-of-scope finding.
