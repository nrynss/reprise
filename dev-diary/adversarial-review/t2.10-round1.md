# T2.10 round 1 review

Reviewed hash `483e6c71eca724a567e29b9d9003005930fc2d86` in detached
worktree `/home/nryn/work/reprise-wt/t2.10-rev-r1`. `git log -1
--format=%H` returns the handed hash. No commit made. Working tree
reviewed only through `git diff 15fc489..483e6c7`. No live call ran.
Spend stays at zero.

## Scope

The tracked diff holds exactly two new files: `internal/broker/timeouts.go`
(42 lines, five constants) and `internal/broker/timeouts_test.go` (29
lines, one floor test). Both sit inside the task owns
(`dev-diary/probes/latency.md`, `internal/broker/timeouts.go`, plus the
test beside the owned file). The audit record
`dev-diary/probes/latency.md` is gitignored and ships beside
`voice-agent.md` as the task demands. No call site changed, which the
handoff declares openly.

## Verification performed

* `go test -race ./internal/broker/` passes. `go vet ./internal/broker/`
  is clean.
* Full `./tools/check.sh` passes (`OK all checks passed`) after `npm ci`
  in the worktree. Before the install it stopped at `svelte-kit sync`
  for missing `web/node_modules`, which is environmental in a fresh
  worktree and unrelated to this Go-only commit. All 20 Go packages pass.
* Mutation on the pin: lowering `AssumedReplyLatency` to 30 seconds fails
  `TestSlowProviderFloors`. Restoring the file passes again. `git status`
  is clean and the hash is unchanged afterwards, so the pin is
  load-bearing and the tree still carries the handed commit only.
* Audit completeness, re-enumerated from landed code against the 20-row
  table: mint client 30 s (`token.go:62`), token expiry 60 s
  (`token.go:22`), cap range 60 to 10800 (`token.go:27`), sessions
  fetch and delete 30 s (`terminate.go:92`), artifact fetch 30 s
  (`reconcile.go:950`), reconcile kind 3 attempts with idempotent resume
  (`reconcile.go:405`), sweep kind 3 attempts with idempotent resume
  (`sweep.go:227`), over-cap margin 60 s (`reconcile.go:38`), sweeper
  margin with no production value wired (`main.go` carries no
  `NewSweeper`), socket `reply.done` wait unbounded (no timer in
  `socket.ts`), socket `end` wait unbounded (waiters settle only on
  `session.ended` or the pre-open short-circuit), cap timer ends at the
  minted cap with a 60 s warn default (`cap.ts:58`), mint POST with no
  `timeoutMs` (`record-state.ts:417`), 500 ms display polls
  (`record-state.ts:280`, `processing-state.ts:91`), no server timeouts
  (`main.go` builds `http.Server` bare), drain budget from the session
  cap with a 1 s poll and 30 s HTTP close grace (`main.go:59`), batch 90
  s client with 5 s poll gap under a caller context (`batch.go:135`),
  lease bound to the session cap, per-minute gate bursts (`main.go:406`),
  cookie lifetime 180 days (`identity.go:45`). Nothing on the session
  path is missing from the table.
* Constant values: 60 s assumed latency rounds up the recorded greeting
  replies (51.53 s, 46.84 s, 51.88 s in the T0.8 handoffs, near 47 s in
  the same series) against the 4 s T0.5 baseline. 90 s minimum wait is 60
  plus 30 headroom, asserted arithmetically in the test. Both rationales
  are stated in the constant comments and in `latency.md`.
* Library claims checked against shipped code: the Chaaya `api` client
  applies a deadline only when the caller passes `timeoutMs`
  (`client.js:79`), and the record page passes none, so the mint POST
  row holds as written. Keel `v0.3.0` `job` carries no attempt deadline
  outside tests, so the reconcile and sweep rows hold as written.
* Follow-up row 4 (severity C) checked true: no `NewReconciler`,
  `NewSweeper`, kind registration, or `job` use appears in `cmd` or in
  `internal` outside `internal/broker` itself. The sweep never runs in
  production.
* Follow-up row 1 checked true: `socket.ts` registers `onOpen` and
  `onMessage` only while the handle exposes `onClose`, and a pending
  `end` waiter settles on no other event.

## Findings

No blocking findings. Counts: C 0, H 0, M 0, L 0.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| (none) | | | | |

### OUT_OF_SCOPE

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L | `internal/broker/reconcile.go:410`, `internal/broker/sweep.go:232` at the reviewed hash | The literals `MaxAttempts: 3` and the margin default stay inline. The new constants name the same values but no call site reads them, so a later edit that lowers a call site bound passes the floor test. | `git grep -n 'MaxAttempts: 3' internal/broker/` still matches outside `timeouts.go`. Lower one call site to 1 and the gate stays green. | Restore the literal to reintroduce the gap. |

This row never blocks APPROVE. It restates the task's own follow-up row
3, which already owns the swap with its pin and owning paths. The task
block asks for wiring and also orders that anything outside
`timeouts.go` becomes a follow-up row rather than a drive-by edit, while
`owns` forbids touching the call sites here. The implementer followed
the coherent reading: constants plus record now, swaps under their
owning paths later. The DONE criterion holds because the audit found no
failing bound (the swaps are value-identical, 3 to 3 and 60 to 60), the
record lists every bound checked, and the gate passes with the fast path
untouched.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   commit adds constants and a value test only. No call path changes.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? Not through this commit. The audit records the
   unwired sweep as follow-up row 4 (severity C, owning path
   `cmd/reprise/main.go`), verified true above. It mirrors the P2 exit
   criterion on abandoned sessions and needs a task before P2 closes,
   but it is pre-existing and outside this task's owns.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Nothing here touches media or serving paths.
4. Does any edit apply without a revertible record? No edits apply at
   all. No behavior changes.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. The 60 s figure comes from recorded probe runs cited
   above, and no prompt text changes.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. `grep` for task ids, phase ids, and planning file
   names over both new files is empty, and the gate plan-reference scan
   passes. The `T0.8` and `T0.2` citations live in the gitignored probe
   record, where naming the measurement source is the point.
7. Did this task rebuild something Keel or Chaaya provides? No. It names
   bounds and pins floors. Attempt deadlines stay the job runner's job,
   the mint deadline stays the caller's choice, and neither library was
   patched.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes, confirmed at the top of this file. The one transient mutation
   was restored, the status is clean, and the hash is unchanged.
9. Can any paid job kind resume after a restart and spend twice? No
   change here. The audit confirms both kinds stay idempotent with
   resume, matching the landed T2.3 and T2.6 code.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The new test asserts values only and runs
    in milliseconds. The 25 s probe-runner abort is explicitly excluded
    from the product in `latency.md`.

## Notes for the orchestrator

* The severity C follow-up (unwired reconciler, sweeper, and job runner)
  is real, well-formed, and release-relevant. Scope it into a task that
  owns `cmd/reprise/main.go` before P2 closes.
* The Chaaya no-default-deadline surprise is audited in scope with a
  correct Holds verdict, not an unfiled finding. It is an opt-in library
  design, not a bug, so no library issue follows.
* The 90 s floor deserves its owning call sites when row 3 lands. Until
  then no reply give-up exists anywhere, so nothing can trip below it.

## Verdict

APPROVE with zero findings across all severities. Residue against prior
rounds: none, this is round 1 and every checked claim holds.
