# T7.16 round 1 review

Reviewed hash: `8026d0ec5a94b07dd4b2964134ce4f4df35df200`
Worktree: `/home/nryn/work/reprise-wt/t7.16-rev-r1` (detached, confirmed via `git log -1`)
Base: `19305b6`. Range reviewed: `git diff 19305b6..8026d0e` (commits `26dac47`, `8026d0e`). Nothing else.

## Done-criteria pins, verified in this worktree

Gate: `npm ci` in `web/`, then `./tools/check.sh`. Exit 0, `OK all checks passed`.
That run executes vitest plus the full Playwright spec, so the pins below ran here.

1. Real rows newest first: `threads.spec.ts` live test stubs `/api/episodes`
   with three rows and asserts headings read Third, Second, First, with zero
   fixture rows. Passes.
2. Progress on a running card: live test stubs a rendering row whose detail
   names `job-9`, streams one progress event, asserts the progressbar reaches
   at least 75 percent, reloads, and asserts the high-water mark holds. Passes.
3. Thread item opens the episode at its quote: live test stubs `/api/threads`
   with one quoted moment, clicks the quote link, and asserts the URL is
   `/episode/e1?w=5` with `Quoted moment at word 5` shown. The fixture test
   (`?fixture=1`) asserts the playhead position equals the quote offset, which
   is the playback half the live API cannot supply yet (see OUT-1). Passes as
   far as the owned paths reach.
4. Fixture set stays but no longer renders by default: gallery, episode, and
   threads mount live unless `?fixture=1`. The old default-render tests were
   updated to the fixture flag. Passes.

## Rulings requested

(1) `web/src/routes/episode/[id]/+page.ts` (`export const prerender = false`):
WIDEN BY DECISION. One line, byte identical to the sibling lines at
`web/src/routes/episode/[id]/edit/+page.ts:1` and
`web/src/routes/share/[token]/+page.ts:1`. The layout opts the whole tree
into prerender (`web/src/routes/+layout.ts:1`), and the live-first gallery
emits no episode links for the crawler, so the build fails without it. No
behaviour change. No new finding.
(2) API gaps: recorded below as OUT_OF_SCOPE follow-ups with owning paths.
None blocks this verdict.

## Findings

No in-scope findings. Counts: C 0, H 0, M 0, L 0.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| OUT-1 (M) | `web/src/routes/episode/[id]/+page.svelte:84-92` surface, owned by `internal/api/episodes.go:178` detail | The wired detail answers no render address, so a live episode shows the wait copy and never plays, and the spec line about playing the render with chapters and a following transcript does not hold live. Words, chapters, and duration are likewise absent from the detail shape. | Live episode view against a stubbed detail asserts `audioUrl === ''` and empty words. Fails once the detail carries a stream address. | Delete the `snap.live` wait branch so the view renders nothing where playback belongs. |
| OUT-2 (M) | `web/src/routes/+page.svelte:68` surface, owned by `internal/api/episodes.go:155` list | The season list answers id, number, title, state, and visibility only. Live cards show no cover art and no duration, against the spec line naming cover, title, state, and duration. | Live gallery test asserts `duration === null` on every live row. Fails once the list carries durations. | Drop the `meta` fallback so live cards render an empty duration line. |
| OUT-3 (L) | `web/src/routes/threads/threads.ts` `GalleryController.mountLive`, owned by `internal/api/episodes.go:178` detail | Each unfinished card needs its own detail fetch to learn its job id, so one gallery mount fans out one request per running episode. | Count `/api/episodes/{id}` requests during a two-running-row gallery mount. Asserts two today. Falls to one once a list field or batch route names the jobs. | Cache the first detail job id for every row. |
| OUT-4 (L) | `web/src/routes/threads/+page.svelte:199` surface, owned by `internal/api/episodes.go:178` detail | A live thread quote link parks on the covering proposal (`?w=` offset plus proposal highlight) instead of seeking audio, because there is no audio to seek. This is the correct degradation, recorded so the follow-up task owns the seek. | Live thread test asserts URL `/episode/e1?w=5` without asserting a playing state. A playing assertion fails until the detail carries audio. | Link live quotes to the bare episode route with no offset. |

## In-scope notes (no finding)

- Svelte runes only. No `export let`, no `$:`, no store imports in the owned
  routes. Grep over the three owned views is clean.
- Accessibility: progressbars keep minima, maxima, and values. Chapter,
  word, and quote controls keep labels. The empty season and missing
  episode states use headings plus status copy.
- No plan references in code, comments, tests, or strings. Grep for task,
  phase, plan, product, project, handoff, and section marks is clean apart
  from ordinary words such as surrounding rendering maths.
- `JobStream.attach()` effect-context workaround is contained to the owned
  controller with explicit cleanup runners. The handoff flags it for the
  library owners. No fork or patch of Chaaya ships here.

## The ten questions

1. Spend without reserving budget first: no. No paid call paths touched.
2. AssemblyAI session left open: no. No session code touched.
3. Private episode, stem, or transcript reaching a non-owner: no. Views read
   the owner-scoped handlers and add no fetch path.
4. Edit without a revertible record: no. No edit path touched.
5. Spoken callback or count from the prompt: no. Quotes render stored thread
   rows with episode ids and offsets.
6. Plan, phase, or task cited in code, comments, tests, or migrations: no,
   verified by grep.
7. Rebuilt Keel or Chaaya capability: no. Controllers reuse `AudioPlayer`,
   `JobStream`, and `activeWordAt` from Chaaya.
8. Review run on the handed hash in its own worktree: yes, stated above.
9. Paid job kind resuming after restart and spending twice: no. No job kinds
   touched.
10. Development check needing a person, microphone, or wall-clock threshold:
    no. Playwright feeds stubbed APIs and generated audio. Progress polls
    use bounded waits against evented state, and the gate passed here.

## Docs audit

`python3 dev-diary/tools/audit_docs.py` reports the planning tree is
consistent.

## Verdict

APPROVE. Zero in-scope findings across all severities. Four OUT_OF_SCOPE
follow-ups name the `internal/api/` detail and list shapes that own them.
The `+page.ts` prerender line is widened by decision, not a violation.
