# T3.5 remediation round 1

Reviewed hash: `1bdfbbcd7d531b251a62aacfce8c75ee076b6bac`.
Worktree: `/home/nryn/work/reprise-wt/t3.5-rem-r1`, branch `task/t3.5-rem-r1`.
New head: `8998a3201944b1bb588420ea69d0423746afb2a3` (one commit on the reviewed hash, no rebase, no amend).
Scope: `internal/analysis/chapters.go` plus `internal/analysis/chapters_test.go`. Both sit inside the task owns.

## Findings fixed

| Severity | Where | Fix | Pin |
|---|---|---|---|
| H | `internal/analysis/chapters.go` (`middleBoundary`, via `splitLongest`) | `middleBoundary` now skips every word start that leaves either half under `MinChapterMs`. The nearest to middle survivor wins. When no start satisfies the floor on both sides, the split fails and `ensureCount` keeps fewer chapters. Merge then split order and the continued marking stay unchanged. | `TestNormalizeSplitFloorKeepsTenSeconds` runs the review pin shape, one `Whole` draft over `scheduleWords(40, 800)` at 32000 ms. It failed before the fix with chapters at 0, 8000, and 16000. It now passes with two sixteen second chapters on the word grid with the continued mark. `TestNormalizeShortEpisodeKeepsFewer` pins a 25 second episode at two chapters, each above the floor. |

## Prior pins intact

The full package suite passes with race. `TestNormalizeSplitsToThree`, `TestNormalizeMergesShortSpans`, `TestNormalizeDropsTrailingSliver`, `TestNormalizeDedupesEqualStarts`, and `TestNormalizeKeepsShortEpisodeWhole` all pass unchanged. Run, store, and offset tests pass unchanged.

## Untouched per instruction

Scripted doubles, merge order, `OffsetOf` edges, the replant warning, `GatewayPrice` rates, `Estimate`, sink names, `Kind` flags, and the `deleted()` convention all stay as reviewed.

## Gate

`./tools/check.sh` passes in the remediation worktree after `npm ci`, including `go test -race`, staticcheck, and every web check through playwright.
