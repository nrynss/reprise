# T7.10 round 1 review

Reviewed hash: `c192ea9d265d9fcb8dea7a469b663b9ce856155c`.
Worktree: `/home/nryn/work/reprise-wt/t7.10-rev-r1` (detached HEAD).
Confirmed with `git log -1` before reading the diff.
Reviewed diff only: `git diff 7f39a5f..c192ea9d265d9fcb8dea7a469b663b9ce856155c`.
Base is main at `7f39a5f`. Spec is the live T7.10 block in
`dev-diary/PHASE-7-production-issues.md` at `/home/nryn/work/reprise/`.
Handoff is `dev-diary/adversarial-review/t7.10-impl-handoff.md`.
No code was committed from this worktree.

## Scope

The diff touches two paths. `dev-diary/probes/production.md` gains a
253 line round 4 append. `dev-diary/PHASE-7-production-issues.md`
loses one status line. Owns allows only the probe file.

## Verification performed

Prefix check: the first 842 lines of the reviewed probe file are byte
identical to the base file. `cmp` against `git show 7f39a5f:...`
reports identical. Rounds 1 through 3 stand untouched.

`dev-diary/tools/audit_docs.py` passes. It prints
`OK planning tree is consistent` with exit 0.

Secrets scan of the appended section finds no key material. Tokens
read as redacted. Long hex strings are session, episode, media, and
provider session ids plus the edge version hash. Cookie jars lived in
`/tmp/t710` and the note states deletion.

Workstation scope holds. The note states every check ran from the
workstation against `https://reprise.nryn.dev`, never from the box.
The helper script stays untracked at `/tmp/t710/live.py`.

No T or P cites appear in the append. `grep` for task ids, phase ids,
plan names, and section marks returns nothing. The `---` lines are
markdown rules and `--data-binary` is a curl flag.

Spot check 1, read only: spend arithmetic holds. 51.7 connected
seconds at 4.50 dollars per hour equals 6.46 cents. The note claims
about 6.5 cents. The failed batch pass bills nothing because the
provider refused it before transcribing.

Spot check 2, read only: the owning path claim for the new defect is
accurate. `internal/assemblyai/batch.go:227` wires
`SpeechModels: []string{c.model}`. `cmd/reprise/main.go:1262` feeds
`loaded.TranscriptionModel`. `internal/settings/settings.go:38`
defines the setting. Nothing was minted and no edge call ran.

## Round 3 close, both halves

Failed surfaces: first completion answered `moved` true and
`scheduled` true with the running outcome beside the same job id
(`production.md:958`). Detail then showed `state` failed with zero
proposals and the full provider rejection beside that job id, stable
across six polls (`production.md:1013`). The silent vanish is gone.

Repeat reports standing outcome: the second completion answered
`moved` false, `scheduled` false, and empty job id with the same
error text (`production.md:1032`). Nothing scheduled twice. The blind
repeat is gone.

## New batch model defect, carry against owed evidence

The pipe now speaks a provider 400 naming only `universal-3-pro`,
`universal-2`, and `universal-3-5-pro`. No episode reaches proposals
on this build while that value stands. The note scopes this as a
follow up naming `internal/assemblyai/` plus the model id setting
seam (`production.md:1062`). That scoping is correct. T7.10 owes a
recorded live outcome with command and output, not proposals. A fix
here would edit paths outside owns. Carry is the right call.

## Ten questions

1. Does any path spend money without reserving budget first. No. The
   mint reserves through the designed path and the failed batch
   billed nothing.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap. No. The client sent `session.end` and the end
   record answered HTTP 200.
3. Can a private episode, stem or transcript reach anyone but its
   owner. No. The episode lists privately under the owner cookie.
4. Does any edit apply without a revertible record. No edits apply.
   This task appends a probe note only.
5. Does any spoken callback or count come from the prompt instead of a
   stored row. No. Transcript lines are provider output and spend
   derives from connected seconds.
6. Does any code, comment, test or migration cite a phase, a task, or
   a planning file. No. The append names rounds only.
7. Did this task rebuild something Keel or Chaaya provides. No. It
   writes a probe note.
8. Was this review run on the handed hash, in a worktree of its own.
   Yes. Detached worktree confirmed by `git log -1`.
9. Can any paid job kind resume after a restart and spend twice. No.
   The repeat scheduled nothing and reported the standing outcome.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold. No. All speech is generated and no person
    appears.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `dev-diary/PHASE-7-production-issues.md:288` at reviewed hash | The commit reverts the T7.10 status line to `not-started` outside its owns line. Owns allows only `dev-diary/probes/production.md`. Landing this hunk corrupts orchestrator state. It must drop before landing. | `git diff 7f39a5f..c192ea9d265d9fcb8dea7a469b663b9ce856155c -- dev-diary/PHASE-7-production-issues.md` shows the revert. The live T7.10 block reads `owns: dev-diary/probes/production.md`. The two facts together prove the violation. | After the fix, set line 288 back to `status:     not-started`. The pin then fires again. |

Counts: C 0, H 0, M 1, L 0.

## Verdict: REMEDIATE

One M finding blocks approval. Drop the phase file hunk as a new
commit on the reviewed commit. Nothing else needs change. The round 4
append, the round 3 close on both halves, and the new defect carry
with its owning paths all stand as written.
