# T2.5 remediation round 1

Reviewed hash: `bca8f89b85dae27ed7b9cf041099f59ef2c7669d`
Remediation head: `d3c00e7fa4418c03b25cc41c866f436cbbca1f6e`
Worktree: `/home/nryn/work/reprise-wt/t2.5-rem-r1`, branch `task/t2.5-rem-r1`.
Five new commits sit on the reviewed commit. Nothing was rebased or amended.
All changes stay inside `internal/align/`. The full gate passes in the
worktree, including `go test -race` and the plan reference scan.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/align/fixture_test.go` (`measureSession`, `checkFixtureSession`) | A session directory with a wrong layout skipped silently. Missing or unreadable wanted files inside an existing directory now return an error, and the checker fails the test on it. Only the absent fixture root still skips. A synthetic layout proves the driver measures. One aligned session passes with zero drift. A drifted variant with 400 extra host channel samples fails the check at 400 samples of drift. A ran count pins both layouts measured, and the recorder run pins exactly three sessions once fixtures land. | `TestDriverMeasuresSyntheticSessions` (aligned passes, drifted fails), `TestDriverFailsLoudOnWrongLayout` (renaming `user.wav` errors), `TestFixtureSessions` (fails unless three directories measure) | Rename any wanted file in a valid layout: the session flips from measured to failed. Dropping the `measured != 3` assertion lets an empty recorder root pass. |
| L | `internal/align/align_test.go` (`TestDriftLimitBoundary`) | The exact 40 ms boundary had no pin. A new case asserts that exactly 40 ms passes per the `<=` semantics. | The exact case in `TestDriftLimitBoundary`. The remediator flipped `<=` to `<` and watched this case fail, then restored the operator. | Change `<=` to `<` in `Pass`: the new boundary case fails while the 30 ms and 50 ms cases stay green. |
| L | `internal/align/align.go` (`ResampleLinear` doc) | The doc claimed the output keeps the last input sample at every ratio. That holds for exact ratios only. The remediator narrowed the claim instead of pinning the tail: the doc now states that other ratios truncate the tail by integer division. | `TestResampleLinearExact` (dyadic pins already fail if the `frac` term drops). No new pin was added, because the fix is a narrowed claim rather than a behavior contract. | Drop the `frac` term in the interpolation loop: the existing dyadic pins fail. |
| L | `internal/align/fixture_test.go` (file header, `TestDriverRejectsStoredBytes`) | The transcoding step was implicit. The header now states that fixtures arrive as pre-transcoded 16 bit PCM WAV while the server stores the provider recording as OGG. A native sample transcode pin is not obtainable in a pure unit test without shelling out to an encoder. The test pins the dependency from the other side instead. OGG magic fails decode with `ErrWAV`, and a session whose provider file holds stored bytes errors instead of measuring. | `TestDriverRejectsStoredBytes` (direct decode plus full driver run against OGG bytes) | Replace a fixture `provider.wav` with OGG bytes: the driver errors on magic instead of measuring. |

## Owed rerun

The rerun against the three real recorded fixtures is owed after they land.
The fixture task is another orchestrator workstream and still runs, so this
remediation touches neither `testdata/sessions/` nor any probe file. The
recorder run currently skips on the absent root and will pin exactly three
measured sessions once the fixtures arrive. Transcode fidelity of real OGG
artifacts rides with that rerun.
