# T3.1 remediation round 1

Reviewed hash `3e6c08ea5e8ffe26fd8690cf00971248d7a98cab`, remediated in
worktree `/home/nryn/work/reprise-wt/t3.1-rem-r1` on branch
`task/t3.1-rem-r1`. New head `2b28fc3`. Two commits, one per finding, on
top of the reviewed commit with no rebase. Tree is clean. The gate passes
in this worktree after `npm ci`, including `go test -race` for both
packages and the full web checks (`OK all checks passed`).

| Finding | What changed | Pin now passing |
|---|---|---|
| M, receipt error names no id | `internal/transcript/job.go`: the receipt store failure returns `transcript: run %s: receipt store: %w` with the transcript id. The `%w` wrap stays, so `errors.Is` still reaches the store error, and the survive-for-retry behavior is unchanged. Committed as `46611b1`. | `TestRunKeepsProviderCopyOnReceiptFailure` in `internal/transcript/run_test.go` now also asserts the run error names `tx-9`. The mutation check (dropping the `%s` verb and argument) fails the test with `want it to name tx-9 for the explicit retry`, and the fix passes it. |
| L, create body as map | `internal/assemblyai/batch.go`: `Create` sends a named `createBody` struct (`audio_url`, `speech_models`, `keyterms_prompt` with `omitempty`) instead of `map[string]any`. The JSON shape is identical, and `TestCreateSendsModelAndKeyterms` passes unchanged. Committed as `2b28fc3`. | `grep -rn 'map\[string\]any' internal/assemblyai/batch.go` no longer hits production code. The remaining hits in the package are test doubles. |

The 50 ms note from the round needed no change and was left alone.
Nothing outside the task `owns` paths was touched.
