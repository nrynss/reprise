# T7.5 round 1 review

Reviewed hash: `c5f8934ceaf9595cc402d8937959b1af44fec941`
Worktree: `/home/nryn/work/reprise-wt/t7.5-rev-r1` (detached, confirmed with `git log -1`)
Base: `464f0c8`
Diff reviewed: `git diff 464f0c8..c5f8934ceaf9595cc402d8937959b1af44fec941` (commit only, not the working tree)
Verdict: REMEDIATE
Counts: C 0, H 0, M 0, L 2 (one OUT_OF_SCOPE, one in scope)

## Scope

Task T7.5 owns `dev-diary/probes/production.md` ONLY. The commit touches two paths:
`dev-diary/probes/production.md` (327 added lines, zero deleted lines) and
`dev-diary/PHASE-7-production-issues.md` (one status line). The second path is
outside owns and is recorded below as OUT_OF_SCOPE.

## Spec coverage check by check

Round 2 is appended after line 267. Round 1 is byte untouched. The first 267
lines of the head file hash identically to the full base file (`e0af56068d7f1d6ef28626c9a4b37a87993467e47ad5dc236d76934fb05397b3`).
The diff on the probes file shows zero deleted lines.

Edge identity carries command plus output and names the required P7 build
version `464f0c895fe355c435bca21aded9fc57cc34202d`. I re-ran `curl
https://reprise.nryn.dev/healthz` read only from this worktree and got
`ok 41b9e8a8796a09f06ee3170db29f1f03 version=464f0c895fe355c435bca21aded9fc57cc34202d`.
The version matches. I minted nothing.

Check 1 carries command plus output for mint (HTTP 201), the live generated
speech call (`session.ended duration=16.406599`), the session end POST
(HTTP 200), and the gallery reads under the owner cookie. Result pass.

Check 2 carries command plus output for the three handler refusals (409
`illegal_transition`, 404 `proposal_not_found`, 400 `invalid_request`). It
carries forward with its reason in words: the episode stays in `recording`
with zero proposals because nothing schedules the transcript and editorial
kinds and nothing calls the stems uploaded transition. The carry names its
follow up task descriptively: wire stem completion into the transcript and
editorial schedule and the draft transition, then re-run this check live. No
T number exists yet, so the orchestrator opens that task. One sub claim in
this check lacks command plus output (see Finding 2).

Check 3 carries command plus output for both anonymous reads (HTTP 404 size
19, `cache-control: private, no-store` on both the real blob and the unknown
id). I re-ran the unknown id GET read only from this worktree and got HTTP/2
404 with `cache-control: private, no-store`. The header gap from round 1 is
closed. I touched no real blob.

Check 4 carries command plus output for the owner round trip (HTTP 200 size
32000, hash match, `content-type: audio/ogg`). Result pass.

Check 5 carries command plus output for the Sessions API read (completed,
`client_end`, 16.801372 seconds against socket 16.406599) and for the settle
observables (end 200, post end mint 201, limits 403 `owner_required`
measured). It closes on its workstation observable terms. The direct ledger
row comparison waits on the owner login, same gate as check 6. That names
T7.4 as the waiter.

Check 6 carries command plus output for the pause refusal (403
`owner_required`) and records SKIPPED with its reason: the owner login lands
after dogfooding, so T7.4 stays out of this round. It states the site was
left unpaused and a fresh mint answered 201 after. The spec asks for exactly
this statement either way.

Check 7 records deferred, not a gate, with its reason: the catalog is empty,
so the host opens with the fallback greeting from check 1. The re-run waits
on the operator catalog, as the task states.

No secrets appear anywhere. A grep for key, token value, password, and
private key patterns across the probes file returns nothing. Tokens and the
account key are described as redacted, ids shown are session, episode, blob,
and provider session ids, which the verification needs. Cookie jars are
stated as deleted from `/tmp`. Every check states it ran from a workstation,
never from the box. The only box mentions in round 2 are that statement.
Spend totals about 2.1 cents for one 16.8 second live session.

`audit_docs.py` passes: `OK planning tree is consistent`.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L, OUT_OF_SCOPE | `dev-diary/PHASE-7-production-issues.md:159` at `c5f8934` | The commit writes outside owns. It flips the T7.5 status line from `in-progress:implement:t7.5-impl` to `not-started`. Only the orchestrator writes status lines, and T7.5 owns the probes file only. | In this worktree run `git diff 464f0c8..c5f8934ceaf9595cc402d8937959b1af44fec941 --name-only`. It must list only `dev-diary/probes/production.md`. It currently also lists `dev-diary/PHASE-7-production-issues.md`, so the pin fails. | One line edit reintroduces it: on the phase file set line 159 back to `status:     not-started`. The pin fails again. Fix by reverting that hunk as a new commit on the task branch, or let it drop at landing. It never blocks APPROVE. |
| L | `dev-diary/probes/production.md:417-420` at `c5f8934` | The SSE idle stream sub claim in check 2 has no command plus output block. The text asserts a GET on `/api/jobs/does-not-exist/events` with an event stream accept header answers HTTP 200, holds open, times out at 8 seconds with zero bytes. No bash command and no output block support it, while every other check 2 assertion carries both. | In this worktree run `sed -n '/## 2\. Decisions/,/## 3\. Private/p' dev-diary/probes/production.md \| grep 'curl.*api/jobs'`. It exits nonzero now, so the pin fails. After the fix adds the GET command it exits zero. | One line edit reintroduces it: delete the added `curl` line for the jobs events GET. The pin fails again. Fix by appending the exact GET command with its accept header plus its output block (status, byte count, wait) inside check 2. A read only GET needs no mint and no state change. |

## Ten questions

1. Does any path spend money without reserving budget first? No. No code
   changed. The probe spent about 2.1 cents on one live session through the
   existing reserve path.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. Check 1 shows an explicit `session.end`, a `session.ended`
   answer, and provider close reason `client_end`.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   Anonymous reads answer 404 with `private, no-store`, and the gallery lists
   only under the owner cookie. I verified the anonymous refusal headers live.
4. Does any edit apply without a revertible record? No. No product edit is in
   this commit.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. Check 7 claims no callback greeting and defers to the operator
   catalog. No spoken count is asserted.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Only the probes doc changed, and docs must name tasks
   for carries. No source file changed.
7. Did this task rebuild something Keel or Chaaya provides? No. No code
   changed.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   `git log -1` gives `c5f8934ceaf9595cc402d8937959b1af44fec941` in detached
   worktree `t7.5-rev-r1`. I committed nothing and changed no tracked file.
9. Can any paid job kind resume after a restart and spend twice? No. No job
   code changed.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. All speech is generated. My spot checks were two read only
    GETs. The 0.4 second duration agreement is an observation, not a gate.

## Observations for the orchestrator (not findings)

The item 2 carry names stem completion scheduling descriptively but carries
no T number. Open that task before closing P7, then re-run check 2 live.

The commit message names the P7 build. The gate checks tracked source files,
not messages, and remediation cannot amend a reviewed commit, so this stays
an observation. Reword at landing if desired.

My live spot checks ran two read only GETs (healthz and the unknown media
id). I minted nothing, flipped nothing, and uploaded nothing.
