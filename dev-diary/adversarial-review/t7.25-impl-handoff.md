# T7.25 implementer handoff

Commit `2f42eed395f6915a2213ccf73f6be664c840ee64` on branch `task/t7.25` in worktree `/Users/narayan/reprise-wt/t7.25-impl`. Base is `742c0363db343743fcd4143e0b4aa65cfd1e7293`. The worktree is clean. This file is not in the commit.

## What I built

`goProcessing` returns without assigning when the record page has already dropped the controller. End and retry both use that function. The draft move itself still finishes. A move that settles while the guest is still on the record page still assigns processing.

`destroy` sets that flag before it removes listeners. It does not cancel the in-flight move.

## Widening

The pin is outside `web/src/lib/voice/`. I added it to `web/src/routes/record/gallery-link.spec.ts`, which already uses `src/routes/record/mock.playwright.config.ts`. No other path was edited.

The existing hold aborts `**/stems/complete` on release. That hides the late assign. The new helper fulfills HTTP 200 with the standing draft body instead.

One proof ends the mock take, opens the gallery, then fulfills. The path stays `/`. A second proof does the same after a refused move and a retry. Putting the assign back makes both proofs receive `/processing`.

## Commands and results

`npm ci` in `web/` installed 236 packages. Exit 0. `node_modules` was missing in this worktree.

`npx eslint src/lib/voice/record-state.ts src/routes/record/gallery-link.spec.ts` from `web/`. Exit 0.

`npx svelte-kit sync` then `npm run check` from `web/`. 0 errors and 0 warnings.

`npm run build` from `web/`. Exit 0.

`npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts` from `web/`. 10 passed in 2.4s.

`npx playwright test -c src/routes/record/mock.playwright.config.ts end-control.spec.ts cap.spec.ts stems-complete.spec.ts mock-session.spec.ts` from `web/`. 13 passed in 11.7s.

`npx playwright test -c src/lib/voice/mock.playwright.config.ts controller-complete.spec.ts session-guard.spec.ts --workers=1` from `web/`. 5 passed in 1.5s. Those specs still reach processing while the guest stays on the record page.

An earlier run of those two voice specs with two workers failed the first start on each worker. The page was still on the preflight notice and On air never appeared. The same start passed in that run's later tests, and the serial rerun passed. I did not change that start path.

`./tools/check.sh` was not run. The ffmpeg pin can fail that gate before later stages, and I did not change the pin.

## What surprised me

The preview server answers 404 for `/api/episodes`. The season heading still renders, so the gallery proofs do not need a backend.
