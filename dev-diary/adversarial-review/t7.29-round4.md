# T7.29 review, round 4

- **Hash reviewed:** `680102b53dfd02f344e37dc66fb5b6ffcc22cd42` (`git log -1` confirmed it: "Repair queued render handoff")
- **Worktree:** `/Users/narayan/reprise-wt/t7.29-rev-r4`, detached and clean after every temporary probe was removed
- **Base:** `78f8ab3`. I read `git diff 6ece425..680102b` for this remediation and `git diff 78f8ab3..680102b` for the whole task.
- **Scope:** The remediation changes only the task-owned finish scheduler, episode service, editor and gallery paths.

**Verdict: REMEDIATE**

| Severity | In scope | Out of scope |
|---|---:|---:|
| C | 0 | 0 |
| H | 0 | 0 |
| M | 1 | 0 |
| L | 0 | 0 |

## Gate

`cd web && npm ci` exited 0. `./tools/check.sh` then stopped only at the accepted workstation exception: both media tools report ffmpeg `9.0.2`, while the repository pins `9.0.1`.

With a temporary untracked `dev-diary/probes/go.mod` (`module probes`, `go 1.27.1`) to keep review probes out of the product Go module, I ran every later gate step. I removed that marker and every copied probe afterward.

| Step | Exit |
|---|---:|
| `gofmt -l cmd` | 0, no output |
| `go list ./...` | 0 |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| Svelte 4, consumer-name and planning-reference scans | all empty, each grep exited 1 |
| `go test -race ./...` | 0 |

The round-3 editor, gallery and API refusal probes pass at this hash. I also ran the preserved round-1 and round-2 pins at the hash. Their documented mutations remain load-bearing. The remediation's own changes are covered by the round-3 mutations: routing a queued reply through `onRenderStarted`, routing a refused live reply through the fixture branch, restoring the old dead-retry answer, and retaining a refused gallery row each fails its corresponding pin.

## The ten questions

1. **Spend without reservation?** No. This retry starts only a render, which has no paid provider call. The paid analysis chain remains gated before every provider call.
2. **AssemblyAI session left open?** No. This task opens no live session.
3. **Private data to a non-owner?** No. Retry begins with the owner-scoped `Get`, and the existing route repeats that owner check. The queued field contains no private artifact.
4. **Edit without a revertible record?** No. This task changes no edit decision.
5. **Callback or count from the prompt?** No.
6. **Plan citations in code?** No. The source scan is empty and the added comments describe behaviour directly.
7. **Rebuilt Keel or Chaaya?** No. The scheduler uses Keel job kinds, limits and cancellation. The frontend continues to use Chaaya's job stream.
8. **Handed hash, own worktree?** Yes. I verified `680102b53dfd02f344e37dc66fb5b6ffcc22cd42` in the detached worktree.
9. **Paid kind resumes and spends twice?** No. A retry reaches only the render queue. The existing scheduler lock and stored render linkage still prevent two render starts. Paid kinds remain non-idempotent and are interrupted after restart.
10. **Check needs a person, microphone or wall-clock threshold?** No. The review probes use fixtures, fake timers and stored states. Their deadlines are hang guards only.

## Residue

| Earlier round | Result |
|---|---|
| 1 | C 0, H 0, M 0 and L 0 residue. The waiting-render walk, render-link ownership, hook wiring and settled-outcome drain pins all pass, and their mutations are killed. |
| 2 | C 0, H 0, M 0 and L 0 residue. A busy render still returns one queued 202 response, starts no duplicate render and ships when the slot frees. Its mutation is killed. |
| 3 | C 0, H 0, M 0 and L 0 residue. The editor distinguishes queued, refused and fixture results. A failed detail leaves the queued recheck set. An initial start fault can be retried through the route. The previous findings no longer reproduce. |

The round-1 out-of-scope retention and render-attempt items, the round-1 mock test configuration item, and the round-2 render-concurrency item remain out of scope with their recorded owners. This review adds no out-of-scope finding.

## Finding

### 1. A retry can strand the episode as an unbounded queued render

| Column | Value |
|---|---|
| Severity | M |
| Where | `cmd/reprise/finish.go:553-560` and `cmd/reprise/finish.go:586-588`, reached after `internal/episode/done.go:39-43` retries a failed episode into `rendering`. |
| What | The new retry returns 202 with an empty job id and relies on `startWaitingRenders` to start the render. A capacity refusal is safely retained as queued. Any other `startStamped` failure, including a render-job stamp failure after its job is cancelled, is only logged. The episode stays `rendering` with no linked render. The scheduler tries again every interval, while the gallery keeps polling it as queued. Thus a persistent start or stamp fault is no longer visible as `failed`, nor can the owner issue another meaningful retry. |
| Pin | A fixture that makes the initial `RequestRender` fail, retries through `POST /done`, then makes the scheduler's `startStamped` return a non-`job.ErrLimit` error must observe `failed` after `advanceAll`, with no linked render. At this hash it observes `rendering` with no render row, and every later `advanceAll` repeats the same empty queue. This is independent of the editor and proves the stored lifecycle state. |
| Mutation | After remediation separates `job.ErrLimit` from other start errors and transitions the latter from `rendering` to `failed`, remove that transition. The fixture again observes the permanent empty queue. |

The explicit retry fixes the prior dead 409. It needs this final branch so a failed scheduler start does not replace a visible failed state with an endless queued state.
