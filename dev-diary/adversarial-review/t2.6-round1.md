# Review T2.6 round 1: abandoned session sweep

Reviewed hash: `e3f6c3fc2bba687faa6c4f2c31e0985b7e1c6df4`
Worktree: `/home/nryn/work/reprise-wt/t2.6-rev-r1` (detached HEAD, confirmed with `git log -1 --format=%H`)
Base for diff: `ff483a1` (claim commit; `git diff ff483a1..e3f6c3f` holds only the six owned files)
Owns check: diff holds `internal/broker/sweep.go`, `internal/broker/reconcile.go`,
`internal/assemblyai/terminate.go`, `internal/assemblyai/terminate_live_test.go`,
plus `internal/broker/sweep_test.go` and `internal/assemblyai/terminate_test.go` beside them.
The task owns the first four plus the tests beside them. Clean. No commits made, working tree clean.

Verdict: **REMEDIATE**
Counts: C 0, H 0, M 1, L 0. Out of scope findings: 2 (recorded, never blocking).

## Verification run

- `git log -1 --format=%H` equals the handed hash. No commits made. Probe file removed after run.
- `go test -race -count=1 ./internal/broker/ ./internal/assemblyai/` passes in this worktree (non-live only).
- `gofmt -l` clean over both packages. `go vet` clean over both packages.
- Full `tools/check.sh` web build not rerun here. The task owns Go files only, and the handoff records
  a green gate in its own worktree. The race suites are the load-bearing check, and they pass.
- Live test NOT run (spend rule). Verified by reading: `live` build tag, 120 second caps
  (`voiceLiveTokenCapSeconds = 120`), in-test spend cap assertion
  (`terminate_live_test.go:137`), raw responses in `dev-diary/probes/voice-agent.md`.
  The gate runs `go test -race $GO_PKGS` with no `-tags live`, so the probe never runs in CI.
- T2.3 regression: full broker suite (including every `reconcile_test.go` case) passes untouched.
  The `reconcile.go` diff removes two lines only (one comment line extended elsewhere,
  `row, err =` becoming `row, err :=` inside the extracted `settleRead`). No behavior drift.

## Scrutiny answers (a) through (i)

- (a) Settle-then-delete is enforced in code, not just intent. `sweepOne` (`sweep.go:348-359`)
  calls `ReconcileAbandoned` first and `EndSession` after, sequentially, with no concurrency
  between them. The consequence of the reverse order is pinned: `TestSweepGoneRecordReviews`
  proves a gone record settles nothing, holds the estimate untouched, and raises review.
- (b) Shortfall settle is pinned with exact amounts. `TestSweepSettlesCompletedPastCapExactlyOnce`
  settles 3600 seconds against an 1800 second reservation (`recRate * 1800`): global spent and
  owner spent both read exactly `recRate * 3600`, held reads 0, and the row carries 3600.
- (c) Idempotent delete is pinned (`TestTerminateSessionIsIdempotent`: 404 repeat returns
  success with `Deleted` false, server saw exactly one delete). The record delete as hygiene,
  not a stop, is stated in `sweep.go:1-8`, `terminate.go:1-5`, `EndResult` docs, the
  always-false `ServerEnded` field, and the handoff. Safe against double-sweep billing.
- (d) `SweepSource` linkage is declared openly (`sweep.go:38-41`, `sweep.go:95-97`), the same
  way T2.3 declared its carried linkage. No sweep can enumerate on its own before the migration.
- (e) `reconcile.go` diff is limited to the three seams (claim-guard-sharing `settleRead`
  extraction, `IsSettled`, `AlertSweepOpen` plus `ReconcileAbandoned`). Full broker suite green.
- (f) Live discipline holds: 120 second caps, about 10 cents total across two runs under the
  1 dollar budget, absolute config path note in the probe record, `live` tag, gate exclusion
  confirmed in `tools/check.sh` (no `-tags` flag on the `go test -race` line).
- (g) No provider error-text matching. Every branch keys on status codes
  (`StatusNotFound`, `StatusOK`, `StatusNoContent` in `terminate.go`). Clean.
- (h) Two expected out-of-scope rows below. Both verified by reading.
- (i) No plan refs in any owned file (pattern scan clean). Go style holds: one sentinel per
  condition (`ErrSweep`, `ErrProviderGone`, `ErrFetch`, `ErrTerminate`, `ErrSessionGone`,
  reused `ErrInvalid`), `%w` wrapping throughout, `ctx` first on I/O, no naked returns,
  no `panic`, every exported identifier documented.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/broker/sweep.go:399-401` | On alert-delivery failure, `alert()` writes the sweep row with empty provider status and zero seconds, and first-write-wins (`INSERT OR IGNORE` in `markSwept`) drops the real outcome write that follows. The money settles correctly, but the queryable audit row reads `""`/`0` instead of the provider status and connected seconds. The detail text retains the numbers, which is the workaround. Same pattern at both call sites (`sweepOne` still-open path and `reviewGone`). | Temporary probe `TestProbeAlertFailureKeepsOutcome` (written in this worktree, run, then deleted): failing `Alerter` plus an open 3600 second fixture, then read `sweep_state`. Fails on the reviewed code with `row status="" seconds=0 detail="settled 3600 seconds at $4.50, ... (alert failed: ...)"`, want `created`/`3600`. The probe fails, so the pin is load-bearing. | Restore the `markSwept` call inside `alert()` on delivery failure ahead of the outcome write (i.e. revert the fix that defers failure recording until after the outcome row lands). The probe then fails again with `""`/`0`. |

## Out-of-scope rows (recorded, never blocking)

| Severity | Where | What | Pin | Mutation | Owner |
|---|---|---|---|---|---|
| H | `internal/assemblyai/token.go:24-26,67-69` | Stale cap comment says the provider ends the session at the cap even when the browser vanishes. The T0.2 probe and this task's measurement both prove it does not. Comment misdescribing behavior carries the behavior's severity, but the file is T2.1's and outside this task's owns. Left untouched as instructed. | T0.2 probe record plus the T2.6 `Server side end` section in `dev-diary/probes/voice-agent.md`: DELETE returns 204, socket sees nothing, later `session.end` bills full wall time. | Reword the comment to claim the cap ends sessions. The probe record contradicts it. | T2.1 (`internal/assemblyai/token.go`) |
| M | `internal/broker/reconcile.go` (pre-existing `SessionReader`/`ProviderSession` shape) | The reconciler seam assumes a flat session body with `duration_seconds`, `recording_url`, `timeline_url`. Live records show the real fetch shape: `status`, nullable `duration_seconds`, and an `artifacts` array. The handoff declares this openly for the reconciler-reader owner. Untouched by this diff. | Live `before delete` record in the probe file: `status created`, `duration_seconds null`, `artifacts []`, no flat URL fields. | Decode the flat shape against the real body. The fetcher finds no `recording_url` field. | Provider-adapter owner (reconciler reader wiring) |

## The ten questions

1. Does any path spend money without reserving budget first? No. The sweep only settles
   mint-time holds through the reconciler. The shortfall case is pinned on both ceilings.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap?
   No new path. The sweep cannot end a live session (measured: no server end exists), so it
   settles, alerts (`sweep_open`), and deletes the record. The browser timer owns the stop,
   the minted cap bounds only the well-behaved case, and the code states this wherever the
   claim could be misread. The known gap is declared, not hidden.
3. Can a private episode, stem or transcript reach anyone but its owner? No. Artifact fetch
   runs through the reconciler's existing private persist path. No new serving.
4. Does any edit apply without a revertible record? No user edits. Writes are durable
   settle and sweep state.
5. Does any spoken callback or count come from the prompt instead of a stored row? Nothing speaks.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file?
   No. Pattern scan over all six diff files is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. It stands on `keel/cost`
   settle, `keel/job` kinds with resume, and `keel/sqlite`. The alert flag and sweep rows
   are broker-owned state, not library state.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Detached worktree
   at `e3f6c3f`, confirmed by `git log -1`, diffed against `ff483a1`, pins run inside it,
   no commits, tree clean afterwards.
9. Can any paid job kind resume after a restart and spend twice? No. The `sweep` kind is
   idempotent with three attempts, `Resume` rebuilds from the progress snapshot, settling
   runs at most once through the reconciler claim guard, and the resweep tests prove no
   second delete, no moved spend, and no new alert.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No.
    Sweep and terminate unit tests run on fixed clocks and a fake server with no sleeps.
    The live probe streams generated speech and its wall-clock windows are observation
    bounds inside a spend-capped live measurement, never CI.

## Verdict

**REMEDIATE.** One in-scope M finding: alert-delivery failure poisons the sweep audit row
through first-write-wins. Fix the ordering so the outcome row lands with its real status
and seconds and the alert failure is recorded beside it, not instead of it. Everything else
in the scrutiny list verifies clean, with zero residue claimed on no other point because no
other point raised a finding.
