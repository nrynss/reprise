# T7.12 round 1 review

Reviewed hash: `234d65cf950e67a75858bd2014778d960f188c29`.
Worktree: `/home/nryn/work/reprise-wt/t7.12-rev-r1` (detached, confirmed by `git log -1`).
Base for diff: `5415221`. No commits were made from this worktree.

## Scope

The diff touches three files, all inside `owns` (`web/src/lib/voice/`).
`record-state.ts` posts the stored stem pair through the T7.11 driver
before the processing navigation. It keeps the pending pair on refusal
and retries through `retryCompletion` without uploading twice. The new
spec proves both paths. The harness grows retry, state, and text hooks.

## Verification run

* `npm ci` in `web/`, then `./tools/check.sh` from the worktree root.
  Result: `OK all checks passed`.
* `controller-complete.spec.ts` (own pins): 2 passed.
* `cap.spec.ts` at the handed hash: 2 failed, both at `waitForURL(/processing/)`.
* `mock-session.spec.ts` (`leaving the page`) at the handed hash: 1 failed,
  also at `waitForURL(/processing/)`. Session close counts still assert
  green just above each failure, so the gate sits on navigation alone.
* Probe (uncommitted, reverted): a same-flow completion stub added to the
  three failing tests turned all three green. Revert confirmed clean.
* Probe (scratch spec, deleted after the run): after a failed automatic
  post the status names a retry, but no visible retry button exists.
  The button assertion failed. Tree confirmed clean after deletion.
* `dev-diary/tools/audit_docs.py`: `OK planning tree is consistent`.
* Plan-reference scan of the diff with the gate pattern: zero matches.
* No Svelte 4 idioms in new code. Failure text lands in the existing
  `role=status` region. No new template surface ships here.

## The ten questions

1. No path spends without a budget reservation. The diff posts only the
   free completion route. It opens no paid call.
2. No path leaves a session open. Voice end, HTTP end, and guard close
   all run before the completion post. A refusal keeps data only.
3. No private material reaches anyone new. The post carries the take's
   own episode and stem ids. Ownership checks are untouched.
4. No edit applies without a revertible record. Nothing here edits an
   episode. The change gates navigation only.
5. No spoken callback or count comes from the prompt. No spoken content
   was added.
6. No code, comment, test, or migration cites the plan. The gate pattern
   over the diff matches nothing, and the gate step passes.
7. Nothing rebuilds Keel or Chaaya. The change reuses the T7.11 driver
   plus the Chaaya uploader, guard, and audio helpers.
8. Yes. This review ran on the handed hash in its own detached worktree.
9. No paid job kind can resume and spend twice. No job kinds were touched.
10. No check needs a person, a microphone, or a wall-clock threshold.
    The proofs use the mock flag, generated input, and the harness clock.

## Ruling 1: the three failing sibling specs

Fix here, not a follow-up. This commit changed the navigation contract
those specs pin, from unconditional navigation to post-then-navigate.
Against a stub-less preview every completion answers 404, so the take
never navigates and all three time out. The fix is a stub-only edit in
the same flow this task already uses, about fifteen lines per file. My
probe proved it sufficient: all three turned green, then I reverted.
Landing known-red proofs with no owner is not acceptable. The default
gate does not run these suites, so the gate stays green while the
cap-expiry and pagehide proofs lie. Widen `owns` by decision to
`web/src/routes/record/cap.spec.ts` and `mock-session.spec.ts` for the
stub installs only. Severity M (below). This is finding F1.

## Ruling 2: the template retry-control gap

Real defect, severity H, fixed here. This is finding F2. A failed
automatic post writes refusal text that names a retry, but production
offers no control that performs it. The template `#stems-retry` button
drives the page-level driver, whose `run` only the mock-only handle
calls. The controller retry lives only on the mock window handle. The
take then sits on the ending-phase page with no in-page recovery. The
stems stay stored, so this is not a C. There is no workaround in the
page, so this is not an M. The done criteria demand a working retry,
and a console-only retry is not one. Widen `owns` by decision to the
record template for one retry section wired to controller state.

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | M | `web/src/routes/record/cap.spec.ts:73,102` and `mock-session.spec.ts:272` at the reviewed hash | Three landed proofs pin unconditional navigation to processing. This commit gates navigation on the completion post, so all three time out against a stub-less preview where every completion answers 404. | `cap.spec.ts` (2 cases) and `mock-session.spec.ts` (`leaving the page`) fail at the handed hash with `waitForURL(/processing/)` timeouts. Ran in this worktree. Session close counts assert green directly above each failure. | Restore the unconditional `window.location.assign` in `endTake`, dropping the `if (!moved) return` gate. All three pass again. The diff is the only behavior delta, so the gate reintroduces the red. |
| F2 | H | `web/src/lib/voice/record-state.ts:349-357` with `web/src/routes/record/+page.svelte:141-159` at the reviewed hash | A failed automatic post promises a retry in words but ships no control that retries the controller path. `#stems-retry` drives the page driver, which never ran outside the mock handle. `retryCompletion` is reachable only through the mock window handle. The user dead-ends on the ending-phase page. | Scratch probe (deleted after the run): failed stubbed post, then status contains `retry` (passes), then `getByRole('button', { name: /retry/i })` visible (fails after 5 seconds). Ran in this worktree. | Wire one template retry control to `retryCompletion` through controller state. The pin passes. Remove the wiring and the pin fails again. |

## Dismissed observations (no findings)

* Concurrent `retryCompletion` calls can assign the same processing URL
  twice. The driver latch permits only one post, and the URL is
  identical. No observable harm follows, so this is not recorded.
* `record-state.ts` imports the driver from the record route module.
  The task spec mandates reusing that driver, no cycle results, and the
  gate passes. Structure alone is not a defect.

## Verdict and counts

Verdict: REMEDIATE.

Counts: C 0, H 1, M 1, L 0. Out-of-scope findings: 0. False positives: 0.

Both findings need remediation on this commit. F1 needs the stub installs
in the two sibling spec files. F2 needs a visible retry control for the
controller path. Re-review must rerun the three sibling specs with stubs,
the own specs, the new retry-button pin, and the full gate.
