# T7.34 remediation, round 1

- **Reviewed hash:** `13248a4fc1c24d6179d15e270bfc38b12ad7407a`
- **New hash:** `ef473d81e3fa16236c5622981e27bed482dcc710`
- **Worktree:** `/Users/narayan/reprise-wt/t7.34-rem-r1`, on `task/t7.34-rem-r1`, built on the reviewed
  commit with no rebase and no amend

## Findings fixed

| Severity | Where | What | Fix | Pin |
|---|---|---|---|---|
| L | `web/src/lib/editor/draft.ts:255, 292, 331` (creation sites) and `:509` (read) | `this.cutProposals[cutId] ?? ''` read the inherited member for `constructor`, `toString`, `hasOwnProperty` and `__proto__` instead of `''`, and writing `__proto__` was silently dropped. The Map it replaced returned `''` for every unknown key. | Built `cutProposals` with `Object.create(null)` at all three sites that create the record, instead of `{}`. A null-prototype record has no inherited members, so every read and write acts on its own keys only. The read at line 509 needed no change once the record itself carries no prototype. | `web/src/lib/editor/draft.prototype-keys.test.ts`, moved from the reviewer's pin at `dev-diary/probes/t7.34-rev-r1/draft.prototype.test.ts` and renamed with no task id |

## Pin verification

- With the fix in place: `npx vitest run src/lib/editor/draft.prototype-keys.test.ts` passed 5 of 5.
- With the reviewer's mutation applied (`Object.create(null)` reverted to `{}` at all three creation
  sites): the same run failed 4 of 5, on `constructor`, `__proto__`, `hasOwnProperty` and
  `toString`, matching the round 1 finding. The `cut-z` case still passed, since an ordinary unbacked
  id was never affected.
- The mutation was then reverted, and the pin passed 5 of 5 again before the commit.

## Gate

`npm ci` in `web/` exited 0. Every step of `tools/check.sh` after its ffmpeg check ran verbatim, in
bash, in order, from the worktree root. The ffmpeg 9.0.2 against 9.0.1 mismatch is accepted by the
owner and was not run.

| Step | Exit |
|---|---|
| `gofmt -l cmd` | 0, empty output |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0, 22 packages ok |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0, 322 files, 0 errors, 0 warnings |
| `npm run lint` | 0 |
| `npm run test` | 0, 224 of 224 (219 plus the 5 moved pin cases) |
| `npm run build` | 0 |
| `npm run test:e2e` | 0, 4 passed |
| svelte 4 leakage `git grep` | empty (raw grep exit 1, no match) |
| consumer names `git grep` | empty (raw grep exit 1, no match) |
| plan references `git grep` | empty (raw grep exit 1, no match) |
| `go test -race ./...` | 0 |

The whole scan set is empty. `git status --short` was clean after the commit, aside from this file,
which stays outside the commit as instructed.

## Style

The commit carries no semicolons, no em dashes, and no task id. Its longest sentence runs 26 words.
