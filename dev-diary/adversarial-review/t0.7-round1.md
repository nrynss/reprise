# T0.7 round 1 review

Reviewed commit `4d2c372a3c12a0dd702e1be93b2e58729af70a70`.
Worktree `/home/nryn/work/reprise-wt/t0.7-rev-r1` held that hash detached.
The review confirmed the hash with `git log -1` before reading anything.
This review judges that commit only, not later main.
Round 1 is the first round, so no prior residue exists.

## Commit file list

The commit touches four files.
`.github/workflows/ci.yml`, `Dockerfile` and `tools/check.sh` sit inside T0.7 owns.
`deploy/README.md` sits outside owns and Section 4 judges it.

## 1 Owned paths

The workflow and the Dockerfile name one digest between them.
The digest is `mwader/static-ffmpeg:9.0.1@sha256:54e55b0cb8f672870fc38ceb2e6c411855cb3b39c505f5f3b2505ee01ed5f2b7`.
It appears in the workflow at line 27 and in the Dockerfile at line 32.
The workflow copies the pair out of the image, not out of the dead archive.
The mechanism runs `docker create` on the pinned image.
It then runs `docker cp` for `/ffmpeg` and `/ffprobe` into `/usr/local/bin`.
It then runs `docker rm` on the container and prints `ffmpeg -version`.
`check.sh` refuses a non-pinned ffmpeg with a message naming the pin.
The refusal call reads `fail "tools" "$tool is $have, not the pinned $FFMPEG_PIN"`.
That call sits in the version loop at lines 43 through 51.
The local machine carries real ffmpeg `n9.0.1` and real ffprobe `n9.0.1`.
The gate block passes on both tools here.
A fake ffmpeg reporting `7.1` fails the same block.
It prints `FAIL [tools] ffmpeg is 7.1, not the pinned 9.0.1`.

## 2 Done-when

The workflow installs the pinned 9.0.1 image, then runs `ffmpeg -version`.
The image evidence is on file.
The Dockerfile opens a `mediatools` stage `FROM` the pinned digest.
It copies `/ffmpeg` and `/ffprobe` from that stage into `/usr/local/bin`.
No image build ran here, per the brief.
`check.sh` sets `FFMPEG_PIN=9.0.1` and refuses any other build by name.
Strictly, `check.sh` names the version, not the digest.
Its comment points at the digest the image and the workflow share.
That matches the task text, which demands the refusal in `check.sh`.
The task text demands the digest only in the first two places.
The done-when holds on file evidence plus the local gate run.

## 3 Triple-green evidence

The gate-change rule is general, not T0.1 specific.
AGENTS.md demands three fresh worktree passes with exit codes in the review file.
That rule covers this pin change because this pin change edits the gate.
No triple-green evidence exists anywhere.
The commit message claims a local pass and a mutation refusal without logs.
The repository has no remote, so CI never ran either way.
This review ran the gate version block once, not the full gate three times.
The finding stands as an evidence gap.
It stands as an L, not an H.
The pin is right and the mechanism is right.
Nothing is broken, so High would inflate a correct change.
A missing log on verified-correct ground is process debt.

## 4 README hunk

The `deploy/README.md` hunk removes duplication rather than adding owned content.
It deletes the repeated `7.1` version and the repeated author claim.
It points at the version and digest the Dockerfile holds.
The words are correct and cannot drift from the Dockerfile now.
The close review already calls the words correct.
This review recommends one of two resolutions.
Widen the T0.7 owns line with `deploy/README.md` as a documented exception.
Or let the hunk stand as process, recorded here.
Either way the hunk needs no rework.

## Ten questions

1. Does any path spend money without reserving budget first. No. The commit moves binaries and checks versions only.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. The commit touches no session code.
3. Can a private episode, stem or transcript reach anyone but its owner. No. The commit touches no episode paths.
4. Does any edit apply without a revertible record. No. Every hunk lands as one revertible commit.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. The commit holds no prompts or counts.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. The diff scan found no plan references.
7. Did this task rebuild something Keel or Chaaya provides. No. It adopts the exact digest Keel measures with.
8. Was this review run on the handed hash, in a worktree of its own. Yes. The worktree stayed detached at the handed hash.
9. Can any paid job kind resume after a restart and spend twice. No. The commit defines no job kinds.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Every check reads fixed versions or exact text.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L, non-blocking | Gate change at `4d2c372`, `tools/check.sh` lines 43 through 51 | The gate change ships without triple-green evidence. No round file, message log, or CI history records three fresh passes | The version block passes on real 9.0.1 here and refuses fake 7.1 by name, while `git remote -v` prints nothing and no `t0.7-round` file predates this review | Drop the version loop and keep the presence loop. The gate then passes on fake 7.1, so one green run hides the gap |
| L, non-blocking | `deploy/README.md` lines 203 through 212 at `4d2c372` | The commit edits a path outside T0.7 owns. The path belongs to done T6.1 ground | `git show --name-only 4d2c372` lists `deploy/README.md` while owns names only the workflow, the Dockerfile, and `check.sh` | Restore the `7.1` version sentence in the notes. The text then repeats a version the Dockerfile can outgrow |

## Verdict

APPROVE.
The pin itself is correct in all three places.
The copy mechanism matches the render library.
The refusal names the pin and fires on the wrong build.
Both findings above are evidence-gap and process L items.
Neither blocks use of this change.
The phase close can carry the triple-green debt and the owns exception.
This review committed nothing.
The worktree tree reads clean at the end.
