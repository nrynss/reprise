# T3.2 remediation round 1

Reviewed hash: `1f778d3c2183758da0ef105171e7547bfca4df16`.
Worktree: `/home/nryn/work/reprise-wt/t3.2-rem-r1`, branch `task/t3.2-rem-r1`.
New head: `abfeb08dd4c21ddbef4d6fd40a71a04572c1855a`.
Gate: `./tools/check.sh` passes in this worktree after `npm ci` in `web/`, with `go test -race` green and `OK all checks passed`.
Keel `v0.3.0` confirms the choice. `Settle` books the price a paid call cost and frees its hold. `Release` frees a hold the caller never spent.

| Finding | Fix | Pin | Mutation | Commit |
|---|---|---|---|---|
| F1 (M): receipt failure and store-after-spent-call paths released instead of settling, under-booking one estimate | Both post-spend error paths in `internal/editorial/run.go` now call `Settle(estimate, estimate)` before returning the error, and mark the hold settled. A failed settle joins both errors so neither is lost. The pre-call refusal and the model-failure fallback still release, since no call was spent there. | Committed `TestRunSettlesSpentCallOnStoreFailure` drops the `decisions` table so `store` fails after one billed call, then asserts one reserve, one settle, and no release. `TestRunKeepsProviderCopyOnReceiptFailure` now asserts one settle and no release. | Removing the `Settle` on the store path fails the new test with `settled:[] released:[500000]`. Restoring it turns the suite green. | `77777709c6cb830b97f34970920c3395a890eac1` |
| F2 (L): `Usage` comment claimed usage prices the settle, while the run settles estimate against estimate | Corrected the comment on `EditorialAnswer` in `internal/gemini/schema.go` to state the run settles the audio minute estimate and no settle reads `Usage` today. Usage pricing stays unwired. | The review probe showed billion-token usage still settles `Estimate(12)`, which now agrees with the text. A grep for `.Usage` over non-test `internal/editorial` sources still returns nothing. | Restoring the old sentence reopens the same contradiction the probe exhibits. | `c696e9f61c68f347cdff8cd2a70eadf1c831d22c` |
| F3 (L): reserved and settled amounts unpinned, so zeroing the estimate kept the suite green | `TestRunStoresDraft` now asserts `reserved[0]` equals `Estimate(12)` and the settled pair equals `{estimate, estimate}`. | The committed assertion is the pin. It runs in the existing draft test. | Replacing the estimate with `cost.Price(0)` now fails the test with `reserved = $0.00, want $0.0005`. | `abfeb08dd4c21ddbef4d6fd40a71a04572c1855a` |

Scope notes from the review were respected. Offsets, the placeholder minute rate, the deferred live cold-open agreement, and the `genai` boundary were left untouched.
No verdict was changed. Nothing outside the three findings was fixed.
