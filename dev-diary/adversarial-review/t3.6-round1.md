# T3.6 round 1 review

Reviewed hash: `69d268d7fd93cd41ec3340e9bd627b4caa4557de`
Worktree: `/home/nryn/work/reprise-wt/t3.6-rev-r1` (detached HEAD, confirmed `git log -1 --format=%H` equals the handed hash)
Base: `1ef2a7a` (`git diff 1ef2a7a..69d268d` touches `internal/cover/` only, 8 files, all inside the declared `owns`)
Verdict: REMEDIATE
Counts: C 0, H 0, M 0, L 1

The reviewer never committed, never edited tracked code, and ran every pin below in its own worktree. `go test -race -count=1 ./internal/cover/...` passes. `go vet ./internal/cover/...` is clean. `gofmt -l internal/cover/` reports nothing. A temporary reviewer probe (since removed, tree verified clean with `git status --short`) confirmed rerun behavior directly.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L | `internal/cover/cover.go:232-267` (`storeImage`) | The handoff claims a rerun overwrites the same file and replaces the row, and the code does (DELETE then INSERT under `UNIQUE(episode_id)`), but no committed test runs `Run` twice on one episode. Dropping the DELETE breaks reruns with a UNIQUE violation and nothing committed catches it. | Reviewer probe `TestReviewerProbeRerunOverwrites` (run, passed, then removed): seed one episode, `Run` twice with a fresh `Dir`, assert the second `Run` returns no error, both results share one SHA256, `SELECT COUNT(*) FROM covers` is 1, and the model heard 2 calls. | Delete the `DELETE FROM covers WHERE episode_id = ?` statement in `storeImage`: the probe's second `Run` fails on the `UNIQUE(episode_id)` constraint. |

## What was verified and holds

- Paid seam: `Run` reserves before the model call and returns `cost.ErrOverBudget` with zero model calls and zero cover rows on refusal (`TestRunRefusesWithoutBudget`). Every post-spend error path (receipt failure, rejected art, store failure, success) settles exactly once; the unspent model-failure path releases with no settle (`TestRunFallsBackOnModelFailure`, `TestRunFallsBackOnRejectedArt`, `TestRunKeepsProviderCopyOnReceiptFailure`). No retry loop exists anywhere on these paths.
- Kind: `Limit 1`, zero-value non-idempotent, nil `Resume`, `MaxAttempts 0` which Keel `v0.3.0` documents as one attempt (`TestKindNeverResumes`, confirmed against `go doc github.com/nrynss/keel/job.Kind`).
- Fallback: same number draws identical bytes, different numbers differ, always 1024 square and `Validate`-clean (`TestFallbackDeterministic`). Forced model failure and all three rejected-art classes store the marked fallback (`TestRunFallsBackOnModelFailure`, `TestRunFallsBackOnRejectedArt`).
- Validation: scripted panel accepted; rectangle rejected with `ErrNotSquare`; flat panel with `ErrBlank`; garbage with `ErrFormat` (`TestValidateRejectsBadPanels`).
- Prompt: names the stored title and notes, refuses faces, people, animals, and all lettering, and states the app sets the title (`TestPromptConstrainsTheRender`, `internal/cover/prompt.go:12-25`).
- Brief contract: `loadBrief` reads `reason` for kinds `title` and `show_notes` from the diary `proposals` table, matching the constants and storage shape T3.2 landed (`internal/editorial/store.go:20-23,95-99`). Missing rows read empty rather than failing the run.
- Seam hygiene: no `genai` import anywhere in `internal/cover/`; the live binding stays behind the `Model` interface for `internal/gemini` to adopt, as the stack rule requires. `ScriptedModel` records calls, model id, and request under a mutex and answers deterministically offline.
- Schema: `covers` is created with guarded `CREATE TABLE/INDEX IF NOT EXISTS` beside the diary tables with no migration; the `REFERENCES users (id)` and `REFERENCES episodes (id)` targets exist in `internal/store/migrations/0001_schema.sql`. Files land as `<episode-id>.png` with mode `0600` and a SHA256 row.
- Style: every exported identifier carries a doc comment starting with its name; single sentinel per condition wrapped with `%w`; `ctx` first; no `**T`, `interface{}`, naked returns, `panic`, or goroutines. `git grep` for plan ids, phase ids, planning filenames, and section marks over `internal/cover/` returns nothing.
- Fixture coverage: `TestEveryEpisodeGetsSquareCover` seeds three episodes through the real diary migrations with T3.2-shaped proposal rows and draws one square cover each. Synthetic seeding is adequate here because the pass reads only `episodes` plus `title`/`show_notes` proposals and never touches stems.

## The 10 questions

1. Does any path spend money without reserving budget first? No. `Reserve` precedes `GenerateImage` on every path, and refusal returns before any call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This task touches no AssemblyAI surface.
3. Can a private episode, stem or transcript reach anyone but its owner? No new exposure. Cover bytes are written `0600`, rows carry `owner_id`, and serving/export wiring is outside this task's `owns`.
4. Does any edit apply without a revertible record? No. The only overwrite is cover regeneration onto its own episode-named file, disclosed in the handoff, with no editorial state touched.
5. Does any spoken callback or count come from the prompt instead of a stored row? Not applicable. The brief reads stored `title` and `show_notes` proposal rows.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No, verified by `git grep` over `internal/cover/`.
7. Did this task rebuild something Keel or Chaaya provides? No. It consumes `keel/cost`, `keel/job`, `keel/id`, and `keel/sqlite` plus the diary store.
8. Was this review run on the handed hash, in a worktree of its own? Yes, stated and confirmed above.
9. Can any paid job kind resume after a restart and spend twice? No. The kind is non-idempotent with no resume path and one attempt, and `Run` makes exactly one model call per invocation.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Every test runs offline through the scripted double with exact assertions.

## Verdict

REMEDIATE. One L finding: commit a rerun test pinning the claimed overwrite safety (the reviewer probe above is a ready template). No other residue. Everything else in the task pins its claim with a load-bearing test.
