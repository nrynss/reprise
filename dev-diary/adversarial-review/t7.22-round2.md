# T7.22 round 2 review

Reviewed hash: `1ba4c759ea532d3be7f75bc0c82ab802f6529754`
Worktree: `/Users/narayan/reprise-wt/t7.22-rev-r2` (detached HEAD)
Parent: `a4567352b28bf769c2364d93e8cf1b59012eb76c` (round 1 hash)
Base: `f55733629126d3d5f8772f4457f6352759711449`

`git log -1` prints that hash. Its parent is the round 1 hash. `git status --short` was empty at the start and empty after every temporary mutation was restored. No code was committed.

```
commit 1ba4c759ea532d3be7f75bc0c82ab802f6529754
Author: nrynss <ID+nrynss@users.noreply.github.com>
Date:   Tue Sep 22 19:01:10 2026 +0800

    Record the provider id a connected socket learned
```

## What the remediation changes

The socket keeps the first non-empty provider id. `session.ready` supplies `session_id`. `session.updated` supplies `config.id`. A later empty frame does not clear it. A later different id does not replace it. `closeSession` posts that id as JSON. The end control passes the id the socket learned. The retry posts the same id. The store returns the id the row holds after the call. An empty incoming id does not replace a stored one. The handler echoes the stored id, not the empty body. A blank body still decodes as no id. Malformed JSON still answers 400. An empty close before any id stays a skip. `SettleInput` still returns `ErrNoClose` for that row.

`SessionGuard` in Chaaya `0.2.0` accepts only a URL. Its close calls `sendBeacon` with that URL and no body. If the beacon is missing, it sends a POST with no body. The guard was left unchanged. An empty beacon after a stored id does not clear the column. A take that ends only through that beacon, before any shaped post, still stores no id. That limit is the guard's API. It is acceptable because the beacon cannot wipe an id the end control already stored.

## Ten questions

1. Does any path spend money without reserving budget first? No. This commit adds no paid call. The mint still reserves before the token request. A connected close starts the existing reconcile against that hold. An empty close with no stored id does not start one.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The browser still sends `session.end` on the socket before the diary post. A live page hide still ends the socket. Mints still carry the token cap.
3. Can a private episode, stem or transcript reach anyone but its owner? No. A foreign empty end answers 404. The handler still requires a guest before it reads a row. Success bodies still set `no-store`.
4. Does any edit apply without a revertible record? No. No decision or render path changes.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No prompt or spoken copy changes.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. A search of the nine changed files found none of those marks.
7. Did this task rebuild something Keel or Chaaya provides? No. The page still uses `SessionGuard` for the unload notice. The diary post carries the id because that guard cannot.
8. Was this review run on the handed hash, in a worktree of its own? Yes. The hash above is `git log -1` in this worktree. Every pin ran there. No code was committed.
9. Can any paid job kind resume after a restart and spend twice? No. No job kind changes. The reconciler claim still settles money once. A repeat end can start another job. That claim keeps the money path to one settle. An empty beacon after a stored id is now a repeat end of that same kind.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The new checks use recorded frames and fixture rows. None of them wait on a clock, a person, or a microphone.

## Residue against round 1

Round 1 counts were C 0, H 1, M 1, L 0.

C residue is zero. Round 1 had no C, and this commit adds none.

H residue is zero. A connected end posts the learned provider id. The row keeps it. `SettleInput` does not return `ErrNoClose` for that row. An empty close before any id still returns `ErrNoClose`.

M residue is zero. An empty post after a stored id leaves the column and the echo unchanged. A blank id field does too. A refused body does too.

L residue is zero. Round 1 had no L, and this commit adds none.

## Pins

These ran in this worktree on the handed hash.

`go test -race -count=1 -timeout 180s -run 'TestSessionEnd|TestRecordSessionEndStoresProviderClose' ./internal/api/ ./internal/episode/` passed.
`go test -race -count=1 -timeout 180s -run 'TestSettleInputNeedsProviderClose|TestListOpenSkipsUnclosedRows' ./internal/broker/` passed.
`go test -race -count=1 -timeout 120s -run TestEpisodeDetailReadsBackDecisions ./internal/api/` passed. An empty decisions post is still 400.
`npx vitest run src/lib/voice/socket.test.ts src/lib/voice/session-calls.test.ts` passed, 32 tests.
`gofmt -l` on the four Go files was empty. `go vet` on `internal/api`, `internal/episode`, and `internal/broker` was clean. `staticcheck` v0.8.1 on those three packages was clean. Eslint on the five edited voice files was clean.

`./tools/check.sh` exits 1 with `FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`. That stop is the environment. This commit does not touch the pin. The gate does not reach later checks on this machine.

## Mutations

Each mutation was restored before the next one. The tree was clean after the last restore. The pins above were run again on that restored tree and passed.

The blank read still returns a zero close at `internal/api/session_end.go:72`. That return is the round 1 mutation, and it is still the code. It no longer wipes a stored id, because the store does not write an empty id. A connected close is no longer that blank read. Re-applying the return changes nothing, and the keep tests still pass.

Writing an empty id again, by dropping the empty guard in `internal/episode/sessions.go`, failed three pins.

- `TestSessionEndEmptyCloseKeepsStoredProvider` failed with `empty echo = "", want prov-9`.
- `TestConnectedCloseStoresProviderForSettle` failed with `empty close stored "" echo "", want "sess_19abadc210e7411baacee7c23c6e7243"`.
- `TestRecordSessionEndStoresProviderClose` failed with `empty end = ep-1 , want ep-1 prov-9`.

Clearing the posted id in the handler before the store call failed the connected settle pin. `TestConnectedCloseStoresProviderForSettle` failed with `connected close stored "" echo "", want "sess_19abadc210e7411baacee7c23c6e7243"`. `TestSessionEndRecordsProviderClose` failed on the same empty echo. The early half of the settle test still requires `ErrNoClose` when the column is empty, and that half passed. An empty column is still not a close `SettleInput` can use.

Dropping the remembered id in `web/src/lib/voice/socket.ts` failed the three socket pins. The ready frame, the updated frame, and the close post all saw an empty id. The recorded id they require is `sess_19abadc210e7411baacee7c23c6e7243`.

Posting a zero provider id from `closeSession` failed the close pins. The body was `{"provider_session_id":""}`. The session-call pins require `prov-9` on the first post and on the retry. The socket pin requires the learned id from the recorded frames.

## Counts

C 0, H 0, M 0, L 0. No in-scope findings. No out of scope rows.

## Verdict

APPROVE
