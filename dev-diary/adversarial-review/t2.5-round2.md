# T2.5 round 2 review (re-review)

Reviewed hash: `d3c00e7fa4418c03b25cc41c866f436cbbca1f6e`
Worktree: `/home/nryn/work/reprise-wt/t2.5-rev-r2` (detached HEAD, confirmed
`git log -1 --format=%H` equals the handed hash). No commits written. Working
tree reviewed only through `git diff bca8f89..d3c00e7` (five commits on the
reviewed `bca8f89`, nothing rebased or amended). Reviewer probe file was
created, run, and removed afterwards (`git status --short` clean).

`go test -race -count=1 ./internal/align/...` passes in the worktree.
`go vet` clean. `gofmt -l` clean. `git grep` over `internal/align/` finds no
plan references. Every pin below was executed by the reviewer, including the
mutations.

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. No new findings. Residue against round 1 is zero
in every severity, stated per severity below.

## Residue vs round 1

* M (silent-layout skip, `fixture_test.go:25-31` at `bca8f89`): ZERO.
  `measureSession` returns an error for every missing or unreadable wanted
  file, and `checkFixtureSession` fails on it. Only the absent fixture root
  still skips. `TestDriverFailsLoudOnWrongLayout` pins the rename mutation
  (suite PASS, re-run here). `TestDriverMeasuresSyntheticSessions` proves the
  driver measures through real files with zero drift aligned and exactly
  -400 samples drifted. `TestFixtureSessions` pins exactly three measured
  sessions once fixtures land.
* L (exact-40 boundary unpinned): ZERO. `TestDriftLimitBoundary` now asserts
  exact 40 ms passes with no warning. Reviewer mutation `<=` to `<` fails
  this case (`align_test.go:226`) and passes on restore, so the pin is
  load-bearing.
* L (resample doc claimed universal endpoint preservation): ZERO. The doc now
  states exact-ratio doubling keeps both endpoints while other ratios
  truncate the tail by integer division. Reviewer probe confirms
  `ResampleLinear([]float64{0, 1, 2}, 3, 2)` returns `[0 1.5]`, so the
  narrowed operative claim is true. Existing dyadic pins still guard the
  `frac` term.
* L (implicit transcoding step): ZERO. The file header states fixtures arrive
  as pre-transcoded 16 bit PCM WAV while the server stores OGG, and
  `TestDriverRejectsStoredBytes` pins rejection from both sides (direct
  decode plus full driver run, suite PASS, re-run here).
* C: ZERO. H: ZERO. Round 1 recorded none and this round found none.

## Scope note: fifth commit

The top commit `d3c00e7` ("Convert the clock shape instead of rebuilding
it") replaces a four-line struct literal with `sessionOffsets(clock)` in
test-only `writeDriverSession`. Both shapes carry identical fields in
identical order, and the conversion marshals under `sessionOffsets` tags, so
the JSON is byte-identical and no behavior changes. It sits inside `owns`,
touches no production path, and introduces no defect. Not a finding.

## New-defect hunt (no findings)

* Partial layouts fail loudly. Reviewer probe: corrupt `offsets.json`
  bytes error through the wrapped unmarshal path, and corrupt stem bytes
  error through decode. Both return errors from `measureSession`, which
  `checkFixtureSession` turns into failures. No skip path remains for an
  existing directory.
* Ran-count counts measured sessions. `TestFixtureSessions` increments per
  directory and fails unless the count is exactly three, while each counted
  directory runs the loud `checkFixtureSession`. An empty present root fails
  (`0 != 3`) instead of skipping.
* Synthetic driver exercises the full file path. `writeDriverSession` writes
  real WAV bytes plus `offsets.json`, and `measureSession` decodes, parses,
  resamples, correlates, and aligns. The drifted subtest asserts `!Pass`,
  exact sample drift (`-400`), and a non-empty warning, so the verdict logic
  round 1 asked for is covered even though the test calls `measureSession`
  rather than the fataling wrapper (which a must-fail case cannot call).
* Boundary pin is exact. The 40 ms case passes `Seconds: 0.04` as a literal,
  so no float dust enters the comparison, and `Warning()` is asserted empty.
* Doc narrowing is accurate where it acts. The dyadic example keeps
  endpoints (verified in code: final position lands exactly on the last
  sample) and the truncation warning is hedged with "may", matching the
  measured `[0 1.5]` tail. The divisibility phrasing is loose under a strict
  reading but states no caller-facing falsehood, so it is an observation,
  not a finding.

## What the reviewer verified and keeps

* Round 1's kept properties still hold: chirp correlation sign and lags,
  dyadic resample exactness, 30 ms pass with 50 ms fail, WAV round trip and
  rejects, neighbour shape alignment.
* The owed rerun against the three real T0.5 fixtures is stated in the
  remediation file and in the `TestFixtureSessions` comment. It is a
  legitimate remeasure, not residue: the driver path it will run is already
  proved by the synthetic layouts.

## The ten questions

1. Does any path spend money without reserving budget first? No. Pure
   functions only. No provider calls, no budget seam touched.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. No session handling in this diff.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   No serving, no URLs, no auth paths.
4. Does any edit apply without a revertible record? No. No edits apply.
   `Report` stays a read-only measurement with a display warning.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. No prompts, no counts. Offsets come from `offsets.json` and
   correlation lags.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. `git grep` for task ids, phase ids, planning file
   names, and section marks over `internal/align/` is empty.
7. Did this task rebuild something Keel or Chaaya provides? No. Correlation,
   resampling, and the episode clock remain product-specific measurement.
   The minimal WAV codec serves the fixture proof only.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Detached HEAD at `d3c00e7` in `/home/nryn/work/reprise-wt/t2.5-rev-r2`,
   diff read as `git diff bca8f89..d3c00e7`, no commits written, probe file
   removed afterwards (`git status --short` clean).
9. Can any paid job kind resume after a restart and spend twice? No job kind
   here. Nothing durable, nothing retried.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. All pins assert exact sample math on generated chirps.
    No device, no thresholds.

## Verdict: APPROVE

Zero findings across all severities, with zero residue against round 1
stated severity by severity above. The rerun against the three real T0.5
fixtures stays owed after they land.
