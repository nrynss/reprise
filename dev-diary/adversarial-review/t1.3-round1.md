# T1.3 round 1 review

Reviewed hash: `6a383d0d6545faf12025e5726a1027c103a5800e`.
Worktree: `/home/nryn/work/reprise-wt/t1.3-rev-r1`, detached HEAD.
`git log -1 --format=%H` equals the handed hash. The review ran on the handed commit only.
Diff base: `d1428a9..6a383d0d6545faf12025e5726a1027c103a5800e`. Six new files, no edits to old files.

Verdict: APPROVE.
Counts: C 0, H 0, M 0, L 0. False positives recorded: 1. OUT_OF_SCOPE findings: 0.

## Findings

None. No row follows because no defect survived measurement.

## Owns scope verdict (the one false positive)

The handoff notes four companions beside the two owned paths. These are
`internal/api/routes_test.go`, `web/src/lib/api/types.test.ts`, and the two
golden files under `web/src/lib/api/testdata`.

The done criteria demand the golden decode test. A test has to live beside
the code it pins. The companions are therefore required by the task text,
not a reach outside it.

I record this as a false positive, not a finding. No observable defect
exists. No pin can measure one. The mutation for such a claim would be
deleting the tests, which only thins the pins the done criteria demand.

Recommendation for the orchestrator: widen the `owns` line to cover the
test and golden paths before landing, so the record matches the change.

## Evidence (pins I ran myself)

All pins ran in the review worktree above, against the handed hash.

- `go vet ./internal/api/...` passes with no output.
- `go test -race -count=1 -v ./internal/api/...` passes all five tests.
- `npx vitest run src/lib/api/types.test.ts` passes all three tests.
- `npx tsc --noEmit` passes with no output.
- `./tools/check.sh` passes end to end. The final line reads OK.
- Golden decode: `routes.json` holds 13 entries matching the spec table
  order. `stub-refusal.json` carries code `not_implemented` with a message.
- The browser test parses the refusal through Chaaya `parseErrorEnvelope`
  and asserts `ok` plus the code. I watched it pass.
- Load bearing probe: I renamed one route to `/api/threadz`, reran the Go
  tests, and watched them fail. I reverted with `git checkout` and confirmed
  `git status` clean and the hash unchanged.
- `gofmt -l internal/api/` reports nothing.
- `go.mod` pins Keel `v0.3.0`. `web/package.json` pins Chaaya `0.2.0`.
- Grep over the diff finds no task ids, no phase ids, no planning file
  names, and no section marks.
- `routes.go` imports only `net/http` and `keel/wire`. `types.ts` imports
  only the `ErrorEnvelope` type from Chaaya wire. No envelope code of its own
  exists on either side. The test-only `errorShape` struct reads refusals and
  writes none.

## The ten questions

1. Does any path spend money without reserving budget first. No. Every
   handler is a stub. No paid call exists.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap. No. No session code exists in this change.
3. Can a private episode, stem or transcript reach anyone but its owner. No.
   Every route refuses with 501. No content is served.
4. Does any edit apply without a revertible record. No. No edit path exists.
   The route table is static data.
5. Does any spoken callback or count come from the prompt instead of a stored
   row. No. No callbacks or counts exist in this change.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file. No. Grep finds none, and the gate plan reference scan passes.
7. Did this task rebuild something Keel or Chaaya provides. No. Errors flow
   through `wire.WriteError`. Parsing flows through Chaaya `parseErrorEnvelope`.
8. Was this review run on the handed hash, in a worktree of its own. Yes.
   Confirmed at the top of this file.
9. Can any paid job kind resume after a restart and spend twice. No. No job
   code exists in this change.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold. No. The Go tests use `httptest`. The browser test decodes
    golden files. Nothing listens or waits.

## Verdict

APPROVE with zero findings across all severities. Zero residue claim: this
is round 1, so no prior round exists, and this round leaves nothing open.
C residue is zero. H residue is zero. M residue is zero. L residue is zero.
