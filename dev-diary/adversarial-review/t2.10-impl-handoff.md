# T2.10 implementation handoff

Worktree `/home/nryn/work/reprise-wt/t2.10-impl`, branch `task/t2.10`,
base `15fc489`. Final hash `483e6c7`. No live call ran. Spend stays
at zero.

## What it built

`internal/broker/timeouts.go` (new): five named constants with reason
comments. Assumed reply latency 60 seconds, minimum reply wait 90
seconds, reconcile and sweep attempts 3 each, over cap margin 60
seconds. `internal/broker/timeouts_test.go` (new): value only floor
assertions, no timing. `dev-diary/probes/latency.md` (new record, beside
`voice-agent.md`): every bound checked with verdicts, plus four
follow-up rows.

No call site changed. Call sites sit outside this task's owns, so every
swap waits for its owning path. Fast path behavior is unchanged.

## Bounds table

Twenty bounds checked across mint, token expiry interplay, socket reply
waits, reconcile reads, sweep settle and delete, job attempts, resume,
cap timer, drain, batch poll, lease, gate, and cookie lifetime. The full
table lives in `latency.md`. No bound below 60 seconds survives on the
reply path. The socket waits unbounded on `reply.done` and on
`session.ended`, the job runner sets no attempt deadline, and every HTTP
timeout guards control plane calls that slow replies never slow.

## Surprises

The Chaaya `api` client sets no default deadline. The record page mint
POST therefore stays open through a slow mint with no code to thank.

The probe runner's 25 second greeting abort was tooling only. The record
says so explicitly, so nobody copies it into the product.

## Contract changes

None. No interface changed. No signature changed.

## Follow-up rows

Four rows live in `latency.md`, each with severity, pin, and owning
path. The heaviest is severity C: nothing wires the reconciler, the
sweeper, or a job runner in production, so the sweep never runs and
abandoned sessions bill unattended. Owning path `cmd/reprise/main.go`.
The socket close event gap is severity M, same for the missing waiting
states on the record screen. The constant swaps into `reconcile.go` and
`sweep.go` are severity L.
