# T0.5 round 2 review

- Reviewed hash: `a150012312dc78bd3b2f24e31e2e662fe37b3db2`
- Worktree: `/home/nryn/work/reprise-wt/t0.5-rev-r2` (detached HEAD, confirmed via `git log -1`; branch `task/t0.5-rem-r1`)
- Base of remediation: `828dd44791f353fd4d54c2a3b9ae420d9298685f` (round 1 hash)
- Remediation diff: `git diff 828dd44..a150012` touches exactly 8 files, all under `testdata/sessions/`: `README.md`, `recreate.sh`, and the six user wavs (`steady/user-a.wav`, `steady/user-b.wav`, `pauses/user-a.wav`, `pauses/user-b.wav`, `bargein/user-trigger.wav`, `bargein/user-bargein.wav`). No other file changed. No Go files in scope.
- This review ran no live session and spent nothing. Every measurement below is offline against the committed artifacts in this worktree.

## 1. Remediation scope

`git diff 828dd44..a150012 --name-only` returns the 8 files above, nothing else. `grep -E '\.go$'` over the diff file list returns nothing. The remediation record (`t0.5-remediation-round1.md`) claims "only testdata/sessions", and the diff confirms it. Pass.

All checks below ran in this worktree against committed files: `ffprobe -hide_banner`, `sha256sum`, `python3` (`wave`/`struct`/`json`), `grep`, `bash -n`, `git diff`/`git status`. No network, no Docker, no synthesis.

## 2. Round 1 findings, each re-pinned

### F1 (M, corrupt wav headers) — FIXED

- `ffprobe -hide_banner` on all six user wavs now prints codec lines only, with zero `Packet corrupt` / `Estimating duration from bitrate` warnings: `pcm_s16le, 22050 Hz, mono`, durations 2.79 / 2.66 / 2.74 / 2.39 / 2.87 / 3.41 s, matching the README table exactly.
- Python `wave` now reports real frame counts (61543 / 58745 / 60412 / 52612 / 63216 / 75252) and durations (2.79 / 2.66 / 2.74 / 2.39 / 2.87 / 3.41 s) instead of `nframes 1073739776` (~48696 s).
- Raw header check: `RIFF size == file size - 8` holds on all six files (e.g. steady/user-a: real 123130, RIFF 123122, data 123086). The eight patched bytes per file are header sizes only; the remediation record's PCM-payload-hash claim is consistent with identical durations, codecs, and file sizes (each file is byte-identical in length to its round 1 version).
- Original F1 pin (`ffprobe` warns; `wave` reports ~48696 s) no longer reproduces. Fixed.

### F2 (M, unpinned toolchain) — FIXED

- `recreate.sh:13` pins `ubuntu:24.04@sha256:b3cc40b7...bb3ae15` (64-hex digest, verified all-hex). `recreate.sh:14` pins `ESPEAK_VERSION="1.51+dfsg-12build1"`, and line 19 installs `espeak-ng=${ESPEAK_VERSION}`. The script comment names both pins. The old pin (`grep -c 'sha256:'` → 0) now returns 1 digest line plus the exact-version install line.
- Every synthesis line uses seekable `-w` output (six `-w /out/...` lines), closing the `--stdout` cause of F1.
- Original F2 pin (no digest, unversioned install) no longer reproduces. Fixed.

### F3 (M, recreate script rebuilds nothing) — FIXED

- `recreate.sh` now writes all six committed wav paths directly: `/out/steady/user-a.wav`, `/out/steady/user-b.wav`, `/out/pauses/user-a.wav`, `/out/pauses/user-b.wav`, `/out/bargein/user-trigger.wav`, `/out/bargein/user-bargein.wav` — one `espeak-ng -w` line per committed clip, with guest-line text matching the README. `grep -c '\.wav'` over the script finds all six write sites.
- For what the script cannot do offline it is honest: header and echo tail state that live sessions need a provider key and network, that the script writes only the offline clips, and that `events.json`/`timeline.json`/`metadata.json`/`recording.ogg` come from the stream-and-store step (`grep` finds all four names plus "needs network access and a provider key"). The README Recreate section agrees: "regenerates the six guest clips with the pinned Docker image" plus key/cap/delete disclosure.
- Original F3 pin (zero wav write sites, `.raw` dumps plus manual prose) no longer reproduces. Fixed.

### F4 (L, wrong barge timing) — FIXED

- README now reads "about 5.4 seconds after the story audio starts". Independent probe over `bargein/events.json` (`type` field): story `reply.started` (`resp_9e78…`) at 7.34 s, second `input.speech.started` at 12.72 s, story `reply.done` status `interrupted` at the same stamp. Delta 12.72 − 7.34 = **5.38 s**, which rounds to the claimed ~5.4 s. Timeline still marks the story turn `interrupted` and the barge turn `completed`.
- Original F4 pin (5.37 ≠ 2.5) no longer reproduces against the new text. Fixed.

## 3. Round 1 passes still hold

- **18/18 SHA-256 match (recomputed).** Parsed all 18 `path + hash` rows from the README tables and compared against `sha256sum` of the committed files: 18 listed, 0 mismatches. The six wav hashes moved (header repair, same byte lengths); the twelve other hashes (3 ogg, 3 events, 3 timeline, 3 metadata) are unchanged from round 1, confirming nothing else moved.
- **Session shapes differ as claimed.** Event counts 1168 / 1083 / 1936 and turn counts 3 / 4 / 3 match the README. Codecs: six wavs `pcm_s16le, 22050 Hz, mono`; three recordings `opus, 48000 Hz, stereo` (18.41 / 30.66 / 25.56 s containers vs 18.11 / 30.38 / 25.24 s `session.ended` durations, same ~0.3 s trailing as round 1). `transcript.user` texts equal the timeline `user_transcript` values in all three sessions (garden-gate/brushes, lake/heron, trigger/oven-timer). Metadata `session_id` matches timeline `session_id` in all three sessions. Steady back-to-back, pauses gaps, barge-in interruption all re-observed.
- **Offline proof repeatable.** The P3-shaped read (decode `events.json` + stems with `ffprobe`/`python3`, no network) was re-run here: schema `at/event/offset/type`, transcript equality, session-id linkage, and barge cut timing all reproduced from committed bytes.

## 4. Residue, severity by severity

- F1 (M): fixed, nothing else changed (only header bytes in the six wavs).
- F2 (M): fixed, nothing else changed (pins plus `-w` lines in `recreate.sh`).
- F3 (M): fixed, nothing else changed (six wav write sites plus honest live-step prose).
- F4 (L): fixed, nothing else changed (one number plus six wav hashes in `README.md`).
- No new in-scope defect found: no secret values in any JSON (only `resume_token: <redacted>` placeholders, same as round 1), no planning cites in `README.md`/`recreate.sh`, no consumer names in `testdata/sessions/`, no Go/probe/planning files in the remediation diff.
- Zero-residue claim: every round 1 finding is fixed by its own pin, and the remediation diff contains nothing beyond those four fixes.

## 5. Gate

- No Go files in scope (confirmed by diff file list). `bash -n testdata/sessions/recreate.sh` passes clean.
- Full `tools/check.sh` not rerun: it needs `npm ci` in `web/` plus network-dependent steps (staticcheck fetch, Playwright browsers) and measures the whole tree, while this diff touches only six binary wavs and two fixture-text files with no Go, web, or gate-relevant content. Reasoning it stays green: the gate's `consumer names` scan with its own exclusions (`git grep -InEi 'hackathon|lablab' -- ':!tools/check.sh' ':!AGENTS.md' ':!product.md' ':!dev-diary'`) returns nothing, and the `plan references` scan over the two changed text files returns nothing, so neither scan can trip on this diff.
- Known OUT_OF_SCOPE mockup hit: the tracked file `dev-diary/mockup/V1 Reprise-Interactive Prototype.html` (plus planning files) matches `hackathon|lablab`, but the gate itself exempts `dev-diary/` and the gate-exclusion grep above returns no hits. Recorded here, not touched, blocks nothing.

## 6. Ten review questions

1. Does any path spend money without reserving budget first? **No.** Data-only diff; no code path, no token minting, no spend. The script's live step names the key, the 120 s caps, and session deletion.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? **No.** No code added; artifacts still show `session.ended` on all three sessions, and the README/script record 120 s caps plus deletion.
3. Can a private episode, stem or transcript reach anyone but its owner? **No.** No code; fixtures are generated speech, no real voice, no secret values in any JSON, reply audio redacted in events.
4. Does any edit apply without a revertible record? **No.** The remediation commit is the record; this review committed nothing and the tree is clean.
5. Does any spoken callback or count come from the prompt instead of a stored row? **No.** No code; event transcripts equal timeline transcripts; counts recomputed from files.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? **No.** `README.md`/`recreate.sh` contain no task/phase ids or planning filenames.
7. Did this task rebuild something Keel or Chaaya provides? **No.** No code of any kind; a shell synthesis script plus data.
8. Was this review run on the handed hash, in a worktree of its own? **Yes.** `git log -1` = `a150012312dc78bd3b2f24e31e2e662fe37b3db2`, detached at `/home/nryn/work/reprise-wt/t0.5-rev-r2`.
9. Can any paid job kind resume after a restart and spend twice? **No.** No jobs or resume logic in scope.
10. Does any development check need a person, a microphone, or a wall-clock threshold? **No.** All pins use fixed files with `ffprobe`/`sha256sum`/`python3`/`bash -n`; timing claims compare clocks within one recorded run, never a live threshold.

## Findings

No in-scope findings. Zero C/H/M/L.

OUT_OF_SCOPE: the known tracked-mockup `hackathon|lablab` hit under `dev-diary/mockup/` (gate-exempt path; the gate's own exclusion grep returns clean). Recorded, untouched, non-blocking.

## Verdict

**APPROVE** — F1, F2, F3, F4 all fixed against their round 1 pins, 18/18 hashes recomputed clean, round 1 passes re-verified, remediation diff confined to `testdata/sessions/`, tree clean, no commits made.
