# T5.1 remediation round 1

Reviewed hash `dd0a8bbef7a3bcc64de3386592f94a1d4764040e`. Worktree
`/home/nryn/work/reprise-wt/t5.1-rem-r1` on branch `task/t5.1-rem-r1`.
Three commits sit on the reviewed hash, one per finding. New head
`68d9eeb261c73de271e39299c2f260670f6ffe97`. Nothing outside
`internal/export/` changed. No verdict change. Nothing else fixed.

| Severity | Where | Fix | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/export/export_test.go:222` | Added a `lateStart` rejection fixture at 5000, 15000 and 25000 ms over 35000 ms. Every span reads exactly 10000 ms, so only the first-at-zero rule can reject it. Commit `79ef6cb`. | `go test ./internal/export/ -run TestValidateChaptersRejects -count=1` passes on the fixed tree. A scratch copy with the zero check deleted fails at the new fixture line with `want ErrChapters, got <nil>`. | Delete the `if chapters[0].StartMs != 0` branch in `ValidateChapters`. The suite now fails. |
| L | `internal/export/export_test.go:269` | Tightened the video duration window from 150 ms to 50 ms in the bound and the failure text. Commit `7b6c127`. | `TestVideoIs1080PWithAudio` passes with `-count=2` at 50 ms, plus one full gate run. Three passes, zero flakes. Pinned ffmpeg and ffprobe report `n9.0.1`. | Trim the source audio by about 77 ms and compare against the full render length. The 50 ms predicate fails while 150 ms passed. |
| L | `internal/export/export.go:56` | Rewrote the `ErrInvalid` doc to name what callers pass. It now lists an empty path, blank word text, empty cover bytes, a non-square cover, a missing duration, and an unreadable video. Commit `68d9eeb`. | Grep for `empty id` and `nil dependency` over `internal/export/` returns nothing. Every `ErrInvalid` site passes one of the listed inputs. | Restore the words `empty id` and `nil dependency` in the doc comment. The inaccuracy returns. |

Gate ran in the remediation worktree after `npm ci` in `web/`.
`./tools/check.sh` ends with `OK all checks passed`, including
`go test -race ./...`. Untouched as ordered: the AAC copy path, the
numbered WebVTT parser, the short-episode `ErrChapters` fail-closed
path, the wiring contract, and the cover checks.
