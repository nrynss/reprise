# Remediation round 1

Reviewed hash is 44d724c738364c7ef1ce488a4afc1839f0cd7400.
Remediation commit is 8d7a9bf2f9f2e502292f85bf19f35e51947def1a.
Worktree is /home/nryn/work/reprise-wt/t1.1-rem-r1 on branch task/t1.1-rem-r1.
This remediation builds on the reviewed commit. It never amends it.

## Finding fixed

H1 reports that the new keel/sqlite import needs transitive module entries the frozen go.sum lacks. Every clean tree Go command fails in setup. The schema itself stays correct.

## Fix applied

Ran go mod tidy in the remediation worktree. Changed only go.mod and go.sum. Added no version edits. Added no replace directive. Owned paths stay byte identical to the reviewed hash.

## Tidy evidence

go.mod groups the existing pelletier require into a block. It adds ten indirect requires. The list includes dustin/go-humanize, google/uuid, mattn/go-isatty, ncruces/go-strftime, remyoudompheng/bigfft, golang.org/x/sys, modernc.org/libc, modernc.org/mathutil, modernc.org/memory and modernc.org/sqlite at v1.58.0. go.sum gains fifty lines. git diff on internal/store and internal/store/migrations against the reviewed hash prints nothing. No LICENSE file exists.

## Verification evidence

gofmt -l on internal/store prints nothing. go vet on internal/store exits 0. go test with race and count 1 on internal/store passes. staticcheck over project packages exits 0. The full gate runs through Go stages and web stages. It then fails at the consumer names scan. The scan flags one line in a tracked mockup file. That file landed in dc69195 before this work. The failure predates this remediation. It sits outside owned paths.
