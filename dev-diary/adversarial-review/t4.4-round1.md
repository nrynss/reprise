# T4.4 round 1 review

Reviewed hash: c504d03f47db531a8fe438e1be6c83824ac4822d.
Worktree: /home/nryn/work/reprise-wt/t4.4-rev-r1 (detached HEAD, confirmed
with `git log -1 --format=%H`). No commits made. Working tree only.

Base: fb96913. Diff scope matches declared owns: `internal/privacy/`,
`web/src/routes/share/`, `web/src/routes/privacy/`.

## What was re-run

* `go test -race ./internal/privacy/`: pass.
* `go vet ./internal/privacy/`: pass.
* `npm ci`, `svelte-kit sync`, `npm run check` (0 errors), `npm run lint`
  (clean), `npm run test` (12 files, 92 tests, all pass).
* `npm run build` plus the route Playwright proofs
  (`share.playwright.config.ts`): 3 passed.
* Both pins below were executed as temporary probe tests in this worktree
  and both failed against the handed hash exactly as described. The probe
  files were removed afterwards. `git status --short` is clean.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| C | `internal/privacy/publish.go:130-163` (`Unpublish`, with `Publish` at 119-126) | Unpublish returns only the latest render row blobs to private. Any earlier render opus blob made public by a previous publish stays public forever. A signed-out fetch of the old blob still returns 200 after unpublish, so revoked audio keeps playing. The comment at 130-133 claims every render blob returns to private, which the code does not do. | Publish episode, insert a second render row with new opus and aac blobs, publish again, unpublish, then fetch the first opus blob signed out through the media store. Got 200 with the opus bytes. Want 404. (Executed as a probe in this worktree. Failed as described.) | In `Unpublish`, keep the latest-row-only blob flip (the current code) instead of flipping every render row opus blob. The pin then fails with 200. |
| H | `internal/privacy/publish.go:119-126` (`Publish`) | Publish marks the episode row public before flipping the render blob. When the blob flip fails with `ErrMediaMissing`, the function returns the error but the episode stays public: a dead public episode whose token was never handed out and whose retry can never succeed while the blob is gone. | Delete the opus blob row, call `Publish`, observe `ErrMediaMissing`, then read episode visibility. Got `public`. Want `private`. (Executed as a probe in this worktree. Failed as described.) | In `Publish`, keep the episode UPDATE before `setVisibility` (the current order) instead of flipping the blob first or wrapping both writes in one transaction. The pin then fails with `public`. |
| L | `internal/privacy/erase_test.go:17-20`, `internal/privacy/privacy_test.go:242-327` | The erase pin asserts zero rows for eleven tables, but the fixture seeds only sessions, stems, renders, analyses, and words. Turns, proposals, decisions, mentions, and callbacks are never seeded, so the cascade the erase relies on is unmeasured for five of ten content tables. | Drop `ON DELETE CASCADE` from `mentions.episode_id` in a scratch copy of the schema. The suite still passes, because no mention row ever exists to orphan. | Seed one row per content table in `seedOwner` (or delete the cascade and watch the new seed fail). |

## OUT_OF_SCOPE

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/privacy/erase.go:217-247` (`ReErase`), no route | `ReErase` exists and is pinned, but no HTTP route exposes the retry control. The handoff declares this openly. Exposing it belongs to the wiring and route table follow-up, not to this task, so it does not block APPROVE. | Call a retry endpoint for a stuck erasure job. No such route exists. | N/A (missing wiring, not a regression). |
| M | Handoff "Live remeasure is owed" | Signed-out share checks ran handler level with provider doubles. Workstation checks of a published and a revoked link still owe a run. Recorded in the handoff, same deferral pattern as before. Non-blocking. | Open a published and a revoked share link from a workstation. Not run. | N/A. |

## The ten questions

1. Does any path spend money without reserving budget first? No. The erase
   fan-out performs deletes only. No paid call exists in the owned paths.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. Erasure ends the provider session through an explicit
   `sessionTarget`. No streaming session opens in the owned paths.
3. Can a private episode, stem or transcript reach anyone but its owner?
   YES. Finding 1 (C): a superseded render opus blob keeps serving 200 to a
   signed-out visitor after unpublish. The rest holds: stems, the export
   copy, and metadata behind revoked tokens refuse (re-ran green), and
   revoked, malformed, unknown, and unpublished tokens share one identical
   404 body (re-ran green). Cover bytes gate on `LookupShare` first, and a
   missing cover answers the same identical 404.
4. Does any edit apply without a revertible record? No. Publish and
   unpublish are reversible state flips by design. Erasure is the delete the
   task specifies, recorded in the job ledger.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. Share metadata (title, number, audio, cover) is read from stored
   rows only. No callbacks or counts exist in the owned paths.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Ran the exact `check.sh` plan-reference pattern set
   over tracked files. Clean. (The word `phase` in the share page is a UI
   loading state, not a plan citation, and it matches none of the patterns.)
7. Did this task rebuild something Keel or Chaaya provides? No. It wires
   `keel/erase`, `keel/id`, and the media store, and parses the Chaaya wire
   envelope on the client. The direct media-row visibility flip is declared
   in the handoff as a library-side gap, not a local rebuild.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Hash confirmed, detached worktree, no commits, no other orchestrator
   files touched.
9. Can any paid job kind resume after a restart and spend twice? No. The
   erasure kind carries no paid calls, every target treats a missing
   row, blob, file, session, or transcript as confirmed, the transcript
   target confirms with a fresh fetch, `ReErase` checks ownership against
   the recorded ref, and a finished erasure answers `ErrComplete`. The
   restart probe path re-ran green.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. Playwright proofs run on fixtures (re-ran green, 3
    passed). The Go polls use timeout bounds only, never timing assertions.

## Verdict

REMEDIATE. Counts: C 1, H 1, M 0, L 1, plus two OUT_OF_SCOPE notes that do
not block. No zero-residue claim is possible: a signed-out visitor can still
play audio this task promised to revoke.
