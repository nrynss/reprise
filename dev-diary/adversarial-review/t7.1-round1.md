# T7.1 round 1 review

Reviewed hash: `690f17a6481518ed0d19f45613281974d3e107f6`
Worktree: `/home/nryn/work/reprise-wt/t7.1-rev-r1` (detached HEAD, confirmed with
`git log -1`; diff read as `git diff 25d4a03..690f17a6481518ed0d19f45613281974d3e107f6`).
Base: main at `25d4a03`.

Scope: `internal/api/`, `internal/episode/` only. The diff touches 13 files,
all inside those two paths. No other path changes.

## Done-clause pins (all run in this worktree, all pass)

| Done clause | Pin | Result |
|---|---|---|
| Stub refusal golden still decodes for routes with no handler | `TestMountKeepsStubsForUnmountedRoutes` (publish POST/DELETE, episode DELETE stay stub) | PASS |
| Newly wired routes answer through the middleware chain | `TestMountServesHandlersThroughChain` (sessions end, episodes list/detail/decisions/done, threads, admin) | PASS |
| Passing and refusing sides pinned | `TestEpisodeListServesOwnerOnly` (owner 200 plus stranger empty 200 plus no-middleware 401), `TestMountGateRefusesBeforeMiddleware` | PASS |
| Fixture episode lists, decides, marks done, reads back, no network | `TestEpisodeDetailReadsBackDecisions`, `TestMarkDoneStartsRenderOnce` | PASS |
| Owner isolation, honest 200s plus foreign-user refusals | list, detail, done, session end, threads tests each mint a foreign guest and require 404 or empty | PASS |
| Done starts exactly one render | `TestMarkDoneStartsRenderOnce` (repeat tap 409, starter calls stay 1), `TestRequestRenderStartsOneJob` | PASS |
| Starter faults fail the episode for explicit retry | `TestMarkDoneReportsStarterFaults` (429 plus 503, state `failed`) | PASS |
| Session end repeat-safe | `TestSessionEndRecordsProviderClose` (repeat 200, foreign 404, missing 404, provider id read back from the row) | PASS |
| Threads counts from stored rows only | `TestThreadsCountsComeFromRows` (Mara 2 mentions across 2 episodes, winter garden across 3, stranger empty) | PASS |
| Method plus pattern routing, DELETE stays stub | `TestMountKeepsStubsForUnmountedRoutes` covers `DELETE /api/episodes/e01` | PASS |
| `no-store` on diary reads | `TestEpisodeListServesOwnerOnly` asserts `Cache-Control: no-store` | PASS |

Gate: `npm ci` in `web/`, then `./tools/check.sh` from the worktree root
prints `OK all checks passed`, including the plan-references check.
`go test -race ./internal/episode/ ./internal/api/` passes.
`go vet` on both packages is clean.
`python3 dev-diary/tools/audit_docs.py` prints `OK planning tree is consistent`.

## Findings

None. Zero findings across all severities.

## OUT_OF_SCOPE

None met as defects. Note (not a finding, never blocks): no constructor in
`cmd/reprise/main.go` builds these handlers yet. That file is T7.2's, and
the T7.1 spec orders exactly this outcome through a contract change, which
the implementer handoff records. Nothing is demanded here.

## Ten questions

1. Does any path spend money without reserving budget first? No. The new
   code starts one local render job and records rows. It opens no provider
   session and makes no provider call.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. This task opens no sessions. Session end only stores the
   provider close the browser already sent, ready for the settle.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   Every read scopes on the request guest. Unknown and foreign ids both
   answer 404, pinned per route in both directions.
4. Does any edit apply without a revertible record? No. Decisions are
   append-only rows with latest-wins reads, pinned accept then revert.
   Mark done moves through the guarded draft to rendering transition.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. Threads serve `memory.RecurringNames` and
   `memory.CircledTopics` over stored mentions, pinned row by row.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The gate plan-references check passes. A repo grep
   finds only benign words (`middleware`, `round trip`).
7. Did this task rebuild something Keel or Chaaya provides? No. It uses
   `keel/sqlite`, `keel/wire`, `keel/job`, and the existing `memory`
   queries. No new general capability.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Detached worktree above, `git log -1` confirms `690f17a`, diff read
   against base `25d4a03`, pins run locally, no code committed.
9. Can any paid job kind resume after a restart and spend twice? No. The
   only job started here is the local render kind. No paid kind is added
   or re-armed.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. All pins run against seeded SQLite rows and fake
    starters with exact assertions.

## Verdict

APPROVE. Counts: C 0, H 0, M 0, L 0. No residue, since this is round 1 and
no prior round exists.
