# T5.2 round 1 review

Reviewed hash: `8e34b08ce183c86ad9f9c32d3c45074936b8431c`.
Worktree: `/home/nryn/work/reprise-wt/t5.2-rev-r1` (detached, confirmed with
`git log -1`). Base: main at `4fb64e0`. The diff under review touches only
`internal/seed/` and `data/season/`, which matches the task owns line.

## Done-when verification

Each clause below names the independent pin this reviewer ran in this
worktree. All pins passed.

| Done clause | Pin | Result |
|---|---|---|
| Empty catalog, new guest sees no seeded episodes and can record | `TestEmptyCatalogCopiesNothing` | Pass. Zero episodes, no receipt. Nothing blocks later inserts. |
| Fixture catalog, new guest sees rows as their own | `TestCopyGivesGuestOwnRows` | Pass. Same numbers and titles under fresh ids. Seeded flag set. Receipt written. |
| Two guests hold distinct ids | `TestCopyGivesGuestOwnRows` | Pass. Copy ids differ from each other and from the catalog. |
| Drop removes that user copy only, catalog and neighbor stay | `TestDropKeepsCatalogAndNeighbor` | Pass. Catalog rows, other guest rows, and catalog audio survive. Checked with a real `index.Get` on the blob. |
| No reimport after drop | `TestDropKeepsCatalogAndNeighbor` plus a throwaway probe that drops all copies and relists | Pass. Receipt survives the drop and blocks the second copy. Probe file removed after the run. |
| New empty account gets a copy | `TestEmptyAccountGetsCopy` | Pass. A `member` kind user with no season receives two seeded rows. |
| Owner gets none | `TestOwnerGetsNoCopy` | Pass. No rows and no receipt. |
| T4.2 callback resolves on a copied mention | `TestCallbackResolvesOnCopies` | Pass. Runs the real `memory.RecurringNames` and `memory.Select` on copied rows. Opens on the copied Jonas thread in episode 1. |
| Reserved seed user skipped by sweep | `TestSweepSkipsCatalogUser` plus a throwaway probe running the real sweep shape | Pass. Kind is `seed`, never `guest`. The landed sweep at `internal/retention/sweep.go:224` selects `kind = 'guest'`, so the catalog owner falls outside it. |
| No schema columns added | Read `internal/seed/migrations/0001_seed.sql` and `internal/store/migrations/0001_schema.sql` | Pass. The new migration creates only `seed_catalog` and `seed_receipts`. The `seeded` column already lives in the diary schema. |
| No privacy files touched | `git diff 4fb64e0..8e34b08 --name-only` | Pass. Seven files, all inside owns. |
| No fixture ships in `data/season/` | `ls data/season/` | Pass. Only `README.md` ships. All test catalogs use per-test temp dirs. |
| No phase, task, or plan references in code | `grep` for plan patterns plus `./tools/check.sh` plan-references gate | Pass. Clean. |
| Signup keeps copies | `TestSignupKeepsCopies` | Pass. Kind flip keeps rows and triggers no second copy. |
| Late catalog reaches an empty user | `TestLateCatalogReachesEmptyUser` | Pass. No receipt on the empty run, copy lands once files arrive. |
| Seasoned user gains no copy and no receipt | `TestSeasonedUserGetsNoCopy` | Pass. |
| Operator remove retires the entry and stops new copies | `TestOperatorRemoveStopsNewCopies` | Pass. Retired audio returns not found. Existing copies keep working. |
| Bad catalog files fail the whole sync | `TestSyncRejectsBadFiles` | Pass. Six defect shapes, each rejected with no rows left behind. |
| Unknown and empty user ids fail | `TestCopyUnknownUser` | Pass. `ErrNotFound` and `ErrInvalid`. |
| Copies stay private, catalog audio stays public | Throwaway probe reading episode visibility and blob visibility | Pass. Copies read `private`. The catalog blob reads `public`. Probe file removed after the run. |

Gate: `npm ci` in `web/`, then `./tools/check.sh` prints `OK all checks
passed`. `go test -race -count=1 ./internal/seed/` passes. `go vet` is
clean. `dev-diary/tools/audit_docs.py` prints `OK planning tree is
consistent`. Grep confirms no caller of `internal/seed` exists outside the
package, so every done clause above is pinned at the package seam, exactly
where this task owns.

## The ten questions

1. Does any path spend money without reserving budget first? No. The package
   touches SQLite and the media store only. No paid call exists here.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. The package never opens a session.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   Copies insert with `visibility private` under the visitor id (pinned by
   probe). Only the shared catalog audio bytes are public, and the spec
   names them identical demo material. Catalog episode rows belong to the
   reserved seed user.
4. Does any edit apply without a revertible record? No user edit path lives
   here. Catalog swaps and drops are operator and owner actions on flagged
   rows, and the spec defines both.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. The planted callback is a stored `callbacks` row pointing at a
   stored `mentions` row, remapped per copy. The selector test proves it.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Grep over `internal/seed/` is clean and the gate
   agrees.
7. Did this task rebuild something Keel or Chaaya provides? No. It builds on
   `keel/sqlite`, `keel/id`, and `keel/mediastore` through their public
   seams.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   `git log -1` shows `8e34b08ce183c86ad9f9c32d3c45074936b8431c` in
   `t5.2-rev-r1`. No code was committed from this worktree. The probe file
   used for extra pins was deleted after the run.
9. Can any paid job kind resume after a restart and spend twice? No job kind
   lives here.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. Tests use generated bytes and exact row counts. Receipt
    timestamps use the clock but no test asserts on wall time.

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0 in scope. Four OUT_OF_SCOPE rows below. None of
them blocks this verdict.

The done criteria pass without the pending wiring. Every clause is pinned at
the package seam this task owns. The wiring gaps below are real, recorded
with owning paths, and owned by later work.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| OUT_OF_SCOPE, M | Season list handler, outside `internal/seed/` (no caller exists yet) | `Sync` plus `EnsureCopy` run on no request path. Guests receive no copy until some list calls them. | `grep -rn 'internal/seed' --include='*.go' .` outside `internal/seed/` returns no callers in this worktree. | Resolving edit lives outside owns: call `Sync` and `EnsureCopy` from the season list handler. |
| OUT_OF_SCOPE, M | `internal/privacy/handlers.go` or the episode delete handler beside it | No delete path branches on the seeded flag. A seeded copy sent through the erasure fan-out would delete the shared catalog audio for every visitor. | Same caller grep: no delete handler imports `internal/seed`. The `Drop` versus fan-out branch exists only as package API. | Resolving edit lives outside owns: branch deletes on `seeded` toward `seed.Drop`. |
| OUT_OF_SCOPE, M | `internal/retention/sweep.go` | The sweep does not route seeded episodes through `seed.Drop`. Per-copy media handling still assumes unshared blobs. | `grep -n 'seed' internal/retention/sweep.go` returns nothing in this worktree. | Resolving edit lives outside owns: use `Seeded` plus `Drop` for flagged rows in the sweep. |
| OUT_OF_SCOPE, M | `Dockerfile` | The image copies no `data/season` directory. The catalog directory the package reads never ships. | `Dockerfile` in this worktree names no data copy step. Only the binary, the web build, media tools, and box config arrive. | Resolving edit lives outside owns: add a `COPY data/season` step to the runtime stage. |

Notes for the orchestrator. The handoff already raises all four wirings as
contract changes, so no new task discovery is needed beyond scoping them.
The retire path deletes catalog audio on file removal. That stays safe only
while copies hold no media rows. A later change that gives copies their own
media rows must recheck sharing first. The handoff names this trap and the
code upholds it today.
