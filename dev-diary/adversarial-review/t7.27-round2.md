# T7.27 round 2 review

Reviewed hash: `2c8ba807ab3726ab6a0c66aca8f592fe6df8ddf9`
Worktree: `/Users/narayan/reprise-wt/t7.27-rev-r2` (detached HEAD).
Parent is `a67c6af79346951ecdedb88cf9af00c3cf03c998`, the round 1 hash.
`git log -1` matches the handed hash. The subject is "Keep the welcome browser check on Chromium."
`git status --short` was empty before the pins and empty again after the mutation was restored.
Diff reviewed: `git diff a67c6af79346951ecdedb88cf9af00c3cf03c998..2c8ba807ab3726ab6a0c66aca8f592fe6df8ddf9` and nothing else.
No code commits. The tree is still that hash.

## The ten questions

1. Does any path spend money without reserving budget first? No. This diff changes only the welcome Playwright project list. It adds no paid call. The mint path is unchanged from the parent.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This diff does not touch capture, the socket, or the token. The parent still opens the microphone before the mint. A live take still ends through `voice.end` and the pagehide guard.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The diff changes no media URL, cookie, or owner check.
4. Does any edit apply without a revertible record? No. Nothing applies an edit.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The config comment still names the default command. It does not cite a task id, a phase id, or a planning file. The commit message does not either.
7. Did this task rebuild something Keel or Chaaya provides? No. The commit removes a WebKit project from a Reprise config. The silent-prime defect stays in Chaaya 0.2.0. It is not patched here.
8. Was this review run on the handed hash, in a worktree of its own? Yes. This detached worktree is the only tree the review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The gesture proof still stubs `getUserMedia` and holds the mint. The welcome command still uses scripted fixtures. This diff adds no clock threshold. The 10 second poll in the teaser spec is older than this commit.

## What the commit does

The welcome config lists Chromium only, at `web/src/routes/welcome/welcome.playwright.config.ts:15`. The WebKit project from the parent is gone. The welcome spec, the gesture proof, the root Playwright config, and the welcome page are unchanged.

## Pins

`npm ci` in this worktree's `web/` added 236 packages. Exit 0. `node_modules` was missing here.
`npm run build` from `web/` exited 0. Preview serves `web/build`.

`npx playwright test` from `web/` passed 4 tests in 1.1s. Exit 0. The shell spec and the gesture spec passed on Chromium and on WebKit.

`npx playwright test -c src/routes/welcome/welcome.playwright.config.ts` from `web/` used no project filter. It passed 4 tests in 1.5s. Exit 0. Chromium was the only project. The seeded teaser passed.

## Mutation

The cleared M is load-bearing. The WebKit project was put back beside Chromium in `welcome.playwright.config.ts`. The same welcome command then failed. Exit 1. 7 passed and 1 failed in 11.8s. WebKit `the seeded teaser plays after the first gesture` polled `playing` for 10 seconds and got false at `welcome.spec.ts:56`. The four Chromium proofs passed.

`git checkout` restored that file. `git status --short` was empty. `git diff` was empty. `git log -1` was still the handed hash.

## Residue

C. Round 1 had none. Residue is zero.
H. The only H was out of scope. It remains out of scope. It is not in-scope residue, and it does not block.
M. The in-scope M is gone. The default welcome command passes on Chromium only. Putting the WebKit project back makes that command fail again. Residue is zero.
L. Round 1 had none. Residue is zero.

## Findings

No in-scope findings.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H, OUT_OF_SCOPE | `web/src/routes/welcome/welcome.ts:254` at the reviewed hash. The player lives in Chaaya 0.2.0. Neither path is in `owns`. | `togglePlay` still awaits `player.play()`. Chaaya still primes that call with a silent clip that WebKit rejects. The record link on the same page still works. Filed as https://github.com/nrynss/chaaya/issues/5. This commit does not edit the welcome page. | Putting the WebKit project back and running the default welcome command. WebKit `playing` stayed false for the 10 second poll at `welcome.spec.ts:56`. Exit 1. With that project absent, the Chromium teaser passed. | This row does not block. Do not weaken `welcome.spec.ts`. The in-scope fix was the project list, and that fix holds. |

## Counts

C 0, H 1 (out of scope), M 0, L 0. In-scope findings: zero. Residue against the round 1 M: zero. The out-of-scope H does not block.

## Verdict

APPROVE

The welcome config's default run passes on Chromium only. The start-gesture proof still passes on WebKit and on Chromium. The round 1 M has no residue.
