# T7.20 implementer handoff

Commit `3a5e2d49ad75ed82832e2ae3856db1160e28eade` on branch `task/t7.20`
in worktree `t7.20-impl`. Base is main at `511f4a1`. Gate passes in
the worktree. Only `web/src/lib/voice/` changes. No phase file
touched.

## What it built

Voice take calls fail loudly by name and the take clock freezes on
failure. New `session-calls.ts` reuses the record route guard for
mint (no retry, a retry would reserve twice), session end (one retry
on network drop, idempotent), and the job poll (also fixes an
uncaught throw on HTML answers). `record-state.ts` wires the landed
completion driver and controller retry. Failed chunks and failed end
surface named with the pair kept, and the elapsed interval stops
when the take ends or fails.

## Validation

14 vitest cases plus 3 Playwright proofs (HTML on chunk, end, mint
routes, each named, clock frozen, retry finishes). Mutation on the
clock stops fails exactly the two clock assertions.
