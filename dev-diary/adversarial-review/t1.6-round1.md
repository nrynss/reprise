# T1.6 round 1 review

Hash reviewed: `5056e610cfd80c4b0364dc1dbfcb7d7efed78243`.
Worktree: `/home/nryn/work/reprise-wt/t1.6-rev-r1`, detached at the handed hash.
I confirmed the hash with `git log -1` before reviewing.
Base: `4632df2`.
Diff under review: `git diff 4632df2..HEAD`.

## Scope

Owned paths: `internal/settings/` and `config/reprise.local.toml` and `config/reprise.box.toml` and `.env.example`.
Recorded exception: `go.mod` and `go.sum` may change only through added indirect lines.
Files in the diff: `.env.example` and `config/reprise.box.toml` and `config/reprise.local.toml` and `internal/settings/settings.go` and `internal/settings/settings_test.go`.
Nothing else changed.
`go.mod` and `go.sum` stay byte identical to base.
I verified the name list with `git diff --name-only 4632df2..HEAD`.
I verified module identity with `cmp` against base for both files.

## Reasoning source

I read `dev-diary/project.md` under the heading about Gemini reaching Reprise through Vertex AI.
It says Vertex authenticates a service account so the secret is a JSON key file.
It says the box runs no metadata server so the file is explicit.
It says `keel/config` reads the file through a `file` source.
It says project id and location sit inline in TOML as ordinary settings.
The branch matches that shape exactly.

## Gate

I ran `npm ci` in `web/` and then the gate in the review worktree.
All Go and web checks passed through the svelte leakage scan.
`gofmt -l internal/settings/` printed nothing.
`go vet ./internal/settings/` passed.
`staticcheck` for the owned package passed.
`go test -race -count=1 ./internal/settings/` passed.
The gate then stopped at the consumer name scan with exit 1.
The single hit is outside owned paths and it exists at base too.
With the tracked plan exemptions from commit 34e20b9 both name scans report clean.
I record that hit below as out of scope and it never blocks approval.

## Done-when probes run independently at the reviewed hash

All probes used fresh temp files and distinct dummy values.
I never opened the real key file.
I read only its mode with `stat` and it reported mode 600.
I built the settings test binary once with `go test -c`.
I used a probe config with a temp env file and a temp key file.
No probe file remains and the worktree tree is clean.

1. Readable key boots and the plan names file with path. I ran the helper in plan mode with `REPRISE_CONFIG` pointing at the probe config. Exit was 0. The credential line read `secrets.gemini_credential` with source `file` and path `/tmp/t16probe/gemini-sa.json` and status `resolved`. Inline rows named `vertex_project` and `vertex_location` as resolved from the file.
2. Missing file refuses and names key and source and path. I pointed the credential at `/tmp/t16probe/missing-sa.json`. Exit was 1. Stderr named `secrets.gemini_credential` and source `file` and the missing path.
3. Group readable key refuses and names key and source and path. I set the temp key to mode 640. Exit was 1. Stderr named `secrets.gemini_credential` and source `file` and the loose path with mode 640.
4. Boot log carries no key fragment. I probed plan output for all four planted values. Each probe returned absent. The values were `probe-assembly-aaa111` and `probe-signing-bbb222` and `probe-key-id-ccc333` and `probe-private-ddd444`.
5. Tests pin TOML and example agreement. `TestTrackedFilesAgreeOnSecretNames` loads both tracked TOML files with rewritten temp paths. It checks vertex inline values and file source and key paths. It checks `.env.example` lists the two env names and never names the credential variable. It passed in the race run.

## Contract and style checks

Both TOML files set `vertex_project` to `nryn-personal` and `vertex_location` to `europe-west1` as inline strings.
Both set `[secrets.gemini_credential]` to `source = "file"` with the specified path.
Local path is `/home/nryn/.config/reprise/gemini-sa.json` outside the repository.
Box path is `/etc/reprise/gemini-sa.json`.
`.env.example` lists only `ASSEMBLYAI_API_KEY` and `SESSION_SIGNING_KEY`.
No TOML file carries a value and no tracked file carries a dummy value.
The account name appears only in comments and never travels as code.
Region choice is not a finding. No planning file names a region. T0.3 asks for location from settings but leaves the value open. The handoff names `europe-west1` as the documented default. Main uses another value but that history postdates this worktree. I judged this branch against the task text only.
Commit message carries no banned token and uses active voice.
Comments carry no semicolons and no banned dashes.
My sentence scan over owned sources found no sentence over 30 words.
No `LICENSE` file exists.
Mutation coverage from the implementer tests is load bearing for the file source.

## Ten review questions

1. Does any path spend money without reserving budget first. No. Load reads one TOML file and resolves local references only.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. This change holds no session code.
3. Can a private episode or stem or transcript reach anyone but its owner. No. No tracked file holds a secret value. The plan probe shows named sources with no planted value present.
4. Does any edit apply without a revertible record. No. This change applies no editorial edit. Settings files stay tracked in git.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. This change holds no spoken content.
6. Does any code or comment or test or migration cite a phase or a task or a planning file. No. Grep over owned paths returns empty. The gate plan scan with tracked exemptions returns clean.
7. Did this task rebuild something Keel or Chaaya provides. No. Loading and refusal and the plan come from `keel/config`. Reprise owns only the struct and two sentinels and two inline fields.
8. Was this review run on the handed hash in a worktree of its own. Yes. `git log -1` showed the handed hash in detached worktree `t1.6-rev-r1`. Every pin above ran there.
9. Can any paid job kind resume after a restart and spend twice. No. This change holds no job code.
10. Does any development check need a person or a microphone or a wall clock threshold. No. Tests use temp files and a helper process with exact string checks and exit codes.

## Findings

In scope severity counts: C 0 and H 0 and M 0 and L 0.
One out of scope observation follows and it never blocks approval.

OUT_OF_SCOPE, Severity L.
Where: `dev-diary/mockup/V1 Reprise-Interactive Prototype.html` line 11 at the reviewed hash.
Owning path: `dev-diary/mockup/`, outside T1.6 ownership.
What: the line contains the word `hackathon` so the old gate consumer scan fails in this worktree.
Pin: I ran `git grep -InEi hackathon minus lablab` with the old exclusions and it reported that line. I ran the same grep at base `4632df2` and it reported the same line. I ran both name scans with the tracked plan exemptions and both reported clean.
Mutation: add the word `hackathon` to line 11 of that mockup file and the old scan fails again. Remove that word and the old scan passes.

## Verdict

APPROVE. Zero in scope findings across all severities. Round 1 is the first round so the baseline is clean. No prior round residue exists. I committed nothing. Probes are removed. The worktree tree is clean.
