# T4.1 remediation round 1

Reviewed hash `b81c13f6509e3f95f2dc319ca06653ac43c0aa60`. Remediation branch
`task/t4.1-rem-r1` in worktree `/home/nryn/work/reprise-wt/t4.1-rem-r1`.
Four new commits sit on the reviewed hash. The head now reads
`e4eb7fae38344f2ad03618b654a551087202561c`. The tree is clean. Every fix
stays inside `internal/memory/`. The out of scope analysis rerun orphan
is untouched.

## Rows

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| H1 time travel close | `StoreResolution` loads both episode numbers and fails with `ErrInvalid` unless evidence is strictly later. `ResolveEpisode` checks the same order before reserving budget, so an invalid request never spends. | `TestStoreResolutionRejectsBackdatedClose` and `TestStoreResolutionRejectsSameEpisodeClose` fail backdated and same episode closes with `ErrInvalid` and no rows. `TestResolveEpisodeRejectsBackdatedEpisode` fails the model path with `ErrInvalid`, zero provider calls, and zero reservations. | Flip the comparison in either entry point. Each pin returns nil or done again. |
| H2 reclose deletes evidence | A second close deletes only the resolution link. Every evidence mention stays stored. | `TestStoreResolutionRecloseKeepsHistory` closes in episode 2 and again in episode 3, then checks both doing rows survive, one link remains, and the gallery reads the latest evidence. | Restore the old evidence mention delete. The first evidence count drops to zero. |
| L1 trailing garbage | Both parsers take a second decode and fail with `ErrModel` unless it meets `io.EOF`. | `TestParseRejectsTrailingGarbage` fails trailing data inputs on both parsers and still accepts clean bodies. | Drop the second decode. The trailing inputs parse cleanly again. |
| L2 unknown and foreign ids | `StoreResolution` looks up the commitment scoped to the caller. A missing row fails with `ErrNotFound`, so unknown ids and foreign owned ids never write. | `TestStoreResolutionUnknownCommitment` fails an unknown id with `ErrNotFound`. `TestStoreResolutionForeignCommitment` fails another owner id with `ErrNotFound`, no rows, and an empty resolved list. | Remove the scoped lookup. The probes return the driver error and nil again. |

## Verification

`go test -race -count=1 ./internal/memory/...` passes. `go vet` is clean.
`gofmt` reports nothing. The full `./tools/check.sh` passes with exit 0,
including every web check. Each commit built and passed the memory suite
before the next patch landed.
