# T2.4 remediation round 1

Reviewed hash: `eee9fb5bd105c76a73862cc3eaa40100f7b82395`.
New head: `e471725264588e4bb0487e2cfa5d986c2de71558`.
Worktree: `/home/nryn/work/reprise-wt/t2.4-rem-r1`, branch `task/t2.4-rem-r1`.
Built on the reviewed commit with new commits only. No rebase, no amend.

## Fixes

| Severity | Where | What changed | Pin | Mutation |
|---|---|---|---|---|
| H-1 | `web/src/lib/voice/socket.ts` (open handler, new `settleEnd`) | The open handler checks the end latch first. When `end()` already ran, it skips `session.update`, marks the take ended so a later `end()` resolves at once, resolves every waiting end promise, and closes the transport. No provider session starts, so no `session.end` can go missing and no waiter pends. | New test `settles a pre-open end without starting a session` in `web/src/lib/voice/socket.test.ts`: calls `end()` before `open()`, opens, races the promise against a 2 s liveness bound, then expects no `session.update` and no `session.end` in the sent log, one transport close, and an at once late `end()`. Passes. Restoring the old open handler makes it fail (verified by stashing the `socket.ts` fix and rerunning: 1 failed). | Remove the latch check in the open handler, so it sends `session.update` unconditionally. The new test then sees `session.update` with no `session.end` and its race rejects. |

## Prior pins intact

All 10 pre-existing socket tests pass unchanged (11 total with the new one). The full `./tools/check.sh` passes in this worktree after `npm ci`, including `svelte-check`, `eslint`, `vitest`, `vite build`, the Playwright mock suite, the default end to end suite, Go vet, staticcheck, `go test -race`, and the runes, consumer name, and plan reference scans. Exactly once `session.end` on every post open path, late `end()` after `session.ended`, and single close on double pagehide all still hold, since the normal open path and the `session.ended` route are untouched.
