# T1.2 review round 1

Reviewed hash: `6ead8a8b7f522e0de58e6712f5e0fb2f7bd21fc1`.
Worktree: `/home/nryn/work/reprise-wt/t1.2-rev-r1` (detached HEAD, confirmed `git log -1 --format=%H` equals the handed hash).
Diff base: `d1428a9`. Owns: `internal/episode/lifecycle.go`, `internal/episode/lifecycle_test.go`. The diff touches only those two files.

## Findings

No findings. Counts: C 0, H 0, M 0, L 0.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| None | None | None | None | None |

## Verification performed in this worktree

* `go test -race -count=1 -v ./internal/episode/...` passes. All 9 tests pass, including the 36 pair table test and the concurrent mark done test.
* `go test -race -count=3 -run 'TestConcurrentMarkDoneStartsOneRender|TestTransitionTable'` passes three times in a row. The single winner property holds under repetition.
* `go test -race ./...` passes across the whole tree.
* `go vet ./...` passes. `gofmt -l internal/episode/` reports nothing.
* `git grep` for plan references (`T<N>.<M>`, `P<N>`, planning file names, `§`) over the owned paths reports nothing. No `**T`, `interface{}`, `panic`, `log.Fatal`, or naked return exists in the owned paths.
* The full `./tools/check.sh` web leg (`npm ci`, svelte check, build, playwright) was not run. The diff touches only Go files, the Go legs all pass here, and the handoff records a passing gate in the implementation worktree.

## Spec conformance checked by reading

* The `allowed` table matches the spec table exactly. `recording` reaches `draft` and `failed`. `draft` reaches `rendering` and `failed`. `rendering` reaches `analysing` and `failed`. `analysing` reaches `ready` and `failed`. `ready` reaches only `failed`. `failed` reaches the four working states plus itself, and `Retry` further refuses `ready`, `failed`, and unknown targets. `Begin` inserts `recording` with `private` visibility.
* Every move is one SQL update guarded on the current state, except `Fail` whose update needs no guard because any state may fail. A repeated call is harmless in each path.
* `Fail` keeps stems. The test seeds a stem, fails the episode, and counts the stem afterward.
* `MarkInterrupted` moves the four working states to `failed`, leaves `ready` untouched with `ErrIllegalTransition`, treats a late interrupt on `failed` as success, and reports a missing id as `ErrNotFound`. No path silently reruns paid work.
* Error style follows the repo rules. One sentinel per condition, wrapped with `%w`. No nil value accompanies a nil error. `ctx` comes first on every I/O function. Every exported identifier carries a doc comment starting with its name.

## The ten questions

1. Does any path spend money without reserving budget first? No. This task moves episode rows only. It starts no provider call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This task holds no session handle.
3. Can a private episode, stem or transcript reach anyone but its owner? No. New rows start private, and this task serves no bytes.
4. Does any edit apply without a revertible record? No. State moves are not content edits, and each move is a guarded update.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. This task produces no speech or counts.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep over the owned paths finds no such reference, and comments are clean.
7. Did this task rebuild something Keel or Chaaya provides? No. It builds on `keel/sqlite` for storage and `keel/id` for the share token. It ports no library behaviour.
8. Was this review run on the handed hash, in a worktree of its own? Yes. The worktree is detached at the handed hash, confirmed by `git log -1`, and this review commits nothing.
9. Can any paid job kind resume after a restart and spend twice? No. Restart recovery goes through `MarkInterrupted`, which parks the episode in `failed` and waits for an explicit user retry.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The concurrency test uses a start barrier with no sleep and no timeout. All assertions compare exact stored states.

## Verdict: APPROVE

Zero findings across all severities. This is round 1, so there are no prior rounds and zero residue holds vacuously. The shipped tests pin every spec claim, and each pin passed in this worktree under `-race`.
