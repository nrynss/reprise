# T7.29 remediation, round 1

- **Reviewed hash:** `9892c64652e75b629366afac456726dd050129d9`
- **Worktree:** `/Users/narayan/reprise-wt/t7.29-rem-r1`, branch `task/t7.29-rem-r1`
- **Final hash:** `813cacae29384028190134e2050cf2d2fca33f8e`
- **Commits on the reviewed hash, oldest first:**
  1. `50ab0a9` Scope the render link through its episode (finding 2)
  2. `72df445` Render every stuck episode in turn (finding 1)
  3. `feb9c94` Drop settled pass outcomes once an episode leaves analysing (finding 4)
  4. `813caca` Test the real boot wiring (finding 3)

No amend and no rebase. Nothing outside the findings changed. No lambo server exists here.

## Findings

| # | Sev | Fix | Commit | Pin | Mutation check |
|---|---|---|---|---|---|
| 1 | H | Keel `StartKind` never queues. It returns `job.ErrLimit` at once, and only resumed attempts queue. So the binary retries. `advanceAll` now calls `startWaitingRenders` first. That walks every `rendering` episode oldest first. Under the schedule lock, it starts a render only for an episode still `rendering` with no render job at all. A refused start stops the walk, and the next advance (boot, then every tick) tries again. `recoverRender` no longer starts a render itself. It leaves the never-started case to the advance. A new race opened between mark done and the sweep, so `StartEpisodeKind` for the render kind now takes the schedule lock and answers with the running render for that episode, if any. | `72df445` | `TestEveryStuckRenderShipsOneAtATime` (three stuck episodes, ticks `advanceAll` by hand until all ship, then asserts one render and one analysis job each). `TestMarkDoneJoinsTheWaitingRender` pins the join. The reviewer probe `TestProbeRecoverRendersEveryStuckEpisode` passes. | Deleting `j.startWaitingRenders(ctx)` from `advanceAll` fails the first test (no episode ever ships). Replacing the join condition `last.Found && !jobTerminal(last.Status)` with `false` fails the second (a second render starts). |
| 2 | M | `rendered_sources` drops its `owner_id` column, and `ReplaceWords` no longer writes it. The link names only the episode and the render, and both cascade. The episode carries the owner, so `retention.Keep` needs no change for this row. `0001` was edited in place, since it has not landed. `ReplaceWords` keeps its signature, because the words rows still take the owner. | `50ab0a9` | `TestKeptEpisodeLeavesTheGuestDeletable` in `cmd/reprise/finish_test.go` stores words through `ReplaceWords`, runs the real `retention.Keep`, checks the link still names its render, then runs the sweep's guest delete. | On the reviewed schema (owner column back, fix reverted) it fails with `FOREIGN KEY constraint failed (787)`. The reviewer probe `TestProbeKeepLeavesRenderLinkOnTheGuest` now fails at its first read, `no such column: owner_id`, because it reads the column the fix removed. Its delete half is what the new test pins. |
| 3 | M | `wireAPI` takes a `bootPlan` with the features and the advance period. `main` passes `features()` and the default period. The four lines the script mutates stay as they were. `jobs.advancePeriod` feeds the loop ticker. New `cmd/reprise/boot_test.go` loads a real settings file (file secrets, a generated service account key whose token URI refuses locally) and calls `wireAPI` with a probe feature. It asserts the mount hook ran once with a full wiring and its route answers, `rendered_sources` exists, and a planted shippable episode reaches `ready` through the loop. A second boot with a feature reusing `render` must refuse with `errDuplicateKind`. | `813caca` | `TestBootWiresEveryHook` | `wire_mutation.sh` from the worktree root prints `killed` for all four mutations and exits 0. Run by hand, each fails the new test itself: mount calls = 0, no such table `rendered_sources`, clashing boot error = nil, and the loop never ships the episode (fails at its hang guard). |
| 4 | L | `advance` now calls `forgetSettled` for the episode after `MarkReady`, and whenever it finds the episode gone or past `analysing`. So the map holds entries only for analysing episodes. | `feb9c94` | `TestSettledOutcomesDrainOnShip` ships one episode, advances again, and requires `len(j.settled) == 0`. The reviewer probe `TestProbeSettledMapDrains` passes. | Making `forgetSettled` return at once fails the test with the leftover outcomes. |

## Contract changes

- None outside the owned paths. `wireAPI` gains a `bootPlan` parameter, which is internal to `cmd/reprise`.
- `analysis.ReplaceWords` keeps its signature. The `rendered_sources` table loses `owner_id`. T7.31's note stands: a seeded copy that adds a render row adds its link row too, now with just `(episode_id, render_id)`.
- The round 1 probe file needs its `TestProbeKeepLeavesRenderLinkOnTheGuest` owner read dropped to run against this hash.
- At runtime, a mark done that meets a busy render slot still fails its episode with `ErrStart`, as at the reviewed hash. Nobody found that, so it is unchanged.

## Gate

`dev-diary/probes/go.mod` (`module probes`, `go 1.27.1`) was created untracked before the gate and removed afterwards. `./tools/check.sh` exits 1 at `FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`, the accepted issue. Every later step ran through `dev-diary/probes/t7.34-rev-r2/gate.sh` from the worktree root, on `813caca`.

| Step | Exit |
|---|---|
| `cd web && npm ci` | 0 |
| `gofmt -l cmd` | 0, no output |
| `go list ./...` (web filtered) | 0, 22 product packages |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| svelte 4 leakage `git grep` | 1, empty (not even the known `draft.ts` hit) |
| consumer names `git grep` | 1, empty |
| plan references `git grep` | 1, empty |
| `go test -race ./...` | 0 |
| rebuild, then `playwright test -c src/routes/gallery.playwright.config.ts` | 0, 8 passed |
| rebuild, then `playwright test -c src/routes/threads/threads.playwright.config.ts` | 0, 10 passed |
| `wire_mutation.sh` | 0, four `killed` |
