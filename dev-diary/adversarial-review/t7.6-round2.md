# T7.6 round 2 review

Reviewed hash: `b8dcf6ae854312c46575db077fac55a4f47dae90`
Worktree: `/home/nryn/work/reprise-wt/t7.6-rev-r2` (detached HEAD, confirmed with
`git log -1`; tree clean). Code judged: `04bb0e6` (the tip `b8dcf6a` is the
orchestrator-owned remediation record, paperwork only, excluded from the code
verdict). Diff judged: `git diff 39f90382fdcbd94f9a47928f5527fbd3bb512168..04bb0e6`
(6 files: `cmd/reprise/draft_schedule_test.go`, `cmd/reprise/main.go`,
`internal/episode/complete.go`, `internal/episode/complete_test.go`,
`internal/store/migrations/0002_stems_pair_unique.sql`,
`internal/store/store_test.go`). No code committed by this reviewer. No probe
files remain in the tree.

## Ten questions

1. Does any path spend money without reserving budget first? No. No new
   spending path exists. The stamp writes job progress rows only, and the
   schedule paths still call the existing runs that reserve first.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Session handling is untouched by this diff.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Ownership checks are untouched by this diff.
4. Does any edit apply without a revertible record? No edits apply here.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? Untouched.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Grepped the six files for task ids, phase ids, and
   planning file names, clean. The gate plan-reference scan passed.
7. Did this task rebuild something Keel or Chaaya provides? No. The fix
   uses the Keel job store and runner only.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes, stated above. The paperwork tip was excluded by hash, not by trust.
9. Can any paid job kind resume after a restart and spend twice? No.
   Both edit kinds stay non-idempotent with no resume func. The stamp runs
   synchronously under `schedMu` inside the start, so a listed job always
   carries its episode before the lock releases. A stamp failure cancels
   the fresh job and returns the error, so no silent job keeps running.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The pins use generated sine audio, and
    `waitDescribed` polls async job reports with a deadline only and
    asserts nothing about timing.

## Verification runs

- Gate: `npm ci` in `web/`, then `./tools/check.sh` reports OK all
  checks passed, in this worktree at the handed hash.
- `python3 dev-diary/tools/audit_docs.py` reports OK planning tree is
  consistent.
- M1 committed pins plus M2 pin, `-race -count=10`, all pass, zero flakes:
  `TestSilentTranscriptJobNeverBlocksAnotherEpisode`,
  `TestTranscriptJobStillCoversItsOwnEpisode`,
  `TestSilentEditorialJobNeverBlocksAnotherEpisode`,
  `TestDescribedEditorialJobStillCoversItsEpisode`,
  `TestFullEditorialQueueAnswersRetry`.
- M3 committed pin, `-race -count=10`, passes: exactly one pair every run.
- No throwaway probe was needed. The committed pins already shape each
  round 1 pin exactly, and each passed repeated runs here. Nothing was
  created, so nothing needed deleting.
- `go vet` on the three touched packages is clean.

## Residue, severity by severity

- M1 (silent job wedges every episode): zero residue. `unfinishedEpisode`
  no longer reports `unknown`, silent records are skipped, and every start
  stamps its episode under `schedMu` before returning. The silent pins
  schedule beside the silent job, and the cover pins still refuse a second
  start for the same episode. Both halves passed `-count=10` here.
- M2 (editorial full queue answers 500): zero residue. The editorial
  branch now mirrors the transcript branch line for line: `job.ErrLimit`
  answers 429 `pipeline_busy`, every other error still answers 500
  internal. The committed pin proves the 429 path, and the unchanged else
  branch keeps the 500 path by construction.
- M3 (concurrent completions store orphan stem rows): zero residue.
  `linkStems` inserts each role with `ON CONFLICT(episode_id, role) DO
  NOTHING` under the new pair index, and the migration dedupes first, so
  databases that already hold orphans converge too. The eight-racer pin
  holds exactly one pair across `-count=10` here.
- Round 1 OUT_OF_SCOPE (no browser caller posts to the new route): still
  accurate and still non-blocking. It never gated APPROVE and needs no
  re-pin.

## Owns ruling

The handed owns line reads `cmd/reprise/main.go, internal/episode/`. The
M3 fix adds `internal/store/migrations/0002_stems_pair_unique.sql` and
updates one ledger assertion in `internal/store/store_test.go`, both
outside that line. Ruling: widen the owns line by decision to cover those
two paths, for these reasons. Round 1 finding M3 explicitly cited the
schema path, so the review itself directed the fix there. The cited file
`0001_schema.sql` is already landed on `main`, so editing it in place is
forbidden and a new migration is the smallest correct shape. The
`store_test.go` change is a forced mechanical consequence, because the
ledger test enumerates migration files and any new migration breaks its
count. No other file outside owns changed, and the migration carries no
plan reference. The new test file `cmd/reprise/draft_schedule_test.go`
sits beside the owned `main.go` and holds only pins round 1 demanded, so
it falls under the same decision. The orchestrator records the widened
line, since only it edits phase files.

## Findings

None. No new defect was found in the judged diff, and every round 1
finding verifies closed above.

## Verdict

APPROVE. Counts: C 0, H 0, M 0, L 0, plus one carried non-blocking
OUT_OF_SCOPE. Zero residue against round 1, severity by severity, as
stated above.
