TestFixtureSessions now measures each recorded session through measureRealSession and fails past 40 ms of drift. Locator pins and detector gap logs stay as they were. With no clips.json in testdata the suite blocks loudly naming the record, and a transient probe on real steady audio plus a start record measured with zero drift before removal.
