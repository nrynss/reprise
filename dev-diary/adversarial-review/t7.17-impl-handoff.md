# T7.17 implementer handoff

Commit `29c3d29e983570570a0332e174d13067b02484fa` on branch `task/t7.17`
in worktree `t7.17-impl`. Base is main at `9d5f5fe`. Gate passes in
the worktree. Only `web/src/lib/voice/` changes, plus the usual
status revert for review to flag.

## What it built

The socket reads the live `data` key on reply audio. Both new
pinning tests failed before the fix with zero host audio delivered,
proving the empty stem. The old `audio` key still passes, so
transition builds keep working. A scratch probe showed full scale
audio loses 1 LSB through the float path, so the byte exact pin uses
a moderate signal that round trips exactly.
