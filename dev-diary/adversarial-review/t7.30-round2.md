# T7.30 round 2 review

Reviewed hash: `9dcf009f6dcabc5367178816b4f751b8bbfc4443`
Worktree: `/home/nryn/work/reprise-wt/t7.30-rev-r2` (detached HEAD, `git log -1` confirms the handed hash, tree clean at open and at close)
Base: `de367d2`. Prior reviewed hash: `3f37fec4428f6aa38d81e5a4c1465179b0a17dfd`. The remediation is one commit on the reviewed hash, no rebase, no amend (`git log` between the two shows exactly that one commit).
Task text: `dev-diary/PHASE-7-production-issues.md` T7.30 block. Prior round files: `dev-diary/adversarial-review/t7.30-round1.md`, `dev-diary/adversarial-review/t7.30-remediation-round1.md`, `dev-diary/adversarial-review/t7.31-round1.md`. All three read.
Lambo: no lambo tools exist in this environment, so nothing was recalled or recorded outside git.
Reviewer never implemented or remediated this task, wrote no code, and commits nothing.

## Scope check

The remediation diff (`git diff 3f37fec..9dcf009`) touches six paths: `internal/privacy/erase.go`, `internal/privacy/seeded_erase_test.go`, `internal/retention/sweep.go`, `internal/retention/seeded_sweep_test.go`, `web/src/routes/episode/[id]/+page.svelte`, `web/src/routes/threads/threads.spec.ts`. The first five sit inside `owns` (the page was widened into scope this round by dispatch). The sixth names no new source path: it only appends the live-controls walk to the existing threads spec as the pin vehicle for the M fix, the same reading round 1 applied when it counted the implementation's `threads.test.ts` change as inside `owns`. No behavior-bearing edit lands outside `owns`. The full task diff against `de367d2` adds nothing beyond these plus the already reviewed implementation files.

## Gate record (all run in the review worktree)

`./tools/check.sh` stops at the standing workstation exception (`ffmpeg is 9.0.2, not the pinned 9.0.1`), same as round 1 and the remediation. Every later step ran by hand from the worktree root. `npm ci` ran in `web/` first. `svelte-check` needed `npx svelte-kit sync` first (missing generated tsconfig is a fresh-checkout artifact, not a defect). Playwright ran with the default browser (Chromium). WebKit cannot launch on this host, CI must cover it.

| Step | Exit |
|---|---|
| `gofmt -l cmd internal` | 0, no output |
| `go vet ./cmd/... ./internal/...` | 0 |
| `go test -race ./cmd/... ./internal/...` (full) | 0, all packages ok |
| `npm ci` in `web/` | 0 |
| `npx svelte-kit sync`, `npm run check` | 0, 0 errors, 0 warnings |
| `npm run lint` | 0 |
| `npm run build` | 0 |
| `npm run test -- --run src/routes/threads/threads.test.ts` | 0, 32 passed |
| Playwright threads config | 0, 11 passed (fixture release states plus the new live walk) |
| Playwright share config | 0, 3 passed |
| Playwright episode edit config | 0, 5 passed |
| svelte 4 leakage scan over touched web files | empty |
| plan reference scan over all touched files | empty |
| commit message | clean (`Keep shared catalog audio on seeded copy erase`) |

staticcheck is not installed here, so that step was not re-run. `go vet` plus the full race suite passed.

## Fix 1 re-verified: round-1 M, live episodes render no publish or erase controls

- Probe re-run on the fixed tree: the first `{#if !snap.live}` block is lines 121-123 (fixture link line only). Control hits (`publishState`, `controller?.erase`, `<h2>Release</h2>`) sit at lines 119, 124, 126, 132, all outside the block. Exit 0.
- Mutation: with the unpatched page checked out, the guard block is lines 118-135 and all four hits sit inside it, zero outside. Exit 1. The probe measures exactly this defect.
- Fixture DOM unchanged: the fixture release-states walk (`threads.spec.ts:252`) passes in the same 11-test run, and the fixed section keeps fixture button order and text (Publish, Revoke, Export, Erase) with only the fixture link line and the export button still guarded.
- New live walk (`threads.spec.ts:265`) passes on the fixed build (11 of 11 in the threads config) and fails on a rebuilt unpatched build at the Publish click, because no Publish button renders for the live row. Load-bearing.

## Fix 2 re-verified: T7.31 OOS H, seeded copy erase deletes the shared catalog audio

- `TestEraseKeepsSharedCatalogAudio` passes with `-race -count=2`. `TestSweepKeepsSharedCatalogAudio` passes with `-race -count=2`.
- Mutation: with only the two source fixes reverted to the reviewed hash (tests untouched), both fail with `media holds 0 catalog blob rows, want the shared audio to survive`. Restored after, tree clean, handed hash confirmed, both pass again.
- No other behavior changed: the full race suite passes, so recorded-episode erasure still removes every copy (`TestPrivacyEraseRemovesEveryCopy` asserts no episode row, no media row, no media file, no cover file, deleted provider session plus transcript), the sweep still expires idle recorded guests, and share open and revoke are unchanged (share Playwright config 3 passed).

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| none | none | none | none | none |

OUT_OF_SCOPE defects: none found. No real defect outside `owns` was observed.

## The ten review questions

1. Does any path spend money without reserving budget first? No. The seeded branches delete local rows only. Publish, revoke, and erase remain DB plus deletion calls with no metered session or generation call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. Seeded copies own no provider session, so skipping the fan-out for them strands nothing. Recorded erasure still ends the provider session.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The Release section change only renders owner controls the routes already gated. Share resolution is untouched.
4. Does any edit apply without a revertible record? Not applicable beyond round 1. The seeded drop is an explicit deletion through the same erasure and sweep paths, not an editorial edit.
5. Does any spoken callback or count come from the prompt instead of a stored row? Not applicable. Nothing here prompts a model.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep over every touched source, test, and spec file is empty, and the commit message is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. The fixes use the existing store, job, and media plumbing. No library workaround.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` shows `9dcf009f6dcabc5367178816b4f751b8bbfc4443` in `/home/nryn/work/reprise-wt/t7.30-rev-r2` (detached), the tree is clean, and every pin ran there. Temporary mutation checkouts were fully restored. Nothing was committed.
9. Can any paid job kind resume after a restart and spend twice? No. The seeded drop repeats safely (a vanished row reads as done, a row owned elsewhere errors instead of double deleting). No paid call was added.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The new Go tests run against fixtures with fail-loud job waits, and the browser walk stubs every route.

## Residue

Round-1 M (live episodes render no publish or erase controls): resolved. The probe passes on the fixed tree and fails unpatched, and the new live walk passes on the fixed build and fails on a rebuilt unpatched build.
T7.31 OUT_OF_SCOPE H (seeded copy erase deletes the shared catalog audio): resolved. Both new tests pass with race and fail with the exact defect signature when only the source fixes are reverted.
Severity by severity: zero C residue, zero H residue, zero M residue, zero L residue.

## Counts

C: 0. H: 0. M: 0. L: 0. OUT_OF_SCOPE: 0.

APPROVE
