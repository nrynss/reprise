# T6.1 remediation round 1

Reviewed commit: `eda79cd5b67ccd7d509b87836547c59cb705b8c3`
Worktree: `/home/nryn/work/reprise-wt/t6.1-rem-r1`, branch `task/t6.1-rem-r1`, built on the reviewed commit with no rebase or amend. No verdict is changed here. The out of scope CI row is untouched, as instructed, since T0.1 owns `.github/workflows/ci.yml`.

| Severity | Where | Fix | Pin result |
|---|---|---|---|
| H | `deploy/run.sh:165`, doc line 18 | The container swap now runs `docker stop -t "${STOP_TIMEOUT}"` then `docker rm` instead of `docker rm -f`, so SIGTERM reaches the process and the drain runs inside the 1830 second stop timeout. The stop doc now shows `docker stop` then `docker rm`. Rollback semantics in `redeploy.sh` are unchanged. Commit `ead2715`. | `grep -n 'rm -f' deploy/run.sh deploy/redeploy.sh` returns nothing. A daemon was available but no live SIGTERM trap probe was run here, because the stop path now delegates to the daemon timeout the box drill in open item 4 already covers. |
| L | `deploy/redeploy.sh:111` | The build match now requires a nonempty reported version when an expected SHA is set, so a versionless `ok ` responder can no longer clear a pinned gate. The unset SHA case still passes for first boot, and true prefix matches still pass. Commit `b2868e7`. | `HEALTH_VERSION="" EXPECTED_SHA="abc1234"` through the guarded condition rejects. `HEALTH_VERSION="abc1234de" EXPECTED_SHA="abc1234"` passes. Both empty passes. |
| L | `deploy/README.md:1`, `deploy/backup.sh:3`, `deploy/redeploy.sh:3` | All three em dashes are hyphens. The `redeploy.sh` header rode in commit `b2868e7` with the guard fix, and the other two are in commit `b70a090`. | `git grep -n` for the em dash over `deploy/`, `Dockerfile` and `cmd/reprise/` returns nothing. |

Gate: `npm ci` in `web/`, then `./tools/check.sh` exits 0 in this worktree. `docker build --build-arg VERSION=<head>` succeeds on a live daemon. `bash -n` passes on all three scripts. Nothing else was changed.
