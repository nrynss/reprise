# T6.1b round 2 review

Reviewed hash: `4ca528d4ecc6693970baf0516b7d301d74c8cd09`
Worktree: `/home/nryn/work/reprise-wt/t6.1b-rev-r2` (detached, confirmed with `git log -1`)
Base: `5d33a24`
Reviewed commit: `2211a19a5e111e2fbd6bbb4df2c9a945ab409187`
Remediation diff: `git diff 2211a19a5e111e2fbd6bbb4df2c9a945ab409187..4ca528d4ecc6693970baf0516b7d301d74c8cd09`
Base record diff: `git diff 5d33a24..4ca528d4ecc6693970baf0516b7d301d74c8cd09`
Scope: `dev-diary/probes/production.md` only, per `owns` in live `dev-diary/PHASE-6-ship.md` T6.1b block.

## What the remediation does

One line. It restores the orchestrator owned T6.1b status line in
`dev-diary/PHASE-6-ship.md` from `not-started` to
`in-progress:implement:t6.1b-impl`. The owned record in
`dev-diary/probes/production.md` is byte identical to the round 1 commit.
No code ships. No production state changes.

## Evidence checks this reviewer ran

* `git log -1` returns the handed hash `4ca528d` in detached worktree `t6.1b-rev-r2`.
* `git diff --stat 2211a19..4ca528d` lists exactly one file, `dev-diary/PHASE-6-ship.md`, one insertion and one deletion.
* `git diff 2211a19..4ca528d -- dev-diary/probes/production.md | wc -l` returns 0. The record is untouched.
* `git diff --name-only 5d33a24..4ca528d` lists exactly one file, `dev-diary/probes/production.md` (267 lines). The owns violation is gone.
* `git diff 5d33a24 HEAD -- dev-diary/PHASE-6-ship.md | wc -l` returns 0. The status line matches the base.
* `dev-diary/tools/audit_docs.py` prints `OK planning tree is consistent`, exit 0.
* Secret scan of the record finds no key material. Token and cookie values are redacted. Exit 1 on the key pattern grep means no match.
* Plan-reference scan of the record finds no task id, phase id, or planning file citation.
* Doc style holds. The one semicolon match is inside pasted `content-type` output. The one `--` match is a curl flag in a command block. No em dashes. No sentence runs past 30 words outside quoted output.
* Spot check 1 (read-only): `curl https://reprise.nryn.dev/healthz` answers `ok 1ec2ef2bd41f39186df5bf935afc2afd version=5d33a24...` and `/` answers 200. Both match the recorded edge identity exactly.
* Spot check 2 (read-only): anon `GET /media/dd231fc014f56515f6282f77d5c2e998` answers 404 with body `404 page not found` and no `Cache-Control` header. This reproduces the record mixed verdict on check 3 independently.
* No sessions were minted, no switches were flipped, and nothing was uploaded by this reviewer. Both spot checks were anonymous reads.
* `git status --short` is clean. Nothing uncommitted was reviewed.
* Live `dev-diary/PHASE-7-production-issues.md` exists, requires T6.1b, and states its goal is to close every gap the first verification measured. Its round 2 task owns `dev-diary/probes/production.md` for the re-measure. The round 1 out of scope rows now belong there.

## Residue against round 1, severity by severity

Round 1 in-scope: C 0, H 1, M 0, L 0. Round 1 out of scope (non-blocking): C 0, H 4, M 2, L 0.

* C residue: zero. Round 1 recorded no C finding. This round records none. No demo break, lost recording, spend leak, or private episode exposure appears in the remediation diff or the record diff.
* H residue: zero. The single in-scope H owns violation (`PHASE-6-ship.md` status revert) reads fixed. The base diff now names only the owned record file. The remediation diff touches only the status line and restores the base value. The four round 1 OUT_OF_SCOPE H rows (unwired episode and thread routes, uploader owner mismatch, missing Gemini path, unwired settle) stay non-blocking by rule. They now belong to phase 7, which requires T6.1b and owns the round 2 re-measure. None was re-fixed here and none blocks approval.
* M residue: zero. The two round 1 OUT_OF_SCOPE M rows (admin pause and limits behind stub auth, missing `Cache-Control` on anon media refusals) stay non-blocking by rule and now belong to phase 7. The anon media spot check above reproduces the header half of the privacy row unchanged. No new M defect appears in either diff.
* L residue: zero. Round 1 recorded no L finding. The remediation note calls its own row L, but the row describes the same H owns violation fix, and the diff proves the fix. No polish defect appears in either diff.

## The ten review questions

1. Does any path spend money without reserving budget first? No. Both diffs are docs and status only. Reference round 1: the recorded live run held budget before connecting.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. Reference round 1: the record shows explicit `session.end` with close reason and completed status.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The anon media read answers 404 with the same body as the owner read, and the record carries no secret values. The missing `Cache-Control` header half is a phase 7 row, non-blocking.
4. Does any edit apply without a revertible record? No. The change is git commits only. Nothing applies to production state.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Reference round 1: the record makes no spoken-claim assertion and defers the seeded greeting re-run per the task.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The scan finds none in the record. The status line change carries no citation.
7. Did this task rebuild something Keel or Chaaya provides? No. No code ships in either diff.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` returns `4ca528d` in `t6.1b-rev-r2`, detached. No code was committed.
9. Can any paid job kind resume after a restart and spend twice? No. No job kinds are touched. Reference round 1: the one live session ended explicitly.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. All speech is generated. Both spot checks here were anonymous reads.

## In-scope findings

None.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| None | None | None | None | None |

## Out-of-scope production defects (non-blocking, carried from round 1)

Recorded honestly in `dev-diary/probes/production.md` with command and output. Now owned by phase 7. None blocks approval. No change was made to any of them here.

| Severity | Where | What | Pin | Mutation | Owning path |
|---|---|---|---|---|---|
| H (OUT_OF_SCOPE) | record lines 82-104 | Episode, thread, decision, done, and publish routes answer 501, so no render, analysis, or gallery exists on the deploy. | Recorded `curl` answers 501 `not_implemented`, plus `internal/api/routes.go:213` `handleStub`. | Add a case for `/api/episodes` in `handlerFor` returning nil again. | Phase 7 owns the follow-up. Wiring lives outside this task. |
| H (OUT_OF_SCOPE) | record lines 151-165 | The uploader opens blobs with literal owner `guest` while the authorizer compares the resolved user id, so the uploader reads 404 on its own blob. | Recorded owner-cookie read answers the same 404 as the anon read. | Revert the open call to send literal `guest` as owner. | Phase 7 owns the follow-up. |
| M (OUT_OF_SCOPE) | record lines 167-194 | Admin pause and limits answer 403 behind stub auth, so the kill switch cannot be exercised live. | Recorded `POST /api/admin/limits/pause` answers 403 `owner_required`. | Restore stub auth after wiring the owner login. | Phase 7 owns the follow-up. |
| H (OUT_OF_SCOPE) | record lines 196-209 | No Gemini path exists in the deployed binary, so the box-to-Gemini check is blocked. | `grep` for Gemini wiring in `cmd/reprise/main.go` returns nothing. | Remove the Gemini wiring after it lands. | Phase 7 owns the follow-up. |
| H (OUT_OF_SCOPE) | record lines 240-253 | Settle is unwired, so each mint holds about 2.25 dollars with no release path. | Recorded `/end` 501 plus reconciler with no caller. | Disconnect the reconciler caller after wiring it. | Phase 7 owns the follow-up. |
| M (OUT_OF_SCOPE) | record lines 138-160 | Anon media refusals carry no `Cache-Control: private, no-store`. | This review anon read returns 404 with no `Cache-Control` header. | Strip the header from refusals after adding it. | Phase 7 owns the follow-up. |

## Counts

In-scope: C 0, H 0, M 0, L 0.
Out-of-scope (non-blocking): C 0, H 4, M 2, L 0.

## Verdict

APPROVE. Zero residue against round 1, severity by severity. The one in-scope H owns violation reads fixed: the base diff names only `dev-diary/probes/production.md`. The out of scope production rows stay non-blocking and now belong to phase 7.
