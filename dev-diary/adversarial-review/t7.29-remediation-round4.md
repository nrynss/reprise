# T7.29 remediation, round 4

- **Reviewed hash:** `680102b53dfd02f344e37dc66fb5b6ffcc22cd42`
- **Worktree:** `/Users/narayan/reprise-wt/t7.29-rem-r4`, branch `task/t7.29-rem-r4`
- **Final hash:** `1e3c691dafa4b6ba2442b4b6ccf9a93c58a852b8`
- **Commit:** `1e3c691 Return failed queued renders to retryable state`

No amend and no rebase. No lambo server exists here.

## Finding

| # | Sev | Fix | Commit | Pin | Mutation check |
|---|---|---|---|---|---|
| 1 | M | `startWaitingRender` now distinguishes `job.ErrLimit` from every other start failure while it still holds `schedMu`. Capacity leaves the episode in `rendering` for the next scheduler pass. A create or linkage failure moves it from `rendering` to `failed`, so the owner can use the explicit retry route again. | `1e3c691` | `TestRetryFailedRenderStartReturnsToFailed` begins with a real 503 through the done route, retries through that route, then refuses the queued render's linkage write. It requires the stored state to return to `failed`. | I removed the transition that follows the non-capacity start error. The pin failed with `state after failed queued start = rendering, want failed`, which reproduces the permanent empty queue. |

The pin uses the real runner and durable fixture database. Its store wrapper makes only the scheduler's linkage write fail. The runner still creates and cancels the job, so the test measures the stored lifecycle state rather than a scheduler log.

## Verification

| Step | Exit |
|---|---:|
| Focused mutation pin with the fix | 0 |
| Focused mutation pin with the transition removed | 1, expected |
| `go test -race ./cmd/reprise ./internal/episode ./internal/api` | 0 |
| `go test -race ./cmd/reprise -run 'TestRetryFailedRenderStartReturnsToFailed|TestEveryStuckRenderShipsOneAtATime|TestMarkDoneWhileRenderBusyWaitsAndShips' -count=1` | 0 |
| `cd web && npm ci` | 0 |
| `./tools/check.sh` | 1 only because ffmpeg is 9.0.2 and the pin is 9.0.1 |
| Remaining gate steps | 0: format, list, vet, staticcheck, race tests, Svelte check, lint, Vitest, build, e2e, source scans and full race tests |

The temporary probe module used to keep review probes out of the product Go module was removed before this record. The established ffmpeg 9.0.2 versus 9.0.1 pin exception remains the only gate exception.
