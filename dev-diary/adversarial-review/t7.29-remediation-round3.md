# T7.29 remediation, round 3

- **Reviewed hash:** `6ece425710d2e6f7be4da00ef66533de340c68f8`
- **Remediation worktree:** `/Users/narayan/reprise-wt/t7.29-rem-r3`
- **Commit:** `680102b53dfd02f344e37dc66fb5b6ffcc22cd42`

| Finding | Fix | Pin and verification |
|---|---|---|
| M1, queued live render reads as a fixture | The editor reads `queued` from the accepted response and names the queued render without opening a job stream. | Round 3 editor probe passes. `npm run test`, `npm run check`, `npm run lint`, and `npm run build` pass. |
| M2, refused live render reads as running | A fixture refusal still uses the fixture. A live refusal now reports a stopped render request and points to the gallery. | Round 3 editor probe passes. |
| M3, start-failure retry dead-ends | A failed render returns to the rendering waiting set on an explicit repeat. The scheduler starts a new render after that owner action, including after a terminal failed render. This preserves the requirement that a start fault does not strand the episode. | Round 3 refusal-retry probe passes. `go test -count=1 ./internal/episode ./internal/api ./cmd/reprise` passes. |
| L1, queued card polls a refused detail forever | A refused detail removes its row from the queued recheck set. | Round 3 gallery probe passes. Gallery Playwright suite passes with 8 tests. Threads Playwright suite passes with 10 tests. |

`./tools/check.sh` stops only at the accepted local tool mismatch: ffmpeg is 9.0.2 while the repository pins 9.0.1. With a temporary `dev-diary/probes/go.mod`, removed before this handoff, every later gate step passed: formatting, vet, staticcheck, race tests, Svelte sync, check, lint, unit tests, build, end-to-end tests, source scans, and full race tests.
