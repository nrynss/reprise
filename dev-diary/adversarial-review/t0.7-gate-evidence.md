# T0.7 gate evidence: three fresh-worktree passes

T0.7 round 1 approved the ffmpeg digest pin with a non-blocking L finding.
The gate change shipped without three fresh-worktree passes. This file
closes that finding with the three passes.

## Runs

Fresh detached worktree at `/home/nryn/work/reprise-wt/t0.8-gate-evidence`,
opened at HEAD `4ce30c48011f6593558d97ebdee51d71d2024d76`. The tracked tree
at that HEAD matches the T0.7 landing for every gate file. The HEAD commit
touches only the phase file.

Each pass ran `npm ci` once up front in `web/`, then `./tools/check.sh`
from the worktree root. The tool run below used the pinned build:

`ffmpeg version n9.0.1 Copyright (c) 2000-2026 the FFmpeg developers`

| Pass | Exit code |
|---|---|
| 1 | 0 |
| 2 | 0 |
| 3 | 0 |

All three passes print `OK all checks passed`. The full gate ran each time:
gofmt, go vet, staticcheck, `go test -race`, svelte-check, ESLint, Vitest,
Playwright, the Svelte 4 leakage guard, the consumer name scan, and the plan
reference scan. `npm ci` exited 0 before the first pass.

## Window

Start `2026-09-19T02:41:24Z`. End `2026-09-19T02:42:45Z`. The fresh worktree
was removed after the runs. Full per-pass logs stay outside the repository.
