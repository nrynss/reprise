# T5.3 round 1 review

Reviewed hash `3aba2f09f4e84ccac5ac2cc85fda9fe3ef6a4b87` in worktree `/home/nryn/work/reprise-wt/t5.3-rev-r1`. The worktree holds a detached HEAD at the handed hash. `git log -1 --format=%H` confirms the hash. `git status --short` is clean. This review commits nothing and reviews the handed commit only. The diff base is `55a4051` across two commits. Both commit messages carry no plan references.

## Scope

Declared owns are `internal/limits/` and `web/src/routes/admin/`. The diff touches eight files and all eight sit inside those paths. No other tracked path changes.

## Verdict

APPROVE. In-scope findings are zero across all severities. One OUT_OF_SCOPE row follows. It never blocks approval. No prior round exists, so residue is zero by construction.

## Counts

In-scope C: 0. In-scope H: 0. In-scope M: 0. In-scope L: 0. OUT_OF_SCOPE: 1 (H, owned by `internal/api` plus main wiring).

## In-scope findings

None. Every checklist item below passed on evidence run in this worktree.

## OUT_OF_SCOPE rows

| Severity | Where | What | Pin | Mutation | Owner path |
|---|---|---|---|---|---|
| H | `internal/api/routes.go` and `web/src/lib/api/types.ts` at the reviewed hash | The three admin patterns (`GET /api/admin/limits`, `POST /api/admin/limits/pause`, `POST /api/admin/limits/owner`) are served by `internal/limits` handler code, yet no route table entry and no browser mirror entry mounts them, so the served app answers the page fetch with 404 | `grep -c 'api/admin' internal/api/routes.go web/src/lib/api/types.ts` returns 0 for both files, and a routes test asserting the three patterns are present fails | Once the follow-up adds the mount, deleting those added lines reintroduces the 404 | `internal/api/` plus main wiring |

The handoff declares this gap loudly and names its owner. The implementer could not mount the routes without leaving its owns. The orchestrator should scope a follow-up for the mount.

## Verification run here

- `go test -race -count=1 ./internal/limits/` passes. All twelve test functions pass, including the switch alias test, the config validation table, the no-restart flip test, the quota both-sides test, the settled reservation spend tests for owner and global figures, the owner override test, and the four handler tests.
- `gofmt -l internal/limits/` is empty. `go vet ./internal/limits/` is clean. Exported identifiers carry doc comments that start with their names. No banned constructs appear. No naked returns appear outside void HTTP handlers.
- `npx vitest run src/routes/admin/limits.test.ts` passes all twelve cases after `npm ci` inside this worktree. `svelte-check` reports 0 errors and 0 warnings across 260 files. `eslint` on the two admin TypeScript files is clean.
- Plan reference scan over the eight new files is clean. No Svelte 4 idioms appear. The page component carries no type syntax. Commit messages carry no plan references.
- The broker contract was read in the main checkout as reference only. `CodePaused` is `sessions_paused`. `CodeQuota` is `guest_quota_reached`. `KillSwitch` is named `sessions_paused`. The broker provisions the owner ceiling through `SetLimit` on `cost.ErrUnknownOwner`.

## Scrutiny answers

(a) Quota code aliasing is correct. The landed broker answers `guest_quota_reached` and preflight consumers branch on it. Renaming the wire inside this task would break those consumers and would touch `internal/broker`, which this task does not own. The alias keeps both sides on one string, the alias test breaks on any drift, and the handoff records the task text discrepancy loudly for the product decision. No finding.

(b) The owner ceiling equality is documented, not pinned, and that is the correct call within these owns. Both `OwnerDefaultLimit` here and `OwnerSessionLimit` in the broker are wiring-time values, and settings carry no shared field for them, so no test inside this package can assert their future equality. The Config comment and the handoff state the MUST loudly. The in-package fallback behavior is pinned by the settled reservation tests. Silent divergence remains a wiring risk for the landing checklist, not a defect in this commit. No finding.

(c) Both sides are pinned despite the stub. The refusal leg is pinned by the stub denial test on every endpoint. The pass leg is pinned at handler level with a test-only allow auth seam and at page level with fixed-body parser and formatting tests. Production uses the stub by design until the open login decision lands, and the page renders the denied shell for exactly that code. The done criterion is met at every layer this task owns. No finding.

(d) Handler-level proof is sufficient here and the missing mount is follow-up scope. The handler serves correct JSON over real stores through real HTTP, and the page parses and formats that exact shape. Mounting needs `internal/api` and main wiring, both outside owns. Rule: record OUT_OF_SCOPE, never block APPROVE. Done above.

(e) Kill switch immediacy is pinned through the shared store. The flag is the broker declaration itself, not a copy. The name test pins `sessions_paused` on both sides. The flip test writes through the service and reads back through the broker declaration on the same store with no restart. Cross-instance store sharing is main wiring and outside owns. No finding.

(f) No cross-day history is claimed anywhere. The page states today-only twice. The spend type documents a daily figure with no history. No history UI exists. No finding.

(g) Retention is correctly absent. No delete, expiry, sweep, or retention code appears in the eight files. No finding.

(h) Frontend constraints hold. The page uses runes only, native buttons, a labelled form, section labels, and status and alert roles. Keyboard access follows from native controls. No finding.

## Observations (not findings)

- A process restart drops admin-set owner ceilings from the in-memory override map, so per-owner figures fall back to the default until the next edit. Enforcement is unaffected because the Keel store stays durable. The spec formula covers the in-process value and the done criteria never mention restart, so this stays an observation with a clear workaround. A future task may own durable ceiling memory.
- The TypeScript shapes mirror the Go handler by hand. Key-by-key comparison shows no current drift. Future drift across the language boundary would not fail any test, since each side tests its own copy. A future task may add one golden-body contract test.

## The ten questions

1. No path spends money without reserving budget first. This change adds no paid call. Tests reserve before they settle.
2. No path leaves an AssemblyAI session open. This change touches no session code.
3. No private episode, stem, or transcript reaches anyone but its owner. Every admin route sits behind owner auth and the stub denies all. JSON answers set `no-store` and carry no secrets.
4. No edit applies without a revertible record. The ceiling edit is an operator control, not a content edit, and the content edit model is untouched.
5. No spoken callback or count comes from the prompt. Spend figures derive from ledger headroom. Labels derive from fetched snapshots and fixed inputs.
6. No code, comment, test, or migration cites a phase, a task, or a planning file. The scan is clean.
7. This task rebuilds nothing Keel or Chaaya provides. Flags, keyed budgets, the cost store, and the wire envelope all come from the libraries.
8. Yes. This review ran on the handed hash in its own detached worktree, confirmed above, with no commits.
9. No paid job kind is added, so none can resume and spend twice.
10. No development check needs a person, a microphone, or a wall-clock threshold. Spend tests use fixed prices. Frontend tests use fixed bodies. Formatting uses fixed inputs.
