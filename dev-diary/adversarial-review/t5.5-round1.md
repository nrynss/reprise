# T5.5 round 1 review

Reviewed hash: `83a617d4b1524081043b02e293800db57f1f131c`
Worktree: `/home/nryn/work/reprise-wt/t5.5-rev-r1` (detached, confirmed by `git log -1`)
Base: `cc71a3b`. Diff read: `git diff cc71a3b..83a617d4b1524081043b02e293800db57f1f131c`
Scope: `web/src/routes/welcome/` only. Five new files, 634 insertions, no deletions.

## What the commit builds

A first visit screen. An empty catalog renders one record link to `/record`.
A seeded catalog (`?seed=1` or `?fixture=1`) shows a teaser section for episode
four, plays thirty seconds of generated tone through Chaaya `AudioPlayer` after
the first gesture, then offers the record link for episode five. Pure helpers
(`welcomeMode`, `formatClock`, `buildTone`, `encodeWavBytes`, `initialWelcome`)
carry the logic. `WelcomeController` owns the player and the ticker. Vitest pins
the helpers. Playwright pins the empty button, the two click flow, play after
gesture, and both library gates.

## Evidence this reviewer ran

- `npm ci` in `web/`, then `./tools/check.sh`: OK, all checks passed.
- `npx vitest run src/routes/welcome/welcome.test.ts`: 10 passed.
- Route Playwright on chromium: 4 passed (empty button, two click flow,
  play after gesture with `playing` true and source set, both gates).
- SSR probe: served the preview build with `curl`, no browser. The prerendered
  `/welcome` carries `<a id="welcome-record" href="./record">` with the text
  `Record your first episode`. No play button renders in the empty shell.
- WebKit: attempted, could not launch. Detail lives below.
- `python3 dev-diary/tools/audit_docs.py`: OK, planning tree is consistent.

## WebKit result (recorded, environment blocked)

The WebKit browser binary exists here (`webkit-2359`). Every WebKit test fails
at launch because the host lacks system libraries. Playwright names them:
`libicu74`, `libxml2`, `libflite1`. This box grants no sudo, so the reviewer
could not install them. Chromium proves the gesture unlock wiring (`playing`
true, source set, position readout live). The WebKit play after gesture proof
remains unverified. No code change inside `owns` can fix missing host
libraries, so no finding below carries this. The orchestrator should re-run
`welcome.spec.ts` under WebKit on a host with the libraries before it calls
the done clause fully pinned.

## Findings

None in scope. Counts: C 0, H 0, M 0, L 0.

## OUT_OF_SCOPE notes (do not block APPROVE)

- The catalog state comes from the query string and the teaser audio is a
  generated tone, not the latest seeded episode over the season list. The
  handoff discloses both. No list endpoint wires the seed copy into a fetch
  this route could call (`internal/api` names no seed copy trigger, and the
  T5.2 handoff log lists season-list Sync plus EnsureCopy as standing work).
  Fixture-ok is yes, so the fixture seam is legitimate. Owning paths for the
  real wiring: `internal/api/` and a later welcome wiring task.
- The seeded path flashes empty then seeded after mount, because prerender
  cannot read the query string. The handoff discloses this gallery pattern.
  The empty path shows no flash and no dead control. The record link stays a
  plain link in every state.

## The ten questions

1. Does any path spend money without reserving budget first? No. This route
   makes no network call and mints nothing.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. This route opens no session.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   The route serves static copy and generated tone. No user media flows.
4. Does any edit apply without a revertible record? No. Nothing here edits.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. The teaser lines are fixed display copy, never presented as a
   query result or a count.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The gate plan reference scan passes. Comments state
   their own reasons.
7. Did this task rebuild something Keel or Chaaya provides? No. Playback runs
   through Chaaya `AudioPlayer`. Gates run through Chaaya `testing`.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   `git log -1` prints the handed hash. All commands ran in the review
   worktree. No code was committed.
9. Can any paid job kind resume after a restart and spend twice? No. No job
   kind lives here.
10. Does any development check need a person, a microphone, or a wall-clock
   threshold? No. The gesture test clicks a rendered button and polls a state
   flag. It asserts no audio progress against a clock.

## Verdict

APPROVE with zero findings. One residual risk stands outside the commit: the
WebKit play after gesture proof could not run on this host. Chromium evidence
plus the shared player path supports landing, with a WebKit re-run where the
host libraries exist.
