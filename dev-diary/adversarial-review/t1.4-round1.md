# T1.4 round 1 review

Reviewed hash: `0d82641fd4f2067fea736c001f52dfae312f2e42`.
Worktree: `/home/nryn/work/reprise-wt/t1.4-rev-r1` (detached HEAD, `git log -1 --format=%H` matches the handed hash).
Diff read: `git diff d1428a9..0d82641fd4f2067fea736c001f52dfae312f2e42`, three files, all under `internal/identity/`.

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0. False positives recorded: 0.

## Findings

No findings. Every done-criterion pin below ran green in this worktree with `go test -race ./internal/identity/...` (10/10 pass), `go vet` clean, `gofmt` clean, full `go test -race ./...` green, and the `tools/check.sh` plan-reference scan over tracked files returns CLEAN.

Done-criteria coverage, each measured by a shipped test that ran here:

- Passes, mint: `TestFirstVisitMintsGuestAndSession` creates one `users` row (kind `guest`, fixed-clock stamps) plus one `guest_sessions` row in a single transaction, and the `Set-Cookie` carries only `sessionID.signature` with `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`.
- Passes, own reads: `TestCookieReadsOwnEpisode` gets 200 on its own episode. `TestMediaServesOwnerWithRange` gets 200 with the exact bytes, `Cache-Control: private, no-store` (set by Keel, asserted here), and a `Range: bytes=0-3` slice gets 206 with body `0123` and `Content-Range: bytes 0-3/16` (Keel `http.ServeContent`, asserted here).
- Refuses: `TestAnonymousGets404` gets 404 for another guest and for a cookieless request on both the episode reader and the media store. `TestRevokedSessionFailsNextRequest` reads 200 before revoke, then 404 on the next episode and media requests. `TestForgedAndUnknownCookiesFail` gets 404 on a tampered signature and on a deleted session row.
- Movement and hygiene: `TestResolveTouchesLastSeen` moves `last_seen_at` to the fixed clock. `TestOpenTwiceMigratesCleanly` reopens with no row change. `TestPublicBlobPassesAuthorizer` passes public blobs and refuses anonymous private ones. `TestNewRefusesBadConfig` refuses nil DB and empty key.

Adversarial checks applied and cleared:

- Cookie split uses `strings.Cut` on `.`. Safe, because `keel/id` ids are 32 lowercase hex characters and never contain a dot, and `verify` rejects anything failing `id.Valid` before any lookup.
- Signature uses HMAC-SHA256 compared with `hmac.Equal`. Forgery is pinned by test.
- `Owns` defaults to false when no middleware ran or the owner id is empty, so a handler that forgets the middleware refuses rather than leaks.
- `AuthorizeMedia` resolves from the request cookie on each call, so media stays guarded even where the middleware never ran. Private blobs persisted with an empty owner never match, so the safe default is refusal.
- Revocation is a single row update read on every request, so no cache stands between revoke and the next request. Pinned by test.
- `Middleware` mints a guest per anonymous request and discloses the quota note in the handoff. The task text orders this shape (first visit mints, every diary read checks ownership), and quota enforcement belongs to the later task that owns spend and quota state, so no finding here.
- `Secure` on the cookie plus the localhost note in the handoff matches the task text, which mandates `Secure`. No finding.
- Migration `0001_sessions.sql` references `users(id)` with no cascade clause. Cascade on episode delete is T1.1's property over diary content tables, and user erasure belongs to the later erasure task, so this lays no claim any reviewer must pin. No finding.
- Style: no `**T`, no `interface{}`, no `panic`, no env reads in the package (key arrives via `Config.SigningKey`), `ctx` first on I/O, zero values returned beside errors, one sentinel per condition wrapped with `%w`, doc comments on every exported identifier starting with its name, no plan references in tracked code (scan CLEAN), commit message cites no task or phase. The three bare `return` lines sit in a result-less handler closure, so none is a naked return.

OUT_OF_SCOPE: none. The diff touches only `internal/identity/identity.go`, `internal/identity/identity_test.go`, and `internal/identity/migrations/0001_sessions.sql`. No companions outside `owns`.

## The ten questions

1. Does any path spend money without reserving budget first? No. The package touches SQLite rows and HMAC only. No provider call exists.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The package never talks to AssemblyAI. Its `guest_sessions` rows are browser sessions, distinct from the provider `sessions` table, as the handoff states.
3. Can a private episode, stem or transcript reach anyone but its owner? No. Episode reads go through `Owns` (false by default), media through `AuthorizeMedia` with Keel answering refusal as 404, and private blobs carry `private, no-store`.
4. Does any edit apply without a revertible record? No. Nothing here edits diary content.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No prompt, no count, no callback exists here.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The `check.sh` plan-reference scan over tracked files is CLEAN.
7. Did this task rebuild something Keel or Chaaya provides? No. Keel lists users as a non-goal per `libraries.md`. The package consumes `keel/id`, `keel/sqlite`, and `keel/mediastore` (404 shape, Range, `private, no-store`) instead of rebuilding them.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Detached worktree at `0d82641fd4f2067fea736c001f52dfae312f2e42`, hash confirmed before reading, every pin rerun there. No code committed.
9. Can any paid job kind resume after a restart and spend twice? No. No job kind exists here.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. All ten tests run on a fixed hand-moved clock under `go test -race` with no device and no tolerance.

## Verdict

APPROVE with zero findings across all severities. Zero residue against prior rounds, vacuously true for round 1.
