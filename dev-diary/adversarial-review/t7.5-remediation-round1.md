# T7.5 remediation round 1

Reviewed hash: `c5f8934ceaf9595cc402d8937959b1af44fec941`
Worktree: `/home/nryn/work/reprise-wt/t7.5-rem-r1` (branch `task/t7.5-rem-r1`, new commits on top, no rebase, no amend)
Review file: read from `/home/nryn/work/reprise-wt/t7.5-rev-r1/dev-diary/adversarial-review/t7.5-round1.md`
(live plan copy at `/home/nryn/work/reprise/dev-diary/adversarial-review/` carries no `t7.5-round1.md` yet)

## Fixes (one row per finding)

| Finding | Fix |
|---|---|
| L, in scope, `dev-diary/probes/production.md:417-420`: SSE idle stream sub claim had no command plus output | Ran the exact read only GET from this workstation and appended the command plus its output block inside check 2. Measured `http=200 bytes=0 time=8.001395s exit=28` with `curl: (28) Operation timed out after 8001 milliseconds with 0 bytes received`. This matches the claim, so the claim stands with evidence now. Minted nothing, changed nothing, spent nothing. Pin `sed -n '/## 2\. Decisions/,/## 3\. Private/p' dev-diary/probes/production.md \| grep 'curl.*api/jobs'` now exits zero. |
| L, OUT_OF_SCOPE, `dev-diary/PHASE-7-production-issues.md:159`: commit wrote outside owns, flipping T7.5 status to `not-started` | Reverted that one hunk to the base line `status:     in-progress:implement:t7.5-impl` as a new commit on the task branch. `git diff 464f0c8..HEAD --name-only` now lists only `dev-diary/probes/production.md` for content, and the phase file matches base again. |

## Validation

- `python3 dev-diary/tools/audit_docs.py` gives `OK planning tree is consistent`.
- Round 1 stays byte identical: `head -267 dev-diary/probes/production.md | git hash-object --stdin` equals the base blob hash `1523e9f72e91fe2c155d3ccbf32a1bae3acb94ed` (base file is 267 lines). Note: the review cites `e0af56...` for this, but both the base blob and the head prefix hash to `1523e9f...` here, so the substance holds. The diff on `production.md` adds 16 lines and deletes zero.
- `production.md` round 1 untouched apart from the check 2 append. No secrets added. Doc style kept (no semicolons, no em dashes, short sentences).
- Live probe was one read only GET with an 8 second cap. No mint, no upload, no state change, no spend. A session cookie arrived with the answer and its value is omitted here.

## Note for the orchestrator

The remediation file itself is left uncommitted in this worktree for the orchestrator to commit. The task commit carries only the two doc fixes.
