# T7.5 implementer handoff

Commit `c5f8934ceaf9595cc402d8937959b1af44fec941` on branch `task/t7.5`
in worktree `t7.5-impl`. Base is main at `464f0c8`. Edge verified as
`ok ... version=464f0c895fe355c435bca21aded9fc57cc34202d` before any
check ran. All checks ran from a workstation, never from the box. No
real voice. Spend about 2.1 cents (one 16.8 second live session).

## Per-check results

1. Guest episode: close. Mint 201, generated-speech call, explicit
   end at 16.41 seconds, end POST 200, episode lists privately in
   state `recording`, threads 200.
2. Decisions and render: carry. Done answers 409
   `illegal_transition`, decisions refuse at their boundaries, job
   events hold an idle SSE stream. The episode never leaves
   `recording` with zero proposals: the binary registers transcript
   and editorial kinds but schedules neither, and nothing calls the
   stems-uploaded transition. Needs a task wiring stem completion
   into transcript and editorial schedule plus the draft transition.
3. Anon media: close. Blob and unknown id both 404 with
   `Cache-Control: private, no-store`.
4. Uploader roundtrip: close. Owner resolves to the session user,
   chunk and complete pass, owner reads exact bytes back.
5. Ledger: close on observable terms. Sessions API completed and
   `client_end` at 16.80 seconds against socket 16.41. End recorded
   with reconcile scheduled, post-end mint 201. Direct ledger rows
   stay behind owner login, same gate as item 6.
6. Kill switch: skipped until owner login lands. Pause 403,
   untouched, unpaused.
7. Seeded greeting: deferred until the operator catalog.

## For the reviewer

The commit touches `dev-diary/PHASE-7-production-issues.md` outside
its owns line: it reverts the task status to `not-started`. Flag it
as an owns violation. The carry in item 2 names its follow-up task.
Only `dev-diary/probes/production.md` was otherwise written, round 1
untouched, no secrets, jars deleted.
