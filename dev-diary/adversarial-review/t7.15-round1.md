# T7.15 round 1 review

Reviewed hash: `aa0f71a73cc339c5ae26e19394ad95cdc1a21387`
Worktree: `/home/nryn/work/reprise-wt/t7.15-rev-r1` (detached HEAD, confirmed `git log -1`, tree clean)
Base: `afd1b4d`
Diff: `dev-diary/probes/production.md` only, +261 lines. Nothing else touched, so `owns` holds.

## Scope checks

- Round 5 appended under `# Round 5: full-pipe live proof after the batch model fix`. Rounds 1 through 4 byte-untouched: the pre-marker prefix equals the base file after stripping the added `---` separator (verified by script).
- Every section except one carries its command plus output: edge identity, mint plus live call plus session end plus gallery, stem uploads plus completion, detail polls with 3 proposals, Sessions API cross-check.
- No secrets: `sk-`/key patterns absent. Token redacted, account key never printed, artifact URLs redacted, cookie jars in `/tmp` deleted.
- Workstation-only: stated for every check, script at `/tmp/opencode/t715/live.py` untracked, Sessions API read from the workstation. No box commands.
- Round-4 close holds: round 4 carried the `speech_models` 400 rejection with zero proposals. Round 5 shows detail `state=draft` with outcome `done`, title `Winter Gate Painting Prep`, and 3 proposals (title, show notes, callback) with command plus output. The rejection is gone on the live build.
- Audio-key observation scoped correctly: named as follow-up with owning paths `web/src/lib/voice/socket.ts` and `web/src/lib/voice/mock.ts`, and the verdict does not depend on it. Matches the handoff and the T7.17 task that owns `web/src/lib/voice/`.
- Read-only spot-checks from this worktree, nothing minted: `healthz` answers `ok` plus 200 on `/`. Edge version has since moved past the recorded `80ddeff`, which is expected for a historical record and not a defect.
- `audit_docs.py` reports OK.
- Doc style: no semicolons, no em dashes, no sentence over 30 words outside code blocks, active voice.

## Ten questions

1. Spend without reserving budget first: No. Docs only, no code path.
2. AssemblyAI session left open: No. The record shows explicit `session.end` with `client_end` on the Sessions API.
3. Private episode, stem, or transcript reaching anyone but its owner: No. Gallery and detail reads use the owner cookie.
4. Edit applied without a revertible record: No edits apply here.
5. Spoken callback or count from the prompt instead of a stored row: No. Proposals come from the landed transcript plus editorial pass.
6. Code, comment, test, or migration citing a phase, task, or planning file: No. `T7.`/`P7`/`handoff` patterns absent from the added text. `Round N` mentions are the spec-required record structure.
7. Rebuilt something Keel or Chaaya provides: No.
8. Review run on the handed hash in a worktree of its own: Yes, shown above.
9. Paid job kind resuming after restart and spending twice: No. No code touched.
10. Development check needing a person, microphone, or wall-clock threshold: No. Generated speech only. Poll timestamps are manual observations, not test thresholds.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| L | `dev-diary/probes/production.md`, section 4 (`Provider cross-check and settle`) at reviewed hash | The settle support claims a fresh honest mint after the end answered 201, but no command plus output block records it. The only mint block in the round is the initial one in section 1. The ceiling is not leaking therefore reads as asserted, not measured. | `python3` probe splitting the file at `## 4. Provider cross-check and settle` and counting `POST https://reprise.nryn.dev/api/sessions` blocks before `## Verdict`. Fails today with count 0, exit 1. Ran in this worktree. | Delete the fresh-mint command plus output block added under section 4. The pin count returns to 0. |

Counts: C 0, H 0, M 0, L 1.

No out-of-scope findings.

## Verdict

REMEDIATE. One L finding: record the post-end fresh mint with its command plus output (a mint that never connects bills nothing), or drop the claim.
