# T7.18 implementer handoff

Commit `12f97eb1f8c071b7ba48a8f5bbc1d5242ad8c0a4` on branch `task/t7.18`
in worktree `t7.18-impl`. Base is main at `fa3d6b5`. Gate passes in
the worktree. Only `web/src/routes/record/` changes, plus the usual
status revert for review to flag.

## What it built

New `api-guard.ts` validates the answer content type before trusting
the body and names the failed call. `stems-complete.ts` posts through
it, and the existing banner plus retry surface the named failure.
Unit pins cover HTML on 200 and 403, missing and malformed bodies,
and unchanged JSON refusal codes. A Playwright proof serves HTML on
the completion route and sees the named banner with a working retry
and no second request.

## Follow-ups outside owns

The session mint, chunk uploads, and session end calls run through
the same unchecked client in the voice layer, and the elapsed
display keeps ticking in the ending phase after a failed completion.
Both need a follow-up owning `web/src/lib/voice/`.
