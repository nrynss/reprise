# T1.6 round 2 review

Hash reviewed: `ce6b8011acc2556d0c0b5438ec54a0b28edd7fa1`.
Worktree: `/home/nryn/work/reprise-wt/t1.6-rev-r2`, detached at the handed hash.
I confirmed the hash with `git log -1` before reviewing.
Base: `4632df2`, the same base the loop branch used.
Diff under review: `git diff HEAD~1..HEAD`.

## Scope

T1.6 owns `internal/settings` and both TOML files and `.env.example`.
The commit changes exactly those owned paths plus the owned test file.
I verified the name list with `git diff --name-only HEAD~1..HEAD`.
The five files are `.env.example` and both TOML files.
They also include `settings.go` and `settings_test.go`.
Nothing else changed.
`go.mod` and `go.sum` stay byte identical to base.
I verified both with `diff` against `git show HEAD~1`.
I read only the real key mode with `stat`.
It reported 600.
I never opened the real key file.

## Reasoning source

I read `dev-diary/project.md` under the Vertex AI heading.
It says Vertex authenticates a service account.
So the secret is a JSON key file.
It says the box runs no metadata server.
So the file stays explicit.
It says project id and location sit inline in TOML.
I read T0.3 in `PHASE-0-ground.md`.
It takes the location from settings.
It leaves the value open.
The task text carries the account string in its own body.

## Gate

I ran `npm ci` in `web`.
Then I ran the gate in the review worktree.
`gofmt -l internal/settings` prints nothing.
`go vet ./internal/settings` passes.
`go test -race -count=1 ./internal/settings` passes.
The gate passes every Go stage.
It passes every web stage through the svelte leakage scan.
The plan reference scan reports clean.
The gate then stops at the consumer name scan.
It exits 1 there.
The single hit sits outside owned paths.
It exists at base too.
I proved that with `git show HEAD~1` plus `grep`.
With that pre-existing hit aside, every owned stage passes.

## Done-when probes run independently at the reviewed hash

All probes used fresh temp files.
All probes used distinct dummy values.
I never opened the real key file.
I built the settings test binary once with `go test -c`.
I used a probe config with a temp env file.
I used a temp key file beside it.
I removed every probe.
The worktree tree is clean.

### Probe 1: readable key boots

I ran the helper in plan mode.
`REPRISE_CONFIG` pointed at the probe config.
Exit was 0.
The credential line read `secrets.gemini_credential`.
It named source `file` and the temp key path.
Its status read `resolved`.
Inline rows named `vertex_project` and `vertex_location`.
They read resolved from the file.

### Probe 2: missing file refuses

I pointed the credential at a temp path.
That path holds no file.
Exit was 1.
Stderr named `secrets.gemini_credential`.
It named source `file`.
It named the missing path too.

### Probe 3: group readable key refuses

I set the temp key to mode 640.
Exit was 1.
Stderr named `secrets.gemini_credential`.
It named source `file`.
It named the loose path with mode 640.

### Probe 4: boot log carries no key fragment

I probed plan output for four planted values.
Each probe returned absent.
The first two were `probe-assembly-aaa111` and `probe-signing-bbb222`.
The last two were `probe-key-id-ccc333` and `probe-private-ddd444`.

### Probe 5: tracked files agree on secret names

Both TOML files list the two env file names.
They list the credential as `file` with its path.
`.env.example` lists exactly those two names.
I loaded both tracked files with rewritten temp paths.
Both resolved with exit 0.

## Known differences judged

Account name.
The task text carries the account string in its own body.
It never orders the string into code or comments.
The key file itself carries the client email.
The build sends no name at runtime.
I find no defect in its absence from comments.

`.env.example` comment.
The task drops `GEMINI_CREDENTIAL` and asks nothing more.
The added lines carry no value.
They tell the reader the key is a file.
They say its path lives in settings.
That helps and it breaks no rule.
I find no defect here.

Region.
No planning file names a region in either direction.
T0.3 takes the region from settings.
It leaves the value open.
Any value here is a placeholder.
T0.3 will correct it.
I record no finding on `us-central1`.

Test details.
Runtime behavior matches the approved branch on every probe above.
The suite itself is thinner in two places.
The findings below cover exactly those two places.
Nothing else in the test diff changes the contract.

## Ten review questions

1. Does any path spend money without reserving budget first. No. Loading reads one TOML file. It resolves local references only.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. This change holds no session code.
3. Can a private episode or stem or transcript reach anyone but its owner.
No. No tracked file holds a secret value. The plan probe shows named sources.
It shows no planted value present.
4. Does any edit apply without a revertible record. No. This change applies no editorial edit. Git still tracks the settings files.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. This change holds no spoken content.
6. Does any code or comment or test or migration cite a phase or task or planning file.
No. Grep over owned paths returns empty. The gate plan scan returns clean.
7. Did this task rebuild something Keel or Chaaya provides. No. Loading and refusal and the plan come from `keel/config`. Reprise owns only the struct and the inline fields.
8. Was this review run on the handed hash in a worktree of its own.
Yes. `git log -1` showed the handed hash. The worktree is detached `t1.6-rev-r2`.
Every pin above ran there.
9. Can any paid job kind resume after a restart and spend twice. No. This change holds no job code.
10. Does any development check need a person or microphone or wall clock threshold.
No. Tests use temp files. They use a helper process with exact string checks.
They use exit codes too.

## Findings

In scope severity counts: C 0 and H 0 and M 2 and L 0.
One out of scope observation follows.
It does not affect the verdict.

### Finding 1

Severity: M.
Where: `internal/settings/settings_test.go` line 425 at the reviewed hash.
That is `TestShippedFilesAgreeOnSecretNames`.
What: The test reads only the local TOML file.
What: It never opens the box file.
What: A box-only name drift keeps the suite green.
What: Then the box boot fails.
Pin: I renamed one box variable to `SESSION_SIGNING_KEY_DRIFT`.
Pin: I ran the agreement test alone.
Pin: The test passed.
Pin: So the suite cannot see box drift.
Mutation: change `var = "SESSION_SIGNING_KEY"` to `var = "SESSION_SIGNING_KEY_DRIFT"`.
Mutation: apply it on line 29 of `config/reprise.box.toml`.

### Finding 2

Severity: M.
Where: `internal/settings/settings_test.go` line 213 at the reviewed hash.
That is `TestBootLogShowsPlanWithoutValues`.
What: The test asserts the plan output contains `file`.
What: Every inline row already carries source `file`.
What: The string `env_file` contains `file` too.
What: The test never asserts the key path.
What: So a credential on the wrong source still passes.
Pin: I pointed a probe credential at `env_file`.
Pin: I ran the helper in plan mode.
Pin: Exit was 0.
Pin: The output held every string the test wants.
Pin: The credential line read `env_file`.
Pin: It lacked `file` with a key path.
Pin: So the test assertions would all pass there.
Mutation: change the credential block to `source = "env_file"`.
Mutation: add `path` plus `var = "GEMINI_CREDENTIAL"` there.

### Out of scope observation

Severity: L.
Where: `dev-diary/mockup/V1 Reprise-Interactive Prototype.html` line 11.
That line sits at the reviewed hash.
Owning path: `dev-diary/mockup`, outside T1.6 ownership.
What: The line holds the word `hackathon`.
What: So the gate consumer scan fails in this worktree.
Pin: I ran the consumer grep at the reviewed hash.
Pin: It reported that line.
Pin: I ran the same grep against the base content.
Pin: It reported the same line.
Pin: So the hit predates this commit.
Mutation: add the word `hackathon` to line 11 of that mockup file.
Mutation: then the scan fails again.
Mutation: remove that word and the scan passes.

## Comparison note and done-when statement

Round 1 approved branch commit `5056e61`.
It carried zero in-scope findings.
That suite pinned the file source with the key path.
It also loaded both tracked files.
This commit keeps the runtime behavior.
It thins exactly those two pins.
My probes match the round 1 pins on behavior.
The suite no longer pins them.
So the verdict differs from round 1.
ce6b801 satisfies every done-when behavior clause on its own.
A readable key boots.
The plan names file with its path.
A missing key refuses.
It names key and source and path.
A group readable key refuses.
It names key and source and path.
The boot log carries no key fragment.
The tracked files agree on secret names.
It fails the test pin the last clause asks for.
The agreement test covers only the local file.

## Verdict

REMEDIATE.
Two in-scope M findings need a small follow-up on the owned paths.
The follow-up should assert the credential plan line with its key path.
It should also load both tracked files.
ce6b801 is already on main, so no amend can fix it.
I committed nothing.
I removed every probe.
The worktree tree is clean.
