# T7.34 review, round 2

- **Hash reviewed:** `ef473d81e3fa16236c5622981e27bed482dcc710` (confirmed with `git log -1`)
- **Base:** `68039d0`. Round 1 reviewed `13248a4`.
- **Worktree:** `/Users/narayan/reprise-wt/t7.34-rev-r2`, detached at the hash
- **Remediation diff:** `git diff 13248a4..ef473d8` touches `web/src/lib/editor/draft.ts` (three
  creation sites move from `{}` to `Object.create(null)`, one comment grows) and adds
  `web/src/lib/editor/draft.prototype-keys.test.ts`.
- **Task diff:** `git diff 68039d0..ef473d8`, 2 files, 57 insertions and 6 deletions.
- **Probes:** `/Users/narayan/reprise/dev-diary/probes/t7.34-rev-r2/`

## Residue against round 1

1. The round 1 pin `dev-diary/probes/t7.34-rev-r1/draft.prototype.test.ts` was copied into
   `web/src/lib/editor/` and run with `npx vitest run`. 5 of 5 pass. It is byte identical to the
   committed `draft.prototype-keys.test.ts`.
2. Mutation applied, `Object.create(null)` back to `{}` at all three creation sites. The pin fails 4
   of 5, on `constructor`, `toString`, `__proto__` and `hasOwnProperty`. `cut-z` still passes, as
   expected.
3. Reverted with `git checkout`. The pin passes 5 of 5 again, and the copy was removed.
4. The stored `__proto__` write case from round 1 has no committed pin, so I probed it
   (`probes/t7.34-rev-r2/draft.stored-keys.test.ts`). Stored ids `__proto__`, `constructor`,
   `toString` and `abc123` each post their own id on revert. 4 of 4 pass at `ef473d8` and at base
   `68039d0`. With the `{}` mutation, `__proto__` fails.

**Residue: zero at C, zero at H, zero at M, zero at L.** Round 1 finding 1 (L) is closed. The
false "keeps the same behaviour" sentence in the `13248a4` message stays in history, since no role
may amend a reviewed commit. The cumulative change now makes that sentence true at runtime.

## Behaviour against the old Map

| Site | Old | New | Same? |
|---|---|---|---|
| Field init, `draft.ts:255` | `new Map()` | `Object.create(null)` | Yes |
| Fixture reset, `:292` | `new Map()` | `Object.create(null)` | Yes |
| Live build, `:331` | `new Map()` | `Object.create(null)` | Yes |
| Write, `:340` | `.set(id, id)`, last wins | `[id] = id`, last wins | Yes, `__proto__` included |
| Swap, `:371` | assigns the new Map | assigns the new record | Yes |
| Read, `:509` | `.get(id) ?? ''` | `[id] ?? ''` | Yes, inherited names included |

No other use of `cutProposals` exists under `web/`. Nothing iterates, sizes, spreads or serialises
it, so key order and `.size` never mattered. At runtime the change is identical.

## Types

`Object.create(null)` returns `any`, so the compiler never checks the three creations. The declared
`Record<string, string>` types the read at `:509` as `string`. The old `Map.get` typed it as
`string | undefined`. The new type claims every id is backed, and it makes the `?? ''` fallback look
dead. That is finding 1.

## Style

- The two touched comments have no semicolons and no em dashes. Their longest sentence is 23 words.
- No task id appears in code, test names or either commit message.
- The `ef473d8` message has one sentence of 33 words. That is finding 2. Its remediation file says
  the longest runs 26.

## Gate

`npm ci` in `web/` exited 0. Every step of `tools/check.sh` after its ffmpeg check ran verbatim, in
bash, in order, from the worktree root, through `probes/t7.34-rev-r2/gate.sh`. The ffmpeg 9.0.2
against 9.0.1 mismatch is accepted by the owner and was not run.

| Step | Exit |
|---|---|
| `gofmt -l cmd` | 0, empty output |
| `go list ./... \| grep -v web/` | 0 |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0, 22 packages ok |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0, 322 files, 0 errors, 0 warnings |
| `npm run lint` | 0 |
| `npm run test` | 0, 224 of 224 in 22 files |
| `npm run build` | 0 |
| `npm run test:e2e` | 0, 4 passed |
| svelte 4 leakage `git grep` | empty (raw exit 1, no match) |
| consumer names `git grep` | empty (raw exit 1, no match) |
| plan references `git grep` | empty (raw exit 1, no match) |
| `go test -race ./...` | 0 |

The done-when criterion holds. `git status --short` was clean after every probe and the gate.

## The ten questions

1. **Spend without reserving budget?** No. The change touches no paid path.
2. **AssemblyAI session left open?** No. No session code changed.
3. **Private episode, stem or transcript leak?** No. The change is a client lookup table.
4. **Edit applies without a revertible record?** No. A revert still writes its decision row before
   posting. The stored-keys probe shows every backed id posts its own proposal id.
5. **Spoken callback or count from the prompt?** No. Untouched.
6. **Code, comment, test or migration cites the plan?** No. The plan references scan is empty, and
   neither commit message names a task.
7. **Rebuilt something Keel or Chaaya provides?** No.
8. **Review run on the handed hash in its own worktree?** Yes. `ef473d8` in
   `/Users/narayan/reprise-wt/t7.34-rev-r2`. Probes ran there and were removed.
9. **Paid job kind resumes and spends twice?** No. Untouched.
10. **Development check needs a person, a microphone or a wall-clock threshold?** No. The added test
    is a vitest case with stubbed fetch.

## Findings

### 1. The record type says every cut id is backed

| Column | Value |
|---|---|
| **Severity** | L |
| **Where** | `web/src/lib/editor/draft.ts:255` and `:331` declare `Record<string, string>`. The creations at `:255`, `:292` and `:331` assign `any` from `Object.create(null)`. The read is at `:509`. |
| **What** | Reading an unbacked id returns `undefined` at runtime, and the comment at `:503` to `:505` says it reads as empty. The declared type says the read is always `string`. The old `Map.get` said `string \| undefined`. So the type lost the honesty the Map had, and the `?? ''` fallback now looks dead to the compiler. No runtime defect follows, hence L. `Record<string, string \| undefined>` at both declarations fixes it. Lint and tsc stay clean with that change, and the editor tests pass 30 of 30. |
| **Pin** | `/Users/narayan/reprise/dev-diary/probes/t7.34-rev-r2/draft.lookup-type.ts`. Copy it to `web/src/lib/editor/`, then run `npx svelte-kit sync` and `npx tsc --noEmit -p tsconfig.json` in `web/`. At `ef473d8` tsc exits 2 with TS2322 on the probe. With `Record<string, string \| undefined>` at `:255` and `:331` it exits 0. |
| **Mutation** | After the fix, restore `Record<string, string>` at `:255` and `:331`. tsc exits 2 on the probe again. |

### 2. A commit message sentence runs 33 words

| Column | Value |
|---|---|
| **Severity** | L |
| **Where** | Commit message of `ef473d8`, third paragraph, the sentence starting "Adds a test that reverts". |
| **What** | The sentence runs 33 words, over the 30 word limit. The remediation file claims the longest sentence runs 26. A remediator may not amend a reviewed commit, so the orchestrator must choose the fix path, such as rewording at landing. |
| **Pin** | `python3 /Users/narayan/reprise/dev-diary/probes/t7.34-rev-r2/sentence-length.py <worktree> ef473d8` prints the sentence and exits 1. On `13248a4` it exits 0. |
| **Mutation** | After the reword, join the split sentence back into one 33 word sentence. The probe exits 1 again. |

### 3. The task commits a file outside its `owns` path

| Column | Value |
|---|---|
| **Severity** | L |
| **Where** | `web/src/lib/editor/draft.prototype-keys.test.ts`, added in `ef473d8`. The task block owns only `web/src/lib/editor/draft.ts`. |
| **What** | AGENTS.md lets a task edit only the paths in its `owns` line. The remediation added a tracked test file outside it. No live task owns that path, so nothing collides, hence L. The test is useful, and the fix is either the orchestrator widening `owns` to name it, or dropping the file. That choice is the orchestrator's. |
| **Pin** | From the worktree, `git diff --name-only 68039d0..ef473d8 \| grep -vx 'web/src/lib/editor/draft.ts'` prints the test path and exits 0. It should print nothing and exit 1 unless `owns` names the file. |
| **Mutation** | After the fix, add the test file back without widening `owns`. The pin prints its path again. |

## Counts

| Severity | Count |
|---|---|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 3 |
| OUT_OF_SCOPE | 0 |

## Verdict

**REMEDIATE.** Zero residue against round 1. Runtime behaviour matches the old Map at every read and
write site, and the gate and all three scans are clean. Three L findings remain. The record type
claims every id is backed, one commit sentence runs 33 words, and the test file sits outside `owns`.
