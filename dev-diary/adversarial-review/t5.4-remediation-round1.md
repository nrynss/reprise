# T5.4 remediation, round 1

Reviewed hash: `5aed2265468fb59cb885ad29fb366a23aee2e4e4`. New head:
`326a2e6960206951f5d9010435ed0eb994f59c6a` on branch `task/t5.4-rem-r1` in
worktree `/home/nryn/work/reprise-wt/t5.4-rem-r1`. One commit on the
reviewed hash. No rebase, no amend. Tree clean after the commit.

## Fixes

| Severity | Where | What changed | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/retention/sweep.go` (`run`), `internal/retention/sweep_test.go` (`TestReSweepCatchesNewlyExpired`) | `run` reads the cutoff fresh from the clock on every attempt, including resume and re-sweep, and unions the unconfirmed prior guests with the guests expired under that fresh cutoff. The run comment now states exactly that instead of claiming the union alone covers the outage case. Fail-closed direction kept. A read error still fails the run with nothing deleted, and expiry still uses strict less-than so the exact-cutoff guest survives. | `TestReSweepCatchesNewlyExpired`: seeds `guest-old` idle 91 days, `guest-mid` idle 89 days, and `guest-fresh` idle 1 hour on a fixed clock with a 90-day window. Fails the first sweep with a refused transcript delete, opens a second service sharing the same stores with the clock 48 hours ahead, calls `ReSweep`, then asserts `guest-mid` and `guest-old` own zero episodes while `guest-fresh` still owns 2. Passes on the new head. | Restore the freeze (`cutoff := prior.Cutoff` with a zero fallback). The pin fails again with `episodes holds 2 newly-expired rows, want none`. Verified by swapping the two lines in the worktree and rerunning the single test. |

## Verification

* `go test -race -count=1 ./internal/retention/` passes in the remediation worktree (13 tests, including the new pin).
* Full gate `./tools/check.sh` passes in the remediation worktree after `npm ci` in `web/`, ending `OK all checks passed`.
* Prior pins intact. The full suite passes, which includes the expiry, boundary, owner-skip, default-window, restart-resume, and stuck-then-heal pins named in the round 1 review.

## Untouched

The OUT_OF_SCOPE doc line, the wiring notes, the allowlist model, the keep transaction, the cutoff edge semantics, and the seeded-row treatment from the round 1 review are unchanged. No verdict changed here.
