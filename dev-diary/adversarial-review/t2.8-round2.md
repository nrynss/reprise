# T2.8 round 2 review (re-review)

Reviewed hash: `197699b2f4f55cc1307657ceef75512aa245ba81`.
Worktree: `/home/nryn/work/reprise-wt/t2.8-rev-r2`, detached HEAD.
Confirmed with `git log -1 --format=%H` before and after verification.
Diff: `git diff 151fa89..197699b` (remediation alone), two files.
No commit was made here. One mutation probe was applied, observed,
reverted, rebuilt, and the tree is clean after verification.

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0. Out of scope findings: 0.

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with exactly one finding: H-1, the dead
standing end control (`+page.svelte:17` to `26` at that hash, mount
time `getElementById('record-end')` attaching to null).

- **H residue: zero.** The mount time lookup and its add and remove
  listener calls are gone. The document click listener now also
  matches `closest('#record-end')` and calls `controller.endControl()`.
  Both H-1 pin variants (mouse click arms, keyboard Enter arms) pass
  on this hash, 2 passed. The mutation probe below proves they are
  load-bearing against this exact fix.
- **C residue: zero.** Round 1 had no C findings, and this diff adds
  none. No spend, privacy, or recording path changes.
- **M residue: zero.** Round 1 had no M findings, and this diff adds
  none. The warn button branch is untouched and still fires first in
  the else-if chain.
- **L residue: zero.** Round 1 had no L findings, and this diff adds
  none. The replacement comment states its own reason with no plan
  reference.

## Verification performed

- `npm ci` then `npm run build` in this worktree: clean, site writes.
- Full mock suite `mock.playwright.config.ts` against a fresh build:
  9 passed, about 33 seconds. The 2 cap proofs, the 5 session proofs,
  and the 2 new end-control proofs all pass.
- `npx svelte-check`: 0 errors, 0 warnings.
- `npx eslint` on the touched page and the new spec: clean.
- `npx vitest run`: 54 passed across 8 files.
- Mutation probe (applied, observed, reverted, rebuilt, tree clean):
  replacing the new `#record-end` delegation branch condition with
  `false` fails exactly the 2 new end-control proofs while nothing
  else changes. Both pins are load-bearing. After revert and rebuild
  both pass again.
- Full `./tools/check.sh` was not rerun here. Round 1 ran it green on
  the base, and every part this diff touches (build, svelte-check,
  eslint, vitest, mock suite) was rerun green on this hash.
- Read the remediated `+page.svelte` end to end. The surviving direct
  `record-start` listener is pre-existing and out of scope. No direct
  `record-end` listener remains, confirmed by grep.

## Hunted candidates that closed without a pin

- **Double fire from delegation plus a surviving direct listener.**
  Closed. The mount time lookup is removed, not left beside the
  delegation. Each matching click reaches exactly one
  `controller.endControl()` call. The pins assert the single-arm
  state (Confirm visible, still on the record page). A double fire
  would arm and confirm in one click and land on processing, which
  would fail the pins.
- **Delegation scope catching unrelated clicks.** Closed. Both
  branches match `closest()` on unique element ids. A click outside
  either button matches neither and falls through with no call.
- **Keyboard path through delegation.** Closed. The buttons are
  native, so Enter and Space raise click events the document
  listener receives. The Enter pin drives a real key press and
  passes.
- **Warn button behavior changed.** Closed. Its branch is byte
  identical and still first. It arms through the same end control
  as before.
- **Timer and person end racing to two closes.** Closed. Untouched
  by this diff. All 9 mock proofs pass together, including the
  timer end, the wake end, and the page-leave once-only close.
- **Runes, store shapes, plan references, a11y.** Closed. No
  `export let`, no `$:`, no `.get(` shapes in the touched files.
  The `phase` matches are the record phase type. The button markup,
  labels, and disabled states are unchanged.

## Process note for the orchestrator, not a finding

Round 1 noted the base diff touched `web/src/lib/voice/record-state.ts`
outside the owns line. This remediation adds
`web/src/routes/record/end-control.spec.ts`, which the owns line also
does not name file by file (`web/src/routes/record/cap.spec.ts` is the
named spec). It sits inside the owned directory beside the named spec,
so no concurrent task conflicts. No product defect follows, so no
severity attaches.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   browser mints nothing. The delegation only reaches the existing
   end path sooner, which spends less.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. This round restores the standing user end path
   instead of removing one. Timer, warn button, pagehide, and destroy
   are untouched.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Nothing here touches uploads, storage, or URLs.
4. Does any edit apply without a revertible record? Not applicable.
   This task ships no edit feature.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. No prompt, name, count, or episode text changes.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The new comment states its own reason. The
   `phase` matches are the record phase type.
7. Did this task rebuild something Keel or Chaaya provides? No. The
   fix reuses the existing delegation pattern and the landed end
   control.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. The hash matched before any file was read and again at the
   end. Nothing was committed. The mutation was reverted and the
   tree is clean.
9. Can any paid job kind resume after a restart and spend twice? Not
   applicable. No job kinds live in this task.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Both new proofs drive the mock take
    with generated input and assert on rendered state. The only real
    time calls are sub second liveness waits.

## Verdict

APPROVE. H-1 is fixed with load-bearing pins, and zero residue stands
across every severity: C zero, H zero, M zero, L zero.
