# T4.4 round 3 review (re-review)

Reviewed hash: 23deef39e342399fb47cc33146ef8c6bdc612104.
Worktree: /home/nryn/work/reprise-wt/t4.4-rev-r3 (detached HEAD, confirmed
with `git log -1 --format=%H`). No commits made. Temporary probe edits
(publish-order swap, unpublish latest-only restore, scratch migration
cascade drop, scratch `.get(` restore) were all reverted.
`git status --short` is clean.

Base for remediation alone: 8d175584de1193b9811bb343f3325050e668f1de.
`git log --oneline 8d17558..23deef3` shows one commit, no rebase, no
amend of the reviewed commit. `git diff 8d17558..23deef3 --stat` shows
only `web/src/routes/share/[token]/+page.svelte` (+7/-1). Inside owns.

## What was re-run

* `npm ci` in `web/`, then full `./tools/check.sh`: exit 0,
  `OK all checks passed`, including the svelte-4-leakage, consumer
  names, and plan references steps that decide this round.
* `go test -race -count=1 ./internal/privacy/`: pass.
* `TestUnpublishRevokesEveryRender` (round 1 C pin shape): pass.
* `TestPublishKeepsEpisodePrivateWhenBlobMissing` (round 1 H pin): pass.
* Round 1 C mutation re-executed here: restricted the unpublish sweep
  to the latest render row (`ORDER BY rowid DESC LIMIT 1`). The test
  failed with `signed-out first opus status 200, want 404`. Restored,
  suite green.
* Round 1 H mutation re-executed here: moved the episode UPDATE before
  `setVisibility` in `Publish`. The test failed with
  `episode visibility "public", want private`. Restored, suite green.
* Round 1 L mutation re-executed here: dropped `ON DELETE CASCADE`
  from `mentions.episode_id` in a scratch copy of the migration. The
  suite failed loudly (erase never finishes, stuck targets), then the
  file was restored and the suite passed again.
* Round 2 L mutation re-executed here: restored
  `page.url.searchParams.get('fixture')`. The enforced scan flags
  `web/src/routes/share/[token]/+page.svelte:20` exactly as round 2
  recorded. Restored, scan clean.
* Route proofs explicitly, since the rewrite touches the flag they
  drive: `npx playwright test -c src/routes/share/share.playwright.config.ts`:
  3 passed (published plays, revoked opens nothing, privacy copy).
  Share vitest: 8 passed.
* `grep` for `.get(` over the owned routes: no hits remain.

## Residue vs round 1, severity by severity

* C: zero. The every-render sweep still revokes both opus blobs to
  404 with private visibility, and the latest-only mutation still
  fails 200, so the pin still measures the sweep.
* H: zero. The blob-before-episode ordering still holds, and the
  episode-first mutation still reads public, so the pin still
  measures the ordering.
* M: zero. Round 1 filed none in scope.
* L: zero. All ten content tables stay seeded and the cascade-drop
  mutation still fails loudly instead of passing silently.

## Residue vs round 2, severity by severity

* C: zero. Round 2 filed none.
* H: zero. Round 2 filed none.
* M: zero. Round 2 filed none in scope.
* L: zero. The single round 2 L (red leakage scan on the `.get(`
  line) is fixed: the full gate exits 0 at the handed hash, and the
  `.get(` restore still trips the scan, so the fix is load-bearing
  against the enforced pattern.

## Findings

None. No new defects found in the rewrite.

## New-defect hunt over the one commit

* Semantics are identical. `URLSearchParams.get` returns the first
  value, and the loop keeps the first `fixture` value and breaks, so
  duplicate keys resolve the same way. An absent param read `null`
  before and `''` now, and both miss the `published` and `revoked`
  comparisons, so the fallthrough to the live fetch is unchanged.
* `for...of` over `URLSearchParams` is standard iteration, and
  svelte-check, eslint, vitest, build, and both Playwright suites
  pass. No Svelte 4 idiom, no store read, no reactivity change.
* The diff touches one file inside owns and adds no surface. No new
  leak path, no ordering change, no fixture change.

Round 1 OUT_OF_SCOPE rows (missing `ReErase` route, owed workstation
remeasure) and the round 2 gate-pattern breadth note stand untouched
and still do not block.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   touched line reads a query flag. No paid call exists in the owned
   paths.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Untouched by the remediation, still closed
   through `sessionTarget`.
3. Can a private episode, stem or transcript reach anyone but its
   owner? No, with teeth: the committed every-render test publishes
   twice, unpublishes, then fetches both opus blobs signed out and
   reads 404 with private visibility (re-ran green here), and the
   latest-only mutation re-run here reads 200 on the first blob,
   which proves the test still measures the sweep. Stems, the export
   copy, cover gating on `LookupShare`, and revoked-token 404
   identity are unchanged from rounds 1 and 2.
4. Does any edit apply without a revertible record? No. Publish and
   unpublish remain reversible flips. The remediation adds no state.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. Share metadata reads stored rows only. Untouched.
6. Does any code, comment, test or migration cite a phase, a task, or
   a planning file? No. The enforced plan-reference scan passes in
   the full gate run here.
7. Did this task rebuild something Keel or Chaaya provides? No. The
   remediation rewrites one query-flag read with Web API iteration.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Hash confirmed, detached worktree, no commits, all probes
   reverted, tree clean.
9. Can any paid job kind resume after a restart and spend twice? No.
   No paid call touched; erasure targets unchanged.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Full gate plus both Playwright suites
    ran here on fixtures with no live device or person.

## Verdict

APPROVE. Counts: C 0, H 0, M 0, L 0, plus the standing OUT_OF_SCOPE
notes that do not block. Residue is zero against round 1 at every
severity and zero against round 2 at every severity. The gate exits 0
at the handed hash.
