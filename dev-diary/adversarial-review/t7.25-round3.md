# T7.25 round 3 review

Reviewed hash: `390d46e0c97c9b8cd5d4678e1527070db2493da2`
Worktree: `/Users/narayan/reprise-wt/t7.25-rev-r3` (detached HEAD).
Parent is `0249ab9931c47e9d54826f4a2b9869c6c842c9e1`, the round 2 hash.
`git log -1` matches the handed hash. `git status --short` was empty before the pins and empty again after the mutation was restored.
Diff reviewed: `git diff 0249ab9931c47e9d54826f4a2b9869c6c842c9e1..390d46e0c97c9b8cd5d4678e1527070db2493da2`, plus the combined diff from `742c0363db343743fcd4143e0b4aa65cfd1e7293`.
No code commits. The tree is still that hash.

## The ten questions

1. Does any path spend money without reserving budget first? No. The diff adds no paid call. It only drops a browser path check before one navigation.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The draft move still starts only after `voice.end` and the HTTP close. `destroy` on a live take still calls `voice.end`. The token cap is unchanged.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The commit changes no media URL, cookie, or owner check.
4. Does any edit apply without a revertible record? No. Nothing applies an edit. A late success only skips `location.assign`.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The remaining comment describes the dropped controller. It does not cite a task id, a phase id, or a planning file. The comment uses handoff for the processing URL.
7. Did this task rebuild something Keel or Chaaya provides? No. The guard is a product flag on the existing controller. `destroy` still uses Chaaya `SessionGuard`.
8. Was this review run on the handed hash, in a worktree of its own? Yes. This detached worktree is the only tree the review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind. Skipping the assign does not post the move again.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The gallery proofs and the controller proofs use the mock take. None waits on a person, a microphone, or a clock threshold.

## What the commit does

`goProcessing` returns when `destroyed` is set, at `web/src/lib/voice/record-state.ts:517`. `destroy` sets that flag first, at line 274. The path check and `onRecordPage` are gone. End and retry still assign while the controller is mounted. The draft move is not cancelled.

## Pins

`npm ci` in this worktree's `web/` added 236 packages. Exit 0. `node_modules` was missing here.
`npm run build` in `web/` exited 0. Preview serves that build.
The built node `8.3G2zTPO5.js` had `if(this.destroyed)return` and then `location.assign`.

The late-move filter was run twice, back to back, on that build.
The command was `npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts -g "finishes after the gallery"`.
Run 1 passed 2. Exit 0.
Run 2 passed 2. Exit 0.

The full gallery spec then passed 10. Exit 0.
`npx playwright test -c src/lib/voice/mock.playwright.config.ts controller-complete.spec.ts --workers=1` passed 2. Exit 0.
A move that finishes on the record page still reaches processing. The retry on that page does too.

The preview log shows `404` for `GET /api/episodes`. The season heading still renders. That 404 is the preview server, not this commit.

`./tools/check.sh` was not run. This commit does not change the ffmpeg pin.

## Mutation

The `destroyed` return was deleted from `goProcessing`. Nothing else in that function changed. The web build was refreshed, because preview serves `build` and not the source.
The served function assigned at once. It did not read `destroyed`.
`build/record.html` preloaded `nodes/8.Bb_TpXck.js`.

The same late-move filter was run once on that bundle. Exit 1 with 2 failed.
Both proofs settled on pathname `/processing`.
The end proof rendered the Processing heading. Its status named 98304 user bytes and 192000 host bytes.
The retry proof settled on `/processing` as well.
The pin is load-bearing. The destroyed return is what stops the late assign.

`git checkout` restored `web/src/lib/voice/record-state.ts`. `git status --short` was empty. `git diff` was empty.
A later build put the return back. The served node was `8.DwLA-dSu.js`. `goProcessing` again began with `if(this.destroyed)return`. `build/record.html` preloaded that node.

## Findings

None.

## Counts

C 0, H 0, M 0, L 0. In-scope findings: zero. Out of scope findings: zero.

## Residue

Round 2 had one in-scope H and zero at C, M, and L.
That H said the late-move proofs stay green after the path check is removed.
This commit removes the path check. The proofs now fail when the destroyed return is removed, and both reach `/processing`.
Residue against round 2 is zero at C, zero at H, zero at M, and zero at L.

## Verdict

APPROVE
