# T3.6 remediation round 1

Reviewed hash: `69d268d7fd93cd41ec3340e9bd627b4caa4557de`
New head: `efe1d2c3c3dd1791420fb903d9a2f84a253eab68`
Worktree: `/home/nryn/work/reprise-wt/t3.6-rem-r1`, branch `task/t3.6-rem-r1`, built on the reviewed commit with no rebase and no amend. One new commit, one file touched, inside `owns` (`internal/cover/`). No verdict change. Nothing fixed beyond the finding below.

| Severity | Where | Fix | Pin | Mutation |
|---|---|---|---|---|
| L | `internal/cover/cover_test.go` (`TestRerunReplacesCoverRow`) | Committed the rerun pin the review templated: run `Run` twice on one seeded episode in a shared cover dir and assert the second run returns no error, keeps the same `ep-1.png` file and SHA256, records 2 model calls, leaves exactly 1 `covers` row, and preserves the model-path fallback mark (`fallback = 0`). | `TestRerunReplacesCoverRow`, passing under `go test -race -count=1 ./internal/cover/...`. Deleting the `DELETE FROM covers WHERE episode_id = ?` line in `storeImage` makes the second `Run` fail with `UNIQUE constraint failed: covers.episode_id`, so the pin is load-bearing. | Delete the `DELETE FROM covers WHERE episode_id = ?` statement in `storeImage`: the new test fails on the `UNIQUE(episode_id)` constraint. |

Prior pins intact: full `go test -race -count=1 ./internal/cover/...` passes, and `./tools/check.sh` passes end to end in this worktree (after `npm ci`), including `go test -race`, staticcheck, and the full web checks. `gofmt -l` and `go vet` are clean over `internal/cover/`. Untouched as ordered: paid seam, kind flags, fallback determinism, validator, prompt constraints, `genai` boundary, covers table shape.
