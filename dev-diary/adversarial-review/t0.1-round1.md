# T0.1 round 1 review

Reviewed hash: `9a54c2a6222aaf5e9fa7770e1425f3b31a022eac`.
Worktree: `/home/nryn/work/reprise-wt/t0.1-rev-r1`, detached at the handed hash.
Base: `9af1c63`. Diff: `git diff 9af1c63..9a54c2a`, 24 files, all inside the owned paths.

## Verdict: REMEDIATE

Severity counts: C 0, H 1, M 0, L 1. Two findings block APPROVE. No residue claim follows, because residue remains.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `tools/check.sh`, svelte-check stage, lines 58 to 59 | The gate fails on a clean tree. Nothing runs `svelte-kit sync` before `svelte-check`, so `tsconfig.json` extends a missing `.svelte-kit/tsconfig.json`. A fresh worktree with `npm ci` exits 1 at `FAIL [svelte-check]`. The handoff claim of three clean runs at exit 0 each is false as stated. Later runs passed only because an earlier run had materialized `.svelte-kit`. CI would fail the same way, since it runs `npm ci` then the gate with no prior sync or build. | `npm ci` then `./tools/check.sh` in this worktree exited 1 with `FAIL [svelte-check]`. Direct `npm run check` reported `Cannot read file '.../web/.svelte-kit/tsconfig.json'`. Gate runs after a build exited 0 and 0. | `rm -rf web/.svelte-kit` on a passing tree, then `npm run check` exits 1 with the same error. `npx svelte-kit sync` restores exit 0. Both directions were run. |
| L | `cmd/reprise/main.go`, package comment, lines 1 to 4 | The second sentence runs 40 words. The style rule caps sentences at 30 words. | A word count probe over the exact sentence returned 40 against the limit of 30. | Rejoin the split sentences into the single 40 word sentence. The probe then returns 40 again. |

## False positives considered

`web/package-lock.json` names licenses in hundreds of `"license"` fields. That file is npm generated third party metadata. `web/package.json` carries no license field and no `LICENSE` file exists. The project names no license. This is not a finding.

`tools/check.sh` line 3 reads `Nothing here is skipped silently`. The voice is passive. The style rule permits passive where it reads clearer. This negative statement reads clearer than any active rewrite. This is not a finding.

`cmd/reprise/main_test.go` sits outside the literal `owns` line, which names `main.go` only. It is the owned binary's own test. It adds no product surface. This is not a scope defect.

## Out of scope defects

None. Nothing outside the owned paths carries a defect.

## Verification evidence

Gate runs in the review worktree after `npm ci`: run 1 exited 1 (`FAIL [svelte-check]`), run 2 exited 0, run 3 exited 0. Go stages (`gofmt`, `go vet`, `staticcheck` through `go run honnef.co/go/tools/cmd/staticcheck@v0.8.1`, `go test -race`) passed in every run. `staticcheck` is absent from `PATH`, so the pinned `go run` wiring carries it. That wiring works.

Container: `docker build -t reprise:t01-r1 .` succeeded. A run served `/healthz` at 200 with a boot id, `/` at 200 with prerendered `<title>Reprise</title>` and `<h1>Reprise</h1>`, and `/episodes/one` at 200 from the fallback shell. `docker exec sh` failed with executable not found. Image user is `nonroot` with an absolute entrypoint and an absolute `-web` path.

Pins: Keel resolves to `v0.3.0` with no `replace` line. Chaaya is exactly `0.2.0`. Installed Svelte, Kit, Vite, TypeScript, svelte-check, ESLint, Vitest and Playwright satisfy Chaaya 0.2.0 peer and dev ranges. `web/src` uses runes only. No `export let`, no `$:`, no store import exists.

Guard probes, each reverted with a clean `git status`: an `export let` plant tripped the leakage guard. A `$:` plant tripped it. A `T2.1` comment plant tripped the plan reference scan. The consumer name scan, the plan scan and the leakage guard are clean on the reviewed tree.

Auditor: `audit_docs.py` passes on the real planning tree with exit 0. Against a scratch copy with a `requires` line pointing at `T9.9` and a relative link pointing at a missing page, it exited 1 and named both defects. Scratch-only findings for `../AGENTS.md` and `../product.md` reflect the copy missing its parent files. They do not occur on the real tree.

## Ten review questions

1. Does any path spend money without reserving budget first? No. The tree holds no paid call. It serves a static shell and a health endpoint.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. No provider code exists yet.
3. Can a private episode, stem or transcript reach anyone but its owner? No. No episodes, no auth and no media paths exist yet.
4. Does any edit apply without a revertible record? No. No edit paths exist yet.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No callbacks exist anywhere in the tree.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The plan scan over tracked files is clean, with the documented exemptions only.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses `keel/id` for the boot id and ships a placeholder shell that needs no Chaaya export yet.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` reads the handed hash in the detached review worktree.
9. Can any paid job kind resume after a restart and spend twice? No. No jobs exist yet.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Vitest runs on jsdom, Playwright serves a local preview, and Go tests use `httptest`. Timeouts bound failure only.

## Notes for the remediator

Run `svelte-kit sync` inside `check.sh` before `svelte-check`, keeping the mandated check order. Chaaya's own check script runs sync first. That is the reference pattern. Then prove a clean worktree passes three times in a row.

Split the 40 word package comment sentence in `main.go`. Keep both halves under 30 words. Keep the active voice.
