# T6.1b round 1 review

Reviewed hash: `2211a19a5e111e2fbd6bbb4df2c9a945ab409187`
Worktree: `/home/nryn/work/reprise-wt/t6.1b-rev-r1` (detached, confirmed with
`git log -1`)
Base: `5d33a24`
Diff under review: `git diff 5d33a24..2211a19a5e111e2fbd6bbb4df2c9a945ab409187`
Scope: `dev-diary/probes/production.md` only, per `owns` in
`dev-diary/PHASE-6-ship.md` T6.1b block.

## What the commit does

One docs commit. It adds `dev-diary/probes/production.md` (267 lines), the
first-deploy production verification record. It also edits
`dev-diary/PHASE-6-ship.md` outside its `owns` line. That edit is finding 1
below and must be dropped before landing.

## Evidence checks this reviewer ran

* `git log -1` confirms the handed hash in a detached worktree of its own.
* `git diff 5d33a24..2211a19 --name-only` lists exactly two files:
  `dev-diary/PHASE-6-ship.md` and `dev-diary/probes/production.md`.
* `dev-diary/tools/audit_docs.py` passes. It prints
  `OK planning tree is consistent`.
* Secret scan of the record finds no key material. Tokens and cookies are
  redacted. The Sessions API key never appears. Cookie jars lived in `/tmp`
  and the record states they are deleted.
* Plan-reference scan of the record finds no task id, phase id, or planning
  file citation. The commit message carries none either.
* Doc style holds. No semicolons outside pasted output. No em dashes. No
  line runs past 30 words.
* Spot check 1 (read-only): `curl https://reprise.nryn.dev/healthz` from
  this worktree answers
  `ok 1ec2ef2bd41f39186df5bf935afc2afd version=5d33a24...` and `/` answers
  200. Both match the recorded edge identity exactly.
* Spot check 2 (read-only): anon `GET /media/dd231fc014f56515f6282f77d5c2e998`
  answers 404 with body `404 page not found` and no `Cache-Control` header.
  This confirms the record's mixed verdict on check 3 independently.
* Code-claim cross-checks against the reviewed tree (which carries the
  deployed code unchanged, since the diff is docs only): `cmd/reprise/main.go`
  imports no Gemini package, sets `limits.StubOwnerAuth{}` at line 370,
  and `internal/api/routes.go` `handlerFor` returns nil for every unwired
  pattern so `handleStub` answers 501 at line 213. Each matches the record.
* Arithmetic cross-checks: 16.6 connected seconds at 4.50 dollars per hour
  is about 2.1 cents. An 1800 second mint hold is about 2.25 dollars, and
  three mints hold about 6.75 dollars of the 20 dollar ceiling. The mint
  count is internally consistent (check 1 mint, one honest mint, one closing
  mint). All figures in the record check out.
* No sessions were minted, no switches were flipped, and nothing was
  uploaded by this reviewer. Both spot checks were anonymous reads.

## The ten review questions

1. Does any path spend money without reserving budget first? No. The commit
   is docs only. The recorded live run minted sessions that hold budget
   before connecting.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. The record shows an explicit `session.end`, close reason
   `client_end`, and Sessions API status `completed`.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   The anon media read answers 404 with a body identical to the owner read,
   and the record carries no secret values.
4. Does any edit apply without a revertible record? No. The change is one
   git commit. Nothing applies to production state.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. The record makes no spoken-claim assertion, and the
   seeded greeting re-run is deferred per the task.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The scan finds none in the record or the message.
7. Did this task rebuild something Keel or Chaaya provides? No. No code
   ships in this commit.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   `git log -1` returns the handed hash in `t6.1b-rev-r1`, detached.
9. Can any paid job kind resume after a restart and spend twice? No. No job
   kinds are touched. The one live session ended explicitly.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. All speech is generated from `testdata/sessions` clips.

## In-scope findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `dev-diary/PHASE-6-ship.md:120` at the reviewed hash | The commit reverts the T6.1b status line to `not-started`. That path sits outside `owns: dev-diary/probes/production.md` and belongs to the orchestrator. Landing it would corrupt the shared status state that tracks live worktrees. | `git diff 5d33a24..2211a19 --name-only` lists `dev-diary/PHASE-6-ship.md`, which the `owns` line does not name. It fails while the record file alone would pass. | Change the T6.1b `status:` line back to `not-started` after restoring it. That single line edit reintroduces the violation. |

Fix: drop the `dev-diary/PHASE-6-ship.md` hunk before landing. Land only
`dev-diary/probes/production.md`.

## Out-of-scope production defects (non-blocking)

The record reports each defect below honestly with command and output. They
scope follow-up work. None blocks this record's approval.

| Severity | Where | What | Pin | Mutation | Owning path |
|---|---|---|---|---|---|
| H (OUT_OF_SCOPE) | `dev-diary/probes/production.md:82-104` | Episode, thread, decision, done, and publish routes answer 501, so no render, analysis, or gallery exists on the deploy. | Recorded `curl` of `/api/episodes` and `/api/threads` answers 501 `not_implemented`, plus `internal/api/routes.go:213` `handleStub`. Wiring any one pattern in `handlerFor` changes the answer. | Add a case for `/api/episodes` in `handlerFor` returning nil again. | `cmd/reprise/main.go` route wiring (T6.1 shape) and whichever phase owns each handler. |
| H (OUT_OF_SCOPE) | `dev-diary/probes/production.md:151-165` | The uploader opens blobs with literal owner `guest` while the media authorizer compares the resolved user id, so the uploader reads 404 on its own blob. | Recorded owner-cookie read answers the same 404 as the anon read. Sending the resolved user id as owner on open changes the answer. | Revert the open call to send literal `guest` as owner. | Upload open flow and media authorizer configuration in `cmd/reprise/main.go`. |
| M (OUT_OF_SCOPE) | `dev-diary/probes/production.md:167-194` | Admin pause and limits answer 403 behind `StubOwnerAuth`, so the kill switch cannot be exercised live. The refusal path is covered by broker unit tests only. | Recorded `POST /api/admin/limits/pause` answers 403 `owner_required`, plus `cmd/reprise/main.go:370` and `internal/limits/auth.go:28`. Granting a real owner credential changes the answer. | Restore `StubOwnerAuth` on the admin service after wiring the owner login. | `internal/limits/auth.go` and `cmd/reprise/main.go:370`. |
| H (OUT_OF_SCOPE) | `dev-diary/probes/production.md:196-209` | No Gemini path exists in the deployed binary, so the box-to-Gemini check is blocked. | `grep -n 'genai\|gemini\|Gemini' cmd/reprise/main.go` returns nothing at the reviewed hash. Adding the import and credential changes the answer. | Remove the Gemini import and wiring after it lands. | `cmd/reprise/main.go` and `internal/gemini`. |
| H (OUT_OF_SCOPE) | `dev-diary/probes/production.md:240-253` | Settle is unwired (`/end` 501, reconciler has no caller, spend view 403), so each mint holds about 2.25 dollars with no release path. Three mints hold about 6.75 dollars of the 20 dollar ceiling. | Recorded `POST /api/sessions/{id}/end` 501 plus `internal/broker/reconcile.go` with no caller in the binary. Calling reconciliation after end changes the answer. | Disconnect the reconciler caller after wiring it. | `cmd/reprise/main.go` wiring, `internal/broker/reconcile.go`, `internal/api/routes.go:54`. |
| M (OUT_OF_SCOPE) | `dev-diary/probes/production.md:138-160` | Anon media refusals carry no `Cache-Control: private, no-store`, so the header half of the privacy check fails. The 404 body reveals nothing about existence. | This reviewer's anon read of the recorded blob id returns 404 with no `Cache-Control` header. Adding the header on refusals changes the answer. | Strip the `Cache-Control` header from media refusals after adding it. | Keel `v0.3.0` media refusal path, or edge behavior, as the owning phase decides. |

## Counts

In-scope: C 0, H 1, M 0, L 0.
Out-of-scope (non-blocking): C 0, H 4, M 2, L 0.

## Verdict

REMEDIATE. One in-scope H finding, the `PHASE-6-ship.md` owns violation,
must be dropped before landing. The verification record itself is complete:
every first-deploy check carries its command and output, the seeded re-run
is deferred per the task, no secrets leak, no real voice appears, and both
read-only spot checks reproduce the recorded edge state.
