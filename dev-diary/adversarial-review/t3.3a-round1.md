# T3.3a review round 1

Reviewed hash `5e9cc9584c57273b28d667864de6e650e286e2e5` in worktree
`/home/nryn/work/reprise-wt/t3.3a-rev-r1` (detached HEAD, confirmed by
`git log -1`). Base `a314c71` present and confirmed an ancestor. The diff
touches only the declared owns paths (4 files under
`web/src/routes/episode/[id]/edit/` and `web/src/lib/editor/`). The tree
is clean after verification. Nothing was committed.

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. Zero findings, so no findings table.

## Pins re-run in this worktree

- `npx vitest run src/lib/editor/`: 17 passed (draft 13, fixture 4).
  New pins cover each non-cut revert, its fallback value, its decision
  row, the cleared callbacks flag, and the idempotent second revert.
- `npm run check` (svelte-check): 0 errors, 0 warnings.
- `npx eslint` on all owned source files: clean.
- `npm run build`: passes, static site written.
- Route Playwright spec
  (`npx playwright test -c src/routes/episode/[id]/edit/edit.playwright.config.ts`):
  5 passed. The new proof reverts one proposal of each non-cut kind
  (callback by keyboard) and asserts all four decision rows persist in
  the Decisions region plus the cleared callback notice. The preview
  server logged 404s for POST `decisions` during the run, so the
  refused-POST fallback is exercised live while every proof still
  passes. The `?gate=1` proof reads `Gates passed: accessibility and
  contrast.`
- `git grep` leak scan (`export let`, `$:`, `svelte/store`,
  `(writable|readable|derived|get)\s*\(`) over `web/src`: empty.
  `$derived.by` is the same behavior-identical form T3.3 round 1
  accepted, and no `.get(` shape entered.
- Plan-reference scan over the owned dirs: empty. Commit message
  (`Revert each non-cut proposal with one action and a decision row`)
  cites no task, phase, or planning file.
- Mutation spot check: flipping `callbacksCleared: true` to `false`
  fails exactly the callback-revert unit test (1 failed, 12 passed),
  so the pin is load-bearing. Tree restored clean afterwards.

## Library shapes checked against installed Chaaya 0.2.0

`TranscriptEditor.revert` is used only for cuts, as before. Non-cut
reverts reuse the decision POST shape and the local-first pattern,
never a library method, which matches the handoff claim that the
transcript module carries a revert affordance for cuts only. No gap,
so no issue to file.

## Scrutiny answers

(a) `callbacksCleared` is a client flag lost on reload, but so is
every decision row: the whole snapshot is in-memory local-first state
with no persistence, exactly the pattern T3.3 round 1 accepted. The
flag is no more ephemeral than the `prop-callback` row beside it, and
reconciling either against a real backend belongs to the handler task
that serves the stubs. Fixture-only scope, not a defect.
(b) POST ids `prop-cold-open`, `prop-title`, `prop-notes`,
`prop-callback` with decision `reverted` match the fixture proposal
ids in `fixture.ts` and the cut-revert shape from T3.3. The endpoints
are stubs today, so the shape is forward-looking, stated openly in
the handoff. Unit tests pin each row; the Playwright run pins all
four persisting on screen.
(c) The handoff says the render reads the `prop-cold-open` row plus
the reverted flag and the memory change reads the `prop-callback`
row plus the cleared row. T3.4 is unlanded and its handoff records no
such reader, so that half of the contract is unilateral as of this
review. The shared surface that exists, the POST shape, matches the
established pattern, and the flag itself is client snapshot state no
Go render could read directly. Noted here so T3.4 can confirm or
correct on its own round. Nothing to fix in this commit.
(d) Refused POST changes nothing on screen: pinned live. The run
above 404s every decisions POST and still shows every row, because
the local row is written first and the catch swallows the refusal.
(e) The show-notes section is new and follows the existing patterns:
native buttons with accessible names, labelled sections, struck
proposal plus plain notice after revert. The `?gate=1` proof passes
both library gates with the section present, and the callback revert
is driven by keyboard in the spec.
(f) Second-revert idempotence is pinned per kind: each of the four
unit tests reverts twice and asserts exactly one decision row. The
screen answers the second attempt with an already-reverted notice
and no new row.
(g) Runes only, no `.get(` shapes, no plan refs. Both scans empty.

## The ten questions

1. No path spends money. The only network calls are stub POSTs that
   fail into fixture behavior.
2. No AssemblyAI session exists in this task.
3. No private episode, stem, or transcript reaches past the owner.
   The audio is a generated tone, and there is no session or cookie.
4. Every revert writes its decision row locally before anything
   else, cuts and non-cuts alike.
5. The callback quote comes from `quoteRange` over stored words,
   never from invention. No counts are spoken.
6. No code, comment, test, or migration cites a phase, a task, or a
   planning file. Both scans are empty.
7. Nothing rebuilds Keel or Chaaya. Non-cut reverts deliberately do
   not reuse the cuts-only library method.
8. Yes. This review ran on the handed hash in its own worktree, with
   a clean tree and no commits.
9. No paid job kind runs here. The render start is a stub POST fired
   once per confirm, and `JobStream` only follows.
10. No check needs a person, a microphone, or a wall-clock threshold.
    Audio comes from a generated fixture, and every assertion reads
    fixed values.

## Observations, not findings

The title fallback `Episode <id>` differs from the backend model
fallback `Untitled episode (N words)`, but the task block prescribes
the episode-number title explicitly, and `Episode <id>` is this
screen's own label convention. Spec-compliant as written.

Zero residue: round 1 is the only round, and it returns APPROVE with
zero findings across all severities.
