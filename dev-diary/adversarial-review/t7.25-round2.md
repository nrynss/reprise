# T7.25 round 2 review

Reviewed hash: `0249ab9931c47e9d54826f4a2b9869c6c842c9e1`
Worktree: `/Users/narayan/reprise-wt/t7.25-rev-r2` (detached HEAD).
Parent is `2f42eed395f6915a2213ccf73f6be664c840ee64`, the round 1 hash.
`git log -1` matches the handed hash. `git status --short` was empty before the pins and empty again after the mutation was restored.
Diff reviewed: `git diff 2f42eed395f6915a2213ccf73f6be664c840ee64..0249ab9931c47e9d54826f4a2b9869c6c842c9e1`, plus the combined diff from `742c0363db343743fcd4143e0b4aa65cfd1e7293`.
No code commits. The tree is still that hash.

## The ten questions

1. Does any path spend money without reserving budget first? No. The diff adds no paid call. It only skips a browser navigation when the path is not the record page.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The draft move still starts only after `voice.end` and the HTTP close. `destroy` on a live take still calls `voice.end`. The token cap is unchanged.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The commit changes no media URL, cookie, or owner check.
4. Does any edit apply without a revertible record? No. Nothing applies an edit. A late success only skips `location.assign`.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The new comments describe the path check and the gallery case. They do not cite a task id, a phase id, or a planning file.
7. Did this task rebuild something Keel or Chaaya provides? No. The guard is a product navigation check on the existing controller. `destroy` still uses Chaaya `SessionGuard`.
8. Was this review run on the handed hash, in a worktree of its own? Yes. This detached worktree is the only tree the review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind. Skipping the assign does not post the move again.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The gallery proofs and the controller proofs use the mock take. None waits on a person, a microphone, or a clock threshold.

## What the commit does

`goProcessing` returns when `destroyed` is set, at `web/src/lib/voice/record-state.ts:518`. It also returns when `onRecordPage` is false, at line 519. `onRecordPage` strips trailing slashes and keeps only the path `/record`, at lines 528 to 530. End and retry still assign while that path is current. The draft move is not cancelled.

## Pins

`npm ci` in this worktree's `web/` added 236 packages. Exit 0. `node_modules` was missing here.
`npm run build` in `web/` exited 0. Preview serves that build.

The late-move filter was run twice, back to back, on that build.
The command was `npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts -g "finishes after the gallery"`.
Run 1 passed 2. Exit 0.
Run 2 passed 2. Exit 0.

The full gallery spec then passed 10. Exit 0.
`npx playwright test -c src/lib/voice/mock.playwright.config.ts controller-complete.spec.ts --workers=1` passed 2. Exit 0.
A move that finishes on the record page still reaches processing. The path check does not skip that assign.

The preview log shows `404` for `GET /api/episodes`. The season heading still renders. That 404 is the preview server, not this commit.

`./tools/check.sh` was not run. This commit does not change the ffmpeg pin.

## Mutation

The `onRecordPage` return was deleted from `goProcessing`. The destroyed return stayed. The web build was refreshed, because preview serves `build` and not the source.
The served function was `if(this.destroyed)return` and then `location.assign`. `onRecordPage` stayed in the bundle and was not called.
`build/record.html` preloaded that new node, `8.B47mlfX2.js`.

The same late-move filter was run on that bundle. Both proofs passed. Exit 0.
`--repeat-each=15` then passed 30. Exit 0.
None settled on `/processing`. The assign spy did not record a processing URL.
The destroyed flag alone kept the guest on the gallery. The path check is not what these proofs measure.

`git checkout` restored `web/src/lib/voice/record-state.ts`. `git status --short` was empty. `git grep` shows the path check at line 519 again. A later build puts the check back in the served `goProcessing`.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `web/src/routes/record/gallery-link.spec.ts:242` and `:257` | Both late-move proofs stay green after the path check is removed. `goProcessing` then returns only when `destroyed` is set. The record effect has already called `destroy` by the time the held move fulfills. The proofs never open the landing window, where the gallery heading is visible and `destroyed` is still false. They do not show that a non-record path skips the assign. | Delete the `onRecordPage` return, rebuild, and run the late-move filter. The command is `npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts -g "finishes after the gallery"`. That run exited 0 with 2 passed. A repeat of 15 then exited 0 with 30 passed. The served `goProcessing` returns only on `destroyed`, and no proof reached `/processing`. | Delete the `onRecordPage` return inside `goProcessing` at `web/src/lib/voice/record-state.ts:519`. Leave the destroyed return in place. |

## Counts

C 0, H 1, M 0, L 0. In-scope findings: one. Out of scope findings: zero.

## Residue

Round 1 recorded zero in-scope findings. Residue against that round is zero at C, zero at H, zero at M, and zero at L.
The landing failure is not cleared. The new path check is not what keeps these proofs on the gallery. Removing it left every late-move run green.

## Verdict

REMEDIATE
