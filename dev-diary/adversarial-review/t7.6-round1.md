# T7.6 round 1 review

Reviewed hash: `39f90382fdcbd94f9a47928f5527fbd3bb512168`
Worktree: `/home/nryn/work/reprise-wt/t7.6-rev-r1` (detached HEAD, confirmed with
`git log -1`). Diff reviewed: `git diff 6701751..39f90382` (3 files:
`cmd/reprise/main.go`, `internal/episode/complete.go`,
`internal/episode/complete_test.go`). No code committed by this reviewer.
Probe pin files used below were deleted after their runs, so the tree
holds only this file.

## Ten questions

1. Does any path spend money without reserving budget first? No. The new
   schedule paths call the existing transcript and editorial runs, which
   reserve through the owner budget adapter before any paid call. No new
   spending path exists.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Session handling is untouched by this commit.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. The route answers 401 with no session, checks both blobs against
   the media index owner, and owner-scopes the episode move. Unknown and
   foreign blobs both answer 404 `stems_not_found`. Success carries
   `Cache-Control: no-store`.
4. Does any edit apply without a revertible record? No edits apply here.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? Untouched.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The diff greps clean for task ids, phase ids,
   planning file names, and section marks. The gate plan-reference scan
   passed.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses
   the Keel job runner, stores, and media library. The
   StartKind-never-blocks claim was checked against Keel `v0.3.0`
   `job.go` and holds.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes, stated above.
9. Can any paid job kind resume after a restart and spend twice? No.
   Both edit kinds stay `job.Kind{Limit: 2}` with no idempotent flag and
   no resume func, so a restart marks them interrupted and fails the
   episode instead of rerunning. All schedule guards err toward blocking
   rather than double starting. Finding M1 below shows the blocking
   errs too far, toward unrelated episodes, which is liveness, not
   spend.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The committed tests use fixtures. The
    probes used generated sine audio and a poll deadline only to await
    async job reports, with no timing assertion.

## Verification runs

- Gate: `npm ci` in `web/`, then `./tools/check.sh` reports OK all
  checks passed.
- `go test -race -count=1 ./cmd/reprise/ ./internal/episode/` passed
  three times, zero flakes. The handoff names a flaky privacy resume
  test, but no test carries that name. The closest matches
  (`TestRestartFailsInterruptedPaidJobs`, `TestGetHidesForeignEpisodes`)
  passed every run.
- `python3 dev-diary/tools/audit_docs.py` reports OK.
- Committed `complete_test.go` pins the move once, first pair wins,
  healing, and refusals. All pass.
- No committed test touches the new route, either ensure, the chain,
  or the boot recovery. The three pins below were written and run in
  this worktree, failed as stated, then removed.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `cmd/reprise/main.go:1630` and `1686` | One undescribed same-kind job blocks scheduling for every episode. `unfinishedEpisode` sets `unknown` for any job whose progress carries no episode, and `ensureTranscript` (plus the editorial twin) returns without scheduling when `unknown` is set. A completion for an episode no job covers answers 200 with `scheduled` false during the window before the unrelated job reports. | `TestProbeSilentJobBlocksUnrelatedEpisode`: one queued `edit_transcript` job with empty progress, then `ensureTranscript` for another episode. Fails with scheduled false, want true. Ran under `-race`, failed. | Restore `unknown` in the early return, so the condition reads `running \|\| unknown` again. |
| M | `cmd/reprise/main.go:1556` | A full editorial queue answers 500 `internal_error` while a full transcript queue answers 429 `pipeline_busy`. The transcript path maps `job.ErrLimit` to retry, the editorial path maps every error to internal, so the same retryable state reports two codes. | `TestProbeEditorialFullQueueAnswersRetry`: two blocking editorial jobs with descriptors for other episodes, words landed, then a completion through the middleware. Fails with status 500 and `internal_error`, want 429. Ran under `-race`, failed. | Map the editorial ensure error to `CodeInternal` with no `ErrLimit` branch. |
| M | `internal/episode/complete.go:56` and schema `internal/store/migrations/0001_schema.sql:34` | Concurrent completions store duplicate stem rows. `linkStems` reads roles then inserts, with no unique guard on the pair, so two racers both insert one role. Reads stay correct through oldest wins, but orphan rows accumulate. | `TestProbeConcurrentCompleteStemsKeepsOnePair`: eight completions with distinct blobs behind one barrier. Fails with stems 3, want 2. Ran under `-race` with `-count=3`, failed twice. | Keep the plain read then insert with no uniqueness on the episode and role pair. |
| OUT_OF_SCOPE (M, non-blocking) | `web/` record page tree | No browser caller posts to the new route, so only fixture and curl flows reach the schedule. This matches the disclosed contract item in the handoff, and the task done clauses cover fixture flows only. | `git grep -n 'stems/complete' -- web/` returns nothing. Owning path is the record page, outside this task. | Wire the page to post on upload completion. |

## Verdict

REMEDIATE. Counts: C 0, H 0, M 3, L 0, plus one non-blocking
OUT_OF_SCOPE. The move itself is pinned and correct. The scheduling
around it needs the three fixes above, each with a committed test
shaped like its pin.
