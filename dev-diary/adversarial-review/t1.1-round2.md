# T1.1 round 2 review

Hash reviewed: `8d7a9bf2f9f2e502292f85bf19f35e51947def1a`.
Worktree: `/home/nryn/work/reprise-wt/t1.1-rev-r2`, detached at the handed hash.
Confirmed with `git log -1` before reviewing (`8d7a9bf ... Add the missing module entries for the diary store`, `git symbolic-ref` reports DETACHED). Base for the remediation diff is round 1 hash `44d724c738364c7ef1ce488a4afc1839f0cd7400`.
Tree was clean before reviewing and is clean now (`git status --porcelain` empty at the end). Probes used `/tmp` files and one in-module temp probe directory, all removed. Nothing was committed.

## Scope

T1.1 owns `internal/store/` and `internal/store/migrations/`. The remediation was sanctioned to change ONLY `go.mod` and `go.sum` via `go mod tidy`.
Owned files re-read whole this round: `internal/store/store.go`, `internal/store/errors.go`, `internal/store/migrations/0001_schema.sql`, `internal/store/store_test.go`.

## 1. Remediation diff verification

`git diff 44d724c738364c7ef1ce488a4afc1839f0cd7400..8d7a9bf2f9f2e502292f85bf19f35e51947def1a --name-only` prints exactly:

- `go.mod`
- `go.sum`

`go.mod` diff: the single-line `require github.com/pelletier/go-toml/v2 v2.4.3 // indirect` becomes a require block holding that same line plus ten added indirect requires: `dustin/go-humanize v1.0.1`, `google/uuid v1.6.0`, `mattn/go-isatty v0.0.24`, `ncruces/go-strftime v1.0.0`, `remyoudompheng/bigfft v0.0.0-20230129092748-24d4a6f8daec`, `golang.org/x/sys v0.47.0`, `modernc.org/libc v1.75.6`, `modernc.org/mathutil v1.7.1`, `modernc.org/memory v1.12.1`, `modernc.org/sqlite v1.58.0`. The `modernc.org/sqlite` indirect entry round 1 H1 asked for is present.
`go.sum` diff: 50 added lines, no deletions, covering the same transitive closure (humanize, pprof, uuid, golang-lru, isatty, strftime, bigfft, x/mod, x/sync, x/sys, x/tools, cc/v4, ccgo/v4, fileutil, gc/v2, gc/v3, goabi0, libc, mathutil, memory, opt, sortutil, sqlite, strutil, token).
No version edits: `require github.com/nrynss/keel v0.3.0` is byte-identical to round 1. No replace: `grep -c '^replace' go.mod` prints `0`.
Owned-path diff is empty: `git diff 44d724c..8d7a9bf -- internal/store internal/store/migrations` prints nothing. Pass.

## 2. Round 1 H1 pin, re-run uncached, with mutation and restore

Pin: `go clean -testcache && go test -race -count=1 ./internal/store/...` in the review worktree. Exit `0`, all three tests pass:

- `TestMigrateAppliesTwiceWithNoChange` PASS
- `TestDeleteEpisodeErasesContent` PASS
- `TestOpenRejectsNilDatabase` PASS (`ok github.com/nrynss/reprise/internal/store 1.101s`, verbose re-run confirms the same three names)

Mutation, planted in the review worktree then reverted: deleted the added `modernc.org/sqlite v1.58.0 // indirect` require line from `go.mod` and all `modernc.org/sqlite` lines from `go.sum` (python edit, `grep -c` confirms `0`/`0`). The pin then fails in setup with the exact round 1 error:

- `sqlite.go:40:2: missing go.sum entry for module providing package modernc.org/sqlite (imported by github.com/nrynss/keel/sqlite)`
- `FAIL github.com/nrynss/reprise/internal/store [setup failed]`

Restore: `go mod tidy` in the worktree, pin green again (`ok ... 1.101s`), and `diff` of both `go.mod` and `go.sum` against pre-plant backups prints nothing (`RESTORE-IDENTICAL`), `git status --porcelain` empty. The pin is load-bearing and the remediation is exactly the tidy closure. Pass.

## 3. Done-when probes, run independently against the reviewed migration file

All probes ran against `/home/nryn/work/reprise-wt/t1.1-rev-r2/internal/store/migrations/0001_schema.sql` or the reviewed `store.Open`, not just the implementer's test output.

1. Schema shape through `sqlite3` (fresh `/tmp` database, `.read` of the reviewed SQL file). All twelve spec tables exist: `analyses callbacks decisions episodes mentions proposals renders sessions stems turns users words`. `users` pragma is `(id, kind, created_at, last_seen_at)`; `episodes` pragma is `(id, owner_id, number, title, state, visibility, share_token, seeded)`. Every content table holds both `owner_id` and `episode_id` (each returns count `2` over `pragma_table_info`); `users` holds no `owner_id` (count `0`). No `BEGIN`/`COMMIT` in the file (`grep -ci` returns `0`; Keel wraps each file with its ledger row). No spend/cost/flag tables (`sqlite_master` count for `%spend%|%flag%|%cost%` is `0`). Pass.
2. Cascade erase through `sqlite3` with `PRAGMA foreign_keys=ON`. Seeded one owner plus one episode plus one row per content table from the reviewed schema, deleted the episode. After delete: `users|1`, and `episodes sessions stems turns words proposals decisions renders analyses mentions callbacks` all `0`. The owner survives, every content row is gone. Pass.
3. Migrate twice through an independent in-module probe (temporary `probe-t11r2-tmp/main.go`, removed after). It calls reviewed `store.Open` twice on one file and compares the `reprise_schema_migrations` ledger: `first=["0001_schema.sql@1789750836891"] second=["0001_schema.sql@1789750836891"] equal=true`, exit `0`. Ledger equality, one migration file, no change on second boot. Pass. (The probe had to live inside the module because `internal/` cannot be imported from `/tmp`.)
4. Erase over a fresh connection is additionally covered by the implementer's `TestDeleteEpisodeErasesContent`, which closes the Keel handle and re-opens the file with a fresh `sql.Open("sqlite", path)` before counting — verified by reading the test source — and it passes in the pin run above. My sqlite3 probe in (2) is the fully independent copy of the same assertion. Pass.

## 4. Residue re-check of the owned files

Table/column coverage against the T1.1 table: `users` (kind, created, last seen), `episodes` (owner, number, title, state, visibility, share token, seeded), `sessions` (provider session id, token cap, connected seconds), `stems` (media id, role, sample rate, start offset), `turns` (role, text, clock times, provider item id), `words` (text, times, source), `proposals` (kind, word range, reason), `decisions` (proposal link, decision), `renders` (input hash, two media ids, loudness), `analyses` (transcript id, chapters, summary, entities, key phrases), `mentions` (kind, word offset, quote), `callbacks` (mention link, used flag). All present with the stated types in the reviewed SQL.
Cascade coverage: `PRAGMA foreign_key_list` on each of `sessions stems turns words proposals decisions renders analyses mentions callbacks` shows an `episode_id -> episodes(id) ON DELETE CASCADE` row; `decisions` additionally cascades `proposal_id -> proposals(id)`; `callbacks` additionally cascades `mention_id -> mentions(id)`. `episodes.owner_id -> users(id)` has no cascade, so the owner survives. Matches the probe result. No residue.
No spend and no flag tables in the migration (probed above); the file header comment states spend and runtime switches stay in the Keel stores.
Go style: `gofmt -l internal/store/` prints nothing; `go vet ./internal/store/...` exits `0`. `Open` takes `ctx` first, never takes ownership of `db`. Exported identifiers (`Store`, `DB`, `Open`, `ErrInvalid`) carry doc comments starting with their names. No `panic`, no naked return, no pointer to slice, one sentinel wrapping with `%w`. No residue.
Doc style: grep over `internal/store/` for task ids, phase ids, planning file names, `TODO`/`FIXME`/`XXX`/`HACK`, and consumer-name/dash patterns returns empty; `git grep` plan-reference pattern scoped to the owned paths returns `OWNED-CLEAN`. Commit message `Add the missing module entries for the diary store` uses active voice and cites no plan. No `LICENSE` file exists. No residue.

## 5. Keel namespace non-collision against the installed source

Installed Keel is `v0.3.0` at `GOMODCACHE/github.com/nrynss/keel@v0.3.0`. Namespaces found via `Namespace = "..."`: `cost`, `flag`, `job`, `lease`, `mediastore`. The diary migrates under `reprise` (`const schemaNamespace = "reprise"`), producing ledger `reprise_schema_migrations`, disjoint from Keel's `<namespace>_schema_migrations` ledgers. Keel tables found (`cost_charge cost_budget cost_reservation cost_owner_budget flag_value jobs lease_entry media`) collide with none of the twelve diary tables (each checked individually: all `ok`). No residue.

## 6. Gate

`npm ci` run in the worktree's `web/`, then `./tools/check.sh`. Every stage through Go (`gofmt`, `go vet`, `staticcheck`, `go test -race`) and web (`svelte-kit sync`, `svelte-check`, `eslint`, `vitest`, `build`, `playwright`) and `svelte 4 leakage` passes. The run stops ONLY at the `consumer names` scan, which flags one line: `dev-diary/mockup/V1 Reprise-Interactive Prototype.html:11` (the word `hackathon`). The plan-references scan passes whole-tree. No other failure exists.

Note on the handoff: the brief expected this worktree to predate the mockup commit and exit `0` end to end. In fact `dc69195` (the mockup commit) IS an ancestor of this worktree's HEAD — the file exists on disk — while protocol commit `34e20b9` is not an ancestor, so the scan-exemption wording from that commit is not visible here. Judged against the task contract as instructed, the single mockup hit is OUT_OF_SCOPE (owning path `dev-diary/mockup/`, landed outside this task's loop); the exit code is `1` solely because of it. The brief's `exit 0` expectation is the only statement this review contradicts, and the contradiction is explained by ancestry, not by this task. Another round already records the hit; it is not re-recorded as in-scope below.

## Ten review questions

1. Does any path spend money without reserving budget first. No. Schema and module metadata only; no provider dials, no job code.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. No session code; the schema stores `token_cap` and `connected_seconds` as columns only.
3. Can a private episode, stem or transcript reach anyone but its owner. No. Nothing is served; every content row carries `owner_id` and the migration adds no public surface.
4. Does any edit apply without a revertible record. No. No edit applies; proposals and decisions exist as append-only tables.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. No spoken content, no prompts.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. Owned-path greps and the owned-path `git grep` plan-reference scan are empty.
7. Did this task rebuild something Keel or Chaaya provides. No. Migration and pooling come from `keel/sqlite`; the tidy closure pins exactly that dependency tree.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` showed `8d7a9bf2f9f2e502292f85bf19f35e51947def1a`, `git symbolic-ref` reports DETACHED, worktree `t1.1-rev-r2`. Every pin ran there or in `/tmp` scratch files. Nothing was committed.
9. Can any paid job kind resume after a restart and spend twice. No. No job code.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Tests use temp files and fixed ids with exact counts; ledger comparison uses string equality only.

## Residue against round 1, severity by severity

- C (round 1 count 0): no new C found. Zero residue.
- H (round 1 count 1, H1 missing `go.sum` entries): fixed by the remediation commit and verified above — pin passes uncached, mutation reintroduces the exact error, `go mod tidy` restores byte-identical metadata, owned paths untouched, Go and web gate stages pass. Zero residue.
- M (round 1 count 0): no new M found. Zero residue.
- L (round 1 count 0): no new L found. Zero residue.

Explicit claim: zero in-scope residue against round 1 across all severities.

## Findings

Severity counts (in-scope): C 0, H 0, M 0, L 0. No in-scope findings.

### OUT_OF_SCOPE. Consumer-name scan hit in the design mockup

OUT_OF_SCOPE (owning path `dev-diary/mockup/`, outside T1.1 `owns`).
Where: `dev-diary/mockup/V1 Reprise-Interactive Prototype.html:11` at the reviewed hash.
What: the line contains the word `hackathon` (`audience: hackathon judges + product owner; ...`), which the `consumer names` gate scan forbids in tracked files.
Pin: `./tools/check.sh` in this worktree passes every stage through `svelte 4 leakage` and then `FAIL [consumer names]` naming exactly that line; exit `1`.
Mutation: deleting that word from the line clears the scan; restoring it re-trips it.
This does not block the verdict per the protocol and the handoff; another round already records it.

## Verdict

APPROVE. Zero in-scope findings. H1 is fixed and load-bearing, both done-when clauses are proved independently, owned files show no residue, and the Keel namespace does not collide.
