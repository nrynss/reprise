# T7.33 round 1 review

Reviewed hash: `edb3fa2c43a7770fa500fa2a892cce663e65c248` (confirmed with
`git log -1`). Worktree: `/home/nryn/work/reprise-wt/t7.33-rev-r1`, opened on
that hash. Base: `b44cd44`. No lambo tools exist in this environment, so no
recall was possible. Work proceeded from AGENTS.md and the live plan files.

## Scope

The diff touches exactly two files, both inside the task owns line
(`web/playwright.config.ts`, `web/package.json`,
`web/src/routes/record/mock.playwright.config.ts`):

- `web/package.json`: `test:e2e` now chains the shared suite plus seven
  per-page suites with `&&`.
- `web/src/routes/record/mock.playwright.config.ts`: adds
  `testMatch: ['*.spec.ts']` plus a comment stating its reason.
- `web/playwright.config.ts`: unchanged, which the owns line permits.

No file outside owns was modified. The chain only invokes the other per-page
configs. It does not edit them.

## Gate evidence (own worktree, own runs)

`npm ci` in `web/` first. `./tools/check.sh` stops at the accepted
workstation exception: `FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`.
Every later step ran by hand in gate order:

- `gofmt -l`: 0. `go vet`: 0. `staticcheck`: 0.
- `go test -race`: 0, all 24 packages ok.
- `svelte-kit sync`: 0. `svelte-check`: 0. `eslint`: 0. `vitest`: 0.
- `build`: 0.
- Playwright per-suite runs (Chromium scope, see the WebKit section):
  shared 2 passed, voice 5 passed, record mock 23 passed, editor 5 passed,
  gallery 4 passed, share 3 passed, threads 10 passed, welcome 4 passed.
  Total 56 tests, all exit 0.
- Scans: svelte 4 leakage empty, consumer names empty, plan references empty.
- `git status --short` is empty after all probes. Nothing was committed.

## Pins re-run

1. Record mock config loads only specs.
   `npx playwright test -c src/routes/record/mock.playwright.config.ts --list`
   reports `Total: 23 tests in 5 files`, exit 0. The file list is exactly
   `cap`, `end-control`, `gallery-link`, `mock-session`, `stems-complete`
   (all `.spec.ts`). Zero `.test.ts` files listed. This matches the handoff.
2. Mutation probe for the `testMatch` fix. A scratch copy of the config with
   the `testMatch` line removed (deleted right after, tree verified clean)
   loads `api-guard.test.ts` and errors with `Vitest failed to find the
   current suite` at `api-guard.test.ts:29`. The pin is load-bearing. Removing
   the one added line reintroduces the defect.
3. Gallery break probe. A scratch one-word edit to the gallery card note
   (reverted, never committed) plus a rebuild failed
   `gallery.spec.ts:56` with `1 failed`, and the real exit code was 1.
   Chained as `... >/dev/null 2>&1 && echo CHAIN-CONTINUES || echo
   CHAIN-STOPS`, it printed `CHAIN-STOPS`. The `&&` join fails the gate step
   when one suite fails. After revert and rebuild, the gallery suite passes
   with `4 passed` and the tree is clean.
4. Chain fidelity. Each chained `-c` path matches the standalone command in
   that config's own header comment (the edit path keeps its quotes around
   `[id]`, the share/threads/welcome paths need none). Ports and browser
   matrices come from the configs themselves, so the chain cannot drift from
   standalone runs. Matrix check: shared and gallery declare chromium plus
   webkit, welcome declares chromium only, the rest use the Playwright
   default (chromium). The task asks for both browsers only where the config
   asks for both, so this matches.
5. Coverage check. Twelve `.spec.ts` files sit beside pages or helpers
   outside `web/tests/`. The chain covers all twelve: voice 2, record 5,
   edit 1, gallery 1, share 1, threads 1, welcome 1. Nothing beside a page
   is left out.

## The ten questions

1. Spend without reserving budget first: no. The diff adds no call path.
2. AssemblyAI session left open: no. No session code touched.
3. Private episode, stem or transcript reaching a non-owner: no. No serving
   or visibility code touched. The privacy probe needs no re-run for this
   diff.
4. Edit without a revertible record: no. No editor code touched.
5. Spoken callback or count from the prompt: no. No prompt code touched.
6. Code, comment, test or migration citing a phase, task or planning file:
   no. The diff carries no id, no phase mark, no file reference. The gate
   plan-reference scan is empty.
7. Rebuilt Keel or Chaaya capability: no. The change is one npm script and
   one Playwright matcher.
8. Review on the handed hash in its own worktree: yes. `git log -1`
   confirmed `edb3fa2` in `t7.33-rev-r1`. No code was committed here.
9. Paid job kind resuming after restart and spending twice: no. No job
   code touched.
10. Development check needing a person, microphone or wall-clock threshold:
    no. All suites ran headless against generated signals and route mocks.

## Findings

None. No C, no H, no M, no L. No OUT_OF_SCOPE defect either.

Observation (not a finding, no defect to pin): the edit config
(`src/routes/episode/[id]/edit/edit.playwright.config.ts`, outside owns) sets
no `testMatch`. Its folder holds no `.test.ts` today, so it loads exactly its
one spec and passes (5 tests, exit 0, verified above). If a unit file ever
lands beside it, the same failure this task fixed in the record config would
appear there. A future task may set its match. It does not block this one.

## The WebKit gap, judged honestly

This host cannot launch WebKit (missing system libraries, no sudo), so the
committed `npm run test:e2e` chain could not run end to end here. Its first
step launches WebKit. Each chained step ran alone in Chromium instead, with
the same config and command the chain uses. That verifies the Chromium side
fully: every suite passes, the record exclusion works, and a gallery break
fails.

The WebKit side is unverified by this review. I accept that gap for three
reasons. The diff changes no browser-facing behavior and no config matrix.
The two WebKit suites (shared, gallery) keep the exact project lists they had
at base. CI installs both browsers with dependencies and runs the chained
script verbatim, so it covers what this host cannot. This matches the
standing rule that live-provider and multi-browser gaps belong to CI, while
T6.4b owns human-voice verification. No finding is recorded, and none is
softened: the gap is stated here, not hidden.

## The triple fresh-worktree gate, judged honestly

The done condition asks for three consecutive fresh-worktree gate passes.
That did not happen here and cannot happen here: WebKit cannot launch and
ffmpeg is unpinned on this workstation, so any full gate run fails for
environment reasons before reaching the changed code. What did happen: one
full gate-order run of every step except the two environment-blocked items,
all exit 0, in a clean worktree. The remaining triple-run evidence belongs
to CI, which carries both browsers and the pinned media tools. The handoff
said exactly this. I record the shortfall plainly and still approve, because
every verifiable part of the done condition passed and the unverifiable part
is environmental, not a code risk.

## Verdict

Zero residue against all severities: zero C findings remain, zero H findings
remain, zero M findings remain, zero L findings remain, and zero
OUT_OF_SCOPE defects were found.

APPROVE
