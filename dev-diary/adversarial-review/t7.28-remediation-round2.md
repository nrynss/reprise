# T7.28 remediation, round 2

- **Reviewed hash:** `30371cc980142119de1478bdcfcefaada7f722fc`
- **Worktree:** `/Users/narayan/reprise-wt/t7.28-rem-r2`, branch `task/t7.28-rem-r2`
- **Final hash:** `931480ddabbbfb579ab0112bd185e874e2317ba7`
- **Commits on top of `30371cc`**, with no amend and no rebase:
  - `8b66881` fixes finding 1.
  - `7715c72` fixes finding 2.
  - `1451506` fixes finding 3.
  - `931480d` finishes finding 1. The editorial outcome read now needs a transcript kind, as the
    transcript outcome read does. Without it, two existing detail tests got 500 from a database
    with no job rows.

Every edit sits in `owns` or in a test file beside an owned file.

## Findings

| # | Severity | Finding | Fix | Commit | Pin that now passes | Mutation fails it |
|---|---|---|---|---|---|---|
| 1 | M | A draft whose transcript pass read done offered the editor before the editorial pass stored anything. | The detail now sends `editorial_outcome`, read by a new `EditorialOutcome` on `episode.Service` in `playback.go`. `threads.ts` adds `galleryPass`. A draft whose transcript is done and holds no title proposal follows the editorial pass. `galleryCardKind` opens the editor only when the job is done and `row.proposed` holds. A failed, interrupted or cancelled editorial pass shows its reason on the card. A pass that finished with no proposals, or never started, says so. A draft whose job stops while the gallery is open reads its detail once more. A terminal job with no note no longer reads "Working". | `8b66881`, `931480d` | `web/src/routes/gallery.spec.ts`: "a live draft whose editorial pass has stored nothing does not open the editor" (the reviewer's pin, moved) and "a live draft whose editorial pass failed says so on its card". 8 passed across chromium and webkit. `gallery-cards.test.ts` adds live controller cases for a running, a failed and a missing editorial pass, and five `galleryPass` cases. Go: `TestEditorialOutcomeReadsTheEditorialPass`, `TestEditorialOutcomeWithoutPass`, `TestEditorialOutcomeWithoutTranscriptKind` and `TestEpisodeDetailCarriesEditorialOutcome`. The reviewer's own spec, copied into `web/tests/` for one run and removed, passes (2 passed). | Yes. Returning `'editor'` for any done draft job, whatever `row.proposed` says, fails the moved pin in chromium and webkit (`toHaveCount` 0). It also fails 2 unit cases in `gallery-cards.test.ts`. |
| 2 | L | `renderClockWords` ignored the 10 ms crossfade at each join and on each side of the cold open gap. | `renderClockWords` now builds the spans the render keeps, merging touching cuts. It fades a join only when both sides last 10 ms, as `JoinFades` does. Each word moves back by the fades before its span. The cold open lead is the opening's joined length plus the 750 ms gap, less the two gap fades. All arithmetic runs in whole milliseconds, as the render does. | `7715c72` | `render-clock.test.ts`. The moved round 1 pin now expects "Two" at 0.99 s, a seek to 0.99 s and a length of 1.99 s. The cold open case expects "One" at 1.73 s and "Two" at 2.72 s. New cases: "places a word after two joins where the rendered file plays it" (the reviewer's pin, "e" at 1.98 s) and "butts a join together when one side is shorter than the fade". The reviewer's `render-crossfade.pin.test.ts`, copied in for one run and removed, passes. | Yes. Dropping `- fadesBefore(ms(seconds))`, so the edited time plus the lead stands alone, fails 3 of 9 cases in `render-clock.test.ts` and the reviewer's pin (`expected 0.020000000000000018 to be less than 0.001`). |
| 3 | L | The opening comment of `render-clock.test.ts` ran one 34 word sentence. | Split it into three sentences. | `1451506` | `long_sentences.py web/src/routes/threads/render-clock.test.ts` exits 0. | Yes. The joined sentence from `30371cc` makes the script print it and exit 1. |

## Measurements behind finding 2

The reviewer's `crossfade_clock_probe_test.go` stays a probe. It sits beside `internal/render/`,
which this task does not own. I copied it into `internal/render/` for one run and removed it. It
passed and logged `burst lands at 2.4800 s, output length 2.9800 s`.

I measured the cold open case the same way, with a throwaway probe I did not keep. It ran the real
`planAssembly` graph through ffmpeg on a 3.5 s take. The cut covered 1 to 2 s and the cold open 2 to
3 s. Bursts sat at 0.5 s and 2.5 s. The output placed the episode's 0.5 s burst at 2.2300 s, and
the 2.5 s burst at 3.2200 s. The page now places "One" at 1.73 s and "Two" at 2.72 s, each 0.5 s
before those bursts.

## Contract change

The editorial job kind is now named twice. `cmd/reprise/main.go` names it `kindEditorial`, and
`internal/episode/playback.go` names it `EditorialKind`. Both read `"editorial"`. `cmd/reprise`
sits outside `owns`, so I could not point the binary at the shared constant. A task owning
`cmd/reprise/main.go` should set `kindEditorial = episode.EditorialKind`, so the two cannot drift.

## Gate

`cd web && npm ci`: exit 0. `./tools/check.sh`: exit 1 at its tool check,
`FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`. This is the accepted environment issue. I
ran every later step of `check.sh` by hand, in order, on the final hash.

| Step | Exit |
|---|---|
| `gofmt -l cmd` | 0, no output |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| svelte 4 leakage scan | empty |
| consumer names scan | empty |
| plan references scan | empty |
| `go test -race ./...` | 0 |
| `playwright test -c src/routes/gallery.playwright.config.ts` | 0 (8 passed) |
| `playwright test -c "src/routes/episode/[id]/edit/edit.playwright.config.ts"` | 0 (5 passed) |
| `playwright test -c src/routes/threads/threads.playwright.config.ts` | 0 (10 passed) |
