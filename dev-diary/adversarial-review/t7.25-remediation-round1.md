# T7.25 remediation round 1

Reviewed hash: `2f42eed395f6915a2213ccf73f6be664c840ee64`
Worktree: `/Users/narayan/reprise-wt/t7.25-rem-r1` (branch `task/t7.25-rem-r1`)
New hash: `0249ab9931c47e9d54826f4a2b9869c6c842c9e1`
Parent of the new hash is the reviewed hash. It was not amended or rebased.

Round 1 approved the destroyed flag. Landing reran the proofs on that same code and both failed. The gallery path was already `/` before cleanup set the flag. The settled move still assigned `/processing`.

| Landing failure | What changed | Pin | Result |
|---|---|---|---|
| A draft move that fulfills after the gallery heading is visible still assigns `/processing`. `goProcessing` returned only when `destroyed` was set. Cleanup lags the path. | `goProcessing` also returns when the browser path is not the record route. End and retry still assign while that path is the record page. The draft move is not cancelled. | `npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts -g "finishes after the gallery"` | Run 1 passed 2. Run 2 passed 2. The full gallery spec then passed 10. `controller-complete.spec.ts` passed 2. A move that finishes on the record page still reaches processing. |

`npm run check` reported 0 errors and 0 warnings. `./tools/check.sh` exits 1 with `FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`. That stop is the environment. The pin was not changed.
