# T6.3 round 1 review

Reviewed hash: `fe7fac4dfa76274fa4e89d2e34d7f309099ae573`
("Add public README with local run and image build")
Worktree: `/home/nryn/work/reprise-wt/t6.3-rev-r1` (detached at the handed hash,
confirmed with `git log -1`).
Diff reviewed: `git diff e0551fa..fe7fac4` (README.md only, 110 insertions, no
other path touched).
Spec: `dev-diary/PHASE-6-ship.md` T6.3 block, read from the live plan at
`/home/nryn/work/reprise/`.

## Claim-by-claim verification (each pinned with grep)

| README line | Claim | Pin (run in this worktree) |
|---|---|---|
| 21 | Go 1.27.1 | `go.mod:3` reads `go 1.27.1`. `Dockerfile:12` builds `FROM golang:1.27.1`. |
| 21 | Keel `v0.3.0` | `go.mod:7` reads `github.com/nrynss/keel v0.3.0`. |
| 22 | Chaaya `0.2.0` | `web/package.json:34` reads `"@nrynss/chaaya": "0.2.0"`. |
| 26 | ffmpeg and ffprobe `9.0.1` | `Dockerfile:32` pins `mwader/static-ffmpeg:9.0.1@sha256:54e5...`. `tools/check.sh:43` sets `FFMPEG_PIN=9.0.1`. |
| 34 | Node `26` | `Dockerfile:21` builds `FROM node:26`. `.github/workflows/ci.yml:20` sets `node-version: '26'`. |
| 39, 47-51, 56 | `.env.example`, `npm ci`, `npm run build`, `REPRISE_CONFIG=... go run ./cmd/reprise` | `.env.example` exists with the two empty vars. `web/package.json:8` defines `build`. `cmd/reprise/main.go` exists. |
| 59-63 | Listens on `:8080`, `/healthz` answers `ok` with boot id and build version | `cmd/reprise/main.go:157` defaults `addr` to `:8080`. `main.go:288` answers `ok %s version=%s` with boot id and version. `main_test.go:54-61` asserts 200. |
| 71-74 | `Dockerfile` at root builds binary, web app, media tools into one distroless container | `Dockerfile` exists. Stages: `golang:1.27.1` build, `node:26` web, `mediatools` digest-pinned, runtime `gcr.io/distroless/static-debian12:nonroot`. |
| 77 | Image bakes in box settings, serves on `8080`. `deploy/README.md` documents the live box | `Dockerfile` copies `config/reprise.box.toml` to `/srv/config/reprise.box.toml` and exposes 8080. `deploy/README.md` exists and documents host, URL, edge, routing, secrets, state. |
| 81 | `config/reprise.local.toml` local, `config/reprise.box.toml` box, `REPRISE_CONFIG` names the file, fallback to local | Both files exist with the stated contents. `internal/settings/settings.go:21` defines `PathVar = "REPRISE_CONFIG"`, line 24 defines `FallbackPath = "config/reprise.local.toml"`. |
| 83-85 | Secrets are references, boot log prints the resolution plan, missing secret stops boot | Both TOML files carry only `source` references. `cmd/reprise/main.go:165` logs `plan.String()`. |
| 89-96 | `./tools/check.sh` is the gate, needs `npm ci` in `web` first, needs pinned ffmpeg and ffprobe, scans tracked files only | `tools/check.sh` exists. It requires ffmpeg and ffprobe on `PATH` and refuses any build but the pin. Its scans run through `git grep` over tracked files. |
| 102 | `https://reprise.nryn.dev` serves the build, `/healthz` reports the version | The URL matches the phase file handoff. The handler returns the version string. Reachability itself is operator-verified, not re-probed here. |
| 106 | `cmd/reprise`, `internal/assemblyai` only talks to AssemblyAI, `internal/gemini` only talks to Gemini, `web/src`, `config`, `deploy`, `tools` | All directories exist. The AssemblyAI and Gemini isolation is owned and reviewed by earlier tasks, and this commit adds no code that could break it. |
| 110 | No license file ships yet, owner adds terms at public flip | `git ls-files` shows no `LICENSE*`. The commit adds none and names no license terms. |
| 30 | Keel and Chaaya named as the author's prior open source work | Present. No new copy of library capability is described or added. |

## Style and gate conformance

- No semicolons in README.md (`grep -n ';'` returns nothing).
- No em dashes (`grep -n '—'` returns nothing; the only `--` hits are
  markdown table separators).
- Longest sentence is 28 words (measured with a script over non-code,
  non-table lines). Limit is 30.
- Active voice throughout. No passive construction found that reads worse
  in active form.
- No plan references. The check.sh plan-reference scan run against
  README.md returns clean, and the full scan exempts nothing the README
  needs.
- `audit_docs.py` prints `OK planning tree is consistent`, exit 0.
- Diff touches README.md only, which is exactly the task's `owns` line.
  No code, no config, no workflow changed, so no gate run is required for
  this commit. The gate needs pinned ffmpeg 9.0.1 on `PATH`, which this
  workstation does not carry, and a docs-only commit cannot change what
  the gate measures.

## The ten questions

1. Spend without budget first: no. Docs only, no code path added.
2. AssemblyAI session left open: no. No code path added.
3. Private episode reaching anyone but its owner: no. No code path added.
4. Edit without a revertible record: no. No code path added.
5. Spoken callback or count from the prompt: no. No code path added.
6. Plan citation in code, comment, test, or migration: no. README.md
   carries none (scan above is clean), and no other file changed.
7. Rebuilt Keel or Chaaya: no. The README names both as prior work and
   adds no library-shaped code.
8. Review run on the handed hash in a worktree of its own: yes.
   Detached worktree at `fe7fac4`, confirmed with `git log -1`. No
   commit made from this worktree.
9. Paid job resuming after restart and spending twice: no. No code path added.
10. Development check needing a person, a microphone, or a wall-clock
    threshold: no. No check added.

## Residual note (not a finding)

The task's done condition includes a clean-clone build with no access to
this machine. That proof needs a clean machine and network fetches, which
is beyond this workstation and beyond what a diff review can measure. It
is recorded here for the owner to run before the visibility flip, not as
a defect in this commit. Every command and path the README gives for that
build exists in the tree, as pinned above.

## Findings

No findings. Zero across all severities.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| (none) | | | | |

Counts: C 0, H 0, M 0, L 0. No OUT_OF_SCOPE defects found.

## Verdict

APPROVE. The commit adds exactly the 110-line README the task owns. Every
version, command, and path it states resolves to a source in the tree. No
license is committed, no terms are picked, no visibility changes. Style
rules hold. `audit_docs.py` passes.
