# T7.2 round 1 review

Reviewed hash: `80a6ad79396f4165cfdd3ab48687081e5a1a9fc5`
Worktree: `/home/nryn/work/reprise-wt/t7.2-rev-r1` (detached HEAD, confirmed with
`git log -1`). Diff reviewed: `git diff 3fd1447..80a6ad79` (base `3fd1447`
carries the landed T7.1 this commit wires). No code committed by this reviewer.

Verdict: **APPROVE** with zero findings across all severities.

Counts: C 0, H 0, M 0, L 0. No OUT_OF_SCOPE rows. Nothing outside the task's
`owns` paths was touched: the stat lists only `cmd/reprise/main.go`,
`internal/broker/`, and `internal/gemini/`, including their test files.

## What the commit does

`main.go` builds one shared Gemini client from the file credential, registers
eight kinds (six pipeline plus reconcile plus sweep) with paid kinds
non-idempotent, wraps the T7.1 session end handler to start one reconcile job
per recorded close, runs the abandoned sweep every five minutes with an
immediate boot pass, fails episodes left with interrupted paid jobs after
runner recovery, and wires the episode, session end, and threads handlers
against the landed T7.1 contract. `internal/broker/settle.go` (new) owns the
mint linkage table, rebuilds reconciler input for the end path, lists
sweep candidates carrying a recorded provider close only, and adapts the
sessions client to the reader and ender seams with ceiling-rounded durations.
`internal/gemini/` gains chapter, image, commitment, and resolution calls over
the shared generate path. All changes are additive except narrow seams
(`Broker.Config.DB`, `RunFunc` exposures, diary `SetConnectedSeconds`).

## Done-clause pins (each run in this worktree)

1. Every paid kind constructed once with the settings model id:
   `TestPipelineKindsShareOneClientWithSettingsModels` passes. It asserts 8
   kinds, all five paid kinds non-idempotent with no resume, the three free
   kinds resuming, one shared client behind editorial, chapters, cover, and
   marking, the settings model id throughout, and the settings transcription
   model on the batch client.
2. A recorded close settles its reservation exactly once:
   `TestSettleEndSettlesExactlyOnce` passes. It mints through the real broker,
   records the provider close, reconciles to 372 seconds and the exact cent
   price, reads held back to zero, and reconciles again with spend unchanged.
   The adapter half is pinned in `internal/broker` against a recorded
   Sessions API body shape (`TestAdapterReadsClosedSession` rounds 372.4 up
   to 373 and carries both artifact URLs).
3. A restart marks the paid kind interrupted with the episode failed:
   `TestRestartFailsInterruptedPaidJobs` passes. It leaves a paid job running,
   reopens the runner the way a restart does, and asserts the job reads
   `interrupted` with the episode `failed`.
4. No mint stays held after its end plus the sweep margin: the end path pins
   held at zero above. The sweep path reuses the T2.6 settle with
   `DefaultMarginSeconds` (60, positive) passed through, and `startSweep`
   passes `-1`, which `Sweep.Sweep` documents as "use the sweeper default".
   Worst case release lands at cap plus margin plus one five minute tick,
   inside the TTL of cap plus two five minute legs. Margin arithmetic read
   and confirmed, not mutated.

Gate: `npm ci` in `web/`, then `./tools/check.sh` prints
`OK all checks passed` in this worktree. `go test` passes for
`./internal/broker/`, `./internal/gemini/`, and `./cmd/reprise/`.
`dev-diary/tools/audit_docs.py` prints `OK planning tree is consistent`.

## The ten questions

1. Does any path spend money without reserving budget first? No. Every
   pipeline pass takes `Budgets` and calls `Reserve` before the provider call
   (`transcript/job.go`, `editorial/run.go`, `analysis/run.go`, plus cover
   and memory through the same seam, all driven offline in
   `TestPipelineCoverAndMemoryRunOffline` with spend booked). The raw
   `gemini.Client` methods never run except through passes that reserve. The
   mint still reserves before the lease opens, and `settleTTL` sets the hold
   past cap plus sweep interval plus settle lag.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. Mints carry the token cap, the sweep ends provider records
   after settling, and `EndSession` treats an already gone record as success.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   Receipts persist through `receiptSink` as private media under the episode
   owner and group. No visibility flag changed.
4. Does any edit apply without a revertible record? No. `MarkInterrupted`
   moves episodes to `failed` for explicit retry. No editorial decision path
   changed.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. Chapter and commitment calls return drafts the passes shape onto
   stored words. No host opening or spoken count added.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. `check.sh` plan references pass. The one grep hit for
   "phase" ("The phase packages set limits") is plain English with no id and
   matches none of the enforced patterns.
7. Did this task rebuild something Keel or Chaaya provides? No. The settle
   table links this product's mint rows, the adapters bridge seams the phases
   defined, and job recovery comes from `keel/job`, not a local copy.
8. Was this review run on the handed hash, in a worktree of its own? Yes,
   stated at the top.
9. Can any paid job kind resume after a restart and spend twice? No. All five
   paid kinds register non-idempotent with no resume, pinned by the kinds
   test, and recovery marks them interrupted while the restart pass fails
   their episodes. The durable claim row (`INSERT OR IGNORE` plus claimed
   flag) keeps a repeat reconcile at most once, pinned by the double
   reconcile half of the settle test.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. New tests use scripted model answers, canned provider
    bodies, and backdated rows with generous bounds. No sleeps added.

## Disclosed contract items (not findings)

The handoff discloses pinned Vertex text rates and reuse of the editorial
model for chapters, cover, and marking until a settings catalog seam lands.
Both are visible in `newPipeline` and covered by the kinds and offline tests.
They are contract items, not defects, so no rows.

## Notes for the remediator (none needed) and the next developer

No residue. Two observations carry no finding. The binary registers pipeline
kinds but schedules no pipeline job yet, so no paid spend path can fire from
this commit. A paid job interrupted before its first progress report logs for
review instead of failing an episode it never named, which the code documents
at the call site.
