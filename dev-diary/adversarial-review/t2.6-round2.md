# Review T2.6 round 2: abandoned session sweep (re-review)

Reviewed hash: `6c299d2393c47f0a756c811fda98b8dd8a9f5998`
Worktree: `/home/nryn/work/reprise-wt/t2.6-rev-r2` (detached HEAD, confirmed with `git log -1 --format=%H`)
Base for diff: `e3f6c3f` (round 1 reviewed hash; `git diff e3f6c3f..6c299d2` holds the remediation alone)
Owns check: diff holds `internal/broker/sweep.go` and `internal/broker/sweep_test.go` only.
Both sit inside the task owns. `reconcile.go`, `terminate.go`, and `terminate_live_test.go`
are untouched. No commits made, working tree clean afterwards.

Verdict: **APPROVE**
Counts: C 0, H 0, M 0, L 0. Out of scope findings: 0 new (2 round 1 rows restated below).

## Verification run

- `git log -1 --format=%H` equals the handed hash. No commits made. `git status --short`
  reads empty after every probe run.
- `go test -race -count=1 ./internal/...` passes in this worktree. That covers the full
  broker suite (including every T2.3 reconcile case) and the non-live assemblyai suite.
- `TestSweepAlertFailureKeepsOutcome` passes. This is the round 1 pin, now landed as a
  permanent test. It uses a failing alerter plus an open 3600 second fixture, then reads
  `sweep_state` for the real `created`/`3600` row with the failure noted in detail.
- Mutation re-verified by hand in this worktree. Restoring the `markSwept` call inside
  `alert()` ahead of the outcome write makes the new test fail with exactly the round 1
  signature (`row ""/0`, want `created/3600`). Reverted afterwards. Tree clean, test green
  again. The pin is load-bearing.
- `gofmt -l` clean over both packages. `go vet` clean over both packages. Pattern scan
  for plan refs over all four owned files is clean.
- Full `tools/check.sh` web build not rerun here. The remediation touches Go files only,
  the remediator records a green gate in its own worktree, and the race suites across
  every package are the load-bearing check. They pass.
- Live test NOT run (spend rule). Verified by reading: it still opens with `//go:build live`,
  the diff does not touch it, and the gate runs `go test` with no `-tags` flag, so CI
  never executes it.

## Residue vs round 1, severity by severity

- C: zero in round 1, zero now. No spend, privacy, or recording-loss path changed.
- H: zero in round 1, zero now. Settle-then-delete order, shortfall amounts, idempotent
  delete, and reconcile seams are byte-identical to the reviewed commit.
- M: one in round 1, zero now. `alert()` writes no row itself. It returns the delivery
  error, and both callers (`sweepOne` still-open path, `reviewGone`) append the failure
  note to the detail before the single outcome write lands. The real status and seconds
  win the first write at both sites. The landed test pins this.
- L: zero in round 1, zero now. No polish debt added. Comment on `alert()` states the
  new contract in its own terms.

## Out-of-scope restatement (still untouched, never blocking)

- H, `internal/assemblyai/token.go`: stale cap comment claims the provider ends the
  session at the cap. The probe record disproves it. Outside this task owns. Not in
  this diff. Left alone.
- M, reconciler reader shape in `internal/broker/reconcile.go`: the seam assumes a flat
  session body while live records show `status`, nullable `duration_seconds`, and an
  `artifacts` array. Outside this task owns. Not in this diff. Left alone.

## New-findings hunt (no findings raised)

- Alert failure plus outcome-write failure combined: if `markSwept` fails after a failed
  alert, the detail note dies with the write and `Sweep` returns the store error. The
  failure surfaces as a failed sweep run, and a retry re-reads settled state. This needs
  two simultaneous faults (alert sink down plus store write down). It predates the
  remediation and changes none of the done criteria. Not raised.
- Over-cap alert-once flag vs sweep first-write-wins: the flags live in separate tables
  (`reconcile_state.over_cap_alerted` vs `sweep_state`), so no single row carries both.
  The sweep alert writes no row at all now, which removes the only clobber vector. The
  resweep tests prove no second alert on either kind. No interplay defect.
- Settle and delete paths: the diff touches neither `ReconcileAbandoned` nor
  `EndSession` call order. Sweep still settles, then ends, then records. `reviewGone`
  still settles nothing. Full suite green confirms no drift.

## The ten questions

1. Does any path spend money without reserving budget first? No. The sweep still settles
   mint-time holds through the reconciler only. Untouched by this diff.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap?
   No new path. The remediation moves no network call. The declared gap stands as round 1
   recorded it.
3. Can a private episode, stem or transcript reach anyone but its owner? No. No serving
   path changed.
4. Does any edit apply without a revertible record? No user edits. Writes stay durable
   settle plus sweep state.
5. Does any spoken callback or count come from the prompt instead of a stored row? Nothing
   speaks. Detail strings cite measured seconds only.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file?
   No. Pattern scan over the owned files is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. No library surface changed.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Detached worktree
   at `6c299d2`, confirmed by `git log -1`, diffed against `e3f6c3f`, pins and mutation
   run inside it, no commits, tree clean afterwards.
9. Can any paid job kind resume after a restart and spend twice? No. Kind flags, claim
   guard, and first-write-wins are untouched. Resweep tests pass.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No.
    The landed pin runs on a fake clock and a failing stub alerter. The live probe stays
    behind the `live` tag and out of CI.

## Verdict

**APPROVE.** Round 1 residue is zero across all severities: the single M is fixed and
pinned by a load-bearing test whose mutation I re-verified, and C, H, and L hold at zero
with no drift in the paths round 1 already cleared. Both out-of-scope rows remain
untouched as instructed. The new-defect hunt found nothing actionable.
