# T3.2 round 2 review

Reviewed hash: `abfeb08dd4c21ddbef4d6fd40a71a04572c1855a`.
Worktree: `/home/nryn/work/reprise-wt/t3.2-rev-r2` (detached HEAD, confirmed with
`git log -1 --format=%H`). Base for the remediation diff: `1f778d3`.
Declared owns: `internal/gemini/`, `internal/editorial/`. The diff touches only
those paths (3 files: `internal/editorial/run.go`, `internal/editorial/run_test.go`,
`internal/gemini/schema.go`). No commit was made from this worktree.
All pins below ran in this worktree. Throwaway probe files were deleted after
each run, and `git status --short` is clean.

Verdict: REMEDIATE.
Counts: C 0, H 0, M 1, L 0. One new finding, with a pin and a mutation.

## Residue against round 1, severity by severity

M residue: zero. F1 (post-spend receipt and store paths released instead of
settling) is fixed. Both paths in `internal/editorial/run.go` now call
`Settle(estimate, estimate)` before returning the error, mark the hold settled,
and join a failed settle into both errors. The committed
`TestRunSettlesSpentCallOnStoreFailure` (drop `decisions`, full answer, one
billed call, one reserve, one settle, no release) passes here, as does the
updated `TestRunKeepsProviderCopyOnReceiptFailure` (one settle, no release,
`errors.Is` on the receipt error intact). The mutation check passes: zeroing
logic is untouched and settle-before-return is load bearing on both paths.
Keel `v0.3.0` docs confirm the choice: `Settle` books the price a paid call
cost and frees its hold, while `Release` returns a reservation the caller
never spent. The pre-call refusal still returns before the defer is installed,
so no hold is freed that was never taken, and the model-failure fallback still
releases, since no call was spent there.

L residue: zero (two findings, both fixed). F2 (the `Usage` comment claimed
usage prices the settle) is fixed: the comment on `EditorialAnswer` now states
the run settles the audio minute estimate and no settle reads `Usage` today.
Re-ran here with a throwaway probe answering the full draft with two billion
tokens of usage: price and settled pair still equal `Estimate(12)`, which now
agrees with the text instead of contradicting it. A grep for `.Usage` over
non-test `internal/editorial` sources still returns nothing. F3 (reserved and
settled amounts unpinned) is fixed: `TestRunStoresDraft` now asserts
`reserved[0] == Estimate(12)` and the settled pair equals `{estimate,
estimate}`. The zeroing mutation (`estimate := cost.Price(0)`) was applied in
this worktree and fails the suite with `reserved = $0.00, want $0.0005`, then
was reverted with the tree clean. The assertion computes `want` through
`Estimate`, so a legitimate rate change flows through instead of breaking it.

C residue: zero. H residue: zero. Neither round had findings at those levels.

## New findings

### N1

| Column | Value |
|---|---|
| Severity | M |
| Where | `internal/editorial/run.go:162-164` (fallback store after an unusable answer) |
| What | The decode-failure path runs after the provider billed the call (a 200 with unusable JSON), yet its fallback store failure returns `storeErr` with `settled` still false, so the deferred `Release` frees the reservation. The budget under-books one estimate whenever the answer is corrupt and the fallback store fails together. Round 1 cited two post-spend error paths and the remediation fixed exactly those two. This third one keeps the old behavior. |
| Pin | Throwaway probe `TestReviewerR2UnusableAnswerStoreFailure` (removed after the run). It seeds forty words, answers `"not json"`, drops the `proposals` table so the fallback store fails after one billed call, and asserts one settle with no release. Result on the handed hash: `reserved=1 settled=0 released=1`. FAIL, as predicted. Dropping `decisions` does not trigger this path because the fallback draft carries no cuts, so `proposals` is the table that fails it. |
| Mutation | In fixed code that settles before returning the fallback store error, delete the `Settle` call on that path. The probe fails again with `settled = 0`. |

Examined and deliberately not a finding: the model-failure fallback store
failure (`run.go:147-149`) still releases. `GenerateEditorial` returned an
error there, so no billable answer completed, and `Release` matches Keel's
"a reservation the caller never spent". Round 1 blessed this accounting in
scope note (d), and nothing in the remediation moved it.

## The ten questions

1. Does any path spend money without reserving budget first? No. `Reserve`
precedes `GenerateEditorial`, and refusal returns before any call and before
the release defer is installed. Pinned by `TestRunRefusesWithoutBudget`.
2. Can any path leave an AssemblyAI session open without `session.end` or a
token cap? Not applicable. This task opens no AssemblyAI session.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
The remediation adds no serving surface. Writes carry `owner_id`.
4. Does any edit apply without a revertible record? No. `store` is untouched:
one transaction replaces proposals, accepts cuts with decision rows, backs the
callback with mention and callback rows, and sets the title.
5. Does any spoken callback or count come from the prompt instead of a stored
row? No. The callback path is untouched and still persists its quote with
episode and offset.
6. Does any code, comment, test or migration cite a phase, a task, or a
planning file? No. The grep over both owned trees for task ids, phase ids,
planning file names, `§`, and review-round markers is clean, and `go vet`
plus `gofmt` pass.
7. Did this task rebuild something Keel or Chaaya provides? No. The
remediation uses only `errors.Join` from the standard library on top of the
`keel/cost` holds round 1 already recorded. Nothing here duplicates a library.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
Hash `abfeb08` confirmed, detached worktree `t3.2-rev-r2`, diff of the
remediation alone (`1f778d3..abfeb08`) read in full, every pin executed here,
no commits made, tree clean at close.
9. Can any paid job kind resume after a restart and spend twice? No. `Kind`
is untouched (limit 2, not idempotent, no resume path, one attempt), and the
new settle-before-return blocks add no retry. `TestKindNeverResumes` passes.
10. Does any development check need a person, a microphone, or a wall-clock
threshold? No. All pins run on generated words and scripted models, including
the two throwaway probes. `go test -race -count=1` passes for both owned
packages. The full web-side gate was not re-run from here; the change is
Go-only and the remediation passed `./tools/check.sh` at identical content,
with the Go-side portions (gofmt, vet, race tests) re-verified green in this
worktree.

## Verdict

REMEDIATE. One new M finding (N1) blocks approval. All three round-1 findings
are fixed with zero residue at every severity, and no finding was softened
for rarity or cost of fix. Out-of-scope defects: none.
