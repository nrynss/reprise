# T5.4 review, round 2 (re-review)

Reviewed hash: `326a2e6960206951f5d9010435ed0eb994f59c6a` (one commit on
reviewed `5aed2265468fb59cb885ad29fb366a23aee2e4e4`, parent confirmed by
`git log`, no rebase, no amend).
Worktree: `/home/nryn/work/reprise-wt/t5.4-rev-r2`, detached HEAD.
`git log -1 --format=%H` returns the handed hash. Diff reviewed:
`git diff 5aed226..326a2e6` (remediation alone: `internal/retention/sweep.go`
plus the new pin in `internal/retention/sweep_test.go`, both inside the task
`owns` line; no other paths touched). No commit made. Working tree reviewed
only through the handed commit, and left clean after (mutation probe
restored, `git status` empty).

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0 in scope. OUT_OF_SCOPE: 1 (L, doc,
orchestrator-owned, carried from round 1, non-blocking). No new findings.

## Residue against round 1, severity by severity

* C, zero claimed: round 1 held zero C. This round found zero C. No residue.
* H, zero claimed: round 1 held zero H. This round found zero H. No residue.
* M, zero claimed: round 1 held exactly one in-scope M (frozen-cutoff reuse
  on retry at `sweep.go` `run`, plus the over-claiming comment). The handed
  commit removes the freeze, reads the cutoff fresh on every attempt, and
  corrects the comment to state exactly that. The round 1 pin scenario
  (91-day plus 89-day guests, failed sweep, clock advanced 48 hours,
  `ReSweep`) now passes as the committed `TestReSweepCatchesNewlyExpired`,
  with the still-fresh guest intact. No residue.
* L, zero claimed: round 1 held zero in-scope L. This round found zero
  in-scope L. No residue.

## What was verified

* `git log` confirms one commit on the reviewed hash, message "Read the
  sweep cutoff fresh on every attempt".
* `go test -race -count=1 ./internal/retention/` passes in the review
  worktree (13 tests).
* `go vet ./internal/retention/` clean, `gofmt -l` empty.
* Plan-reference pattern scan over `internal/retention/` returns nothing.
  No `panic`, `log.Fatal`, `**T`, or `interface{}`.
* Re-ran the round 1 pin scenario as the now-committed
  `TestReSweepCatchesNewlyExpired`: PASS (newly-expired swept, old swept,
  fresh keeps 2 episodes). Re-ran the round 1 suite pins by name
  (`TestSweepDeletesExpiredKeepsFresh`, `TestKeepSurvivesGuestExpiry`,
  `TestSweepBoundaryKeepsGuestAtCutoff`, `TestSweepSkipsOwners`,
  `TestDefaultWindowSweepsNinetyDayIdle`, `TestSweepRestartResumesAndFinishes`,
  `TestSweepStuckFailsThenHeals`): all PASS.
* Mutation probed directly in the worktree without committing: restoring the
  freeze (`cutoff := prior.Cutoff` with a zero fallback) makes the pin fail
  again with `episodes holds 2 newly-expired rows, want none`. File restored
  after, tree clean, pin green again. The pin is load-bearing.
* Union semantics read precisely at `sweep.go:189-218` (`plan`) with
  `expired` at `sweep.go:222-243`: the union carries only unconfirmed prior
  guests (each confirmed expired by an earlier live DB read under the same
  guest filter) plus guests expired under the fresh cutoff. A read error in
  either leg fails the run before any delete, so nothing sweeps on doubt.
  The corrected `run` comment states exactly when fresh expiry applies
  (every attempt, including resume and re-sweep). Fail-closed direction
  preserved.
* Cutoff edge semantics unchanged: `expired` still uses strict less-than,
  exact-cutoff survival still pinned by `TestSweepBoundaryKeepsGuestAtCutoff`.
  Allowlist model unchanged: `trust.set` still resets from the planned guest
  list each run. Keep path untouched by the diff. Full suite green, so all
  previously green behavior stays green.
* Hunted for new defects from the commit: the only production change is the
  two-line cutoff read. `prior.Cutoff` is now written to new snapshots but
  not consumed, which is harmless (the field still truthfully records the
  cutoff used). No new defect found.

## Findings

None in scope.

## OUT_OF_SCOPE (carried, non-blocking)

| Severity | Where | What | Pin | Owning path |
|---|---|---|---|---|
| L | `dev-diary/project.md:159` | The open-decisions table still lists guest-data lifetime as undecided with `T5.4` as the needer, while the handoff records the owner decision (ninety days) and the code ships `DefaultWindow`. | Read `project.md` line 159 beside `t5.4-impl-handoff.md` ("Retention window: DECIDED") and `retention.go:35` (`DefaultWindow`). | `dev-diary/project.md`, orchestrator-owned. |

Wiring notes from round 1 stand unchanged and are not defects: no route
exposes `ReSweep`, `Keep`, or the stuck-erasure retry control, declared
openly in the handoff.

## The ten questions

1. Spend without reserving budget first? No. The diff adds no provider call
   and no cost path. The sweep still makes zero paid calls.
2. AssemblyAI session left open without `session.end` or a token cap? No.
   Session termination flow untouched by the diff.
3. Private episode, stem, or transcript reaching anyone but its owner? No,
   examined with teeth. The union only ever names guests from the
   `kind = 'guest'` filter: fresh expiries through `expired`, and prior
   guests that entered through that same filter on an earlier live read.
   A guest who stopped being a guest is still skipped by `deleteGuestRows`,
   and every delete stays owner-scoped. The fix moves one kept-direction
   miss toward correct deletion of confirmed-expired guests only. It cannot
   widen the set to owners, keepers, or fresh guests (the fresh guest in
   the pin keeps both episodes). No wrongful deletion, no exposure.
4. Edit applied without a revertible record? No. Deletion remains the
   specified behavior. `Keep` untouched. No editorial edit exists here.
5. Spoken callback or count from the prompt instead of a stored row? No
   prompts, no counts, no greeting. Not applicable.
6. Code, comment, test, or migration citing a phase, task, or planning
   file? No. Pattern scan over `internal/retention/` is empty, including
   the new test and the rewritten comment.
7. Rebuilt Keel or Chaaya work? No. The diff touches only the cutoff read
   and the pin. No library gap surfaced, so no issue was owed.
8. Review run on the handed hash in a worktree of its own? Yes, stated at
   the top. Parent hash confirms no rebase under review.
9. Paid job kind resuming after restart and spending twice? No. The sweep
   still performs only deletes, all replay-safe. The fresh cutoff changes
   which guests list, not what any listed guest costs.
10. Development check needing a person, a microphone, or a wall-clock
    threshold? No. Fixed clock, scripted provider doubles, real SQLite and
    media store. The 30 and 60 second bounds are liveness guards only.

## Verdict restated

APPROVE. Zero in-scope findings at any severity. Round 1 residue is zero
severity by severity: C zero, H zero, M zero (the frozen-cutoff M is fixed
and its pin passes on this hash with a load-bearing mutation), L zero.
