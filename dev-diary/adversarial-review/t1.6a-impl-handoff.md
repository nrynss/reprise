# T1.6a implementer handoff

## What changed

Only `internal/settings/settings_test.go` changed. No runtime file moved.

`TestShippedFilesAgreeOnSecretNames` now opens both shipped files. It
checks each one names the same env variables as `.env.example`. It pins
the credential block as a file source with its shipped key path. It
rewrites both temp paths the same way and loads each file. A wrong name
or a wrong path fails the load.

`TestBootLogShowsPlanWithoutValues` now uses an explicit key path. It
asserts the whole plan carries that path. It splits the plan into tab
fields and finds the credential row. It asserts that row names source
`file` with the key path and status `resolved`. The leak probe still
checks every dummy value stays absent.

Commit `99638ba356355643fc09a855d75ea49177288c31` on `task/t1.6a` holds
the change. The worktree is clean.

## Plant and fail proofs

Both plants used temp or scratch copies only. No tracked file changed.

Plant 1 box drift. I copied `config/reprise.box.toml` to a scratch
dir. I renamed one box variable to `SESSION_SIGNING_KEY_DRIFT`. The
scratch copy lacked the shipped var name, so the new string pin failed.
After rewriting the scratch paths to temp files, `Load` failed too. I
removed the scratch copy after the proof.

Plant 2 wrong source. I built a config with the credential on source
`env_file` and var `GEMINI_CREDENTIAL`. The helper exited 0 and the
output still held the bare string `file`. The new credential row pin
failed with `credential source = "env_file", want "file"`. It never
reached the key path check. I removed the plant after the proof.

## Gate result

`go test -race -count=1 ./internal/settings/...` passes. `gofmt -l`
prints nothing for the owned package. `go vet` passes there too.

The full gate passes with exit 0 after `npm ci` in `web/`. The gate ran
in `/home/nryn/work/reprise-wt/t1.6a-impl`. The consumer name scan now
reports clean because the mockup files are untracked in this worktree.
The round 2 note about the mockup hit stays pre-existing and out of
scope. It names `dev-diary/mockup/`, which this change does not own.

## Final commit

`99638ba356355643fc09a855d75ea49177288c31`
