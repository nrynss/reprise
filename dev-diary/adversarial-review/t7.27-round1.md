# T7.27 round 1 review

Reviewed hash: `a67c6af79346951ecdedb88cf9af00c3cf03c998`
Worktree: `/Users/narayan/reprise-wt/t7.27-rev-r1` (detached HEAD).
`git log -1` matches that hash. The subject is "Open the microphone before the session mint."
`git status --short` was empty before the pins and empty again after both mutations were restored.
Diff reviewed: `git diff 9db88d94fd1baee2b3de2a2d5f5ec9ba3ca592d0..a67c6af79346951ecdedb88cf9af00c3cf03c998` and nothing else.
No code commits. The tree is still that hash.

## The ten questions

1. Does any path spend money without reserving budget first? No. The browser still mints through the same session route. That route reserves the cap before it mints a token. Opening the microphone is local. A refused mint still stops before a provider socket exists.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The provider socket is wired only after the recorder is already recording. A failed open never reaches that socket. A live take still ends through `voice.end` and the guard on pagehide. The minted token still carries `max_session_duration_seconds`. A mint with no provider id stays outside the sweep. Its budget hold ages out by reservation expiry. That release path already existed.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The diff changes no media URL, cookie, or owner check. A failed open closes the microphone it started.
4. Does any edit apply without a revertible record? No. Nothing applies an edit.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. New comments describe the gesture order and the buffered blocks. They do not cite a task id, a phase id, or a planning file.
7. Did this task rebuild something Keel or Chaaya provides? No. Capture still uses Chaaya `AudioRecorder`. The page hide close still uses Chaaya `SessionGuard`. The welcome defect is inside Chaaya 0.2.0 and is not patched here.
8. Was this review run on the handed hash, in a worktree of its own? Yes. This detached worktree is the only tree the review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The new proof stubs `getUserMedia` and holds the mint. The mock take feeds generated blocks. Neither waits on a person, a microphone, or a clock threshold. The welcome spec's 10 second poll is older than this commit.

## What the commit does

`openLiveCapture` runs inside the start click, before `startRealTake` awaits the mint (`web/src/lib/voice/record-state.ts:631`). It creates the audio context, calls `resume`, and calls `recorder.start` at line 648. Chaaya `AudioRecorder.start` calls `getUserMedia` before its own first await. The mint follows in `startRealTake` at line 675. A mock take never calls `openLiveCapture`.

Blocks that arrive before the upload and the voice object exist sit in `earlyBlocks`. `flushEarlyBlocks` drains them in order once those objects exist (line 692). The stem keeps that opening. A failed open clears the buffer and calls `abandonCapture`. That resets the recorder and closes the context.

The root Playwright config runs Chromium and WebKit. `web/tests/start-gesture.spec.ts` holds `POST /api/sessions`. It expects the marks `context`, `resume`, and `microphone` before that hold ends. `.github/workflows/ci.yml:39` installs WebKit beside Chromium.

`web/src/routes/welcome/welcome.playwright.config.ts:17` adds a WebKit project to the default list. The welcome spec was not changed.

## Pins

`npm ci` in this worktree's `web/` added 236 packages. Exit 0. `node_modules` was missing here.
`npx playwright install webkit` from `web/` exited 0.
`npm run build` from `web/` exited 0. Preview serves `web/build`.

`npx playwright test` from `web/` passed 4 tests in 2.0s. The shell spec and the gesture spec passed on Chromium and on WebKit.

`npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts` from `web/` passed 10 tests in 3.8s. Each one reaches the season heading.

`npx playwright test -c src/lib/voice/mock.playwright.config.ts controller-complete.spec.ts --workers=1` from `web/` passed 2 tests in 2.4s. Ending a mock take reaches processing. The retry reaches processing too.

`npx playwright test -c src/routes/welcome/welcome.playwright.config.ts` from `web/` used no project filter. 7 passed and 1 failed in 13.0s. Every Chromium proof passed, including the seeded teaser. The WebKit seeded teaser failed. Playing stayed false for the 10 second poll. The other three WebKit proofs passed.

`ffmpeg -version` reports 9.0.2. The pin is 9.0.1. This commit does not change that pin. `./tools/check.sh` was not run.

## Mutation

The gesture proof is load-bearing. `recorder.start` was moved to after `await mintSession()` in `startRealTake`. The context and `resume` stayed before the mint. `npm run build` ran again, because preview serves `build`.

`npx playwright test tests/start-gesture.spec.ts --project=webkit` then failed in 244ms. The marks were `context` and `resume` only. The expected `microphone` mark was absent.

`git checkout` restored `web/src/lib/voice/record-state.ts`. `npm run build` ran once more so `web/build` matches the hash. `recorder.start` is back at line 648, before the mint.

The welcome default run is also load-bearing. Removing the WebKit project at line 17 left Chromium as the only default project. The same welcome command then passed 4 tests in 1.2s, including the seeded teaser. `git checkout` restored the WebKit project line. `git status --short` was empty after that restore. `git diff` was empty. `git log -1` was still the handed hash.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `web/src/routes/welcome/welcome.playwright.config.ts:17` at the reviewed hash | The default project list now includes WebKit. The comment at the top of that file runs the config with no project filter. That command fails. The seeded teaser spec was not weakened. Chromium in the same run still passes. The teaser was supposed to stay on its existing proof, which was Chromium only. | From `web/`, `npx playwright test -c src/routes/welcome/welcome.playwright.config.ts` with no `--project` filter. Exit 1. 7 passed and 1 failed. WebKit `the seeded teaser plays after the first gesture` polled `playing` for 10 seconds and got false at `welcome.spec.ts:56`. The four Chromium proofs passed. | Remove the WebKit project at line 17. The same command stays on Chromium and passes 4 tests. Put that project line back. The WebKit teaser fails again. |
| H, OUT_OF_SCOPE | `web/src/routes/welcome/welcome.ts:254` at the reviewed hash. The player lives in Chaaya 0.2.0, `dist/audio/playback/player.svelte.js`. Neither path is in `owns`. | `togglePlay` awaits `player.play()`. Chaaya primes that call with a one frame silent WAV. WebKit rejects the prime, so the seeded teaser never starts. The record link on the same page still works. Filed as https://github.com/nrynss/chaaya/issues/5. This task must not edit the welcome page to hide it. | The same default welcome command. WebKit playing stayed false for the 10 second poll. With the WebKit project removed, the Chromium teaser passed. The library constant `silentClip` is the clip `#prime` plays. | This row does not block. Do not weaken `welcome.spec.ts`. The in-scope fix is the project list above, not a change to this page. |

## Counts

C 0, H 1 (out of scope), M 1 (in scope), L 0. In-scope findings: one. Out of scope findings: one.

## Verdict

REMEDIATE

The welcome config's default run does not pass. It fails on the WebKit seeded teaser. A Chromium-only default run passes.
