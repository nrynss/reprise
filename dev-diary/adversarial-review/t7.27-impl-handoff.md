# T7.27 implementer handoff

Commit `a67c6af79346951ecdedb88cf9af00c3cf03c998` on branch `task/t7.27` in worktree `/Users/narayan/reprise-wt/t7.27-impl`. Base is `9db88d94fd1baee2b3de2a2d5f5ec9ba3ca592d0`. The worktree is clean. This file is not in the commit.

No memory graph tools were available, so nothing was recalled or recorded there.

## What changed

A real start now opens the audio context, calls resume, and starts the microphone before it waits on the session mint. The mint path uses that context and that recorder. It does not open a second context or call start again. A mock take still opens no microphone.

Blocks that arrive before the upload and the socket exist wait, then drain in order. A failed open returns the page to preflight and closes the microphone.

The root Playwright config runs Chromium and WebKit. The new spec holds the session mint and checks that the context, the resume, and the microphone call are already recorded. The welcome config runs WebKit beside Chromium. CI installs WebKit beside Chromium.

## Commands and results

`npm ci` in `web/` installed 236 packages. Exit 0. `node_modules` was missing in this worktree.

`npx svelte-kit sync`, eslint on the edited files, and `npx svelte-check` from `web/`. Exit 0. The type check reported 0 errors and 0 warnings.

`npx playwright install webkit` from `web/`. Exit 0. It installed WebKit 26.6.

`npm run build` from `web/`. Exit 0. The static adapter wrote `web/build`.

`npx playwright test` from `web/`. 4 passed. The shell spec and the new gesture spec passed on Chromium and on WebKit.

`npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts` from `web/`. 10 passed.

`npx playwright test -c src/lib/voice/mock.playwright.config.ts controller-complete.spec.ts` from `web/`. 2 passed. A mock take still reaches the gallery and processing.

`npx playwright test -c src/routes/welcome/welcome.playwright.config.ts --project=chromium` from `web/`. 4 passed, including play after the first gesture.

`npx playwright test -c src/routes/welcome/welcome.playwright.config.ts --project=webkit` from `web/`. 3 passed. The seeded teaser proof failed. Playing stayed false after the click.

`ffmpeg -version` reports 9.0.2. The gate pins 9.0.1. That pin was not changed, and `./tools/check.sh` was left unrun.

## Welcome on WebKit

Chaaya 0.2.0 primes playback with a one frame silent WAV. WebKit rejects that clip with media error code 3. The element says the media failed to decode. The play promise stays pending, so the welcome screen never leaves its opening notice. Chromium resolves the same clip and then plays the teaser.

The smallest reproduction is an audio element pointed at that clip, muted, with play called on it. The clip is the `silentClip` constant in `@nrynss/chaaya` `dist/audio/playback/player.svelte.js`. Expected result: play settles and a later clip can start. Actual result on WebKit: error code 3, and the promise does not settle.

The call site is `web/src/routes/welcome/welcome.ts`. The player package and that controller are outside the paths this change may edit. The spec was not weakened.
