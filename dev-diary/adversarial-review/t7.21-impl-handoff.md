# T7.21 implementer handoff

Commit `bd9d4c8b042b65012e22fe13046bc8433f6b6d00` on branch `task/t7.21`
in worktree `t7.21-impl`. Base is main at `c0ec8ef`. Gate passes in
the worktree. Production files change (not test-only), plus the
usual status revert for review to flag.

## What it built

The bound runner sits behind a lock and threads through each
attempt. The race never reproduced locally (both resume tests passed
20 race runs before the fix too), so the fix rests on code
inspection against the CI pins. The same shape existed one layer
deeper than reported: the resumed sweep also reads the inner
erasure runner through erase calls, so privacy gained explicit
runner variants and the sweep calls them with its captured runner.
