# T5.1 round 1 review

Reviewed hash `dd0a8bbef7a3bcc64de3386592f94a1d4764040e` in worktree
`/home/nryn/work/reprise-wt/t5.1-rev-r1`. Detached HEAD confirmed with
`git log -1 --format=%H` before any reading. No commit written. Working
tree reviewed only through the handed commit. Diff base `7956a89` read as
`git diff 7956a89..dd0a8bb`. The diff touches `internal/export/` only,
seven new files, which matches the declared `owns` line.

## Verification performed

`go test -race ./internal/export/ -count=1` passes in this worktree, all
11 tests. `go vet` is clean. Staticcheck `v0.8.1` is clean. Pinned
ffmpeg and ffprobe report `n9.0.1`. `go build ./...` passes. The gate
plan-ref grep over `internal/export/` is clean. Full `./tools/check.sh`
was not rerun, because its web stages sit outside this task's `owns`
lines and the implementer already ran them after `npm ci`.

Independent byte probes ran in this worktree with temp test files that
were deleted afterwards. The tree is clean now. A 35 second generated
tone builds a video whose probed gap against the render reads exactly 0
ms. Audio extracted from the built video with stream copy decodes to PCM
bytes identical to the render decode, both 3087078 bytes. The AAC copy
claim therefore holds on bytes, not on metadata alone.

Code mutations ran in a scratch copy under `/tmp/opencode/t51/src`
archived from the handed hash, never in a worktree. Three mutations went
in. Two fail the suite, which proves those pins load bearing. One stays
green, which is finding M1 below.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M | `internal/export/description.go:43`, `internal/export/export_test.go:222` | The first-at-zero rule has no isolating pin. The `moved` rejection fixture starts at 5000 ms but spans only 7000 ms to its neighbour, so it fails on the span rule even when the zero check is gone. A regression that deletes the zero check ships chapters the upload form ignores. | Scratch copy with the `chapters[0].StartMs != 0` branch deleted: full package suite passes. A probe validating chapters at 5000, 15000 and 25000 ms over a 35000 ms render returns nil on the mutated copy and logs the hole. The same probe against the handed code fails with `first starts at 5000`, so production is correct and only the test is weak. | Delete the `if chapters[0].StartMs != 0` branch in `ValidateChapters`. Suite stays green. |
| L | `internal/export/export_test.go:269` | The video duration bound allows a gap of 150 ms either way. The library documents about 23 ms of AAC priming, and the measured gap on the fixture reads 0 ms. A video missing over 70 ms of tail audio still passes. | ffprobe on bytes: a 35 second render reads 35.000000 s while a copy trimmed with `-t 34.9` reads 34.922812 s, a 77 ms gap that passes the 150 ms predicate and fails a 50 ms one. Tightening the test window to 50 ms passes three consecutive runs here. | Trim the source audio by about 77 ms and compare the built video against the full render length. The 150 ms predicate passes. |
| L | `internal/export/export.go:56` | The `ErrInvalid` doc comment cites an empty id and a nil dependency. This package carries no ids and no dependency seams, so the words describe nothing a caller can pass. | Grep over `internal/export/` finds no id type and no dependency field. Every `ErrInvalid` site passes paths, text, bytes or a duration. | Restore the words `empty id` and `nil dependency` in the doc comment. The inaccuracy returns. |

Counts: C 0, H 0, M 1, L 2. No `OUT_OF_SCOPE` rows. The diff stays
inside `internal/export/`.

## The ten questions

1. Does any path spend money without reserving budget first. No. The
   package shells to local ffmpeg and ffprobe only. No provider call
   exists, so no budget path applies.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap. No. Nothing here touches AssemblyAI.
3. Can a private episode, stem or transcript reach anyone but its owner.
   No. This package transforms caller supplied bytes and timings. It
   performs no serving and no authorization, which stays with wiring.
4. Does any edit apply without a revertible record. No. Export reads.
   It edits no episode state.
5. Does any spoken callback or count come from the prompt instead of a
   stored row. No. Titles, notes and timings arrive from the caller, and
   the contract names the analysis and render rows as their source.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file. No. The gate pattern grep over `internal/export/` is
   clean.
7. Did this task rebuild something Keel or Chaaya provides. No. SRT and
   WebVTT group and render through `keel/caption`. Video renders through
   `keel/waveform` over `keel/ffmpeg`. The package writes neither format
   itself. Numbered WebVTT matches the library fixtures under
   `keel@v0.3.0/caption/testdata`, where `normal.vtt` numbers its cues,
   and cue identifiers are valid WebVTT.
8. Was this review run on the handed hash, in a worktree of its own.
   Yes. `git log -1` reads the handed hash, detached, in `t5.1-rev-r1`.
9. Can any paid job kind resume after a restart and spend twice. No. No
   job kind lives here.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold. No. Tones and stills generate locally. The 150
    ms bound compares two ffprobe readings, so it is deterministic, but
    it is loose enough to pass a truncated tail. Finding L1 covers it.

## Notes for remediation, not findings

Short episodes fail closed correctly. `Description` returns
`ErrChapters` when chapters cannot satisfy the form rules, and no bundle
function gates the other artifacts, so audio, video, captions and cover
remain producible for a short episode. No over refusal exists in this
package. Wiring should decide how it surfaces a missing description.

`CoverSize` reads PNG dimensions through `png.DecodeConfig` without a
full decode, as claimed. Square rejection runs against a real 512 by 256
PNG in the suite, and neutering the check fails `TestCoverSquare` in
the scratch copy.

The store claim checks out. `keel/mediastore` defaults already include
`text/vtt` and `application/x-subrip`, and `cmd/reprise/main.go` lists
both, so the wiring persists caption bytes unchanged as documented.

## Verdict

REMEDIATE. One M and two L findings need fixes. The M fix should add a
rejection fixture whose first chapter starts past zero while every span
reaches ten seconds, for example starts at 5000, 15000 and 25000 ms over
35000 ms. The L fixes should tighten the duration window toward the
measured priming, with 50 ms proven green three times here, and correct
the `ErrInvalid` doc comment. No zero residue claim applies on round 1.
