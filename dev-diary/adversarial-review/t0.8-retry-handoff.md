# T0.8 retry handoff: provider still slow, probe complete

Retry run after the slow night, same spend discipline (120 s token caps, end + delete every
session). One greeting-only probe, then stop per protocol.

## Measurement

- `session.ready` at 1.83 s (transport healthy, same as before).
- `reply.done` at 51.88 s (recovery bar was 12 s).
- `session.ended` duration 50.79 s, `DELETE` 204, session absent from list.
- Probe spend about 6.4 cents. Total T0.8 spend to date about 84 cents.

Three runs now agree (51.53 s, 46.84 s, 51.88 s) against the 4 s T0.5 baseline: the pipeline
runs about ten times slower, consistently, with healthy transport and clean teardown. The
fault sits entirely in provider reply latency.

## Closure (owner-directed)

The probe is complete and the task closes. A probe sent to find the moon 300 km out does
not fail when the moon proves to be 3000 km out. What it found:

- Reply latency is variable night to night (4 s baseline, ~50 s sustained now).
- The product expectation is a safe 60 s ceiling: every timeout, deadline, and give-up on
  the session path must hold at 60 s or more. T2.10 implements exactly that audit.
- No `clips.json` was written and none is owed by any open task. If fixture start records
  are ever wanted, they need fresh instrumented sessions on a fast night plus the T2.9
  verdict flip already approved and held. Nothing here blocks that future work.

Runner files stay reusable at `/tmp/opencode/t08_run.py`, `t08_final.py`, `t08_probe.py`
(this machine only), with the `SESS_DIR` override noted in the first handoff.
