# T1.1 round 1 review

Hash reviewed: `44d724c738364c7ef1ce488a4afc1839f0cd7400`.
Worktree: `/home/nryn/work/reprise-wt/t1.1-rev-r1`, detached at the handed hash.
Confirmed with `git log -1` before reviewing. Base is `4632df2`.
Diff under review: `git diff 4632df2..44d724c`.
Tree was clean before reviewing and is clean now. Probes used temp files only.

## Scope

Owned paths: `internal/store/` and `internal/store/migrations/`.
Files in the diff: `internal/store/errors.go`, `internal/store/migrations/0001_schema.sql`, `internal/store/store.go`, `internal/store/store_test.go`. Nothing else changed.
`go.mod` and `go.sum` equal the base byte for byte. Both `cmp` checks succeed. No `LICENSE` file exists.

## Gate

`gofmt -l internal/store/` prints nothing. The owned Go files are gofmt clean.
`go vet ./internal/store/...` fails at the handed hash. It reports a missing `go.sum` entry for `modernc.org/sqlite`. Exit is nonzero.
`go test -race -count=1 ./internal/store/...` fails the same way. It never runs the tests. Exit is `1`.
`./tools/check.sh` fails before any web stage. It prints the same missing entry error. Exit is `1`.

## Done-when probes run independently at the reviewed hash

All probes ran in the review worktree or in scratch copies under `/tmp`. No probe touched the worktree. The tree stays clean.

1. Schema shape through `sqlite3`. Applied `0001_schema.sql` to a temp database. All twelve spec tables exist. Every named column from the phase table exists with the stated type. Every content table carries `owner_id`. The `users` table has none. No cost table exists. No flag table exists. No spend table exists. Pass.
2. Cascade erase through `sqlite3` with foreign keys on. Seeded one owner plus one episode plus one row per content table. Deleted the episode. Every content table counts zero. The owner row survives. Pass.
3. Migrate twice through the Go test in a scratch copy. Copied the handed tree under `/tmp` and ran `go mod tidy` there only. `TestMigrateAppliesTwiceWithNoChange` passes. Ledger rows match exactly across two opens. Pass with fixed metadata.
4. Erase through the Go test in the same scratch copy. `TestDeleteEpisodeErasesContent` passes. A fresh connection finds zero rows in every content table. The owner survives. Pass with fixed metadata.
5. Same Go tests at the handed hash without `tidy`. All fail in setup with the missing `go.sum` entry. Fail. This is finding H1 below.
6. Mutation check on the erase pin. Removed `ON DELETE CASCADE` from one child reference in a scratch copy. The erase probe leaves an orphan row. The pin turns red. Restored. The pin is load bearing.

## Contract and style checks

Spec tables map one to one. Users hold kind and timestamps. Episodes hold owner, number, title, state, visibility, share token and seeded flag. Sessions hold the provider session id, the token cap and connected seconds. Stems hold media id, role, sample rate and start offset. Turns hold role, text, clock times and provider item id. Words hold text, times and source. Proposals hold kind, word range and reason. Decisions hold the proposal link and the decision. Renders hold input hash, both media ids and loudness. Analyses hold transcript id, chapters, summary, entities and key phrases. Mentions hold kind, word offset and quote. Callbacks hold the mention link and the used flag.
The migration holds no `BEGIN` and no `COMMIT`. Keel wraps each file with its ledger row in one transaction.
`Open` takes `ctx` first. Exported identifiers carry doc comments that start with their names. No pointer to slice exists. No naked return exists. No `panic` exists outside `main`. One sentinel covers the nil database and wraps with `%w`.
Commit message reads `Add the diary schema with cascade erase on episode delete`. It uses active voice. It cites no plan.
Grep over `internal/store/` finds no task id, no phase id and no planning file name. Consumer name grep is empty. Dash grep is empty.

## Keel namespace check against the installed source

Keel `v0.3.0` owns namespaces `cost`, `flag`, `job`, `lease` and `mediastore`. This package migrates under `reprise`. No ledger collides.
Keel tables are `cost_charge`, `cost_budget`, `cost_reservation`, `cost_owner_budget`, `flag_value`, `jobs`, `lease_entry` and `media`. No diary table collides. The handoff namespace notes check out.

## Ten review questions

1. Does any path spend money without reserving budget first. No. This change holds schema only. It dials no provider.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. This change holds no session code.
3. Can a private episode, stem or transcript reach anyone but its owner. No. This change serves nothing. Every content row carries `owner_id`.
4. Does any edit apply without a revertible record. No. This change applies no edit. Proposals and decisions exist as tables only.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. This change holds no spoken content.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. Grep over owned paths returns empty.
7. Did this task rebuild something Keel or Chaaya provides. No. Migration and pooling come from `keel/sqlite`. Reprise owns only its tables.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` showed `44d724c` in detached worktree `t1.1-rev-r1`. Every pin ran there or in `/tmp` scratch copies. Nothing was committed.
9. Can any paid job kind resume after a restart and spend twice. No. This change holds no job code.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Tests use temp files and fixed ids with exact counts. Ledger comparison uses equality only.

## Findings

Severity counts: C 0, H 1, M 0, L 0. No OUT_OF_SCOPE defects found.

### H1. Clean tree cannot build, vet or test after the new `keel/sqlite` import

Severity: H.
Where: `internal/store/store.go:15` at the reviewed hash, the `keel/sqlite` import.
What: The new import needs transitive module entries that the frozen `go.sum` lacks. Every clean tree Go command fails in setup. The schema itself is correct. The metadata is not.
Pin: `go test -race -count=1 ./internal/store/...` in the review worktree prints `missing go.sum entry for module providing package modernc.org/sqlite (imported by github.com/nrynss/keel/sqlite)` and exits `1`. `go vet` and `./tools/check.sh` fail with the same error.
Mutation: Start from a scratch copy where `go mod tidy` added the indirect entries and the pin passes. Delete the added `modernc.org/sqlite` require line and its `go.sum` lines. The pin fails again with the same error.
Remediation note: the fix is `go mod tidy` in the task worktree. That changes `go.mod` and `go.sum`, which the round 1 contract forbade. The orchestrator must allow that change. The byte identical rule cannot hold once Reprise imports `keel/sqlite`.

## Verdict

REMEDIATE. One H finding blocks approval. The schema, the cascade and the tests are correct. The module metadata needs the tidy fix. Round 1 is the first round, so no prior round residue exists beyond H1.
