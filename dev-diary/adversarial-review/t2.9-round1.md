# T2.9 round 1 review

Reviewed hash: `fba4626345fab96a76e0fce60faa8be1e817498d`
Worktree: `/home/nryn/work/reprise-wt/t2.9-rev-r1` (detached HEAD, confirmed
`git log -1 --format=%H` equals the handed hash). No commits written.
Reviewed through `git diff c2f520b..fba4626` (one commit on claim base
`c2f520b`, `--stat` shows `internal/align/fixture_test.go` alone,
+11/-10). `git status --short` clean.

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. No findings. Residue against prior rounds is
stated below. Round 1 has no prior rounds for this task, so zero residue
is vacuously true.

## What the diff does

`TestFixtureSessions` drops the evidence-gate block (fail if
`readClipStarts` succeeds, pass-loud otherwise) and calls
`measureRealSession` instead, failing on its error and on
`!report.Pass()`. The comment is updated to match. Nothing else changes:
no production file, no threshold, no helper, no pin touched.

## Verification (all run by the reviewer in the worktree)

* `TestFixtureSessions` (full): FAILS LOUDLY as intended. All three
  subtests locate both clips with onsets, peaks, and margins matching the
  round-2 table to four decimals (steady 3.8868/11.4111, pauses
  8.0167/21.0339, barge-in 4.3068/11.2708), then each fails with
  `has no clips.json` naming the record and its required shape. Not
  silent, not passing. This is the done criterion's without-record half.
* Prior pins, all green in one run: `TestDriverMeasuresSyntheticSessions`
  (aligned zero drift, drifted exact 400-sample drift), `TestDriftLimitBoundary`,
  `TestResampleLinearExact`, `TestDriverRejectsStoredBytes` (OGG magic
  still `ErrWAV`, genuine provider.ogg zero drift),
  `TestDriverFailsLoudOnWrongLayout`, `TestLocateClipFindsBothPlacements`
  (both placements within one sample), `TestRealSessionVerdictPath`
  (aligned zero drift, 1200-sample drift exact, no-starts names
  `clips.json`), `TestRealLayoutFailsLoud` (all four breakages name
  their file).
* `go vet ./...` clean. `gofmt -l internal/align/` empty. `strings` is
  still imported and still used (no-starts and fails-loud assertions).
* Diff touches only `internal/align/fixture_test.go`, inside the task's
  declared `owns`. No threshold edit, no error-message weakening, no pin
  deletion. The 40 ms bar lives in `align.go`, untouched by this diff.
  The loud error is the same `readClipStarts` error as before, now
  reached through `measureRealSession` instead of directly.
* With-record half verified by reading plus the in-tree proof, not by
  fabricating a record onto real audio: `TestRealSessionVerdictPath/aligned`
  proves the exact verdict path the switch calls measures with zero drift
  on a matching start gap, and `/drifted` proves it fails past drift.
  Fabricating `clips.json` over the real fixtures was judged heavy (each
  fixture run costs ~10 to 14 s of correlation, and any fabricated start
  would be invented evidence, not a measurement). Stated so the next
  agent knows what was and was not re-proved.

## Findings

None. No rows. Every severity count is zero.

## The ten questions

1. Does any path spend money without reserving budget first? No. Test-only
   diff. No provider calls.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. No session handling in this diff.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   No serving, no URLs, no auth paths.
4. Does any edit apply without a revertible record? No. No edits apply.
   `Report` stays a read-only measurement with a display warning.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. Starts come from `clips.json` via `measureRealSession`. Detector
   times stay diagnostic-only through `logVADGap`.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The new comment names only the playback-start record,
   the drift bar, and the ran-count pin. No ids, no file names, no marks.
7. Did this task rebuild something Keel or Chaaya provides? No. Test-only
   switch onto the already proved verdict path.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Detached HEAD at `fba4626` in `/home/nryn/work/reprise-wt/t2.9-rev-r1`,
   no commits written, working tree clean.
9. Can any paid job kind resume after a restart and spend twice? No job kind
   here. Nothing durable, nothing retried.
10. Does any development check need a person, a microphone, or a wall-clock
    threshold? No. All pins assert exact sample math or exact error text.
    No device, no thresholds.

## Verdict: APPROVE

Zero findings across all severities. Zero residue against every prior
round of this task, severity by severity: round 1 is the only round, so
the claim holds vacuously, and residue against T2.5 rounds 1 through 3
is zero as shown by the re-run pins above (synthetic layout, drifted,
boundary, OGG rejection, locator, real-verdict-path, and real-layout
loudness all green, 40 ms bar and loud errors byte-for-behavior).
