# T0.1 round 2 review

This round reviewed `a02280b68dc30d469ef9f1a841270cbdfdd9c673` in `/home/nryn/work/reprise-wt/t0.1-rev-r2`. The worktree stayed detached at the handed hash for the whole round. `git log -1` confirmed the hash before any check ran. The remediation diff is `git diff 9a54c2a6222aaf5e9fa7770e1425f3b31a022eac..a02280b68dc30d469ef9f1a841270cbdfdd9c673`. The full task diff is `git diff 9af1c63..a02280b68dc30d469ef9f1a841270cbdfdd9c673`. Base commit `9af1c63` carries only `.gitignore`.

## Verdict: APPROVE

Severity counts: C 0, H 0, M 0, L 0. No finding blocks the landing.

## Residue against round 1

Round 1 returned C 0, H 1, M 0, L 1 with verdict REMEDIATE. Zero residue remains against any severity.

The H finding is fixed. The gate failed on a clean tree because nothing generated `.svelte-kit/tsconfig.json` before `svelte-check`. The remediator added `npx svelte-kit sync` before `svelte-check` and `npm run build` before the Playwright stage. Both stages keep the mandated order and both failures name themselves. I deleted `web/.svelte-kit`, `web/build` and `web/node_modules`, ran `npm ci` in `web/`, then ran `./tools/check.sh`. It exited 0 with `OK all checks passed`. I then removed the new sync stage in a scratch edit and reran the gate from a clean tree. It exited 1 with `FAIL [svelte-check]`. I restored the stage at once and the tree is clean. A direct `npm run check` with no `.svelte-kit` directory fails with `Cannot read file` against `web/.svelte-kit/tsconfig.json`. The pin is load-bearing and the fix carries it.

The L finding is fixed. The second sentence of the package comment in `cmd/reprise/main.go` ran 40 words against the 30 word limit. The remediator split it after `embedding it`. A word count probe over the three comment sentences now returns 11, 20 and 20. Every sentence stays under the limit. Rejoining the split sentences returns 40 words again, so the probe stays load-bearing.

## Findings

No findings at any severity. The table stays empty because every probe passed.

## Verification evidence

The gate passed from a fully cold tree three times in a row. Run 1, run 2 and run 3 each exited 0 with `OK all checks passed`. Each run started with no `web/.svelte-kit` and no `web/build`. Run 1 also started with no `web/node_modules` after a fresh `npm ci`. All 13 stages ran in order with no silent skips. Go stages (`gofmt`, `go vet`, `staticcheck` through `go run honnef.co/go/tools/cmd/staticcheck@v0.8.1`, `go test -race`) passed in every run.

Guard probes, each reverted with a clean `git status`: an `export let` comment plant plus a `$:` comment plant tripped `FAIL [svelte 4 leakage]`. A `T2.1` comment plant in `main.go` tripped `FAIL [plan references]`. A real-code `export let` plant tripped ESLint before the scans, which also fails the gate. The consumer name scan, the plan scan and the leakage guard are clean on the reviewed tree.

Pins: Keel resolves to `v0.3.0` with no `replace` line. Chaaya is exactly `0.2.0`. Installed Svelte 5.57.0, Kit 2.70.3, Vite 8.3.0, TypeScript 5.9.3, svelte-check 4.7.6, ESLint 10.10.0, Vitest 5.0.1 and Playwright 1.63.0 satisfy Chaaya 0.2.0 peer and dev ranges. `web/src` uses runes only. No `export let`, no `$:`, no store import exists.

Auditor: `audit_docs.py` passes on the real planning tree with exit 0. Against a scratch copy with a `requires` line pointing at `T9.9` and a relative link pointing at a missing page, it exited 1 and named both defects. Scratch-only findings for parent links reflect the copy missing its parent files. They do not occur on the real tree. The scratch copy is removed.

Container: `docker build -t reprise:t01-r2 .` succeeded. A run served `/healthz` at 200 with a boot id, `/` at 200 with prerendered `<title>Reprise</title>` and `<h1>Reprise</h1>`, and `/episodes/one` at 200 from the fallback shell. Image user is `nonroot` with an absolute entrypoint and an absolute `-web` path.

Remediation scope: the diff holds only the two fixes. `e81ce4b` touches `tools/check.sh` only. `a02280b` touches `cmd/reprise/main.go` only. Both commit messages use short active sentences with no semicolon. `git ls-files` names no `.svelte-kit` path and no build output. The full task diff names 24 files, all inside the owned paths. `cmd/reprise/main_test.go` is the owned binary's own test and adds no product surface. No `LICENSE` file exists. `dev-diary/tools/audit_docs.py` lives in the gitignored planning area by design, and the handoff records that placement.

CI runs the same gate on Go 1.27 and Node 26. It installs `npm ci` dependencies, a Playwright chromium, and pinned static ffmpeg and ffprobe at v7.1.

## Out of scope defects

None. Nothing outside the owned paths carries a defect.

## Ten review questions

1. Does any path spend money without reserving budget first? No. The tree holds no paid call. It serves a static shell and a health endpoint.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. No provider code exists yet.
3. Can a private episode, stem or transcript reach anyone but its owner? No. No episodes, no auth and no media paths exist yet.
4. Does any edit apply without a revertible record? No. No edit paths exist yet.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No callbacks exist anywhere in the tree.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The plan scan over tracked files is clean, with the documented exemptions only.
7. Did this task rebuild something Keel or Chaaya provides? No. It uses `keel/id` for the boot id and ships a placeholder shell that needs no Chaaya export yet.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` reads the handed hash in the detached review worktree.
9. Can any paid job kind resume after a restart and spend twice? No. No jobs exist yet.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. Vitest runs on jsdom, Playwright serves a local preview, and Go tests use `httptest`. Timeouts bound failure only.
