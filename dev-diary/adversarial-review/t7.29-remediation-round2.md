# T7.29 remediation, round 2

- **Reviewed hash:** `813cacae29384028190134e2050cf2d2fca33f8e`
- **Worktree:** `/Users/narayan/reprise-wt/t7.29-rem-r2`, branch `task/t7.29-rem-r2`
- **Final hash:** `6ece425710d2e6f7be4da00ef66533de340c68f8`
- **Commits on the reviewed hash, oldest first:**
  1. `12e7f930a9d2d927708242d633e4c3d7e05345d2` Queue a mark done that meets a busy render slot
  2. `6ece425710d2e6f7be4da00ef66533de340c68f8` Show a queued render as waiting on the gallery card

No amend and no rebase. The out of scope L (`RenderConcurrency` unread) belongs to T7.35 and is untouched. No lambo server exists here.

## Findings

| # | Sev | Fix | Commit | Pin | Mutation check |
|---|---|---|---|---|---|
| 1 | H | `episode.Config` gains `WaitWhenBusy`, and the binary's `episodeConfig` sets it. With it set, `RequestRender` answers a start refused with `job.ErrLimit` with an empty job id and no error. The episode stays `rendering` with no render job, and the existing `startWaitingRenders` walk starts it once the slot frees, oldest first. Mark done answers 202 with `queued: true`, an empty `job_id` and `state: rendering`. A repeat tap still answers 409 and starts nothing. Two double start guards: any other refused start now fails only a still `rendering` episode (`Transition` in place of `Fail`), which closes the gap the reviewer noted. `StartEpisodeKind` for the render reads the state under the schedule lock. It joins the running render as before, and also joins the render that already moved the episode past `rendering`. It starts nothing otherwise (`errNotRendering`). The walk keeps its join check unchanged. In the gallery, a `rendering` detail with no render outcome now says "The render is queued. It starts once the render ahead of it finishes." Its pass carries `queued: true`, and the controller reads that row's detail again every four refresh ticks, so the card moves onto the render once it starts. It never reads failed or "Ready". | `12e7f93` (backend), `6ece425` (gallery) | `TestMarkDoneWhileRenderBusyWaitsAndShips` in `cmd/reprise/finish_test.go` is the reviewer's pin, made a test. It also checks the queued body, a 409 on the repeat tap, and no render job while the slot is held. Beside it are `TestRenderStartJoinsTheRenderThatShipped` (cmd), `TestRequestRenderWaitsWhenTheRenderSlotIsBusy` (episode), `TestMarkDoneQueuesBehindABusyRender` (api), and the vitest case "moves a queued card onto its render once the render starts". The reviewer probe `TestProbeMarkDoneWhileRenderBusyStillShips` passes when copied in. | Deleting the `if err != nil && s.waitWhenBusy && errors.Is(err, job.ErrLimit) { return "", nil }` branch restores the `Fail`, and the pin fails with "answered 429 and left the episode failed, want rendering". Restoring the join condition to `last.Found && !jobTerminal(last.Status)` fails `TestRenderStartJoinsTheRenderThatShipped`. Removing the `this.recheckQueued()` call fails the vitest case. Each passes again once restored. |

## Contract changes

- `episode.Config.WaitWhenBusy` is new. `RequestRender` may now return an empty job id with no error, and only then. The `episodeStore` doc in `internal/api/episodes.go` says so.
- `POST /api/episodes/{id}/done` gains a `queued` boolean. `job_id` is empty when it is true. The 429 `render_busy` answer now fires only for a service without `WaitWhenBusy`. Its message no longer promises a retry.
- `GalleryPass` gains an optional `queued` field.
- **Outside my paths, for the orchestrator.** `web/src/lib/editor/draft.ts` treats an empty `job_id` as the fixture render. After a queued mark done, the editor says "Render running: fixture render...". The response itself is honest, but that editor line is wrong for a queued render. The doc comments on `CodeRenderBusy` and `CodeRenderUnavailable` in `internal/api/respond.go` still say the episode "waits in failed for an explicit retry", and no route retries. Nobody raised either as a finding, so I left them.

## Gate

`dev-diary/probes/go.mod` (`module probes`, `go 1.27.1`) was created untracked before the gate and removed afterwards. `./tools/check.sh` stops at the accepted ffmpeg 9.0.2 pin check. Every later step ran through `dev-diary/probes/t7.34-rev-r2/gate.sh` from the worktree root, on `6ece425`.

| Step | Exit |
|---|---|
| `cd web && npm ci` | 0 |
| `gofmt -l cmd` | 0, no output |
| `go list ./...` (web filtered) | 0 |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| svelte 4 leakage `git grep` | 1, empty |
| consumer names `git grep` | 1, empty |
| plan references `git grep` | 1, empty |
| `go test -race ./...` | 0 |
| rebuild, then `playwright test -c src/routes/gallery.playwright.config.ts` | 0, 8 passed |
| rebuild, then `playwright test -c src/routes/threads/threads.playwright.config.ts` | 0, 10 passed |
| `t7.29-rev-r1/wire_mutation.sh` | 0, four `killed` |
