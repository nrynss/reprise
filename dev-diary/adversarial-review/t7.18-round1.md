# T7.18 round 1 review

Reviewed hash: `12f97eb1f8c071b7ba48a8f5bbc1d5242ad8c0a4`
Worktree: `/home/nryn/work/reprise-wt/t7.18-rev-r1` (detached HEAD, confirmed with `git log -1`)
Base: `fa3d6b5`
Diff reviewed: `git diff fa3d6b5..12f97eb1f8c071b7ba48a8f5bbc1d5242ad8c0a4`
Verdict: REMEDIATE

Counts: C 0, H 1, M 0, L 2. OUT_OF_SCOPE rows: 2 (H 1, M 1). Out of scope rows never block approval.

## What the commit does

It adds `web/src/routes/record/api-guard.ts`, a content type guard for JSON calls. It routes `postStemsComplete` through the guard. It pins HTML answers on 200 and 403, missing and malformed bodies, unchanged JSON refusal codes, single request failure with working retry. It also reverts the phase file status line, which sits outside its owns paths.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `web/src/routes/record/api-guard.ts:27,55` | The commit fails the gate. `./tools/check.sh` stops at the Svelte 4 leakage scan. It names both `response.headers.get(` lines. The handoff claims the gate passes, but it does not. | `npm ci` in `web/`, then `./tools/check.sh` from the worktree root. Exit code is 1. Output ends with `FAIL [svelte 4 leakage]` naming `api-guard.ts:27` and `:55`. Ran in this worktree. | Either cited line reintroduces the defect. The scan pattern matches any `get(` call. The base tree has zero matches, so the pin is load bearing on these two lines alone. |
| L | `dev-diary/PHASE-7-production-issues.md:470` | The task commit edits an orchestrator owned status line. It flips T7.18 from `in-progress:implement` to `not-started`. Its owns line covers only `web/src/routes/record/`. | `git diff fa3d6b5..12f97eb --stat` lists the phase file beside the owned paths. Ran in this worktree. | The one line status edit. Reverting that hunk clears the pin. It drops before landing. |
| L | `web/src/routes/record/stems-complete.ts:218-220` | The loud banner names the call twice in a row. It reads `The draft move failed. The draft move answer was not JSON.` The duplication ships user visible text from this commit. | A `vite-node` probe calls `describeCompletionFailure` with the guard error and asserts one `draft move` mention. It exits 1 today with two mentions. Ran in this worktree. | The generic `Error` branch prefix. Wording that already names the call removes the second mention and clears the pin. |

## Out of scope rows

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| OUT_OF_SCOPE H | `web/src/lib/voice/record-state.ts:336,490`, `web/src/lib/voice/processing-state.ts:131` | The voice layer still uses the unchecked shared client. Session mint, session end, and the job poll do not validate content type. A challenge page with status 200 resolves as an empty success there. | A node probe stubs fetch with HTML 200 and calls Chaaya `api()`. It resolves `null` instead of throwing. Ran in this worktree. Call site citations above show the unchecked callers. | Any one cited `api(` call site. Routing it through a content type guard turns the silent null into a named throw. Owning path is `web/src/lib/voice/`. |
| OUT_OF_SCOPE M | `web/src/lib/voice/record-state.ts:265-268,291-296` | The take clock keeps ticking after a failed draft move. The 500 ms interval clears only in `destroy()`. Nothing in `endTake` stops it when the completion fails. The handoff admits this. | `grep -n clearInterval` shows the single call in `destroy()`. The `endTake` path at lines 317 to 349 never clears the timer. Read in this worktree. | The missing clear on the failed completion path. Stopping the interval when the draft move fails freezes the display and clears the pin. Owning path is `web/src/lib/voice/`. |

## Done criteria assessment

The narrow done clause passes for the guarded call. Playwright serves HTML on the completion route and the page shows the named failure with retry. I reran the record mock suite. All four specs pass, including the new challenge proof. Unit suites pass (29 tests across the two files). The normal path is unchanged, with existing move, repeat, and refusal tests green.

The wider spec bullets do not pass. `Every API call on the take path` still fails for mint, session end, and job poll. The take clock still ticks after failure. Both fixes live in `web/src/lib/voice/`, outside this task's owns line. They need a follow up task owning that path. The voice layer rows above scope it.

## Ten questions

1. Does any path spend money without reserving budget first. No. The change touches only the draft move POST. It opens no paid call.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. Session lifecycle code is untouched.
3. Can a private episode, stem or transcript reach anyone but its owner. No. No read path changes.
4. Does any edit apply without a revertible record. No. No episode mutation changes.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. No prompt or copy changes beyond error wording.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. The gate plan reference scan over tracked files returns empty.
7. Did this task rebuild something Keel or Chaaya provides. No. The guard reuses Chaaya `ApiError` and `parseErrorEnvelope`. Its small header helpers preserve the shared refusal shape. They do not fork client logic.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` gives the handed hash in `t7.18-rev-r1`. Every pin ran there. No code was committed.
9. Can any paid job kind resume after a restart and spend twice. No. No backend or job registration changes.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Unit pins use stub fetch with fixed answers. The Playwright proof uses the mock take. No duration threshold appears.

## Gate and docs

`./tools/check.sh` fails at the Svelte 4 leakage scan (finding F1). Build, unit tests, and the record Playwright suite pass. `dev-diary/tools/audit_docs.py` reports the planning tree is consistent.
