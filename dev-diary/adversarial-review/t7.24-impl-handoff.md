# T7.24 implementer handoff

Commit `476fa9cf3253eb5697d8db77902d01ddbceca843` on branch `task/t7.24` in worktree `/Users/narayan/reprise-wt/t7.24-impl`. Base is `f55733629126d3d5f8772f4457f6352759711449`. The worktree is clean. This file is not in the commit.

## What I built

The record page now renders one link, Back to gallery, as the first item in main. It sits outside every phase branch, so preflight, starting, live, ending, recovery, and both draft move blocks all show it. The link is a plain anchor to `/` through the same path helper the other screens use. No extra gesture is required.

## Phases the Playwright pin covers

`web/src/routes/record/gallery-link.spec.ts` clicks that link and expects the season heading on `/`.

- Preflight, before start.
- Starting. The proof pauses the audio clock resume so the phase cannot flip to live.
- Live, after the mock take opens.
- Ending. The proof holds the draft move request. The notice says the take is moving to draft, and the retry block is absent.
- Ending after a refused draft move, with the retry control on screen.
- Recovering, from `/record?mock=1&resume=1`.
- Draft move success, beside the live take.
- Draft move failure, beside the live take.

Every one of those states is reachable with the mock take. None of them needs a live provider, a microphone, or a person.

## Commands and results

`npm ci` in `web/` installed 236 packages. Exit 0. `node_modules` was missing in this worktree.

`npx eslint src/routes/record/+page.svelte src/routes/record/gallery-link.spec.ts` from `web/`. Exit 0.

`npx vitest run src/routes/record/api-guard.test.ts src/routes/record/stems-complete.test.ts` from `web/`. 29 tests passed.

`npx svelte-check --tsconfig ./tsconfig.json` from `web/`. 0 errors and 0 warnings.

`npm run build` from `web/`. Exit 0. The static adapter wrote `web/build`.

`npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts` from `web/`. 8 passed in 2.3s.

`npx playwright test -c src/routes/record/mock.playwright.config.ts end-control.spec.ts cap.spec.ts stems-complete.spec.ts mock-session.spec.ts` from `web/`. 13 passed in 11.3s.

I left `./tools/check.sh` unrun. The ffmpeg pin can fail that gate before later stages, and I did not change the pin.

## What surprised me

The preview server answers 404 for `/api/episodes`. The season heading still renders, so the link proof does not need a backend.

The record Playwright config matches every `*.test.ts` file as well as specs. `api-guard.test.ts` and `stems-complete.test.ts` throw under Playwright because they are vitest files. That collection is older than this change. I ran the spec files by name.

`/usr/bin/git` exits 69 until the Xcode license is accepted. Commits here used `/opt/homebrew/bin/git` 2.55.0.
