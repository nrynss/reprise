# T2.2 round 1 review

Reviewed hash: b49c31f6d04820143f25232a325e7195916e433f
Worktree: /home/nryn/work/reprise-wt/t2.2-rev-r1 (detached HEAD, `git log -1` matches the handed hash)
Base: 8ebcb1d. Diff touches only `internal/host/host.go` (new) and `internal/host/host_test.go` (new). One commit sits on the base. Both paths sit inside the declared `owns`.

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. No findings. No OUT_OF_SCOPE defects.

## Checks run in the review worktree

- `go test -race -count=1 ./internal/host/...` passes, all seven tests.
- `go test -race -count=1 ./...` passes across every package.
- `go vet` on the package reports nothing. `gofmt -l` on the package reports nothing.
- Grep for plan references (task ids, phase ids, planning file names, section marks) across `internal/host` finds none.
- Grep for spend paths (network clients, provider packages, budget and lease calls, environment reads) finds none.
- Confirmed the fixed prompt wording holds no digits and no names. Every digit the host speaks arrives through a format verb filled from a stored row.

## Spec coverage

- Greeting. The golden test pins the exact one-sentence greeting with the callback quote and episode 1. The provenance test checks every quoted span against stored rows, every digit run against stored episode numbers, and exactly one sentence end. The opener case pins the warm greeting and empty keyterms for a season with no callback.
- System prompt. Tests pin the host voice, the interrupt to follow up, one question at a time, exactly three threads, and the use-only-what-given rule.
- Keyterms. The golden order pins frequent first, then recency. The cap test seeds 125 rows and pins exactly 100 with the most frequent first. The dedupe test pins case folding with the most recent wording winning.
- Owner isolation. A used callback falls back to the opener. A second owner hears only its own rows. No owner id leaks into the prompt.
- Input validation. A nil database and an empty owner return `ErrInvalid` beside an empty config.

## Notes, not findings

- `Load` reads only. Nothing marks the callback used, so repeat sessions repeat the greeting until the broker or a later callback task owns that write. The handoff discloses this and names the owner. The task spec asks for reads only, so this is deferred work, not a defect.
- The greeting cites the mention episode (episode 1) rather than the callback row episode id (the upcoming episode 5). The words came from episode 1, so this matches the spec phrase about the episode the callback came from. The handoff discloses the choice.
- A quote with internal sentence ends would add periods inside the quoted span. The template still frames one sentence and the quote travels inside quotation marks. No fixture shows such a quote, so no defect stands proven.

## The ten questions

1. Does any path spend money without reserving budget first. No. The package holds no network code, no provider client, and no budget calls. It builds strings and reads SQLite rows.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. Nothing here opens a session. No token and no socket exist in this package.
3. Can a private episode, stem or transcript reach anyone but its owner. No. Every query filters by owner id. The isolation test covers two owners side by side plus the used-flag case.
4. Does any edit apply without a revertible record. No edits happen here. `Load` reads and `Build` renders. Nothing writes.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. The greeting formats the episode number and the quote from the callback join. The fixed wording holds no digits and no names, and the provenance test enforces that.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. Grep across the package finds no ids, no planning file names, and no section marks.
7. Did this task rebuild something Keel or Chaaya provides. No. Prompt rendering and ranking are product specific. The tests use Keel SQLite and the product store for fixtures, which is what those pieces exist to support.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` returns the handed hash on a detached head. Every pin ran in that worktree.
9. Can any paid job kind resume after a restart and spend twice. No job kinds exist here. The package never touches the job system.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. Seven tests run on seeded SQLite rows with exact assertions under the race detector.

## Verdict

APPROVE. Zero findings across C, H, M, and L. Zero residue stays explicit. This is round 1, so no prior round stands open, and this round opens none.
