# T7.20 round 1 review

Reviewed hash: `3a5e2d49ad75ed82832e2ae3856db1160e28eade`
Worktree: `/home/nryn/work/reprise-wt/t7.20-rev-r1` (detached, confirmed with `git log -1`)
Base: `511f4a1`
Diff reviewed: `git diff 511f4a1..3a5e2d49ad75ed82832e2ae3856db1160e28eade` (6 files, all under `web/src/lib/voice/`, inside `owns`)

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. No findings. No OUT_OF_SCOPE defects.

## What the commit does

New `session-calls.ts` routes the take calls through the record route
guard (`readJsonAnswer`), which checks the content type before trusting
the body and names the failed call. Mint never retries (a retry would
reserve budget twice). Session end retries exactly once on a network
drop (the record is idempotent). The job poll throws by name instead of
landing as an empty row, and `JobStream` keeps its last reading on a
dropped poll (verified in the library source, `#catchUp` swallows the
throw). `record-state.ts` wires mint and close through the guards,
surfaces failed stem uploads and failed closes loudly with the pair
kept for retry, and stops the elapsed interval on every take end path
(end, failure, destroy). `processing-state.ts` polls through the named
guard. 14 vitest cases plus 3 Playwright proofs cover the done clause.

## Pins run in this worktree

1. `npx vitest run src/lib/voice/session-calls.test.ts`: 14 passed.
   Mint on HTML answers throws a named error with exactly 1 fetch call.
   Mint on a network drop throws `network` with exactly 1 call (no
   second reservation). Close on a network drop resolves after exactly
   2 calls. Close on HTML or 429 throws after exactly 1 call (no retry
   loop, shared refusal shape kept).
2. `npx playwright test -c src/lib/voice/mock.playwright.config.ts
   session-guard.spec.ts`: 3 passed. Challenged chunk names the stem
   upload, freezes the clock, stays on `/record` with a working retry.
   Challenged end names the call, freezes the clock, and the retry
   finishes the take to `/processing`. Challenged mint names the call
   and leaves the start control enabled.
3. `./tools/check.sh` in the worktree after `npm ci`: OK, all checks
   passed (eslint, vitest, build, playwright, svelte 4 leakage,
   consumer names, plan references).
4. `python3 dev-diary/tools/audit_docs.py`: OK, planning tree is
   consistent.

## Failing-before and mutation evidence

- Old client throwaway: stubbed HTML-200 through the shared `api()`
  resolves to silent `null` with no name and no throw (1 passed, file
  removed after). The new `mintSession` throws a named error on the
  same answer. The defect the task names is real and the guard fixes
  it.
- Clock mutation: removing both `stopClock()` calls (`endTake` and
  `failTake`), rebuilding, and rerunning the guard spec fails exactly
  the two clock assertions (tests 1 and 2, `Expected: false`), with
  the mint proof still passing. Restored and rebuilt after: tree
  clean, hash unchanged, 3 passed. The freeze pins are load-bearing
  against the real defect. Either stop alone still freezes, so the two
  sites are redundant cover, not dead code. The success path stop in
  `endTake` is belt and braces beside the failure path stop in
  `failTake`.
- No reviewer edits remain. `git status --short` is empty and
  `git log -1` still reads the handed hash. The preview build was
  rebuilt from the restored source after the mutation probe.

## The ten questions

1. Does any path spend money without reserving budget first? No. Mint
   still mints once through the server, which reserves. The close
   retry fires only on a network drop against an idempotent record.
   No new spend path opens.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. The socket close still runs first, the guard
   stays attached on a failed close (its beacon remains), the pending
   record retries, and the token cap bounds an abandoned take.
3. Can a private episode, stem or transcript reach anyone but its
   owner? No. No URL, owner, or visibility value changes.
4. Does any edit apply without a revertible record? No. The task
   applies no edit. The completion driver path is unchanged.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. No prompt or greeting text changes.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. `tools/check.sh` plan references pass. The word
   `phase` in `record-state.ts` is the pre-existing take phase domain
   word.
7. Did this task rebuild something Keel or Chaaya provides? No. It
   reuses the route guard, `ChunkUploader`, `JobStream`, and
   `SessionGuard`. Chunk calls stay inside the library uploader, and
   the controller reads its failed state loudly instead of patching
   it.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Detached worktree above, hash confirmed before and after.
   Nothing was committed here.
9. Can any paid job kind resume after a restart and spend twice? No.
   No job code changes. The close record is idempotent and the server
   reports the standing outcome.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. Vitest runs on stub fetch answers. The
    guard spec runs on generated input under the mock flag and the
    live mint proof answers before any microphone opens. Clock
    assertions read the interval handle, never an elapsed value.

## Remarks (not findings)

- The library uploader counts an HTML-200 chunk body as acknowledged
  and reconciles at completion time, so a challenged chunk surfaces
  at finish rather than at the chunk. The page behavior still meets
  the done clause (named failure, frozen clock, retry) and the proof
  pins it. If the delayed surfacing ever matters, it belongs to the
  library behind its own reproduction, not to this task.
