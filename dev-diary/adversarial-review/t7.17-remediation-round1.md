# T7.17 remediation round 1

Reviewed hash: `29c3d29e983570570a0332e174d13067b02484fa`
New hash: `e14c484d8c1d39489edae1d5eacf701e2421c62c`
Worktree: `/home/nryn/work/reprise-wt/t7.17-rem-r1` on branch `task/t7.17-rem-r1`
Scope: finding F1 only. Finding F2 names a phase file and belongs to the
orchestrator, so this round leaves every planning file untouched.

## Reproduction first

A throwaway node probe replayed the review pin against the reviewed code
before any edit. It measured `mismatches=32768 maxAbs=1 edge32767=32766
edgeNeg=-32767` over the full int16 range and `loud mismatched bytes=316
of 960` on the 0.99 sine. Both match the review exactly, so the loss is
confirmed and the fix below answers the measured defect.

## Fixes

| Finding | Severity | Where | What changed | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | H | `web/src/lib/voice/pcm.ts:7-19` | `floatToPcm16` now scales by 32768 instead of 32767 and clamps the rounded value to the int16 rails. The decode divide by 32768 is unchanged. Every int16 value divided by 32768 stays exact in float32, so rounding the product returns the same value and the pair round trips every code with zero loss. Full scale input clamps to 32767 and minus full scale keeps minus 32768. | Extended byte exact pins in `web/src/lib/voice/socket.test.ts`: the existing 0.3 sine case plus a new loud 0.99 sine case through the data key to the drain, and a full range sweep of all 65536 int16 values through the wire to the drain asserting zero mismatches with both edges pinned. Updated rail expectations in `web/src/lib/voice/pcm.test.ts` (`1` gives `32767`, `-1` gives `-32768`, `-0.5` gives `-16384`). Post fix probe reads `mismatches=0 edge32767=32767 edgeNeg=-32768` and `loud mismatched bytes=0 of 960`. Vitest: 26 passed across `pcm.test.ts` and `socket.test.ts`. | Restore the old scale by replacing `32768` with `32767` in `floatToPcm16` and dropping the rail clamp. The loud sine test and the full range sweep fail again with the 1 LSB loss. |
| F2 | L, OUT_OF_SCOPE | `dev-diary/PHASE-7-production-issues.md` | Not touched. The orchestrator owns all phase files. | None in this round. | None in this round. |

## Validation

`npm ci` in `web/` then `./tools/check.sh` passes in this worktree with
`OK all checks passed` and exit 0. `dev-diary/tools/audit_docs.py`
reports `OK planning tree is consistent`. `git status` shows only the
three owned paths under `web/src/lib/voice/`. No planning file changed.
The remediation file itself stays uncommitted so it never rides the task
commit, per the agent protocol.
