# Review T2.3 round 2: session reconciler remediation

Reviewed hash: `af521f3279856ef6a1dc906f403ddf6633f10c22`
Worktree: `/home/nryn/work/reprise-wt/t2.3-rev-r2` (detached HEAD, confirmed with `git log -1 --format=%H`)
Base for remediation diff: `11a51ec` (`git diff 11a51ec..af521f3` shows only the two owned files)
Owns check: diff holds `internal/broker/reconcile.go` and `internal/broker/reconcile_test.go` only. The task owns `reconcile.go` plus the test beside it. Clean. Remediation commits `d0ca827` (H fix) and `af521f3` (L fix) sit on the reviewed commit with no rebase.

Verdict: **APPROVE**
Counts: C 0, H 0, M 0, L 0. Out of scope findings: 0.

## Residue vs round 1, severity by severity

Round 1 returned REMEDIATE with C 0, H 1, M 0, L 1.

- **H residue: zero.** F1 (expired lease parked the session in review forever, artifacts lost) is gone. `Reconcile` now detects expiry after a settle under its own claim (`reconcile.go:648-657`) and completes the tail (`completeExpiredTail`: marks settled, finishes artifacts) instead of reviewing. The committed `TestReconcileExpiredLeaseCompletesTail` passes in this worktree: no review, 10 connected seconds on the row, recording bytes persisted, spend moved exactly once, retry returns the identical result with no second settle, lease stays expired. The first assertion fails against the reviewed code per the remediation file, so the pin is load-bearing.
- **L residue: zero.** F2 (over cap alert refired on every retry) is gone. The artifact tail alerts once per session on the durable `over_cap_alerted` flag (`reconcile.go:788-797`). The committed `TestReconcileOverCapAlertsOnce` passes here: two reconciles, exactly one alert, spend settled once. The pin fails with two alerts against the reviewed code, so it measures the guard.
- **C residue: zero.** Round 1 recorded no C finding, and this diff adds no spend, privacy, or data-loss path that could hide one. Verified by code read and the passing suite.
- **M residue: zero.** Round 1 recorded no M finding, and no medium defect surfaced in the re-review hunt below.

## Remediation claims checked

- **Clock injection, no sleep.** Confirmed. The expiry test builds the fixture with `newRecFixtureClock` and a fake lease `Now`, moving the clock one second past a 150 ms cap. No `Sleep` appears in the three new tests. The only `Sleep` in the file is the pre-existing restart-test liveness poll (`reconcile_test.go:714`, 20 ms poll against a 15 s deadline), which asserts nothing about timing behavior.
- **Resume on expired still reviews, no double book.** Confirmed by read. `resumeClaimed` has no expired branch, so an expired lease on the resume path falls to the default review without touching money. `budgets.Settle` never runs in `resumeClaimed`. The expired completion runs only in the caller that just settled under its own claim. Re-settling there would book twice, so the split is the safe shape.
- **Migration inside owns, twice-safe, covers old rows.** Confirmed. `ensureAlertColumn` lives in `reconcile.go` (owns) and runs from `NewReconciler`. It returns early when the column exists, so a second open changes nothing. A missing column gets `ADD COLUMN ... NOT NULL DEFAULT 0`, which backfills pre-existing rows with 0. A pre-existing settled over cap row therefore alerts once on its next tail run, then quiets. `TestReconcileAlertColumnMigrates` drops the column, reopens, and proves it returns.

## New-defect hunt

- **Alert-flag ordering vs settle.** The flag writes after successful alert delivery. Delivery failure leaves the flag unset, so a retry alerts again. A mark failure returns an error. A crash between delivery and the mark can duplicate one advisory alert, never money. Acceptable. No finding.
- **Expired path with a genuine over cap duration.** `completeExpiredTail` funnels into `finishArtifacts`, so the once-guarded over cap alert still fires there. The in-cap expired test correctly asserts zero alerts. No finding.
- **Post-expiry retry path.** `markSettled` leaves `claimed` set but sets `settled`, and `Reconcile` checks `settled` first, so retries after an expired tail finish artifacts without review. The crash window between `Settle` and `markSettled` still reviews on resume, which is the documented at-most-once shape, not a defect. No finding.
- **Double lease inspect on the expired close path.** `closeLease` inspects once, then `leaseExpired` inspects again. Redundant but harmless, with no observable defect. No finding.
- **Wall-clock dependence.** None new. `time.Now` appears only for `updated_at` stamps, never in an assertion. No finding.
- **Plan references.** A grep for task, phase, and planning-file patterns over both owned files returns only the substring "around" and schema identifiers. No finding.

## Verification run

- `git log -1 --format=%H` equals the handed hash. No commits made, working tree clean.
- `go test -race ./internal/broker/` passes in this worktree.
- The three committed remediation tests pass: `TestReconcileExpiredLeaseCompletesTail`, `TestReconcileOverCapAlertsOnce`, `TestReconcileAlertColumnMigrates`.
- Full `tools/check.sh` (with `npm ci` web build) was not rerun here. The remediation file records a green gate in its own worktree, and this task owns Go files only. The broker suite with `-race` is the load-bearing check, and it passes.

## Findings

None. No new rows.

## The ten questions

1. Does any path spend money without reserving budget first? No. The reconciler only settles the mint-time hold T2.1 reserved. The expired tail settles under the same claim, exactly once.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This task adds no session lifecycle. The cap is set at mint, and expiry is now handled as terminal instead of reviewable.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The recording persists privately, and the expired tail uses the same persist path. The round 1 private-route check still passes in the suite.
4. Does any edit apply without a revertible record? No. The writes are durable settle state, not user edits.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. Nothing here speaks.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Pattern scan over both owned files is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. The remediation stands on the Keel lease manager clock seam and the existing claim table. The alert flag is reconciler-owned state, not library state.
8. Was this review run on the handed hash, in a worktree of its own? Yes. Detached worktree at `af521f3`, confirmed by `git log -1`, diffed against `11a51ec`, tests run inside it, no commits.
9. Can any paid job kind resume after a restart and spend twice? No. The resume path never calls `budgets.Settle`. The expired completion runs only after this run settled under its claim, and the retry test proves no second settle.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The new tests move a fake clock. The restart poll keeps its pre-existing liveness bound, which asserts completion, not timing.

## Verdict

**APPROVE.** Both round 1 findings are fixed with load-bearing pins, residue is zero in every severity, and the hunt above surfaced no new defect.
