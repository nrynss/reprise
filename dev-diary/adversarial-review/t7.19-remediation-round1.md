# T7.19 remediation round 1

Reviewed hash: `47c65764bbbd114ad34d55e9a319d5ad0dcfac68`
Worktree: `/home/nryn/work/reprise-wt/t7.19-rem-r1` (branch `task/t7.19-rem-r1`, new commits on top, no rebase)

Behavior was already correct. All three in-scope findings were missing
or overstated pins. This round commits lasting pins and one comment fix.
No settings key added. TOML untouched. No contract change.

| Finding | Severity | Fix | Pin (committed) | Mutation check |
|---|---|---|---|---|
| F1 | H | Named burst constants (`takeSessionsBurst` through `outerAPIBurst`) replace the inline literals in `wireAPI`, so the pins run the production numbers instead of copies. Committed `TestTakeGateHonestTakePasses` (one honest two-stem take at real cadence, zero 429s), `TestTakeGateDoubleCadenceTrips` (double cadence trips uploads exactly 12 past the 48 burst with `Retry-After` plus `rate_limited` and header-body agreement), `TestTakeGateRouteBurstsTripAtWiredEdge` (48, 8, and 6 edges pass then trip with honest hints), and `TestTakeGateUploadHammerLeavesMint` (60-hit upload hammer leaves mint at 200). | `cmd/reprise/main_test.go` | `takeUploadsBurst` 48 to 6 fails honest, double, and edge tests. `takeStemsBurst` 8 to 800 fails the edge test. Both verified then reverted. |
| F2 | H | Committed `TestRetriedCompletionGarbageIDsRecoverToDraft` (unknown ids with a linked pair answer 200, state `draft`, scheduled with a job id) and `TestCompletionUnknownIDsNameMissing` (unknown ids with nothing linked answer 404 naming `stems_not_found`, `missing_media_ids`, and both posted ids). | `cmd/reprise/main_test.go` | Returning posted ids as missing without consulting the linked pair turns the recovery retry from 200 to 404. Verified then reverted. |
| F3 | L | Reworded the `protectTake` comment to state the true isolation: a burst on one route leaves the others alone until the shared outer budget runs out, and a hammer that spends the whole take still crowds out every route including a session mint. | Comment only, reviewed by reading | Lowering the outer burst would contradict the new wording. No behavior change. |

F4 is `OUT_OF_SCOPE` (phase status line outside `owns`). Left for the orchestrator at landing. Not touched.

Validation: new pins green (`go test ./cmd/reprise/ -run 'TestTakeGate|TestRetriedCompletionGarbageIDs|TestCompletionUnknownIDs'`), package green with `-race`, full gate (`npm ci` in `web/`, then `./tools/check.sh`) green, `audit_docs.py` reports the planning tree is consistent.
