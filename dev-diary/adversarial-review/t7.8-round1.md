# T7.8 round 1 review

Reviewed hash: 0853b5edb0a90309e9f0c4a17d8b9f542cfea096.
Worktree: /home/nryn/work/reprise-wt/t7.8-rev-r1, detached HEAD.
Base: deb896d.
Confirm: `git log -1` prints the handed hash with the drain message.
Scope: `git diff deb896d..0853b5edb0a90309e9f0c4a17d8b9f542cfea096`, nothing else.

Verdict: APPROVE, with one OUT_OF_SCOPE finding that drops before landing.
Counts in scope: C 0, H 0, M 0, L 0. OUT_OF_SCOPE: 1 (H, phase status revert).

## Done clause checks, all run in this worktree

Leaked hold: a transient probe opened one real lease at a 2 second cap, slept past it, then called the shipped `waitSessions` under a 30 second budget. The drain freed it in about 0.2 seconds. The log names the waiter (`waiting on 1 open session leases`) and the reclaim (`reclaimed 1 expired holds`). The probe file was removed after the run, and the tree is clean.

Honest hold: the same probe opened one real lease at a 60 second cap and called `Reclaim` before the cap. It freed 0 and kept `Active` at 1. A close 300 ms later let the drain finish cleanly. Live sessions keep their full length.

Rollback path: `./deploy/redeploy_test.sh` passes all three cases in this worktree. The failed gate restores the recorded image, stops the failed container fast, verifies the restore, and exits 1 in about 3 seconds. The passing gate exits 0 with no restore and prunes. The missing image case exits 1 and stops nothing.

Gate: `npm ci` in `web/`, then `./tools/check.sh` from the worktree root, prints `OK all checks passed`.
Docs: `python3 dev-diary/tools/audit_docs.py` prints `OK planning tree is consistent`.

## Ten questions

1. Does any path spend money without reserving budget first. No. The drain calls only `Reclaim`, and the library notes that expiry books nothing. The redeploy script runs only docker and curl commands.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap. No. Past cap holds expire under the token cap the design already sets. The drain frees only quota slots. It never extends a provider session.
3. Can a private episode, stem or transcript reach anyone but its owner. No. This commit touches drain polling and deploy scripts only. It adds no read path and no header change.
4. Does any edit apply without a revertible record. No. Lease expiry writes the expired row to the store, so the fact survives a restart. Rollback records the running image first and restores exactly that image.
5. Does any spoken callback or count come from the prompt instead of a stored row. No. This commit adds no prompt text and no spoken output.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file. No. The gate `plan references` check passes. The only new pin is the compile assertion against the lease type, and it names no plan artifact.
7. Did this task rebuild something Keel or Chaaya provides. No. The fix calls the Keel lease `Reclaim` seam directly. It adds no local quota or timer copy.
8. Was this review run on the handed hash, in a worktree of its own. Yes. `git log -1` matches the handed hash. The worktree is detached at that hash. No code was committed here.
9. Can any paid job kind resume after a restart and spend twice. No. `Reclaim` touches session leases only and settles no price. The paid kind map and the restart path are untouched, so paid kinds still mark `interrupted`.
10. Does any development check need a person, a microphone, or a wall-clock threshold. No. `redeploy_test.sh` stubs docker and curl and runs with short timeouts. Its elapsed bound reads one clock in one run with wide margin. The gate passes unattended.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H OUT_OF_SCOPE | `dev-diary/PHASE-7-production-issues.md:230` at the reviewed hash | The commit rewrites the T7.8 status line from the live implement marker to `not-started`. That path sits outside `owns` (`cmd/reprise/main.go`, `deploy/`). Landing the hunk would orphan the live worktrees and lose loop state | Run `git diff --name-only deb896d..0853b5edb0a90309e9f0c4a17d8b9f542cfea096` in this worktree, then filter with `grep -Ev` for the two owned prefixes. The phase file still prints, so the filter fails while the hunk remains. Dropping the hunk makes the filter print nothing | In the phase file, replace the live status line with `status:     not-started`. That one line edit restores the defect and the pin fails again |

Notes for landing: land only the `cmd/reprise/main.go` and `deploy/` hunks. Drop the phase file hunk first. The handoff already flags it as landing excluded.
