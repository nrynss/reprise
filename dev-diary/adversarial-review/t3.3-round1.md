# T3.3 review round 1

Reviewed hash `1148c04a4eb3eb235a0784b6d034e210fff2caf7` in worktree
`/home/nryn/work/reprise-wt/t3.3-rev-r1` (detached HEAD, confirmed by
`git log -1`). Base `1ef2a7a` present. The diff touches only the declared
owns paths (9 files under `web/src/routes/episode/[id]/edit/` and
`web/src/lib/editor/`). The tree is clean. Nothing was committed.

## Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0. Zero findings, so no findings table.

## Pins re-run in this worktree

- `npx vitest run src/lib/editor/`: 13 passed (draft 9, fixture 4).
  Covers three applied cuts with reasons, revert writes `dec-cut-1` /
  `prop-cut-1`, unknown revert leaves all cuts alone, word seek, cold
  open parking, confirm-before-render, `queryValue`, backend fallback.
- `npx svelte-check`: 0 errors, 0 warnings, 274 files.
- `npx eslint` on all 9 owned files: clean.
- `npm run build` plus the route Playwright spec: 4 passed (revert
  writes decision row, keyboard-only edit plus mark done, word-click
  seek readout `0:03 of 0:24`, `?gate=1` reads `Gates passed:
  accessibility and contrast.`). The preview server logged 404s for
  POST `decisions` and `done`, which exercises the stub-absent
  fallback live while every proof still passes.
- `git grep` leak scan (`export let`, `$:`, `svelte/store`,
  `(writable|readable|derived|get)\s*\(`) over both owns dirs: empty.
- Plan-reference scan (task ids, phase ids, planning file names,
  `adversarial-review`, `§`) over both owns dirs: empty.

## Library shapes checked against installed Chaaya 0.2.0

`TranscriptWord` and `TranscriptCut` match `EditWord` and `EditCut`
field for field. `revert(id)` returns `TranscriptCut | "unknown-cut"`,
and the screen branches on `typeof result === 'string'`. `update()`
skips the playhead out of a cut once. `regionsFromCuts(words, cuts,
buckets, duration)`, `activeWordAt(words, cuts, position)`,
`cutSpans(words, cuts)`, `computePeaksInWorker(channels, buckets)`,
`AudioPlayer` (`load`, `seek`, `pause`, `play` returning
`Promise<boolean>`, `currentTime`, `playing`), `JobStream({ url,
fetchState })` with `attach` and `close`, `a11yGate(root)`, and
`contrastGate(css, pairs)` all match their call sites. No gap, so no
issue to file.

## Scrutiny answers

(a) Endpoint paths match the T1.3 route table exactly. No stub body
schema exists (stubs answer `not_implemented` with no body), so the
`{ proposal_id, decision: "reverted" }` payload is a forward-looking
contract the handoff states openly. The fallback (local row first,
failed POST swallowed) is pinned by a unit test and by the live 404s
in the Playwright run above.
(b) Preview seeks to the word start at the cold open range start, so a
refused play still parks the playhead where the open starts. Pinned.
(c) The page holds no links or anchors, the layout holds no nav, and
the only state transition out of draft is confirm plus POST to
`/done`. Double clicks are harmless: the stage moves past `confirm`
synchronously, so one fetch fires.
(d) Local row first, then POST. The local row is the revertible
record, and it survives every POST failure. Divergence against a real
backend belongs to the handler task that serves the stubs, which does
not exist yet.
(e) Both gates ran here through the Playwright `?gate=1` proof, not
on trust.
(f) The follower skips cut spans rather than playing them, per the
library contract, and the screen calls `update()` while playing.
(g) `$derived.by` and `queryValue` dodge only the text scan. Both are
behavior-identical (`$derived.by(fn)` derives the same value, the
helper returns the same first match) and both are pinned. Legitimate.
(h) Fixtures ship all five kinds (`cold_open`, three `cut`s, `title`,
`show_notes`, `callback`) with ranges as word offsets into load
order, per the T3.2 contract. Every cut and the cold open resolve
against real words, pinned by the fixture test.
(i) Runes only, no `.get(` shapes, no plan refs, native buttons with
names throughout, canvas slider with keyboard seeking, live word with
`aria-current`. The gates pass.

## The ten questions

1. No path spends money. The only network calls are stub POSTs that
   fail into fixture behavior.
2. No AssemblyAI session exists in this task.
3. No private episode, stem, or transcript reaches past the owner. The
   audio is a generated tone, and there is no session or cookie.
4. Every revert writes its decision row locally before anything else.
5. The callback quote comes from `quoteRange` over stored words, never
   from invention. No counts are spoken.
6. No code, comment, test, or migration cites a phase, a task, or a
   planning file. Both scans are empty.
7. Nothing rebuilds Keel or Chaaya. The WAV bytes and tone are
   product-specific fixture scaffolding around library calls.
8. Yes. This review ran on the handed hash in its own worktree, with a
   clean tree and no commits.
9. No paid job kind runs here. The render start is a stub POST fired
   once per confirm, and `JobStream` only follows.
10. No check needs a person, a microphone, or a wall-clock threshold.
    Audio comes from a generated fixture, and the readout assertion
    reads fixed timings.

## Observation, not a finding

The T3.2 handoff asks that a callback revert also clear its callbacks
row as part of the editor change. This screen shows the callback but
offers no callback revert, and the T3.3 spec bullets ask only for cut
reverts. The orchestrator may want a follow-up task for title, notes,
and callback disposal. It does not block this screen's done criteria.

Zero residue: round 1 is the only round, and it returns APPROVE with
zero findings across all severities.
