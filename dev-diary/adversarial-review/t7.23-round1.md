# T7.23 round 1 review

Reviewed hash: `6237394df7c2b71a2689ffd133a43f126c79705b`
Worktree: `/Users/narayan/reprise-wt/t7.23-rev-r1` (detached HEAD).
`git log -1` matches that hash. The subject is `Refill upload limits fast enough for one two-stem take.`
`git status --short` was empty before and after the pins.
Diff reviewed: `git diff f55733629126d3d5f8772f4457f6352759711449..6237394df7c2b71a2689ffd133a43f126c79705b` and nothing else.
No code commits.

## The ten questions

1. Does any path spend money without reserving budget first? No. The diff only changes gate refill intervals. Mint still goes through the broker, which reserves before the provider call. The mint burst stays 6 and the mint refill stays one minute.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This diff does not touch session end, the token minter, or the duration cap.
3. Can a private episode, stem or transcript reach anyone but its owner? No. No media path, cookie, or owner check changes.
4. Does any edit apply without a revertible record? No. The commit applies no editorial edit.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The new comments name bursts and refills. They do not cite a task id, a phase id, or a planning file.
7. Did this task rebuild something Keel or Chaaya provides? No. The limits are Keel gate rules. Uploads still use the existing chunk handler. Reprise did not copy either limiter.
8. Was this review run on the handed hash, in a worktree of its own? Yes. The detached worktree above is the only tree this review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The new pins step a fake clock. They do not sleep, and they do not compare against wall time.

## What the commit does

The captured production 429 is the app gate. The handoff records HTTP 429, content-type application/json, Retry-After 16, and body code `rate_limited` with `retry_after_seconds` 16. That is Keel's route refusal, not a Cloudflare HTML page. The 49th upload-route request matches burst 48.

`uploadRefill` is 200ms, one token a second five times. `takeRefill` returns that interval only for the name `take-uploads`. Every other take route keeps `time.Minute`. `outerAPIRule` uses the same 200ms interval for the per-client burst of 256 and the global burst of 4096.

`wireAPI` builds uploads with `protectTake` on `take-uploads` and burst 48. It passes `outerAPIRule()` into `api.Mount`. `Mount` wraps that handler in the outer rule, then guest middleware, then the inner handler (`internal/api/routes.go` around the `Protect` call). One chunk draws the outer budget and then the upload budget.

Session mint, session end, episodes, threads, admin, and media use that same Mount wrap. Their inner interval stays one minute. Stem completion sits beside Mount. Its handler is the outer rule around `protectTake` for `take-stems`. Its refill stays one minute too.

## Pins

`go test -race -count=1 -timeout 180s ./cmd/reprise/` passed. The package reported ok in 3.381s.

`TestTakeGateSustainedUploadClearsOuterBurst` steps the gate clock by 341333333ns. That spacing is two 48 kHz 16-bit stems cut into 65536 byte chunks. The rate is about 2.93 requests a second. The test sends 4224 uploads through the outer rule and then the upload rule. 4224 is past burst 48, past burst 256, and past the shared burst of 4096. The run lasts about 24 minutes, so a one minute refill would add about 24 tokens to the burst of 4096. 4224 is past that sum. Zero of those answers are 429.

The clock then moves one refill interval and stops. Sixty uploads follow at one instant. Forty-eight pass. Twelve return 429. Each refusal is `rate_limited`. Retry-After is 1, and the body wait is 1. Keel rounds a sub-second bucket wait up to one second. The header and the body still agree.

`TestTakeGateMintRefillStaysOneMinute` sends six mints through both budgets. The next refusal carries Retry-After 60. One fake second later the wait is 59. A 200ms mint refill would have allowed that seventh mint. It did not.

`takeRefill` returns one minute for every name except `take-uploads`. `wireAPI` uses `take-session-end`, `take-episodes`, `take-threads`, `take-admin`, `take-media`, and `take-stems`. None of those names is `take-uploads`. Each of those handlers is built with `protectTake` before the outer `Protect` runs. The faster outer rule cannot raise those inner rates. Mint stays at six, then one token a minute. A seventh mint inside that minute would reserve spend sooner. That seventh mint is refused.

## Checked, not filed

The outer global bucket also restores one token every 200ms. After its burst, the whole API sustains five requests a second. One take near 2.9 chunks a second fits under that. Two takes at that rate would not. The slot cap is still 8. The done line asks for one honest take. The old global refill was one token a minute, so several long takes were already refused. This is not a new hole.

`GET /api/jobs/{id}/events` has no inner rule. It now shares the five-a-second outer budget. One event stream is one request. It does not mint, and it does not spend.

The older burst tests still use the process clock. They issue a few dozen in-process calls. This race run finished in 3.381s for the whole package. Those calls sit well under one refill. The sustained pin does not use that clock.

`./tools/check.sh` was not run. This commit does not touch the ffmpeg pin. A 9.0.2 versus 9.0.1 mismatch would be the environment.

## Findings

None.

## Counts

C 0, H 0, M 0, L 0. In-scope findings: zero.

## Verdict

APPROVE
