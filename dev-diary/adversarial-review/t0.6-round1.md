# T0.6 round 1 review

Reviewed hash: 53656ca6697e9795db604cb6d669329f3e7e6c36.
Worktree: t0.6-rev-r1 (detached at the hash, confirmed with `git log -1`).
Round 1 is the first round, so there is no prior-round residue to check.
This review judges the COMMIT, not the branch diff against moved main.

## Commit file list

`git show --name-only --format='' HEAD` returns exactly three files,
all inside the owned path `testdata/speech/`:

- testdata/speech/exchange.wav
- testdata/speech/generate.sh
- testdata/speech/keyterm.wav

No other path moves. The two pcm files are untouched by the commit, as
intended: the handoff claims they are byte-identical to the measured
bytes, and the hash check below confirms it against the committed files.

## Hash check (what was hashed)

Hashed the four committed working-tree files with `sha256sum`:

- `testdata/speech/exchange.wav`:
  `fabab6e47603c5d6b5b239d057eb338a020035ddf353dd7ba9357cc5f724a5b9`
  matches the handoff's new exchange wav hash.
- `testdata/speech/keyterm.wav`:
  `3e3644536eb47ac6c2f34db2e07fca482b0f5e408d996202b8ed4c0cc5bc9bcb`
  matches the handoff's new keyterm wav hash.
- `testdata/speech/exchange-24k.pcm`:
  `9241617ab0c52e5afd4a702516b437183d1bb6d03d3566e5cdfe1726a84f27f5`
  matches the handoff's exchange PCM hash, and matches
  `git show HEAD~1:testdata/speech/exchange-24k.pcm` piped to `sha256sum`
  (identical bytes; pcm unchanged by the commit).
- `testdata/speech/keyterm-24k.pcm`:
  `3fd117e4d2968ff546668e119c320c4aa804acef2f9b24753ba1e6cd46fa6fc1`
  matches the handoff's keyterm PCM hash, and matches
  `git show HEAD~1:testdata/speech/keyterm-24k.pcm` (identical bytes).

PCM sizes are unchanged: 171612 and 190806 bytes, agreeing with the T0.2
record's streamed shapes.

## ffprobe

`ffprobe -hide_banner` on both committed wavs, in this worktree:

- `exchange.wav`: `Duration: 00:00:03.58`, `pcm_s16le, 22050 Hz,
  1 channels`. No `Packet corrupt`, no bitrate-estimation warning.
- `keyterm.wav`: `Duration: 00:00:03.98`, `pcm_s16le, 22050 Hz,
  1 channels`. No warning.

Durations 3.58s and 3.98s match the T0.2 record. Exit 0 both files.

## Wav header and payload identity (recomputed offline, no Docker)

Parsed headers with Python `struct`, walking chunks to the `data` chunk:

- `exchange.wav`: real 157712, RIFF declares 157704 (= 157712 - 8),
  data declares 157668 (= 157712 - 44), data offset 44. Both OK.
- `keyterm.wav`: real 175346, RIFF declares 175338 (= 175346 - 8),
  data declares 175302 (= 175346 - 44), data offset 44. Both OK.
- Old (parent) headers declared RIFF size 2147479588 against real
  157712 / 175346, confirming the placeholder-size defect the task
  describes.
- Audio payload (bytes after the `data` header) is byte-identical
  between parent and commit for both files (`payload_identical= True`);
  the old and new 44-byte heads differ only in the patched size fields.
  Full-file diff is header-only. Payload sha256 (excluding header):
  exchange `8c435d9df58d45137f246bf74154d683db1f70f15455b1c876474f27f2eef653`,
  keyterm `0b8e7bf4aa56a9852110da986e37a403fc41d0ae3f56697cacb0e0253c858e3d`.

No record update is needed. Sizes and durations stand.

## generate.sh read whole (34 lines)

- `-w` synthesis, not `--stdout` pipe: lines 29-30 use
  `espeak-ng -v en-us -s 160 -w /out/<name>.wav "<fixed text>"`.
  No `--stdout`, no redirect into a wav. The old
  `--stdout ... > /out/*.raw` plus `mv` sequence is gone.
  `grep stdout` on the new script returns nothing.
- Wav-to-pcm conversion present with explicit rate and format:
  lines 32-33 run the media image with
  `-ar 24000 -ac 1 -f s16le -acodec pcm_s16le` for each clip.
  One run writes all four files; the final `ls` names all four.
- Image digest pinned: line 21
  `TTS_IMG=...ubuntu:24.04@sha256:b3cc40b72b93588182b5410f723c7aaf142363311c2aa993d8a453ddcbb3ae15`.
- espeak-ng version pinned: line 23 `ESPEAK_VERSION="1.51+dfsg-12build1"`,
  installed with `apt-get install -qy "espeak-ng=${ESPEAK_VERSION}"`
  (line 28). Exact-version install.
- ffmpeg digest pinned and matching the repo media pin: line 22
  `MEDIA_IMG=...mwader/static-ffmpeg:9.0.1@sha256:54e55b0cb8f672870fc38ceb2e6c411855cb3b39c505f5f3b2505ee01ed5f2b7`.
  `grep` confirms the identical digest string in `Dockerfile:32`
  (`FROM ... AS mediatools`), `.github/workflows/ci.yml:27`
  (`docker create ...`), and `tools/check.sh`'s `FFMPEG_PIN=9.0.1`
  version gate (local ffmpeg/ffprobe here report `n9.0.1`).
- Each pin is named: `TTS_IMG`, `MEDIA_IMG`, `ESPEAK_VERSION` variables,
  overridable by `TTS_IMAGE`/`MEDIA_IMAGE` env with the pinned default,
  and the comment block (lines 7-10) names what each pin fixes.
- Style binding holds: no phase/task/planning-file citation in the
  script (`grep -Ei` for planning markers clean), no secret values,
  no real voice (fixed espeak-ng sentences, unchanged from the old
  script). The `-s 160` speed and `en-us` voice are carried over.

## Determinism

The handoff claims two consecutive `generate.sh` runs share hashes for
all four files. Docker synthesis was NOT re-run here (cost, per brief).
Static plausibility holds: every input is pinned (Ubuntu digest,
espeak-ng exact version, media digest, fixed voice/speed/text,
explicit resample flags), and the script contains no timestamp, no
`date`, no random source, and no unpinned `apt-get install`. The claim
is plausible on its face; reproduction is left to a future
Docker-capable run, not asserted from evidence here.

## Gate

- No Go files added or touched (`grep -c '\.go$'` on the name-only
  list returns 0), so gofmt/vet see nothing new.
- `bash -n testdata/speech/generate.sh`: passes (`BASH_N_OK`).
- Full gate: `node_modules` was already installed in this worktree, and
  `./tools/check.sh` was run here: exit 0, `OK all checks passed`
  (eslint, vitest, build, playwright incl. preview server, svelte-4
  leakage, consumer names, plan references). This is the implementer's
  green gate re-run in the review tree, not a borrowed claim.
- The known tracked-mockup consumer-names hit from other worktrees did
  not fire here; the consumer-names check passed clean.

## Review questions

1. Does any path spend money without reserving budget first? No. The
   commit touches fixture files and a generator script only. No network
   probe, no provider call, no new spend path.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Nothing in this commit opens a session.
3. Can a private episode, stem or transcript reach anyone but its
   owner? No. The clips are fixed synthetic sentences; no episode data,
   no secret, no voice of a person.
4. Does any edit apply without a revertible record? No product edits
   apply. The wav bytes change, and the change is a committed diff
   whose payload identity is measured above; the pcm bytes the probe
   streamed are untouched.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. The sentences are fixture text, unchanged from the
   old script, not a product callback.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Word-boundary scan of `generate.sh` is clean;
   comments name pins and flags only.
7. Did this task rebuild something Keel or Chaaya provides? No. It
   consumes the pinned media image (Keel's digest) as a resampler
   instead of rebuilding one, which is the correct direction.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Detached at
   53656ca6697e9795db604cb6d669329f3e7e6c36, confirmed with
   `git log -1`; ffprobe, hashing, header parse, `bash -n`, and the
   full `check.sh` all ran in that tree; nothing committed.
9. Can any paid job kind resume after a restart and spend twice? No job
   kind exists in this diff.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. ffprobe reads committed bytes; the gate
    needs no microphone. Docker regeneration is pinned but was
    deliberately not re-run here.

## Findings

No in-scope findings. Zero across all severities.

Out-of-scope observations, recorded without severity and not blocking:

- OUT_OF_SCOPE: the handoff's sibling note (T0.5 session wavs carry the
  same placeholder sizes and should apply this pinning pattern to their
  own `recreate.sh`) concerns paths outside `testdata/speech/`. Noted,
  not judged; the T0.5 owner decides.
- OUT_OF_SCOPE: `generate.sh` allows `TTS_IMAGE`/`MEDIA_IMAGE` env
  overrides of the pinned defaults. Same pattern as the old script's
  `TTS_IMAGE` override; the default is pinned and the override is a
  test hook, not a defect. Recorded for awareness only.

## Verdict

APPROVE. Zero in-scope findings. The commit owns only
`testdata/speech/`, both wav headers now declare true sizes with
payloads byte-identical to the measured bytes, both pcm files are
untouched and hash-match the handoff, ffprobe reads both wavs clean at
the recorded durations, all three pins are named inline with the media
digest matching the repo pin, `bash -n` passes, and the full gate is
green in the review tree.
