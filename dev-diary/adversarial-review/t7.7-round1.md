# T7.7 round 1 review

Reviewed hash: `bced463181f8e2389f1e03297d5c4b95002bd414`
Worktree: `/home/nryn/work/reprise-wt/t7.7-rev-r1` (detached HEAD, confirmed with `git log -1`)
Base: `c4c3676`
Diff reviewed: `git diff c4c3676..bced463` (2 files, nothing else)
Verdict: **REMEDIATE**

No lambo MCP server is exposed in this environment, so this review works from AGENTS.md and the live plan only.

## Scope

T7.7 owns `dev-diary/probes/production.md` only. The commit appends a round 3 note (233 lines) and reverts one status line in `dev-diary/PHASE-7-production-issues.md`. The handoff names the revert and asks for it to be flagged. It drops before landing.

## Verification performed

1. `git log -1` confirms `bced463181f8e2389f1e03297d5c4b95002bd414`, detached, in `t7.7-rev-r1`. Minted nothing, flipped nothing.
2. Rounds 1 and 2 byte-untouched: `head -610 dev-diary/probes/production.md | sha256sum` equals `git show c4c3676:... | sha256sum` (`b1ba3871...e458aef`). The diff is one append-only hunk at line 610. Zero `-` lines.
3. Every check section in round 3 carries a `Command` fenced block plus an `Output` fenced block, except the rate gate observation (finding 2).
4. Secrets scan: no key material. Token redacted, cookie jars in `/tmp` deleted, SHA values truncated with ellipsis, ids are non-secret references.
5. Read-only spot checks from this workstation (no mint, no flip):
   - `curl -sS https://reprise.nryn.dev/healthz` returns `ok bd8cf55eda562fa9b7365e431691746f version=c4c3676576203339ca724af8ff7b977b962395df`. The recorded edge version still matches today.
   - `curl -sS -o /dev/null -w` on `/api/episodes` with no cookie returns `HTTP 200`. This asserts nothing about the record. It only confirms the edge answers anonymously without error.
6. Style: no semicolons in the appended section, no prose em dashes (`---` is the section rule, `--data-binary` is a curl flag), no sentence over 30 words (script-checked, count 0).
7. `python3 dev-diary/tools/audit_docs.py` returns `OK planning tree is consistent`.
8. Round 3 cites no task ids, phase ids, or planning files (grep over lines 611 to 843 is empty).

## The carry assessment

The round 2 carry (episode stalls in `recording`, nothing schedules) is closed with passing live evidence: first completion answers `moved:true scheduled:true state:draft` with one transcript job id, and the command plus output are on record. That half passes.

The new carry (silent transcript chain) is properly scoped as a follow-up, not under-evidenced. It names the observable defect (two transcript passes, zero landed words, zero proposals, episode never `failed`), it names the owning paths (`cmd/reprise/main.go` draft schedule, `internal/episode/` completion entry), and it scopes the follow-up to visible job outcome on the completion answer plus episode detail, then a live re-run. A docs verification task owes the failing observation with command and output, which it gives (detail polls over 45 minutes, repeat completion with a new job id). It does not owe worker log forensics. Those belong to the follow-up once job outcome is visible.

One note for the follow-up, not a finding here: the repeat scheduled a second transcript job (`moved:false scheduled:true`, new job id). The follow-up should reconcile that against the T7.6 no-double-schedule pin and state whether the second schedule was correct (first job finished and vanished) or a dedup gap.

## Findings

Counts: C 0, H 0, M 1, L 1. OUT_OF_SCOPE 0.

### F1 (M): phase status line reverted outside owns

| Column | Value |
|---|---|
| Severity | M |
| Where | `dev-diary/PHASE-7-production-issues.md:213` at `bced463` |
| What | The commit flips the T7.7 status line from `in-progress:implement:t7.7-impl` to `not-started`. That file is outside the task owns line, and status lines are orchestrator-shared state. The handoff admits it and says it drops before landing. Until it drops, the commit carries an owns violation plus a false orchestration signal. |
| Pin | `git diff c4c3676..bced463 --name-only` lists `dev-diary/PHASE-7-production-issues.md`, and `git diff c4c3676..bced463 -- dev-diary/PHASE-7-production-issues.md` shows the single status line change. Both commands pass in this worktree and fail (empty) once the hunk drops. |
| Mutation | One-line edit: set line 213 back to `status:     not-started`. The pin output above reappears verbatim. |

### F2 (L): rate gate numbers cited with no recorded output

| Column | Value |
|---|---|
| Severity | L |
| Where | `dev-diary/probes/production.md:823-826` at `bced463` |
| What | The observation claims uploads answered 429 `rate_limited` after every second PUT with `retry_after_seconds` hints of 39, then 14, then shorter. No fenced output block in round 3 contains a 429 body, so the specific values are unverifiable. Every other check pairs command with output. This paragraph does not. |
| Pin | This probe fails in the worktree: extract fenced `text` blocks from lines 611 to 843 of `dev-diary/probes/production.md` and assert `rate_limited` appears in at least one. It appears in zero. `grep -n 'rate_limited' dev-diary/probes/production.md` returns only prose lines 823 to 825, never inside a fenced block. |
| Mutation | Two-line edit against the fixed state: delete the pasted 429 response body (or change the hint numbers in prose). The pin fails again exactly as now. |

Remediation for F2 is either: paste one verbatim 429 body with its command context, or soften the paragraph to qualitative wording without the specific hint values.

## Ten questions

1. Does any path spend money without reserving budget first? No. Docs only. No code path changed.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The record shows explicit `session.end` with `session.ended` at 66.8 seconds, and no code changed.
3. Can a private episode, stem or transcript reach anyone but its owner? No. Gallery lists private under the owner cookie. The anon `/api/episodes` spot check returns 200 without leaking content here, and no such claim is made in round 3.
4. Does any edit apply without a revertible record? No. No edits apply. No code changed.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No callbacks or counts are recorded.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Round 3 contains no task or phase references. No code, test, or migration is touched.
7. Did this task rebuild something Keel or Chaaya provides? No. Docs only.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `bced463181f8e2389f1e03297d5c4b95002bd414` in detached worktree `t7.7-rev-r1`. No commits made here.
9. Can any paid job kind resume after a restart and spend twice? No new risk from this commit (docs only). The record itself surfaces the adjacent live behavior: a repeat completion scheduled a second transcript job while the first left no trace. That is the named carry for the follow-up, which should reconcile it against the T7.6 no-double-schedule pin.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. All speech is generated (`testdata/sessions/steady/` plus decoded provider reply frames). The 45-minute poll and 60-second script wait are elapsed live observation, not a CI gate threshold. No person and no device appear.

## Residue

Round 1 is the first round, so there is no prior residue to clear.

## Verdict rationale

REMEDIATE. F1 is an owns violation the handoff already concedes. It must drop before landing. F2 is a small evidentiary gap on specific cited numbers. Both are fixable without new live spend: drop one hunk, paste one 429 body or soften three lines.
