# T7.15 remediation round 1

Reviewed hash: `aa0f71a73cc339c5ae26e19394ad95cdc1a21387`
Worktree: `/home/nryn/work/reprise-wt/t7.15-rem-r1` (branch `task/t7.15-rem-r1`, new commit on top, no rebase, no amend)

| Severity | Where | Fix | Pin rerun |
|---|---|---|---|
| L | `dev-diary/probes/production.md`, Round 5 section 4 (`Provider cross-check and settle`) | Recorded the post-end fresh mint with its command plus output block. Mint ran with `POST /api/sessions` only and never opened the socket, so it bills nothing. Token redacted. No other changes. | `python3` probe splitting at `## 4. Provider cross-check and settle` and counting `POST https://reprise.nryn.dev/api/sessions` blocks before `## Verdict`. Count is now 1. Pass. |

Validation: `python3 dev-diary/tools/audit_docs.py` reports OK. No secrets in the added text. Cookie jars and mint bodies lived in `/tmp` and are deleted.
