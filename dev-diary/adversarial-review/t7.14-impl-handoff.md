# T7.14 implementer handoff

Commit `175db437d7ca3b0550c6ae483b8e50d1314d03e1` on branch `task/t7.14`
in worktree `t7.14-impl`. Base is main at `e3d6d0d`. Test-only fix,
no production code. Gate passes in the worktree.

## Losing interleavings

Retention: the reopened runner resumes sweep attempt 2 while attempt
1 on the old runner still lives. Attempt 1 deletes the episode row
between attempt 2's presence check and its erase, so attempt 2
errors. Privacy: the done waiter fired on the warm superseded
attempt while the ledger read the latest one, so the resumed
attempt's confirm landed too late under load.

## Fix

Both resume tests cancel the superseded attempt on the old runner
after reopening, wait for its terminal in the ledger on the existing
poll beat, and only then release the provider double. Wait helpers
judge the latest attempt only. No sleeps, no thresholds, no library
defect, no quarantine.

## Validation

Both tests pass 20 consecutive race runs plus 40 mixed runs, plus
full race count-3 runs of both packages. Gate green.

## For the reviewer

The commit reverts the T7.14 status line outside its owns line. Flag
it. It drops before landing.
