# T7.17 round 2 review

Reviewed hash: `e14c484d8c1d39489edae1d5eacf701e2421c62c`
Worktree: `/home/nryn/work/reprise-wt/t7.17-rev-r2` (detached HEAD, confirmed with
`git log -1`). Remediation diff under review:
`git diff 29c3d29e983570570a0332e174d13067b02484fa..e14c484d8c1d39489edae1d5eacf701e2421c62c`.
Three files only: `web/src/lib/voice/pcm.ts`, `web/src/lib/voice/pcm.test.ts`,
`web/src/lib/voice/socket.test.ts`. No code committed by this reviewer. Two
scratch probes were added, run, and deleted. The tree is clean at write time.

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with one in-scope H finding (F1) and one
out-of-scope L finding (F2). Residue is zero at every severity.

| Round 1 finding | Severity | Residue | Evidence |
|---|---|---|---|
| F1: host stem not byte exact at high levels (`pcm.ts` asymmetric 32767 scale) | H | Zero. Fixed. | Full range sweep rerun by this reviewer reads `mismatches=0`, `edge32767=32767`, `edgeNeg=-32768`. Loud 0.99 sine reads `loud mismatched bytes=0 of 960`. Rails read `1` to `32767` and `-1` to `-32768`. Decode divide by 32768 untouched. |
| F2: phase file status line edit outside task `owns` | L, OUT_OF_SCOPE | Zero. Untouched, non-blocking. | Remediation diff touches no planning file. `git status` is clean apart from the review file. |

Counts: C 0, H 0, M 0, L 0. No new findings. No OUT_OF_SCOPE findings.

## What the remediation changed

`floatToPcm16` now scales by 32768 instead of 32767 and clamps the rounded
value to the int16 rails. The decode divide by 32768 is unchanged, so every
int16 value divided by 32768 stays exact in float32 and rounding the product
returns the same value. The dual-key `data` first fallback with `audio`
fallback in `socket.ts:148` is intact and untouched by this diff. The comment
on the new scale states its reason itself and cites no plan.

## Evidence the pins are load-bearing

Committed vitest result at the reviewed hash, run here after `npm ci` in
`web/`: `pcm.test.ts` 11 passed, `socket.test.ts` 15 passed, 26 passed total.

An independent sweep probe written, run, and deleted by this reviewer
replayed the full int16 range through the wire to the drain and asserted
zero mismatches with both edges pinned, plus rail mapping and loud sine
exactness. Output: `mismatches=0 edge32767=32767 edgeNeg=-32768` and
`loud mismatched bytes=0 of 960`. It passed and was removed.

A mutation probe replaced the fixed scale with the old `32767` scale and
dropped the rail clamp, then ran the two committed files. Exactly the four
new and updated pins failed: both rail tests, the loud sine test, and the
full range sweep. The fixed file was restored byte for byte afterward.
`git status` confirms a clean tree, and `grep` confirms `32768` is back.

## The ten questions

1. Does any path spend money without reserving budget first? No. The diff
   changes PCM scaling and tests only. No paid call is added or moved.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. Session open and close paths are untouched.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   The stem bytes flow to the same owner upload. No auth change.
4. Does any edit apply without a revertible record? No. No edit path changes.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. No transcript or count logic changes.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The gate plan-reference scan passes.
7. Did this task rebuild something Keel or Chaaya provides? No. It scales
   integers with existing helpers.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Detached worktree above at `e14c484`, diff read against `29c3d29`, pins
   run here after `npm ci` in `web/`.
9. Can any paid job kind resume after a restart and spend twice? No. No job
   code changes.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The pins drive synthetic samples with exact
    assertions.

Runbook: `dev-diary/tools/audit_docs.py` reports `OK planning tree is
consistent`.

Svelte runes: no `.svelte` file changes, so no runes check applies beyond
the passing gate scan.

Gate: `./tools/check.sh` passes in this worktree after `npm ci` in `web/`:
`OK all checks passed`, exit 0.

## Verdict

APPROVE. Zero residue against round 1 at every severity. F1 is fixed and
measured. F2 stays out of scope and untouched.
