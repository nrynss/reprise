# T7.28 remediation, round 3

- **Reviewed hash:** `931480ddabbbfb579ab0112bd185e874e2317ba7`
- **Worktree:** `/Users/narayan/reprise-wt/t7.28-rem-r3`, branch `task/t7.28-rem-r3`
- **Final hash:** `53877f4417fb65b3632b6bb6b186ecb6ec341a1f`
- **Commits on top of `931480d`**, with no amend and no rebase:
  - `53877f4` fixes finding 1.

Every edit sits in `owns` or in a test file beside an owned file. The OUT_OF_SCOPE finding O1 in
`cmd/reprise/main.go` belongs to another task, and I left `cmd/` untouched.

## Findings

| # | Severity | Finding | Fix | Commit | Pin that now passes | Mutation fails it |
|---|---|---|---|---|---|---|
| 1 | M | A failed episode whose editorial pass a restart interrupted showed "Ready. Open the episode." on its gallery card. A failed transcript pass lost its reason the same way. | `galleryPass` in `threads.ts` now handles a `failed` episode on its own path. A stopped transcript pass is followed with its reason. Past a done transcript, the card follows the editorial pass with its reason, or says the pass never started. When no pass stored a reason, the note reads "The episode failed, and no pass stored a reason." So a failed card never falls through to "Ready". Draft, ready and the other states keep the early return they had. | `53877f4` | `web/src/routes/threads/gallery-failed-episode.test.ts`, the reviewer's pin moved and renamed. It holds the reviewer's two cases, plus a failed and an interrupted transcript pass, a failed editorial pass with its reason, a failed episode whose passes all finished, one whose editorial pass never started, and a ready episode that keeps its done transcript pass. 8 of 8 pass. The reviewer's own pin, copied in for one run and removed, passes 2 of 2. | Yes. Restoring `detail.episode.state !== 'draft' \|\|` as the first test of the early return fails 5 of 8 cases in the moved test, including "never shows the failed episode as ready". It also fails both cases of the reviewer's pin. |

## Gate

`cd web && npm ci`: exit 0. `./tools/check.sh`: exit 1 at its tool check,
`FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`. This is the accepted environment issue. I
ran every later step of `check.sh` by hand, in order, on the final hash.

| Step | Exit |
|---|---|
| `gofmt -l cmd` | 0, no output |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| svelte 4 leakage scan | empty |
| consumer names scan | empty |
| plan references scan | empty |
| `go test -race ./...` | 0 |
| `playwright test -c src/routes/gallery.playwright.config.ts`, after a fresh build | 0 (8 passed) |
| `playwright test -c "src/routes/episode/[id]/edit/edit.playwright.config.ts"`, after a fresh build | 0 (5 passed) |
| `playwright test -c src/routes/threads/threads.playwright.config.ts`, after a fresh build | 0 (10 passed) |

`long_sentences.py` from the round 2 probes exits 0 on both changed files.
