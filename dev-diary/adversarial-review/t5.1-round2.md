# T5.1 round 2 review (re-review)

Reviewed hash `68d9eeb261c73de271e39299c2f260670f6ffe97` in worktree
`/home/nryn/work/reprise-wt/t5.1-rev-r2`. Detached HEAD confirmed with
`git log -1 --format=%H` before any reading. No commit written. Working
tree reviewed only through the handed commit, restored clean after one
temporary uncommitted mutation probe (`git status --short` empty at end).
Remediation diff read as `git diff dd0a8bb..68d9eeb`: two files,
`internal/export/export.go` and `internal/export/export_test.go`, nine
insertions and four deletions, all inside the declared `owns` line.

## Verification performed

`go test -race ./internal/export/ -count=1` passes in this worktree, all
11 tests. Targeted `go test -race -run
TestValidateChaptersRejects|TestVideoIs1080PWithAudio -count=3` passes,
so the 50 ms video window held across four video builds with zero
flakes. `go vet` is clean. `go build ./...` passes. Pinned ffmpeg and
ffprobe report `n9.0.1`. The gate plan-ref `git grep` from `tools/check.sh`
over `internal/export/` returns nothing. Full `./tools/check.sh` was not
rerun, because the remediation touched no source behavior and the
remediator already ran the full gate green after `npm ci`.

## Residue against round 1, severity by severity

Round 1 returned one M and two L findings, zero C, zero H.

* M1 (zero-check without isolating pin): zero residue. The `lateStart`
  fixture at 5000, 15000 and 25000 ms over `fixtureDurationMs` 35000
  gives spans of exactly 10000 ms, so only the first-at-zero rule can
  reject it. Temporary deletion of the `chapters[0].StartMs != 0` branch
  fails the suite at `export_test.go:228` with `first past zero with full
  spans: want ErrChapters, got <nil>`. The pin is load bearing.
* L1 (150 ms video window): zero residue. The bound reads 50 ms in code
  and message. A byte probe here builds a 35 s tone (`full: 35.000000 s`)
  and a `-t 34.9` stream copy (`trim: 34.922812 s`), a 77 ms gap that
  fails the 50 ms predicate and passed the old 150 ms one. Four green
  video runs show no flake.
* L2 (ErrInvalid doc cites empty id, nil dependency): zero residue.
  `grep` for both phrases over `internal/export/` returns nothing. Every
  `ErrInvalid` site passes one of the listed inputs: empty path
  (`video.go` twice), blank word text (`captions.go`), empty cover bytes
  and non-square cover (`cover.go`), missing duration
  (`description.go`), unreadable video (no stream, bad duration in
  `video.go`). The comment carries two sentences, no semicolons, active
  voice.
* C residue: zero. H residue: zero. Round 1 recorded none and the
  remediation adds no behavior.

## New findings

None. The remediation commits add one rejection fixture, tighten one
test bound, and rewrite one doc comment. No production logic changed, so
no new defect surface exists.

Counts: C 0, H 0, M 0, L 0. No `OUT_OF_SCOPE` rows. The diff stays
inside `internal/export/`.

## The ten questions

1. Does any path spend money without reserving budget first. No. Local
   ffmpeg and ffprobe only. No provider call exists.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap. No. Nothing here touches AssemblyAI.
3. Can a private episode, stem or transcript reach anyone but its owner.
   No. Caller supplied bytes and timings only. No serving here.
4. Does any edit apply without a revertible record. No. Export reads. It
   edits no episode state.
5. Does any spoken callback or count come from the prompt instead of a
   stored row. No. Titles, notes and timings arrive from the caller.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file. No. The gate pattern grep over `internal/export/` is
   clean.
7. Did this task rebuild something Keel or Chaaya provides. No. Captions
   group through `keel/caption`, video renders through `keel/waveform`
   over `keel/ffmpeg`, unchanged by this remediation.
8. Was this review run on the handed hash, in a worktree of its own.
   Yes. `git log -1` reads the handed hash, detached, in `t5.1-rev-r2`.
9. Can any paid job kind resume after a restart and spend twice. No. No
   job kind lives here.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold. No. Tones and stills generate locally. The 50
    ms bound compares two ffprobe readings from one run, and held four
    times here.

## Verdict

APPROVE. Zero findings across all severities. Zero residue against round
1: M zero, L zero times two, with C and H zero as recorded.
