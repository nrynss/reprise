# Remediation round 1

Base: `9a54c2a6222aaf5e9fa7770e1425f3b31a022eac`. Branch: `task/t0.1-rem-r1`. Result: `a02280b68dc30d469ef9f1a841270cbdfdd9c673`.

| Finding | Fix | Proof |
|---|---|---|
| H: the gate failed on a clean tree because nothing generated `.svelte-kit/tsconfig.json` before `svelte-check`, and nothing built the app before the preview server | `tools/check.sh` now runs `npx svelte-kit sync` before `svelte-check` and `npm run build` before the Playwright stage. Both stages keep their order. Both failures name themselves | Deleted `web/.svelte-kit`, `web/build` and `web/node_modules`, ran `npm ci` in `web/`, then ran `./tools/check.sh`. It exited 0 with `OK all checks passed`. A planted `export let` comment in the page component still failed the gate as `FAIL [svelte 4 leakage]`. I reverted the probe, and the tree is clean |
| L: the second sentence of the package comment in `cmd/reprise/main.go` ran 40 words against the 30 word limit | Split that sentence after `embedding it`. The two new sentences run 20 words each. The meaning did not change | A word count probe over the three comment sentences returned 11, 20 and 20. Every sentence stays under the limit |
Commits: `e81ce4b` carries the gate fix. `a02280b` carries the comment fix. Neither commit touches the reviewed hash.
