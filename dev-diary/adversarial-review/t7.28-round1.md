# T7.28 review, round 1

- **Reviewed hash:** `2252fde2f662c1787135222cb1b7c6bb854383ac` (confirmed with `git log -1`)
- **Worktree:** `/Users/narayan/reprise-wt/t7.28-rev-r1` (`t7.28-rev-r1`), detached at that hash
- **Base:** `f059cf5`
- **Diff stat:** 10 files. Every edited path lies in `owns`, or is a test beside an owned file
  (`internal/api/playback_test.go`, `internal/episode/playback_test.go`,
  `web/src/lib/editor/draft.test.ts`, `web/src/routes/threads/threads.test.ts`).

## Gate

`cd web && npm ci` exited 0. `./tools/check.sh` exited 1 at its tool check with
`FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`. That is this machine's ffmpeg build, not
this commit. I ran every later step of `check.sh` by hand, in order, under bash.

| Step | Exit |
|---|---|
| `gofmt -l cmd` | 0, no output |
| `go vet $GO_PKGS` | 0 |
| `staticcheck@v0.8.1 $GO_PKGS` | 0 |
| `go test -race $GO_PKGS` | 0 |
| `npx svelte-kit sync` | 0 |
| `npm run check` | 0 |
| `npm run lint` | 0 |
| `npm run test` | 0 |
| `npm run build` | 0 |
| `npm run test:e2e` | 0 |
| svelte 4 leakage scan | empty |
| consumer names scan | empty |
| plan references scan | empty |
| `go test -race ./...` | 0 |

The gate is green. The findings below are defects the committed tests do not exercise.

## What the commit delivers

- The detail endpoint returns edit words in seconds, `audio_url` (user stem, host stem fallback)
  and `render_audio_url` (newest render by rowid). Both go through `/media/{id}`. Confirmed by
  `TestEpisodeDetailServesWordsStemAndRender` and `TestPlaybackReadsStoredRows`, both passing.
- The editor reads the real detail shape, plays the stem and shows the stored words. Confirmed by
  the two new `draft.test.ts` cases.
- The episode page plays the render once one exists, keeps "playback waits" without one, and
  links a draft to the editor. Confirmed by the new `threads.test.ts` cases.
- The gallery routes every draft to the editor, and fixture routing is unchanged (e2e 4 passed,
  `seasonHref` test).

**Stem selection.** The edit words the editor cuts against come from the user stem alone. The
binary passes no host replies and zero offsets into the transcript merge
(`cmd/reprise/main.go:2213`). So preferring the user stem matches the words on screen, and the host
fallback only fires when no user words can exist. I record no finding on the selection itself. The
editor does not play the host side of the conversation, and that follows from the transcript scope,
not from this commit.

**Render selection.** Newest by rowid, skipping empty opus ids. A draft with no render answers an
empty address. Renders carry no state column, and a render row exists only after a successful
persist, so failed renders never surface.

## The ten questions

1. **Spend without reserving budget?** No. The commit adds three reads and no paid call.
2. **AssemblyAI session left open?** No. The commit touches no session path.
3. **Private episode, stem or transcript reaching a non-owner?** No. Each new query filters on
   `owner_id` and calls `Get` first. Addresses are only `/media/{id}`, which keel `mediastore`
   serves behind `identity.AuthorizeMedia` (owner equality for private blobs). Stem and render
   blobs persist with the owner id and `Private`. Probe
   `dev-diary/probes/t7.28-rev-r1/privacy_probe_test.go`, run in the worktree as
   `internal/api/privacy_probe_test.go`: a second guest gets 404 on the first guest's detail, the
   body carries neither the word text nor either blob id, and an anonymous request is refused.
   It passes.
4. **Edit applied without a revertible record?** Yes, finding 1. A live revert writes no decision
   row, so the render still applies the cut the user reverted.
5. **Spoken callback or count from the prompt?** No. The commit reads stored rows only.
6. **Code citing a phase, task or planning file?** No. The plan-reference scan is empty, and the
   commit message is clean.
7. **Rebuilt something Keel or Chaaya provides?** No. It uses Chaaya's `AudioPlayer` and
   transcript follower, and keel `mediastore` for serving.
8. **Run on the handed hash in its own worktree?** Yes. `2252fde2…` in `t7.28-rev-r1`.
9. **Paid job resuming twice after restart?** No. No job kind changed.
10. **Development check needing a person, microphone or wall clock?** No. The new tests use stored
    rows and stubbed fetches.

## Findings

Pins live in `/Users/narayan/reprise/dev-diary/probes/t7.28-rev-r1/`. Each was run from the
matching path in the worktree and fails at the reviewed hash.

| # | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| 1 | H | `web/src/lib/editor/draft.ts:144` (`proposalIdForCut`), also the fixed ids at `:482`, `:501`, `:521`, `:536` | A live draft now loads, but every revert posts a fixture proposal id. A cut posts `prop-<stored id>`, and the cold open, title, notes and callback post `prop-cold-open` and similar. The server answers 404 `no proposal lives at this id`, and the catch swallows it. The screen says reverted. The stored decision stays `accepted`, so `render.AcceptedCuts` still removes the words the user kept. | `web/src/lib/editor/live-decisions.pin.test.ts`, "posts the stored proposal id when a live cut is reverted". It fails with `expected 'prop-cut-a' to be 'cut-a'`. | After a fix, restore `return \`prop-${cutId}\`` for live cuts, so the post ignores the stored id. |
| 2 | M | `web/src/lib/editor/draft.ts:180-205` (`readStoredDraft`) | The live loader drops each proposal's stored `decision`. A cut reverted earlier comes back as applied on reload, `appliedCount` counts it, and the waveform marks it cut. The render will not cut it, so the editor shows an edit the render does not make. | Same file, "loads a stored reverted cut as not applied". It fails with `expected 1 to be +0`. | After a fix, drop the `decision` filter again, so every stored cut installs as applied. |
| 3 | M | `web/src/routes/threads/threads.ts:1415-1441` (`EpisodeController.mountLive`) | The episode page plays the render but shows the edit words on the raw take clock. It also takes duration from the last raw word. After any accepted cut, the scrubber runs past the file's end, word highlighting drifts, and tapping a word seeks to the wrong place. A render with zero edit words gets a null duration, so the page hides playback and claims no address exists. | `web/src/routes/threads/render-clock.pin.test.ts`. A 3 s take with an accepted cut on its 1 s middle word must show duration 2 and "Two" at 1 s. It fails with `expected 3 to be 2`. | After a fix, restore `const duration = renderUrl ? (last?.end ?? null) : null;` and the raw `detail.words` mapping. |
| 4 | M | `web/src/routes/+page.svelte:61` | The draft branch now comes before the `row.jobId` branch. An episode is a draft as soon as both stems exist, while its transcript and editorial jobs still run. Such a card loses its progress bar and its failure text, which the page copy promises. It links to an editor that loads "No words stored yet" and never refreshes. At the base, the job card rendered. | `web/tests/gallery-draft-job.pin.spec.ts`, Playwright with routed `/api/episodes` and a running `transcript_outcome`. It expects `progressbar "Job progress for Take"`. At the reviewed hash only the `Take, draft. Open in the editor.` link renders, and the pin fails with `element(s) not found`. | After a fix, move `{#if row.state === 'draft'}` back ahead of `{:else if row.jobId}`. |

## OUT_OF_SCOPE

None.

## Counts

| Severity | Count |
|---|---|
| C | 0 |
| H | 1 |
| M | 3 |
| L | 0 |

## Verdict

**REMEDIATE**
