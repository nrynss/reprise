# T7.17 round 1 review

Reviewed hash: `29c3d29e983570570a0332e174d13067b02484fa`
Worktree: `/home/nryn/work/reprise-wt/t7.17-rev-r1` (detached HEAD, confirmed with
`git log -1`). Diff under review: `git diff 9d5f5fe..29c3d29e983570570a0332e174d13067b02484fa`.
Base `9d5f5fe` is main at dispatch. No code committed by this reviewer. One scratch
probe file was added, run, and deleted. The tree is clean at write time.

## What the commit does

The live provider carries host audio under the `data` key on `reply.audio`.
The browser read only `audio`, so every real take stored an empty host stem
while all tests passed against a mock emitting the old shape. The commit reads
`data` first with fallback to `audio`, switches the mock to the live shape,
and adds two pinning tests: one routing `data` audio to host events, one
asserting the stem is byte exact from the `data` key through `drainHostBlock`.

## Evidence the pins are load-bearing

Live-shape pin fails before the fix. A throwaway worktree at the base
`9d5f5fe` with only the new `socket.test.ts` copied in runs 2 failed and
11 passed. Both new live-shape tests fail with zero host audio delivered
(`expected +0 to be 1`), which is the empty stem from the task spec. The
throwaway was removed after the run. This worktree was never dirtied by it.

Old-key transition still passes. The pre-existing test at
`web/src/lib/voice/socket.test.ts:80` drives `reply.audio` under the `audio`
key and passes at the reviewed hash. Full file result at the reviewed hash:
13 passed. No other reader of `reply.audio` exists outside `socket.ts`, and
the mock is the only emitter, so the dual-key window is transition only.

Byte-exact pin passes for the pinned moderate signal. The `0.3 * sin` case
round trips exactly through base64, PCM16, float, and drain.

## Findings

| # | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | H | `web/src/lib/voice/pcm.ts:40-46` with `web/src/lib/voice/pcm.ts:8-16`, via `web/src/lib/voice/take.ts:37-39` | The host stem is not byte exact at high levels. The socket decodes provider bytes to PCM16, converts to float by dividing by 32768, and the drain converts back by multiplying by 32767 and rounding. A scratch probe over the full int16 range measured 32768 of 65536 values off by exactly 1 LSB, with the edges pinned: 32767 drains as 32766 and -32768 drains as -32767. A loud 0.99 amplitude sine mismatched 316 of 960 stored bytes. The task spec demands the recorded bytes match the provider channel, and the new pin uses a moderate 0.3 signal that avoids the lossy range, so loud real takes silently degrade with no test to catch it. | Scratch vitest probe (added, run, deleted in this worktree): round trip `bytesToPcm16` to `pcm16ToFloat` to `drainHostBlock` to `bytesToPcm16` over every int16 value and log the mismatch count. Measured output: `mismatches=32768 maxAbs=1 edge32767=32766 edgeNeg=-32767`, plus `loud mismatched bytes=316 of 960` on the 0.99 sine. A one-line variant pin: in the byte-exact test at `socket.test.ts:218` change `0.3` to `0.99` and the stored bytes differ from the provider bytes. | In `pcm.ts` keep the asymmetric pair: `pcm16ToFloat` dividing by 32768 while `floatToPcm16` multiplies by 32767. Restoring that pair after any fix reintroduces the 1 LSB loss and fails the pin again. |
| F2 | L, OUT_OF_SCOPE | `dev-diary/PHASE-7-production-issues.md:446` at the reviewed hash | The commit edits the phase file status line (`in-progress:implement` to `not-started`), which is outside the task `owns` of `web/src/lib/voice/` and owned by the orchestrator alone. No product behavior changes. The orchestrator rewrites the status before landing, so this drops on its own. | `git diff 9d5f5fe..29c3d29 -- dev-diary/PHASE-7-production-issues.md` shows the status line edit and nothing else in that file. | Edit the `status:` line of the T7.17 block in the phase file. |

Counts: C 0, H 1, M 0, L 1 (OUT_OF_SCOPE, non-blocking).

## Notes (not findings)

The both-keys-present corner prefers `data` and drops the frame when `data`
fails to decode, without trying a valid `audio` key beside it. No sender
emits both keys, so this never triggers in practice. No action needed.

The malformed-frame test covers a bad `audio` string but no bad `data`
string. Both travel the same decode path, so coverage is equivalent. A
remediator touching the route may add the symmetric case for free.

## The ten questions

1. Does any path spend money without reserving budget first? No. The change
   reads one socket key and decodes base64. No paid call is added or moved.
2. Can any path leave an AssemblyAI session open without `session.end` or a
   token cap? No. Session open and close paths are untouched.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
   The host stem bytes now actually flow, but to the same owner upload the
   page already used. No auth or visibility change.
4. Does any edit apply without a revertible record? No. No edit path changes.
5. Does any spoken callback or count come from the prompt instead of a stored
   row? No. No transcript or count logic changes.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The new comment states the provider key reason itself.
   The gate plan-reference scan confirms it and passes.
7. Did this task rebuild something Keel or Chaaya provides? No. It reads one
   provider key with existing helpers.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
   Detached worktree above at `29c3d29`, diff read against `9d5f5fe`, pins
   run here after `npm ci` in `web/`.
9. Can any paid job kind resume after a restart and spend twice? No. No job
   code changes.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The new tests drive a fake handle with
    synthetic samples and exact byte assertions.

/runbook: `dev-diary/tools/audit_docs.py` reports `OK planning tree is
consistent`.

Svelte runes: no `.svelte` file changes, so no runes check applies beyond
the passing gate scan.

Gate: `./tools/check.sh` passes in this worktree after `npm ci` in `web/`:
`OK all checks passed`, exit 0 (includes vitest, svelte-check, eslint,
build, playwright, runes scan, and plan-reference scan).

## Verdict

REMEDIATE. One in-scope H finding (F1) blocks approval. F2 is out of scope,
does not block, and drops before landing when the orchestrator rewrites the
status line.
