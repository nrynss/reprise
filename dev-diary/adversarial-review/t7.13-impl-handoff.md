# T7.13 implementer handoff

Commit `7bb91f47c972e92a5691f514980a96d6518b4c9d` on branch `task/t7.13`
in worktree `t7.13-impl`. Base is main at `8c35dfc`. Gate passes in
the worktree. Only `internal/assemblyai/` and `internal/settings/`
change, plus the usual status revert for review to flag.

## Root cause

The default batch model was already correct. The settings value
`universal-3.5-pro` overrode it verbatim in the one shared client,
so both edit and analysis paths failed. The client now maps the
dotted settings spelling to the dashed wire id the provider
accepts. Unknown ids still pass through and fail loudly at the
provider, which the visible job outcome surfaces.

## Notes

Strict constructor rejection was off the table because
`cmd/reprise/main_test.go` builds a client with a dummy model and
that path sits outside owns. No new setting key, both TOML files
untouched. `ValidBatchModel` is exported if a boot check is ever
wanted.
