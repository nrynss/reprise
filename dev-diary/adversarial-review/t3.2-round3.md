# T3.2 round 3 review

Reviewed hash: `917d5d078889603c5a4d44355aecc407d292c476`.
Worktree: `/home/nryn/work/reprise-wt/t3.2-rev-r3` (detached HEAD, confirmed with
`git log -1 --format=%H`). Base for the remediation diff: `abfeb08`.
Declared owns: `internal/gemini/`, `internal/editorial/`. The diff touches only
those paths (2 files: `internal/editorial/run.go`, `internal/editorial/run_test.go`).
No commit was made from this worktree. All pins below ran in this worktree.
Throwaway probe and mutation files were deleted after each run, and
`git status --short` is clean at close.

Verdict: APPROVE.
Counts: C 0, H 0, M 0, L 0. No new findings.

## Residue against round 1, severity by severity

M residue: zero. F1 (post-spend receipt and store paths released instead of
settling) stays fixed. Both paths still call `Settle(estimate, estimate)`
before returning the error, mark the hold settled, and join a failed settle
into both errors. The committed `TestRunSettlesSpentCallOnStoreFailure` and
the updated `TestRunKeepsProviderCopyOnReceiptFailure` pass here.

L residue: zero (two findings, both stay fixed). F2 (the `Usage` comment
claimed usage prices the settle) stays fixed. The comment on
`EditorialAnswer` states the run settles the audio minute estimate and no
settle reads `Usage` today. Re-ran here with a throwaway probe answering the
full draft with four billion tokens of usage. Price and settled pair still
equal `Estimate(12)`, which agrees with the text. A grep for `.Usage` over
non-test `internal/editorial` sources returns nothing. F3 (reserved and
settled amounts unpinned) stays fixed. `TestRunStoresDraft` still asserts
`reserved[0] == Estimate(12)` and the settled pair equals `{estimate,
estimate}`. The zeroing mutation (`estimate := cost.Price(0)`) was applied in
this worktree and fails the suite with `reserved = $0.00, want $0.0005`, then
was reverted with the tree clean.

C residue: zero. H residue: zero. Round 1 had no findings at those levels.

## Residue against round 2, severity by severity

M residue: zero. N1 (decode-failure fallback store failure released a spent
reservation) is fixed. The fallback store path now uses the same 4-line
`Settle` pattern as the two round-1 paths. It settles the estimate, joins a
failed settle into the store error, marks settled, then returns the store
error. The committed `TestRunSettlesSpentCallOnFallbackStoreFailure` (drop
`proposals`, answer `"not json"`, one billed call, `reserved=1 settled=1
released=0`) passes here. The mutation check passes. Deleting the new
`Settle` fails the test with `settled=[] released=[500000]`, then was
reverted with the tree clean.

L residue: zero. C residue: zero. H residue: zero. Round 2 had no findings
at those levels.

## Post-spend path sweep

The class has now appeared three times, so every return path after
`GenerateEditorial` succeeded was enumerated once more. All five settle, and
the deferred `Release` can no longer fire on any of them.

| Path (`run.go`) | Billed | Accounting |
|---|---|---|
| Receipt store failure (152-158) | Yes, the model answered | Settles, then returns the error |
| Decode-failure fallback store failure (162-168) | Yes, a 200 with unusable JSON | Settles, then returns the store error (this round's fix) |
| Decode-failure fallback success (169-173) | Yes | Settles, then returns the fallback result |
| Valid-answer store failure (181-187) | Yes | Settles, then returns the error |
| Success (188-192) | Yes | Settles, then returns the draft |

Two release paths remain, and both are unbilled. The model-failure fallback
store failure (147-149) releases because `GenerateEditorial` returned an
error, so no billable answer completed. The model-failure fallback success
(150) releases through the defer for the same reason. Round 1 blessed this
accounting in scope note (d), round 2 reaffirmed it, and nothing in this
remediation moved it. The reserve refusal (131-133) returns before the defer
is installed, so no hold is freed that was never taken.

Examined and deliberately not a finding: on the three error paths a failed
`Settle` returns `errors.Join` without setting `settled`, so the deferred
`Release` also fires. That frees a hold the failed settle never booked, which
prevents a leak without double booking. The pattern matches the two round-1
paths exactly, so changing it here alone would split the three.

## New findings

None. The remediation adds four lines of `Settle` handling plus one test. No
other behavior moved. `gofmt` reports no files, `go vet` passes for both
owned packages, and `go test -race -count=1` passes for both owned packages.

## The ten questions

1. Does any path spend money without reserving budget first? No. `Reserve`
precedes `GenerateEditorial`, and refusal returns before any call. Pinned by
`TestRunRefusesWithoutBudget`.
2. Can any path leave an AssemblyAI session open without `session.end` or a
token cap? Not applicable. This task opens no AssemblyAI session.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
The remediation adds no serving surface. Writes carry `owner_id`.
4. Does any edit apply without a revertible record? No. `store` is untouched:
one transaction replaces proposals, accepts cuts with decision rows, backs the
callback with mention and callback rows, and sets the title.
5. Does any spoken callback or count come from the prompt instead of a stored
row? No. The callback path is untouched and still persists its quote with
episode and offset.
6. Does any code, comment, test or migration cite a phase, a task, or a
planning file? No. The grep over both owned trees for task ids, phase ids,
planning file names, and `§` is clean. Two substring hits (`rounded`,
`grounded`) are ordinary words, not citations. `go vet` plus `gofmt` pass.
7. Did this task rebuild something Keel or Chaaya provides? No. The
remediation uses only `errors.Join` from the standard library on top of the
`keel/cost` holds round 1 already recorded. Nothing here duplicates a library.
8. Was this review run on the handed hash, in a worktree of its own? Yes.
Hash `917d5d0` confirmed, detached worktree `t3.2-rev-r3`, diff of the
remediation alone (`abfeb08..917d5d0`) read in full, every pin executed here,
no commits made, tree clean at close.
9. Can any paid job kind resume after a restart and spend twice? No. `Kind`
is untouched (limit 2, not idempotent, no resume path, one attempt), and the
new settle-before-return block adds no retry. `TestKindNeverResumes` passes.
10. Does any development check need a person, a microphone, or a wall-clock
threshold? No. All pins run on generated words and scripted models. `go test
-race -count=1` passes for both owned packages. The full web-side gate was
not re-run from here. The change is Go-only and the remediation passed
`./tools/check.sh` at identical content, with the Go-side portions (gofmt,
vet, race tests) re-verified green in this worktree.

## Verdict

APPROVE. Zero residue against round 1 at every severity (M zero, L zero for
both findings, C zero, H zero). Zero residue against round 2 at every
severity (M zero for N1, L zero, C zero, H zero). No new findings, and no
finding was softened for rarity or cost of fix. Out-of-scope defects: none.
