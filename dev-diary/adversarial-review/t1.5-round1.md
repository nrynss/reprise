# T1.5 round 1 review

Hash reviewed: `e9d2aa693614e1213ce0f03a476444a88eb8ed9b`.
Worktree: `/home/nryn/work/reprise-wt/t1.5-rev-r1`, detached at the handed hash.
Confirmed with `git log -1` before reviewing. Base: `a02280b68dc30d469ef9f1a841270cbdfdd9c673`.
Diff under review: `git diff a02280b68dc30d469ef9f1a841270cbdfdd9c673..e9d2aa693614e1213ce0f03a476444a88eb8ed9b`.

## Scope

Owned paths: `internal/settings/`, `config/reprise.local.toml`, `config/reprise.box.toml`, `.env.example`.
Recorded exception: `go.mod` and `go.sum` may change ONLY via `go get github.com/nrynss/keel@v0.3.0` plus `go mod tidy`, with no replace directive, Keel exactly `v0.3.0`, and no other dependency.
Files in the diff: `.env.example`, `config/reprise.box.toml`, `config/reprise.local.toml`, `go.mod`, `go.sum`, `internal/settings/settings.go`, `internal/settings/settings_test.go`. Nothing else changed. `cmd/reprise/main.go` is untouched. Its missing wiring is a later contract change, not a finding here.

## Module exception

Respected exactly. `go.mod` keeps `require github.com/nrynss/keel v0.3.0` unchanged and adds one line: `require github.com/pelletier/go-toml/v2 v2.4.3 // indirect`. No replace directive. `go.sum` adds exactly the two `pelletier/go-toml/v2 v2.4.3` lines and keeps both Keel `v0.3.0` lines byte identical. Keel `v0.3.0` lists `go-toml/v2 v2.4.3` as its own direct requirement, so the indirect entry is what `go mod tidy` must add for the build to resolve.

## Gate

Ran in the review worktree: `npm ci` in `web/`, then `./tools/check.sh`. Exit code `0` with `OK all checks passed`. `go test -race` passed for `cmd/reprise` and `internal/settings`. The svelte, eslint, vitest, build, playwright, leakage, consumer-name and plan-reference scans all passed.

## Done-when probes, run independently at the reviewed hash

All probes used fresh temp files and distinct dummy values (`revprobe-assembly-VV11`, `revprobe-gemini-WW22`, `revprobe-signing-XX33`), never the implementer constants. A throwaway in-package probe file (`zz_revprobe_test.go`, seven tests, all passing) was deleted after. The tree is clean and nothing was committed.

1. Missing secret stops startup and names the key, source and path. Built the settings test binary and ran the `REPRISE_SETTINGS_HELPER=1` helper with `REPRISE_CONFIG` pointing at a config whose env file lacks `SESSION_SIGNING_KEY`. Exit `1`. Stderr: `load: settings: secret has no value: config: cannot resolve secret: secrets.session_signing_key (source env_file locator path=/tmp/t15rev-run/env var=SESSION_SIGNING_KEY) ...`.
2. Group-readable env stops startup and names the path. Same helper against a `0640` env file. Exit `1`. Stderr names the path and the mode: `file is group or other readable: path /tmp/t15rev-run/loose.env (mode 0640)`.
3. Boot log shows the plan with no secret value. Ran the helper in `plan` mode against a full `0600` setup and captured stdout. Every row reads `secrets.<name> env_file path=<env> var=<NAME> resolved`. `grep` for each of the three dummy values found nothing. `grep -c env_file` returned `3`.
4. Tests are load-bearing on the secret references. Replaced `configDoc` with a document holding no secret blocks, ran the implementer tests, and saw `TestStartupStopsOnMissingSecret`, `TestBootLogShowsPlanWithoutValues`, `TestLoadResolvesSecrets` and `TestLoaderRefusesInlineSecret` fail. Restored the file byte identical (`diff` against the backup showed no output).

## Keel refusal surfacing, one case each through `Load`, sentinel only

Loose file-source secrets file fails under `ErrSecret`. Inline literal where a reference belongs fails under `ErrSettings`. Unknown key fails under `ErrSettings`. Quoted env value fails under `ErrSecret`. Each case passed with `errors.Is` against the Reprise sentinel, without matching library wording. No refusal logic is re-implemented. `settings.go` registers defaults through `source.RegisterDefaults`, loads through `config.Load`, and maps `config.ErrResolve` and `config.ErrRequired` to `ErrSecret` and everything else to `ErrSettings`.

## Contract and style checks

Exactly three secret references, one per key, in each TOML, each with `source = "env_file"` and the specified path (`/home/nryn/work/reprise/.env` locally, `/etc/reprise/env` on the box). `PathVar` is `REPRISE_CONFIG`. `Search` holds only `config/reprise.local.toml`. `.env.example` holds the three names with empty values. Neither TOML holds a value. Tracked-tree grep for secret-shaped values finds only the `dummy-` test constants, which the contract allows. No `LICENSE` file. No planning names in any tracked file in the diff. Gate plan-reference scan passed and manual grep for task, phase, planning-file and `§` marks is empty. Exported identifiers carry doc comments starting with their names. `Load` takes `ctx` first and is the only I/O entry. One sentinel per condition, both wrapped with `%w`. No `**T`, no `any` wire maps, no panic, no naked returns, no `log.Fatal` outside main. Comments hold no semicolons and no em dashes. Comment sentences audit at 30 words or fewer. Commit message is two sentences, active voice, no semicolons or em dashes.

## Ten review questions

1. Does any path spend money without reserving budget first? No. `Load` reads one TOML file and resolves env references only. It imports nothing that dials a provider or a budget.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. This change holds no session code.
3. Can a private episode, stem or transcript reach anyone but its owner? No. No tracked file holds a secret value. The plan probe shows every source named and all three planted dummy values absent from plan output.
4. Does any edit apply without a revertible record? No. This change applies no editorial edit. The settings files are tracked in git.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. This change holds no spoken content.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. Grep over the diff paths and the gate plan-reference scan both return empty.
7. Did this task rebuild something Keel or Chaaya provides? No. Loading, precedence, validation, refusal and the plan come from `keel/config` and `keel/config/source`. Reprise owns only the struct and two sentinels.
8. Was this review run on the handed hash, in a worktree of its own? Yes. `git log -1` showed `e9d2aa693614e1213ce0f03a476444a88eb8ed9b` in detached worktree `t1.5-rev-r1`. Every pin above ran there.
9. Can any paid job kind resume after a restart and spend twice? No. This change holds no job code.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The tests use temp files and a rerun helper process with exact string assertions and exit codes.

## Findings

None. Severity counts: C 0, H 0, M 0, L 0. No OUT_OF_SCOPE defects found.

## Verdict

APPROVE. Zero findings across all severities. Round 1 is the first round, so the baseline is clean and there is no prior-round residue to carry.
