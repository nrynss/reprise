# T7.24 round 1 review

Reviewed hash: `476fa9cf3253eb5697d8db77902d01ddbceca843`
Worktree: `/Users/narayan/reprise-wt/t7.24-rev-r1` (detached HEAD).
`git log -1` matches that hash. `git status --short` was empty.
Diff reviewed: `git diff f55733629126d3d5f8772f4457f6352759711449..476fa9cf3253eb5697d8db77902d01ddbceca843` and nothing else.
No code commits. The tree was still clean at that hash after the pins.

## The ten questions

1. Does any path spend money without reserving budget first? No. The diff adds one gallery anchor and a mock Playwright spec. It opens no provider call and mints nothing.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. Leaving a live take through the new link runs `destroy`. That sends `session.end` and the guard beacon. Measured on the mock take, both counts move from 0 to 1, and the URL stays `/`. A minted session still carries `max_session_duration_seconds`. This page does not open a session.
3. Can a private episode, stem or transcript reach anyone but its owner? No. The link targets `/` through the same path helper the other screens use. No media URL, cookie, or owner check changes.
4. Does any edit apply without a revertible record? No. The commit applies no edit. It only adds a way off the record page.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No host line or spoken count is added.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. New comments say "record phase" for the on-screen state. They do not cite a task id, a phase id, or a planning file. The gate pattern does not match them.
7. Did this task rebuild something Keel or Chaaya provides? No. The anchor uses SvelteKit `resolve`. The close on leave still uses Chaaya `SessionGuard`.
8. Was this review run on the handed hash, in a worktree of its own? Yes. The detached worktree above is the only tree this review used. `git log -1` equals the handed hash.
9. Can any paid job kind resume after a restart and spend twice? No. The diff does not touch a job kind.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The gallery proofs and the neighbouring record specs use the mock take. None waits on a clock or a person.

## What the commit does

`web/src/routes/record/+page.svelte:87` renders one anchor, Back to gallery, as the first child of `main`. It sits outside every phase branch. Preflight, starting, live, ending, recovery, and both draft move blocks all include it. The href comes from `resolve('/')`. Static HTML writes `href="./"`, which resolves to `/` from `/record`. A request to `/record/` returns 307 to `/record`. After hydration the live href is `/`.

`web/src/routes/record/gallery-link.spec.ts` clicks that link from eight states and expects the season heading on `/`.

## Pins

`npm ci` in this worktree's `web/` added 236 packages. Exit 0. `node_modules` was missing here.
`npm run build` in `web/` wrote `web/build`. Exit 0. Preview needs that build.

`npx playwright test -c src/routes/record/mock.playwright.config.ts gallery-link.spec.ts` from `web/`: 8 passed in 1.9s.
The proofs click through preflight, starting, live, ending, a refused ending, recovery, a finished draft move beside the live take, and a refused draft move beside the live take.
Each one waits for the heading "The season so far" and asserts pathname `/`.
The preview log shows `404` for `GET /api/episodes`. The season heading still renders. That 404 is the preview server, not this commit.

`npx playwright test -c src/routes/record/mock.playwright.config.ts end-control.spec.ts cap.spec.ts stems-complete.spec.ts mock-session.spec.ts` from `web/`: 13 passed in 11.2s.
The new link did not break those specs.

`./tools/check.sh` was not run. This commit does not touch the ffmpeg pin. A pin mismatch there would be the environment, not a finding.

A separate probe held `**/stems/complete`, clicked the link during ending, saw `/`, then fulfilled the move. That probe is the out of scope pin below. It is not one of the committed specs.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| M, OUT_OF_SCOPE | `web/src/lib/voice/record-state.ts:487` at the reviewed hash | The new link uses client navigation. A guest who clicks it during an in-flight draft move does reach `/`. `endTake` keeps running because `destroy` does not cancel it. When the move succeeds, `goProcessing` assigns `/processing`. That page renders no link. Browser back returns to `/`. The assign is outside `owns`. | On the mock preview, hold `**/stems/complete`. End a mock take and click Back to gallery. Wait for the season heading at `/`. Fulfill the held call with a 200 draft move. The URL becomes `/processing?episode=mock-episode&uploads=done&userBytes=98304&hostBytes=192000&mock=1`. `page.goBack()` then returns to `/`, and that page has zero links. A full `location.assign('/')` before the fulfill stays on `/`. The committed ending proof aborts the held call, so it never sees the assign. | Skip `window.location.assign` in `goProcessing` after the page has left. Put that assign back. The probe lands on `/processing` again. |

This finding never blocks APPROVE. The assign lives under `web/src/lib/voice/`. This task owns `web/src/routes/record/` only. The link itself is outside every branch, and every committed proof clicks through to `/`.

## Counts

C 0, H 0, M 1 (out of scope), L 0. In-scope findings: zero.

## Verdict

APPROVE
