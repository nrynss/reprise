# T4.2 review round 1: APPROVE

Reviewed hash: `974828e233ca311d876779b0777d5c96f29a83bf`, confirmed with
`git log -1 --format=%H` in detached worktree `/home/nryn/work/reprise-wt/t4.2-rev-r1`.
Diff read as `git diff 7dbdf35..974828e`. It touches only the owned paths:
`internal/memory/callback.go` (new, 374 lines) and
`internal/memory/callback_test.go` (new, 260 lines). No commit made here.
Throwaway probe tests ran green and were deleted afterwards.
`git status --short` is empty.

Verification: `go test -race -count=1 ./internal/memory/...` passes.
`go vet` is clean. `gofmt` is clean. Grep over both new files finds no plan
reference, no `interface{}`, no `**T`, no `panic`, and no em dash. The full
web gate did not rerun here. The diff is Go only and the implementer reports
a full green gate after `npm ci` in its worktree.

Verdict: APPROVE. Counts: C 0, H 0, M 0, L 0. This is round 1, so no prior
round leaves residue. The claim below is zero residue by construction.

## Committed pins, re-run green

All seven tests pass under `-race`: `TestSelectPrefersOpenCommitment`,
`TestSelectHonorsAgreeingPlantedRow`,
`TestSelectIgnoresDisagreeingPlantedRow`, `TestSelectNeverRepeatsLastChoice`,
`TestSelectFallsThroughTiers`, `TestSelectReturnsNilOnEmptySeason`,
`TestMarkUsed`. The first renders the pick through `host.Build` and asserts
the greeting names episode 1 with the dread quote.

## Extra probes this round (run, then deleted)

- Non-adjacent repeat is allowed. Dread opens episode 2, Maya opens
episode 3, and dread opens episode 4 again. The rule covers adjacent picks
only, which matches the spec wording.
- Reselect while unused stores exactly one row. Two `Select` calls for one
episode leave one callbacks row on the same mention. Crash-before-speech
retry is safe.
- Foreign-owner `MarkUsed` fails with `ErrNotFound` and leaves the victim
row at `used = 0`. No write-side hole.
- `Select` is owner isolated. Owner A picks its own commitment and owner B
gains zero callbacks rows.
- Stale plant fate: after the data pick wins, the stale callbacks row is
gone and its backing mention row survives. The pointer leaves. The quote
stays.
- Reselect after the row was spoken stores a second row on the same
mention. The contract calls `Select` once per episode, so this path needs
caller misuse. It is recorded here as observed behavior, not a finding.

## Scrutiny, item by item

(a) Tiers match the spec exactly. Commitments come from `OpenCommitments`
oldest first. Names come from `RecurringNames` with a two-episode floor.
Topics come from `CircledTopics` with its own three-episode floor. Each
within-tier order defers to its reader contract. Nothing is invented.

(b) Agreement means same mention row or same folded quote. Folded-quote
matching cannot over-agree across threads, because the index groups threads
by that same fold. Two surfaces sharing a key are one thread already.

(c) No-repeat reads only the latest stored row under other episodes. Same
episode rows never count, so the plant cannot block itself. Non-adjacent
repeats stay allowed. The probe above pins both halves.

(d) Rival clearing deletes unread pointer rows only. Every `DELETE` filters
`used = 0` and never touches mentions or proposals. The spoken backing
always survives, so the T4.1 append-only ruling is honored where it counts.
Each statement is atomic. The store path wraps delete plus insert in one
transaction, so no reader observes zero rows.

(e) `MarkUsed` scopes its write to the owner and maps zero affected rows to
`ErrNotFound`. Unknown and foreign ids behave alike. This repeats the T4.1
L2 ruling exactly.

(f) Empty seasons return `(nil, nil)` and store nothing. The committed test
pins the zero-row outcome. Callers branch on nil pick versus non-nil error,
so the quiet path stays distinguishable.

(g) Speech cites the mention episode, never the storage episode. The
`Selection` shape carries no callback-episode field, so leakage has nowhere
to ride. The pick stores under the just-finished episode and leaves its row
unused, which matches the oldest-unused reader.

(h) `oldestHit` resolves thread hits through the oldest matching mention
row, filtered to the thread episode numbers. Duplicate quotes in one
episode resolve to the first occurrence. Offsets never reach speech, so the
worst case is a thread link to the first hearing. That is acceptable.

(i) `Select` and `MarkUsed` take no budget and make no model call. Pure SQL
reads plus one guarded write cannot spend. No new paid path exists.

Two non-findings are noted for the record. `oldestHit` swallows query
errors instead of returning them, but every persistent failure trips the
reader calls first, so the swallow has no reachable consequence. `Select`
trusts the caller episode id without an owner check, which repeats the T4.1
pattern the loop already approved. Neither blocks.

## New findings

None.

## The ten questions

1. Does any path spend money without reserving budget first? No. Both new
entry points run SQL only. No model seam exists in this file.
2. Can any path leave an AssemblyAI session open without `session.end` or a
token cap? No. Nothing here speaks HTTP or mints tokens.
3. Can a private episode, stem or transcript reach anyone but its owner? No.
Every query scopes to the caller owner. The foreign-owner probe returns
`ErrNotFound` with the victim row untouched, and the isolation probe leaves
the second owner with zero rows.
4. Does any edit apply without a revertible record? No. Deletes remove
never-spoken pointer rows. Mention rows, proposal rows, and used rows all
survive. Spoken content keeps its record.
5. Does any spoken callback or count come from the prompt instead of a
stored row? No, with teeth. The pick cites a stored mention id. The quote
and episode number come from stored columns. The greeting test asserts the
episode 1 number and the exact dread wording through the real builder. The
plant wins only on mention-id or folded-quote agreement with the index, and
the stale-plant test pins rejection. `MarkUsed` flips a flag after speech
and writes no content.
6. Does any code, comment, test or migration cite a phase, a task, or a
planning file? No. Grep over both files returns nothing, and the commit
message carries a plain description.
7. Did this task rebuild something Keel or Chaaya provides? No. It reads
the existing readers and the existing callbacks schema, and it uses
`keel/id` for new row ids only.
8. Was this review run on the handed hash, in a worktree of its own? Yes,
as stated above. No code was committed here.
9. Can any paid job kind resume after a restart and spend twice? No job
kind is defined here. Prespeech retry stores exactly one row by probe, and
there is no spend to double.
10. Does any development check need a person, a microphone, or a
wall-clock threshold? No. Seven committed tests seed fixed seasons and run
offline under `-race`. The six probes did the same.
