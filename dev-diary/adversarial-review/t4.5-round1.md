# T4.5 round 1 review

Reviewed hash: d747703724540048a7e96c3da44c9004f1d1aa95.
Worktree: /home/nryn/work/reprise-wt/t4.5-rev-r1 (detached HEAD, confirmed
with `git log -1 --format=%H` before and after all probes). No commits
made. `git status --short` is clean. `npm ci` ran inside the worktree's
`web/` only. The main checkout was never touched.

Claim base: 75bf5c3. `git diff 75bf5c3..d747703` is exactly one line in
one file:

```diff
-	await expect(page.getByRole('heading', { level: 1 })).toHaveText('Reprise');
+	await expect(page.getByRole('heading', { level: 1 })).toHaveText('The season so far');
```

Declared owns: `web/tests/shell.spec.ts`. The diff touches that path
only. Nothing else changed.

## What was verified (all in the review worktree)

* Expected text equals the gallery h1 exactly. Read from the T4.3 tree,
  not from memory: `git show e0417e6f420147faf2db1f9535c4b09c9cd7c1e3:`
  `web/src/routes/+page.svelte` line 35 reads
  `<h1>The season so far</h1>`. The spec expects `The season so far`.
  Exact match.
* Branding intact elsewhere. In the T4.3 tree,
  `web/src/lib/shell.ts` still exports `appName = 'Reprise'`, and the
  root route sets `<title>{pageTitle('Gallery')}</title>`, which renders
  `Gallery · Reprise`. The h1 follows the gallery view. The app name
  still brands the shell.
* No plan refs. Grep over `web/tests/shell.spec.ts` for task, phase,
  planning-file and section marks is clean.
* Paired verification, both directions (the line cannot pass solo
  because the gallery route is unlanded T4.3 work):
  * SOLO (handed hash as-is, fresh build): `npx playwright test`
    `tests/shell.spec.ts` fails, `1 failed`. `Expected: "The season so
    far"`, `Received: "Reprise"`, with the locator resolving to
    `<h1>Reprise</h1>`. This proves the pin measures the pair, not the
    line alone.
  * PAIRED (scratch-merge of reviewed T4.3 tip
    `e0417e6f420147faf2db1f9535c4b09c9cd7c1e3` with `--no-commit`,
    rebuilt): `npx playwright test tests/shell.spec.ts` passes,
    `1 passed`.
  * MUTATION (merge still in place, spec scratched back to `Reprise`,
    no rebuild needed since the spec is source): fails, `1 failed`.
    Restored to `The season so far` immediately after. Load-bearing
    both ways.
  * Merge deleted with `git reset --hard d747703724540048a7e96c3da44`
    `c9004f1d1aa95`. `git log -1 --format=%H` equals the handed hash
    and `git status --short` is empty. No trace of the merge remains.

## Findings

In-scope findings: none.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| (none) | | | | |

OUT_OF_SCOPE rows: none. The diff stays inside its declared owns.

## Note for the orchestrator (not a finding)

Landing order: T4.5's solo-tree gate is red on this spec by
construction (the gallery is absent, so `/` still renders `<h1>Reprise`
and the updated expectation fails, as the solo run above shows).
Either landing order leaves main transiently red on exactly this spec:
T4.5 first fails until T4.3 lands, T4.3 first fails until T4.5 lands.
The pair must land back-to-back. The orchestrator, not this review,
manages that sequence.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   diff is a Playwright expectation string. No paid call exists near it.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. No session code in the touched path.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. A test assertion serves nothing.
4. Does any edit apply without a revertible record? No. No edit
   applies. The file is a spec.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No spoken callback or count exists here.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. Grep over the touched file is clean.
7. Did this task rebuild something Keel or Chaaya provides? No. One
   expectation string changed. Nothing built.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Hash confirmed before and after, detached worktree, no
   commits, scratch merge fully reverted, tree clean.
9. Can any paid job kind resume after a restart and spend twice? No.
   No job involved.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The shell spec loads `/` and asserts
    heading text. No audio, no device, no timing assertion.

## Verdict

APPROVE. Counts: C 0, H 0, M 0, L 0. No residue questions apply; this
is round 1. The one-line change matches the gallery heading exactly,
branding stays intact elsewhere, the pin passes paired and fails solo
and under mutation, and the worktree is clean at the handed hash.
