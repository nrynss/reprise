# T2.4 round 2 review

Reviewed hash: `e471725264588e4bb0487e2cfa5d986c2de71558`.
Worktree: `/home/nryn/work/reprise-wt/t2.4-rev-r2`, detached HEAD.
Confirmed with `git log -1 --format=%H` before reading anything.
Remediation diff: `git diff eee9fb5..e471725`, two files, both under the
declared owns paths (`web/src/lib/voice/`). No commit was made here.

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0. Out of scope findings: 0.

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with exactly one finding, H-1, and zero
findings at every other severity.

- **H: zero residue.** H-1 (end before open orphans a billed session and
  hangs) is fixed. The open handler checks the end latch first. A prior
  `end()` short-circuits: no `session.update` is sent, every waiting end
  promise resolves through the new `settleEnd`, and the transport closes.
  No provider session starts, so no `session.end` can go missing and no
  waiter pends. The new unit test pins the exact pin round 1 prescribed
  (end before open resolves, no `session.update` after). It passes here.
  The mutation is load-bearing: deleting lines 66 to 69 of `socket.ts`
  (the latch check) fails the new test with `pre-open end never settled`
  while the other 10 pass, and the tree was restored clean afterwards.
- **C: zero residue.** Round 1 recorded none, and this diff opens no new
  spend, privacy, or recording loss path. The short-circuit sends nothing,
  so it cannot mint or bill.
- **M: zero residue.** Round 1 recorded none. The normal open, use, and
  end flow is byte for byte intact past the latch check, and the
  exactly-once `session.end` test still passes.
- **L: zero residue.** Round 1 recorded none. The added doc comment on
  `end()` states the pre-open behavior in its own terms.

## New findings

None. The hunt below closed every candidate without a pin.

- **Normal flow intact.** `open, end, session.ended` still sends exactly
  one `session.end` and closes once. The pre-existing test pins it and
  passes. A late `end()` after `session.ended` still resolves at once.
- **Closing an unopened transport is safe.** The browser `close()` on a
  connecting socket aborts the handshake and fires close. It throws
  nothing. The mock opens synchronously, so this path stays unit tested
  only, which matches round 1.
- **The take layer handles the resolved-before-open end.** `endTake` in
  `record-state.ts:220` awaits `voice.end()` and then stops the recorder,
  finishes uploads, and routes to processing. It never waits on `onEnded`,
  which the take wires to a noop (`record-state.ts:381`). A resolving
  pre-open end flows straight through. No hang and no missed callback.
- **Double pre-open end closes once.** Both callers push waiters, the
  `endSent` latch sends nothing twice, and one `settleEnd` resolves every
  waiter with a single close. Single threaded and deterministic by read.
- **Reload and resume unaffected.** The socket carries no persisted state.
  Each take wires a fresh one. The diff touches no upload or store path.
- **No fire of `onEnded` on the short-circuit path is correct.** No
  session ever existed, and the sole consumer ignores the event.
- **The new test's 2 s bound is liveness only.** The pass path resolves
  at once through the promise race. No wall-clock threshold decides a
  result.

## Verification performed

`npm ci` ran clean in this worktree. Full `vitest` passes, 48 tests
across 7 files. `svelte-check` reports 0 errors and 0 warnings. `eslint`
is clean. `vite build` succeeds. The full `./tools/check.sh` passes in
this worktree, including the Playwright suite, Go vet, staticcheck,
`go test -race`, and the runes, consumer name, and plan reference scans.
The tree is clean after verification, including after the temporary
mutation probe, which was reverted with no commit.

## The ten questions

1. Does any path spend money without reserving budget first? No. The
   browser mints nothing. Tokens arrive through the broker, which
   reserves first. The fix sends less, never more.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. H-1 was the last such path, and the short-circuit
   starts no session at all. Every post-open path funnels into the
   latched `end()`.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. Nothing in this diff touches uploads, storage, or URLs.
4. Does any edit apply without a revertible record? Not applicable.
   This task ships no edit feature.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. The setup forward is unchanged.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. The plan reference scan passes.
7. Did this task rebuild something Keel or Chaaya provides? No. The fix
   is five lines of latch logic on the task's own socket.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. The hash matched before any file was read. Nothing was
   committed. A temporary mutation probe was applied and fully reverted.
   The tree is clean.
9. Can any paid job kind resume after a restart and spend twice? Not
   applicable. No job kinds live in this task.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The new test drives a fake handle with
    generated config. The timeout is a liveness bound on the failure
    branch only.

## Verdict

APPROVE. Zero findings at every severity. Zero residue against round 1:
H-1 is fixed, pinned, and mutation verified, and C, M, and L carry
nothing forward.
