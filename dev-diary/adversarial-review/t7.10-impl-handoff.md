# T7.10 implementer handoff

Commit `c192ea9d265d9fcb8dea7a469b663b9ce856155c` on branch `task/t7.10`
in worktree `t7.10-impl`. Base is main at `7f39a5f`. Edge verified as
`version=45d14b752a1ce165d767b8cdfe5f7edcefb058fe` before any check
ran. All checks ran from a workstation with generated speech only.
Spend about 6.5 cents (one 51.7 second session, failed batch billed
nothing).

## Verdict

Close the round 3 carry, with passing evidence on both halves.
Failed pass surfaces: first completion answered with the running
outcome, detail then showed `state:failed` with the same job id,
`status:error`, and the full provider rejection text, stable over
six polls. Repeat reports standing outcome: second completion
answered `moved:false scheduled:false` with empty job id and the
same error. Nothing scheduled twice.

## New defect carried forward

The batch transcript request sends a `speech_models` value AssemblyAI
rejects with 400 (only universal-3-pro, universal-2, universal-3-5-pro
accepted), so no episode reaches proposals on this build. Owning
paths named in the note: `internal/assemblyai/` plus the model id
setting seam. This becomes the next task, not a T7.10 blocker.

## For the reviewer

The commit reverts the T7.10 status line outside its owns line. Flag
it. It drops before landing. Only `dev-diary/probes/production.md`
changed otherwise (253 line append, rounds 1 to 3 byte identical).
