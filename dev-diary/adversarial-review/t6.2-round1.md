# T6.2 round 1 review

Reviewed hash: `dffb2aabb94735e56a41534cf2c8c7480ab79cd9`
Worktree: `/home/nryn/work/reprise-wt/t6.2-rev-r1` (detached, confirmed with `git log -1`)
Diff reviewed: `git diff d5ded02..dffb2aabb94735e56a41534cf2c8c7480ab79cd9` and nothing else
Task owns: `dev-diary/submission/` only
Context: T6.4b has not run. T6.4b slots must be marked and the task cannot close yet. The wait is a carry, not a defect.

## Claim trace (8 checks, each against its source)

Every factual claim below was read in the live plan at `/home/nryn/work/reprise/`, never from memory.

V1. Voice cost 4.50 dollars per connected hour. Source: `product.md:174` states the Voice Agent API bills 4.50 dollars per hour per second of connected time. Draft math checks out. 16.6 seconds bills about 2.1 cents and 66.8 seconds bills about 8.4 cents. Pass.

V2. Connected durations 16.6, 16.8, 66.8 seconds. Source: `dev-diary/probes/production.md:231` (16.595827), `:547` (16.801372), `:681` (socket 66.767942). The overhead note traces to `:690` to `:692`. Pass, with the round 3 caveat recorded in F4 and F5.

V3. Provider close reason `client_end`. Source: `production.md:230` (round 1) and `:546` (round 2). Round 3 (lines 614 to 842) holds no Sessions API read. Only two of three live calls carry provider confirmation. Partial pass, overclaim filed as F4 and F5.

V4. Session mints answer HTTP 201. Source: `production.md:44`, `:313`, `:658`. Three mints across three rounds. Pass.

V5. Anonymous media reads answer 404 with `Cache-Control: private, no-store`. Source: `production.md:486` and `:493` (round 2, real and unknown ids alike). Round 1 lacked the header and round 2 closed that gap. The draft states current build behavior. Pass.

V6. The edge rate gate answers 429 with a retry hint and patient callers recover. Source: `production.md:600` to `:603` (429 with 10 second hint, recovered) and `:823` to `:826` (repeated 429s, every retry waited and recovered). Pass.

V7. Owner reads round trip byte exact. Source: `production.md:519` to `:523` (returned bytes hash to the uploaded chunk exactly, 32000 bytes, HTTP 200). Pass.

V8. Business names, keyterm mechanic, private gallery. Source: `product.md:185` names StoryWorth, Remento, HereAfter. `dev-diary/probes/voice-agent.md:233` to `:234` shows the planted name Zaffranil recognized exactly with keyterms and missed without them. `production.md:369` and `:718` list episodes with `visibility: private` under the owner cookie. Pass.

## Style and marking

No semicolons found. No em dashes found. No sentence runs over 30 words (checked by script). Voice is active throughout. The `T6.4b` markers are required by the dispatch and exempt from the gate, since `tools/check.sh:13` exempts `dev-diary/` from plan reference checks. Every draft that needs a real voice carries an explicit `[WAITS ON T6.4b]` slot: video beats 1 and 4, slide 5 plus the closing line, the descriptions closing line, the cover strapline, and four index slots plus the close statement. `audit_docs.py` reports the planning tree is consistent.

## The ten questions

1. No path here spends money. Docs only, no code changed.
2. No path here can leave a session open. The drafts describe explicit `session.end` closes and the probe records confirm them.
3. No private material reaches anyone new. The drafts restate private by default, the 404 refusal, and the `private, no-store` header, all measured in V5.
4. No edit applies anywhere. The script states nothing applies without review, matching the product rule.
5. No spoken callback or count comes from the prompt. Every callback slot waits on T6.4b, and the recording notes forbid describing a callback the episode does not contain.
6. No code, comment, test, or migration cites the plan. The `T6.4b` markers sit in exempt draft docs and the dispatch orders them there.
7. Nothing here rebuilds Keel or Chaaya. No code at all.
8. Yes. This review ran on the handed hash in its own detached worktree, confirmed above.
9. No paid job kind changes here, so none can resume and spend twice.
10. No check used here needs a person, a microphone, or a wall clock threshold. Only greps, a word count script, and `audit_docs.py` ran.

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | M, OUT_OF_SCOPE | `dev-diary/PHASE-6-ship.md:168` at the reviewed hash | The commit flips the T6.2 status line to `not-started`, outside the task `owns` of `dev-diary/submission/` only. Only the orchestrator writes status lines. | `git diff d5ded02..dffb2aabb94735e56a41534cf2c8c7480ab79cd9 -- dev-diary/PHASE-6-ship.md` shows the `in-progress:implement` to `not-started` flip, while the live plan T6.2 block owns `dev-diary/submission/` only | Edit the line back to any `in-progress` value and the defect returns |
| F2 | M, OUT_OF_SCOPE | `dev-diary/PHASE-6-ship.md:187` at the reviewed hash | The commit flips the T6.3 status line to `not-started`. T6.3 owns `README.md`, which this task does not own. | Same diff, second hunk: the T6.3 status flip beside the T6.2 one | Edit the line back to any `in-progress` value and the defect returns |
| F3 | M, OUT_OF_SCOPE | `dev-diary/PHASE-7-production-issues.md:303` at the reviewed hash | The commit flips the T7.11 status line to `not-started`. That path belongs to another phase and another owner. | `git diff d5ded02..dffb2aabb94735e56a41534cf2c8c7480ab79cd9 -- dev-diary/PHASE-7-production-issues.md` shows the flip | Edit the line back to any `in-progress` value and the defect returns |
| F4 | L | `dev-diary/submission/descriptions.md:19` at the reviewed hash | `closed explicitly with provider confirmed closes` claims provider confirmation in the plural. Only rounds 1 and 2 read the Sessions API. Round 3 closed explicitly on the socket only. | `grep -n 'client_end' dev-diary/probes/production.md` hits only lines 230, 238, 546, 553. No Sessions API read exists between lines 614 and 842 (round 3) | Change `closes` to `close` and the pin stops distinguishing the rounds |
| F5 | L | `dev-diary/submission/slide-deck.md:21` at the reviewed hash | `Provider records confirm client_end closes` makes the same plural claim as F4 on the same two records. | Same pin as F4: two provider `client_end` records exist, the third session has none | Change `closes` to `close` and the pin stops distinguishing the rounds |

F1 through F3 never block an APPROVE under the out of scope rule, and all three drop before landing. The remediator must not touch phase files, since only the orchestrator writes them. The orchestrator drops those three hunks at landing.

## Carry, not a defect

T6.4b has not run, so no real voice session exists, no callback episode exists, and nobody has heard an untouched episode end to end. `index.md:32` to `:34` states the task cannot close until T6.4b lands. That statement is correct and stays. Draft quality is judged now. Closure waits on real voices.

## Verdict: REMEDIATE

Counts: C 0, H 0, M 3 (all OUT_OF_SCOPE), L 2 (in scope).

The drafts trace cleanly to measured probe facts, every T6.4b slot is marked, style rules hold, and the doc audit passes. F4 and F5 need a one word fix each inside `owns`. F1 through F3 ride along in the commit and the orchestrator drops them before landing.
