# T7.22 round 1 review

Reviewed hash: `a4567352b28bf769c2364d93e8cf1b59012eb76c`
Worktree: `/Users/narayan/reprise-wt/t7.22-rev-r1` (detached HEAD)
Base: `f55733629126d3d5f8772f4457f6352759711449`
Diff reviewed: `git diff f55733629126d3d5f8772f4457f6352759711449..a4567352b28bf769c2364d93e8cf1b59012eb76c`

`git log -1` prints that hash. `git status --short` was empty at the start and empty after the temporary probe was removed. No code was committed.

## What the commit does

The diff is only `internal/api/session_end.go` and `internal/api/handlers_test.go`.
The end route reads the raw body up to the existing size cap.
A blank body decodes as the zero close and the handler returns 200.
Malformed JSON still answers 400 `invalid_request`.
The shared body decoder is unchanged.
An empty decisions post still answers 400.
The new test covers an empty reader, a nil body, a repeat empty post, and three malformed bodies.
The older shaped close test still passes.

## Does an empty close satisfy the reconciler

No.
`RecordSessionEnd` writes `provider_session_id` to the empty string.
That is the value the row already held at mint.
`SettleInput` returns `ErrNoClose` for that value (`internal/broker/settle.go:109`).
`settleOnEnd` treats `ErrNoClose` as a wait and does not start a job (`cmd/reprise/main.go:1464`).
`ListOpen` keeps only a non-empty provider id, so the sweep never sees the row (`internal/broker/settle.go:130`).
`validate` rejects an empty provider id, so a job built from this close cannot run (`internal/broker/reconcile.go:470`).
The hold then ages out by reservation expiry.
That expiry is the session cap plus ten minutes.
At the 1800 second cap that is forty minutes.
The documented skip is for a browser that ended before the provider session opened.
A connected take is not that case.
`session.ready` carries `session_id`, and `session.updated` carries `config.id`.
`VoiceSocket.route` drops both frames in the default branch (`web/src/lib/voice/socket.ts:188`).
`closeSession` posts no body (`web/src/lib/voice/session-calls.ts:33`).
The task owns that voice path and the end route.
The server never learns the provider id, so the zero close cannot carry what the reconciler reads.
The page now sees success. The mint stays held, and the provider recording is never fetched.

## Ten questions

1. Does any path spend money without reserving budget first? No. This commit adds no paid call. The mint still reserves before the token request. The empty close does not settle that hold. Finding H records that gap.
2. Can any path leave an AssemblyAI session open without `session.end` or a token cap? No. The browser still sends `session.end` on the socket before the diary post. Mints still carry the token cap. This commit only changes the diary record.
3. Can a private episode, stem or transcript reach anyone but its owner? No. A foreign empty end answers 404 `session_not_found`. A request with no guest answers 401. Success bodies still set `no-store`.
4. Does any edit apply without a revertible record? No. No decision or render path changes.
5. Does any spoken callback or count come from the prompt instead of a stored row? No. No prompt or spoken copy changes.
6. Does any code, comment, test or migration cite a phase, a task, or a planning file? No. The two files in the diff cite none of those.
7. Did this task rebuild something Keel or Chaaya provides? No. The route reads one body because the shared decoder rejects an empty body. Decisions still use that decoder.
8. Was this review run on the handed hash, in a worktree of its own? Yes. The hash above is `git log -1` in this worktree. Every pin ran there. No code was committed.
9. Can any paid job kind resume after a restart and spend twice? No. No job kind changes. An empty close does not start reconcile, so it cannot run one twice.
10. Does any development check need a person, a microphone, or a wall-clock threshold? No. The session end tests use a fixture database and fixed bodies. The broker settle tests do too.

## Counts

C 0, H 1, M 1, L 0. Both findings are in scope. No out of scope rows.

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `internal/api/session_end.go:72` | An empty end returns 200 and stores an empty provider id. `SettleInput` returns `ErrNoClose`. The end wrapper does not start reconcile. The sweep skips the row. A connected take never settles. The full cap stays held until reservation expiry. The provider recording is never fetched. The id was on `session.ready`, and the owned socket drops that frame. | A temporary probe posted an empty end and read `provider_session_id`. It was `""`. The probe failed on the assertion that this is a close the settle can use. Log line: `fresh empty: status=200 echoed={SessionID:sess-2 EpisodeID:ep-2 ProviderSessionID:} stored=""`. `go test -count=1 -run TestSettleInputNeedsProviderClose ./internal/broker/` passes and shows that empty value is `ErrNoClose`. Both ran in this worktree. The probe file was removed. | Return the zero body for a blank read at `session_end.go:73`. The column stays empty and `SettleInput` returns `ErrNoClose` again. |
| M | `internal/api/session_end.go:72,101` | An empty post after a shaped close returns 200 and replaces the stored provider id with the empty string. The base handler refused that body before the store call, so the stored id stayed. The page already posts this empty body from `closeSession` and from the unload beacon. A provider id the reconciler could use does not survive it. | The same probe posted `{"provider_session_id":"prov-9"}` and then an empty body. It failed. Log line: `empty after shaped: status=200 stored="" body={"session_id":"sess-1","episode_id":"ep-1","provider_session_id":""}`. Assertion: `empty post wiped provider id to "", want prov-9`. Ran in this worktree. | The blank branch `return body, true` at `session_end.go:73`. Refusing that branch leaves `prov-9` in place. Restoring the zero return wipes it again. |

## Checks that passed

`go test -race -count=1 -timeout 180s -run TestSessionEnd ./internal/api/` passed.
`go test -count=1 -run 'TestSettleInputNeedsProviderClose|TestListOpenSkipsUnclosedRows' ./internal/broker/` passed.
`gofmt -l` on the two diff files was empty. `go vet ./internal/api/` was clean.
A foreign empty end is 404. An anonymous empty end is 401. A missing session is 404.
Truncated JSON, a list, a number field, trailing junk, and an oversized body are 400.
A repeat empty end echoes the session id and the episode id.
JSON `null` and a null provider field also store an empty id and return 200.
`json.Decoder.Decode` already accepts those two, so they are not a new hole.
A blank whitespace body takes the new empty branch and returns 200.
`./tools/check.sh` exits 1 with `FAIL [tools] ffmpeg is 9.0.2, not the pinned 9.0.1`.
That stop is the environment. This commit does not touch the pin.

## Verdict

REMEDIATE
