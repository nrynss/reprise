# T7.19 round 2 review

Hash: `b81bc02894952eba9abcbc6956b2f7169e41f1cd`
Worktree: `/home/nryn/work/reprise-wt/t7.19-rev-r2` (detached, confirmed via `git log -1`)
Base: `84d75c1` (task base per round 1)
Remediation diff: `git diff 47c65764bbbd114ad34d55e9a319d5ad0dcfac68..b81bc02894952eba9abcbc6956b2f7169e41f1cd` touches `cmd/reprise/main.go` (named constants plus comment fix) and `cmd/reprise/main_test.go` (+258 committed pins)

Verdict: APPROVE
Counts: C 0, H 0, M 0, L 0 (zero new findings; one prior OUT_OF_SCOPE L carried, non-blocking)

## Residue against round 1, severity by severity

Round 1 returned REMEDIATE with C 0, H 2, M 0, L 2 (one L OUT_OF_SCOPE).

- C: round 1 had zero. This round finds zero. Zero residue.
- H: round 1 had F1 (unpinned take-path budgets) and F2 (unpinned retried-completion recovery). Both are pinned by committed tests that run the wired numbers, verified green here, and verified load-bearing by mutation. Zero residue.
- M: round 1 had zero. This round finds zero. Zero residue.
- L: round 1 had F3 (comment overstated isolation, in-scope) and F4 (phase status revert, OUT_OF_SCOPE). F3 is reworded to the true guarantee and matches behavior. F4 is outside `owns`, untouched by the remediation diff, and left for the orchestrator at landing. It never blocks APPROVE. Zero in-scope residue.

## What was checked

Gate: `npm ci` in `web/`, then `./tools/check.sh` in this worktree. Result: OK all checks passed (eslint, vitest, build, playwright, svelte 4 leakage, consumer names, plan references).

Docs: `python3 dev-diary/tools/audit_docs.py`. Result: OK planning tree is consistent.

Committed pins, all run here on the handed hash (`-count=1`): `TestTakeGateHonestTakePasses`, `TestTakeGateDoubleCadenceTrips`, `TestTakeGateRouteBurstsTripAtWiredEdge`, `TestTakeGateUploadHammerLeavesMint`, `TestRetriedCompletionGarbageIDsRecoverToDraft`, `TestCompletionUnknownIDsNameMissing`. Result: all PASS.

Pin details confirmed by reading plus execution:

- Honest cadence green: one honest two-stem take at real cadence (1 mint, 30 uploads, 2 stems, 1 session-end, 6 episodes, 4 media, 2 threads, 1 admin) passes with zero 429s through the wired budgets.
- Burst trips with honest hints: double cadence trips uploads exactly 12 past the 48 burst, each refusal a 429 with `rate_limited`, a positive `Retry-After`, and header-body wait agreement. All other routes see zero refusals.
- Edge bursts: 48 uploads pass and the 49th trips, 8 stems pass and the 9th trips, 6 mints pass and the 7th trips, each with honest hints.
- Mint isolation: 60-hit upload hammer leaves a session mint at 200 (inner-rule level, matching the comment wording).
- Garbage-id recovery: unknown ids with a linked pair answer 200, state `draft`, scheduled with a job id.
- Named-missing 404: unknown ids with nothing linked answer 404 naming `stems_not_found`, `missing_media_ids`, and both posted ids.

Mutation check: flipped `takeUploadsBurst` 48 to 6 on a scratch copy in this worktree. `TestTakeGateHonestTakePasses`, `TestTakeGateDoubleCadenceTrips`, and `TestTakeGateRouteBurstsTripAtWiredEdge` all failed as expected (honest take trips at hit 6, double-cadence refusals read 54 instead of 12). Reverted with `cp` back, `git status --short` clean. The remediator's second mutation claim (short-circuit `resolveStems` turns the 200 recovery into 404) is accepted on its verification note. No file was left dirty.

Comment check: the `protectTake` comment now states a burst on one route leaves the others alone until the shared outer budget runs out, and a hammer that spends the whole take crowds out every route including a session mint. That matches the wiring (`takeRule` per-route inner plus the shared outer, double-wrapped stems) and the tested guarantee. The named-constant block comment states the sizing reason itself with no plan cite.

Plan cites: grep over the remediation diff for task ids, phase ids, planning filenames, `§`, and round names is clean. The `take-*` rule names are descriptive, not plan cites.

No new behavior was introduced. The outer global moved from literal 4096 to `16 * outerAPIBurst` (4096, identical). Per-route numbers are unchanged, only named.

## Findings

None. No new in-scope defect. No OUT_OF_SCOPE defect beyond round 1 F4, which is carried below.

Carried (non-blocking): round 1 F4 (L, OUT_OF_SCOPE), phase status line outside `owns`. Untouched by this diff. The orchestrator resolves it at landing.

## Ten questions

1. Spend without reserving first: no. The diff adds named constants, a comment fix, and tests. No paid call or reservation path changed.
2. AssemblyAI session left open: no. Mint, close, settle, and sweep paths are untouched. Wrapping is unchanged.
3. Private episode, stem, or transcript reaching anyone but its owner: no. Recovery and missing paths revalidate owner and echo only caller-posted ids.
4. Edit without a revertible record: no. Linking still goes through the guarded transition. Nothing applies an edit.
5. Spoken callback or count from the prompt: no prompts touched. Not applicable.
6. Code, comment, test, or migration citing a phase, task, or planning file: no. Diff grep is clean and the gate plan-reference scan passes.
7. Rebuilt Keel or Chaaya work: no. The pins run product rate config over `keel/gate`. No library patched.
8. Review on the handed hash in its own worktree: yes. Detached worktree above, `git log -1` is `b81bc02894952eba9abcbc6956b2f7169e41f1cd`, diff taken against the stated range, every pin run here. No code committed.
9. Paid job resuming after restart and spending twice: no. Paid-kind registration untouched. Restart still marks paid kinds `interrupted`.
10. Development check needing a person, microphone, or wall-clock threshold: no. The new pins assert exact counts and header-body wait agreement with a fixed client key. No sleeps, no wall clock, no device.
