# Remediation round 1

This pass fixes the two round 1 style findings. It changes no meaning and no numbers. The test fix commits on the task branch. The probe record fix stays uncommitted for the orchestrator.

Base hash: `e782df796c98ad4517a09c3621a272eef8c47668`.
Fix commit: `63438174233e189439f1f6589e15b15f22934138`.

| Finding | Location | Before | After | Evidence |
|---|---|---|---|---|
| 1 | `internal/gemini/live_test.go` header | 33 words in the probe sentence | Longest header sentence is 22 words | Word count probe over the header text |
| 2 | `dev-diary/probes/gemini.md` credential paragraph | 32 words in the key JSON sentence | Longest paragraph sentence is 20 words | Word count probe over the paragraph text |

Verification: `gofmt` lists no owned file. Command `go vet -tags live ./internal/gemini/...` exits 0. Plain `go test ./internal/gemini/` passes with no test files. The live test never ran. The worktree is clean after the commit.
