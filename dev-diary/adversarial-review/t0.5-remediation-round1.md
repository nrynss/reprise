# T0.5 remediation round 1

Remediation commit a150012312dc78bd3b2f24e31e2e662fe37b3db2 lands on branch task slash t0.5-rem-r1 above reviewed commit 828dd44791f353fd4d54c2a3b9ae420d9298685f. I touched only testdata slash sessions plus this record. I ran no live session.

| Finding | Fix | Verification evidence |
|---|---|---|
| F1 corrupt wav headers on six user clips | I patched only the RIFF size and data size fields in place and kept every audio byte intact | ffprobe reads all six wavs with no warning and wave reports 2.79 and 2.66 and 2.74 and 2.39 and 2.87 and 3.41 seconds. I hashed each PCM payload before and after and every hash stayed identical. I reran the patch and every file hash stayed stable. |
| F2 unpinned toolchain in recreate.sh | I pinned Ubuntu 24.04 by digest and espeak-ng at 1.51 plus dfsg 12build1 and named both in the script comment | grep finds one sha256 digest line and one exact version install line. The script uses seekable wav output for every clip. |
| F3 recreate script rebuilds nothing | I rewrote recreate.sh so one command synthesizes all six committed wav paths and the prose states that live sessions need a key and spend | bash minus n passes clean. grep finds all six wav write sites plus honest prose for events.json and timeline.json and metadata.json and recording.ogg. The README recreate section agrees with the script. |
| F4 wrong barge timing in README | I changed the claim to about 5.4 seconds after the story audio starts | I measured story reply dot started at 7.34 against barge input dot speech dot started at 12.72 over bargein slash events.json. The delta is 5.37 seconds and the timeline still marks the story interrupted and the barge completed. |

## Hashes

I recomputed all 18 README hashes with sha256sum and all 18 match. The six wav hashes moved because the repaired headers changed eight bytes per file. The twelve other hashes stayed identical.

## Gate

I ran npm ci in web then tools slash check.sh. The gate exits 0 with OK all checks passed. The known mockup hit did not appear in this run, so I record nothing new.

## Spend

Zero. I ran no live session and minted no token. All fixes use committed bytes plus offline checks. Total added spend is zero dollars and zero seconds.
