# T0.2 round 1 review

Reviewed hash: b5246abf1ebde4b02d3d4569228f51a691b2ddab.
Worktree: t0.2-rev-r1 (detached at the hash, confirmed with `git log -1`).
Base for the diff: fd9f5319d973dcbe4d982f0ae70aebb44468e860.
Name-only list verified: dev-diary/probes/voice-agent.md,
dev-diary/project.md, internal/assemblyai/doc.go,
internal/assemblyai/live_test.go, testdata/speech/exchange-24k.pcm,
testdata/speech/exchange.wav, testdata/speech/generate.sh,
testdata/speech/keyterm-24k.pcm, testdata/speech/keyterm.wav.
Round 1 is the first round, so there is no prior-round residue to check.
This review judges the branch against its base and the task text only.

## Bullet-by-bullet measurement check

All seven task rows are measured, and the record carries raw shapes.

1. 60s cap: the record reports three idle runs stayed open past 100
   seconds with no `session.ended` and no `session.error`, and the test
   holds the socket 70 seconds past a 60s cap then fails on any
   close event inside the window (live_test.go:740-764). Honest
   overturn of the plan, recorded in the record and project.md.
2. Billing: `session.ended` carries `session_duration_seconds` with
   `audio_duration_seconds` null, and the Sessions API agrees
   (record lines 151-159). The test bills from `session_duration_seconds`
   only and polls the Sessions API for `duration_seconds` (509-531).
   Billing connected time is a pin, but the null audio field is
   observed-by-log only. See Gap G1 below.
3. Greeting: host speaks first and unprompted, transcript matches the
   configured text exactly (record lines 107-118, test lines 589-601).
   The greeting builds from the sister talk of episode one.
4. Transcript shapes: `transcript.user` carries text only with no
   timings, `transcript.agent.delta` carries `start_ms` and `end_ms`
   on every word (record lines 123-144, test lines 642-663). Both are
   pins with verbatim raw events in the record.
5. Recording: stereo Opus with user left, proved by ffprobe plus
   channel energy and the metadata layout line (record lines 182-216).
   The in-test pin asserts Opus stereo only, not the side mapping.
   See Gap G2 below. The absence of a side pin is consistent with the
   AGENTS.md lane that timed side speech belongs to a cross stem
   measurement, which T0.5 owns. Naming the gap is honest reporting.
6. DELETE: 204 and absence from the list, before and after asserted
   for all five sessions (record lines 218-223, test lines 829-848).
7. Keyterms: paired runs shift the invented name both times, keyed
   reads exact, unkeyed reads differ per run (record lines 225-238,
   test lines 733-738). Pin fails when readings match or the keyed
   reading misses the name.

## Done-when check

The record holds every measurement with raw event or ffprobe output,
except the two explicitly named gaps. The project.md cap update matches
what the record proves: the cap does not end the session, the browser
runs its own timer, and `session.end` goes first. Quote checks:

1. Greeting: the test const `voiceLiveGreeting` reads exactly
   "Hello there. Last week you mentioned dreading a talk with your
   sister. Did it happen?" and the record quotes the same text in the
   `transcript.agent` event. Match.
2. Token log shape: the test logs `tokens minted: %d, connected
   seconds: %.1f, wall time: %s`. The record reports the values that
   fill it (five tokens, about 110 connected seconds). Shape match.

## Fixtures check

generate.sh pins espeak-ng on Ubuntu 24.04 in Docker with fixed voice,
speed, and sentences. Deterministic within that image. Byte-for-byte
reproduction was not re-run here (no docker synthesis run, per brief).
The committed pcm renders match what the test streams: 171612 bytes
(3.58s) and 190806 bytes (3.98s) at 24 kHz mono 16 bit, agreeing with
the record durations. No real voice, no cloud TTS. One fixture defect
found: committed wav headers are corrupt, see Finding F1.

## Gate and vet

`npm ci` in web/ then `./tools/check.sh`: exit 0, OK all checks passed.
`go vet -tags live ./internal/assemblyai/...`: exit 0.
Plain `go test ./internal/assemblyai/`: `[no test files]`, pass.
The live test was not run (spend, per brief). No plants remain outside
the reviewer's own scratch path.

## Mutation checks

1. Inverted keyterm pin in a scratch copy only: flipping
   `plainText == keyedText` to `!=` fails against both record pairs
   (Ranil Viswatrek Malakunar and Elvis Rex Melquinar vs the keyed
   Zaffranil reading). The pin is load-bearing for the shift claim.
2. Missing-secret refusal: pointed no config, used the existing
   `TestStartupStopsOnMissingSecret` pin, which fails startup and
   names key (`session_signing_key`), source (`env_file`), and path.
   Ran green: PASS. The probe loads through the same settings loader,
   so the refusal path covers it.

## Spend accounting

Five tokens, about 110 connected seconds, about 14 cents final run,
about 1.02 dollars total with exploration. Every token capped at 120s
or less (four at 120, the cap probe at 60). Per-run ceiling: five
times 120s at 4.50 dollars per hour is 75 cents, inside the 1 dollar
budget. The test enforces a 100 cent ceiling in code. No uncapped or
uncounted spend. The exploration co-spend note is disclosed.

## Review questions

1. Does any path spend money without reserving budget first? No. The
   probe is a development measurement with a per-run ceiling enforced
   in code (852-854) and token caps of 120s or less. Budget
   reservation is a runtime path owned elsewhere.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. Every socket path sends `session.end`, and
   every token carries a cap. The cap finding means the cap does not
   close the socket, so the client timer plus explicit end carry the
   load, and the record mandates both.
3. Can a private episode, stem or transcript reach anyone but its
   owner? No. No episode data exists in this probe. Secrets travel by
   reference through the settings loader, never in tracked files.
4. Does any edit apply without a revertible record? No. No edits apply
   anywhere. The probe deletes its sessions.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? The greeting is probe text, not a product callback, so
   this question does not apply to the measured path. T0.5 fixtures
   and the product callback still need stored rows.
6. Does any code, comment, test or migration cite a phase, a task, or
   a planning file? No. A word-boundary scan of the owned code paths
   is clean. Two ordinary words (`background`, `review`) matched a
   loose pattern and were cleared by the strict scan.
7. Did this task rebuild something Keel or Chaaya provides? No. Raw
   sockets and hand framing are probe scaffolding, per the handoff.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Detached at b5246abf1ebde4b02d3d4569228f51a691b2ddab, confirmed
   with `git log -1`, gate run in the same tree, no commits made.
9. Can any paid job kind resume after a restart and spend twice? No
   job kind exists in this diff. Tokens are single use, sessions are
   deleted, and a restart mints fresh tokens under the same caps.
10. Does any development check need a person, a microphone, or a
   wall-clock threshold? No. The live probe streams generated speech
   and needs no person. The cap hold is a fixed 70 second sleep, not
   an assertion threshold. The gate needs no microphone.

## Findings

No in-scope findings. Two gaps are recorded as gaps, not findings:
the audio_duration_seconds null has log evidence but no fail-on-value
pin, and the user-left mapping has metadata plus energy analysis but
no in-test side pin. Both are honest reporting consistent with the
task text, which asks for the record to hold the measurement. The
record holds both.

Out-of-scope observations, recorded without severity and not blocking:

- OUT_OF_SCOPE: testdata/speech/exchange.wav and keyterm.wav carry
  corrupt RIFF headers (riff size 2147479588, data size 2147479552,
  `Packet corrupt` from ffprobe). The test never reads them, so no
  task behavior changes. The fixture owner should regenerate or
  document them, since a corrupt committed wav invites a future
  consumer to trip on it. Pin: `ffprobe testdata/speech/exchange.wav`
  warns and estimates duration from bitrate. Mutation: none needed,
  the bytes are the defect.
- OUT_OF_SCOPE: no committed step converts wav to the 24k pcm renders
  (generate.sh writes wav only). Provenance of the streamed bytes is
  therefore by inspection, not by script. The sizes and durations
  agree, so this is a provenance gap only.
- OUT_OF_SCOPE: internal/assemblyai/doc.go duplicates the landed T0.4
  probe file, and internal/assemblyai has no named owner before T2.1.
  Both pre-cleared in the brief. Landing order decides the duplicate.

## Verdict

APPROVE. Zero in-scope findings. The probe measures every task row,
the record holds the raw shapes, the cap-fact update matches the
proof, the gate is green, the pins are load-bearing where measured,
and the spend is capped and counted.
