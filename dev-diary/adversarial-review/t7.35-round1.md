# T7.35 round 1 review

Reviewed hash: `d0badc85bf14805c551eead739a120154400f727`
Worktree: `/home/nryn/work/reprise-wt/t7.35-rev-r1` (detached HEAD, confirmed with `git log -1`)
Base: `de367d2`. Scope reviewed: `git diff de367d2..d0badc85bf14805c551eead739a120154400f727`.
Task text: `T7.35` block in `dev-diary/PHASE-7-production-issues.md`.
Handoff: `dev-diary/adversarial-review/t7.35-impl-handoff.md`.
No lambo tools exist in this environment, so nothing was recalled or recorded outside git.

## Scope check

The diff touches exactly seven files: `cmd/reprise/finish.go`,
`cmd/reprise/finish_test.go`, `cmd/reprise/main.go`,
`cmd/reprise/main_test.go`, `internal/render/kind_test.go` (new),
`internal/render/run.go`, `internal/settings/settings_test.go`.
The widened owns line covers `internal/render/`, `internal/settings/`,
`cmd/reprise/finish.go`, `cmd/reprise/main.go`, `cmd/reprise/main_test.go`.
The two remaining test files sit beside owned code. Nothing falls outside
the widened scope. No OUT_OF_SCOPE defect was found.

## Verification performed

`git log -1` confirms the handed hash. `gofmt` reports nothing.
`go vet ./...` passes. `go test -race -count=1 ./...` passes over all 22
packages. `tools/check.sh` stops at the standing tool exception
(`ffmpeg` 9.0.2 against the pinned 9.0.1) and later steps were already
covered by the runs above plus the implementer's recorded web gate.
No web code changed, so no browser run was needed.

Task claims, each re-measured in this worktree:

- Limit follows the setting. `internal/render/run.go:67` builds
  `job.Kind{Limit: concurrency, ...}`. `main.go:1606` feeds
  `loaded.RenderConcurrency` through `jobsConfig` into the kind call at
  `main.go:1654`, and `finish.go:142` passes `j.renderConcurrency`.
  `TestKindOfFollowsConcurrency` pins limits 1, 2 and 4.
  `TestRenderKindFollowsConcurrencySetting` boots with two and holds two
  running render rows at once. It passed here.
- Refusal by name. `run.go:58` defines `ErrConcurrency` naming
  `render_concurrency`, `run.go:68` returns it below one, and
  `main.go:1654` refuses before the runner opens. Both
  `TestKindOfRefusesConcurrencyBelowOne` and
  `TestRenderConcurrencyBelowOneRefusesBoot` pin zero and negative values
  and the setting name. Both passed here.
- Attempt cap owned by the kind. `run.go:53` sets `renderAttempts = 3`
  and `run.go:71` stamps `MaxAttempts` on the kind. The old constant and
  the `kind.MaxAttempts` patch are gone from `finish.go`.
  `TestKindOfCarriesResumeAttempts` pins idempotence, resume hook, and
  room for a resumed attempt.
- Resume through the kind alone. `TestRestartResumesRenderIntoTheChain`
  passes unmodified in shape and now resumes with no binary patch.
- Shipped files. Both `config/reprise.local.toml` and
  `config/reprise.box.toml` carry `render_concurrency = 1` at line 10,
  and `TestShippedFilesSetRenderConcurrency` loads both tracked files
  and pins a usable value. It passed here.

Mutation checks, re-run by editing the worktree without committing and
restoring with `git checkout` after each:

- Limit fixed to one: `TestRenderKindFollowsConcurrencySetting` fails
  with `start second render: job: kind at capacity, want room for two`.
  The concurrency pin is load-bearing.
- `MaxAttempts` removed from the kind:
  `TestKindOfCarriesResumeAttempts` fails with
  `render MaxAttempts = 0, want room for a resumed attempt`, and
  `TestRestartResumesRenderIntoTheChain` fails with
  `episode state = failed, want ready`. The attempt cap pin is
  load-bearing and the restart path genuinely needs it.

## Ten review questions

1. Does any path spend money without reserving budget first? No. The
   render calls nothing paid, and the diff touches no paid path. The
   restart test still ships paid passes through the existing chain.
2. Can any path leave an AssemblyAI session open without `session.end`
   or a token cap? No. No session path is touched.
3. Can a private episode, stem or transcript reach anyone but its owner?
   No. No serving path is touched.
4. Does any edit apply without a revertible record? No. No editorial
   path is touched.
5. Does any spoken callback or count come from the prompt instead of a
   stored row? No. No host prompt is touched.
6. Does any code, comment, test or migration cite a phase, a task, or a
   planning file? No. A grep of the diff for task ids, plan names, and
   section marks finds nothing in tracked source.
7. Did this task rebuild something Keel or Chaaya provides? No. It wires
   a `keel/job` kind limit from the settings the binary already loads.
8. Was this review run on the handed hash, in a worktree of its own?
   Yes. Detached HEAD at `d0badc8`, confirmed by `git log -1`, in
   `t7.35-rev-r1`. Nothing was committed. The review file is written
   outside the worktree for the orchestrator to commit.
9. Can any paid job kind resume after a restart and spend twice? No. The
   only kind whose resume changed is the free render, and its output
   name hashes its inputs.
10. Does any development check need a person, a microphone, or a
    wall-clock threshold? No. The concurrency test waits on channels
    with a bounded poll that only fails, never passes, on time. The
    full race suite passed in this worktree with no device.

## Findings

None. Zero across all severities.

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| None | None | None | None | None |

Counts: C 0, H 0, M 0, L 0.

Zero-residue claims against every prior round: this is round 1, so there
are no prior rounds and no residue to carry. The single-round record
stands at zero findings in every severity.

## Verdict

APPROVE
