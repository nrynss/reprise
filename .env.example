# Copy this file to .env and fill each value. Never commit the result.
# The Vertex AI service account key is a file, not a variable. Its path is in
# the settings file, and the key never belongs here.
ASSEMBLYAI_API_KEY=
SESSION_SIGNING_KEY=
